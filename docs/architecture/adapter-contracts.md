# Adapter Contracts

CoMath integrates maintained tools through adapters. Adapters provide hints, candidates, evidence, and diagnostics; they never become proof authority by themselves.

## Common Adapter Envelope

Every adapter result must include:

- `adapter_id`
- `provider`
- `capabilities`
- request hash and response hash
- timestamp
- version or API endpoint when available
- license/terms note when content is external
- `proof_authority: "none"`
- artifact or evidence IDs for stored outputs
- blocker details when unavailable

Adapter failures are replayable blockers or degraded capability reports, not silent success.

## Theorem Search Adapters

Providers may include Loogle, LeanSearch, LeanSearchClient, Moogle, LeanExplore, LeanDojo traces, local mathlib indexes, and local external-repo indexes.

Allowed capabilities:

- constant and name search
- subexpression search
- natural-language theorem search
- proof-state search
- premise retrieval

Theorem search results may suggest imports, declarations, lemmas, or proof routes. They may not certify that a theorem is proved or that a statement is equivalent to the FormalSpecLock.

## Proof-Search Backend Adapters

Providers may include LeanRunner, Lean LSP/MCP adapters, LeanDojo, LeanCopilot, Aesop/tactic runners, Codex-compatible backends, and other configured proof-search systems.

Backend output must be an AgentCandidatePack or strict JSON equivalent with:

- locked statement hash
- candidate statement hash
- introduced assumptions
- introduced dependencies
- requested Lean checks
- hard vetoes and blockers
- `proof_authority: "none"`

Any Lean source from a backend must be rechecked by the service-owned LeanRunner and final replay gate.

## Agent Adapter OS Isolation

Agent adapter package launch metadata records whether an adapter currently runs behind `process_boundary_only` or has service-owned OS-isolation evidence. A release-readiness review may accept OS-enforced isolation only when the evidence is produced by a `comathd` service-owned collector and is bound to adapter id, backend, provider, probe id, stdout/stderr/transcript hashes, process isolation, filesystem scope, network isolation, no-new-privileges, and escape-prevention checks.

macOS is outside the current GA environment adaptation scope.

Provider-specific sandbox launch preflight is a separate service-owned manifest. It may record that a configured provider such as OCI, Nix, Firejail, or Windows AppContainer is ready for a future service-owned execution probe, but it does not by itself record adapter execution, kernel/firewall isolation, or GA readiness. Preflight manifests must keep `shell=false`, `network_policy=disabled`, `command_override_allowed=false`, `caller_supplied_success_allowed=false`, `proof_authority="none"`, and `can_certify_ga=false`.

Provider host capability probe manifests record service-observed host capability facts for an adapter OS-isolation provider family before helper host validation. They may include platform applicability, provider-family tool presence, kernel/platform feature observations, sanitized diagnostics, and replayable blockers. The production route path uses a default service-owned host capability probe when no test callback is injected; that default may record the service-observed platform, provider support-table match, bundled provider-helper protocol asset presence/hash, and non-authority diagnostics. For `windows_appcontainer` on Win32 service hosts, the default probe may also record Windows AppContainer host facility diagnostics and `windows_checknetisolation` tool presence/hash metadata without exposing system paths. For `oci_container` on service-supported Linux or Windows hosts, the default probe may record OCI container host facility diagnostics plus Docker/Podman CLI presence/hash metadata without executing those tools, inspecting daemons or sockets, recording versions, or exposing executable paths. For `nix_sandbox` and `firejail` on service-supported hosts, the default probe may record facility diagnostics plus `nix`, `nix-store`, or `firejail` executable-candidate presence/hash metadata without executing those tools, reading store/profile/policy state, recording versions, or exposing PATH/executable paths. Route callers cannot submit platform strings, tool paths, kernel-feature booleans, or success-shaped payload metadata as capability evidence. Pi consumers for this route are host-confirmed thin clients and may forward only sanitized `platform` / `notes` diagnostics, not caller success-shaped capability fields. The manifest is host-capability diagnostics only: it is not provider-helper readiness, not helper self-test or runtime attestation, not executed OS isolation, not collected OS-enforcement evidence, not readiness-review evidence by itself, not mathematical proof authority, not real-Pi execution evidence, and not GA certification. It must keep `adapter_execution_isolation.os_enforced=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Provider-runner contract manifests bind a ready sandbox-launch preflight to a fixed provider argv template and service-owned environment policy. They reject caller-supplied command, argv, env, runner hash, and success metadata. A host operator may configure absolute service-owned helper executables through provider-specific handles such as `COMATH_AGENT_ADAPTER_OSISO_OCI_HELPER`, `COMATH_AGENT_ADAPTER_OSISO_NIX_HELPER`, `COMATH_AGENT_ADAPTER_OSISO_FIREJAIL_HELPER`, or `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_HELPER`, with `COMATH_AGENT_ADAPTER_OSISO_PROVIDER_HELPER` as a host-only fallback. When no env helper is configured but the built package includes the bundled CoMath provider-helper protocol asset, the default resolver may bind the Node executable hash and bundled helper args-prefix hash/count as service-owned protocol material. The default resolver records only executable hashes, prefix hash/counts, and non-secret diagnostics, never helper paths or prefix argument values. Provider-runner manifests still do not record adapter execution, cannot satisfy the readiness gate, and must keep `adapter_execution_isolation.os_enforced=false`, `proof_authority="none"`, and `can_certify_ga=false` until a later service-owned execution probe writes canonical collected evidence.

Provider-helper host-validation manifests bind a ready provider-runner manifest back to its sandbox-launch artifact, selected provider, service-owned helper binary hash, supported host platform list, fixed argv template hash, non-inherited environment policy, and a matching service-owned provider host capability probe. Host validation fails closed before the host validator runs unless the referenced append-only host capability probe is observed and matches the current project id, adapter id, backend, provider, service-observed platform, artifact path/hash, and non-authority flags. The default host validator uses the service-observed host platform, not caller-supplied route platform strings, and fails closed when platform-specific helpers are configured on incompatible hosts. For env-configured helpers and bundled protocol assets, the default host validator also runs a fixed CoMath provider-helper self-test with service-owned argv/env; the self-test stdout must bind the current project id, host-validation id, provider-runner id, and sandbox-launch id, so generic reusable success output fails closed. Public manifests record only self-test exit/status and stdout/stderr/transcript hashes plus args-prefix hash/count, never helper paths, raw prefix args, or stdout/stderr text. Route callers cannot submit helper-host-ready booleans, command overrides, argv, env, binary hashes, self-test hashes, platform strings, host capability success metadata, or version strings as validation evidence. Pi consumers for this route are host-confirmed thin clients and may forward only sanitized `platform` / `notes` diagnostics, not caller helper readiness, helper hash, command, argv, env, proof-authority, or GA fields. Only a service-owned `provider_helper_host_validator` can mark the helper host ready, and even a ready host-validation manifest is still a pre-execution readiness artifact: it does not run the adapter under OS enforcement, does not collect OS-enforcement evidence, cannot satisfy the readiness gate, and must keep `adapter_execution_isolation.os_enforced=false`, `proof_authority="none"`, and `can_certify_ga=false`.

Provider-helper execution attempt manifests bind a ready provider-runner manifest to a prior append-only provider-helper host-validation artifact before any helper process can be configured or spawned. The referenced host-validation manifest must be service-owned, validated, and exactly bound to the same project, adapter, backend, provider, launch artifact, runner artifact, runner binary hash, and helper binary hash; missing, unvalidated, or mismatched host-validation artifacts fail closed before the helper config resolver runs. When binding passes, `comathd` launches the service-owned helper with `shell=false`, an optional service-owned configured or bundled args prefix, fixed argv, a non-inherited fixed `COMATH_*` environment, and a runner-binary hash match; the manifest records the host-validation artifact hash plus only executable hash, args-prefix hash/count, exit status, signal, stdout/stderr/transcript hashes, output sizes, runtime-attestation source/bound status/hash, fixed argv template hash, environment-policy hash, diagnostics, and audit metadata. It never records the helper executable path, prefix argument values, or raw stdout/stderr text. Runtime attestation is accepted only from helper stdout JSON that binds the current project id, helper-execution id, provider-runner id, sandbox-launch id, adapter, backend, provider, disabled network policy, and `proof_authority="none"`; generic reusable runtime success output remains untrusted. Caller-supplied command overrides, argv, env, exit codes, stdout/stderr hashes, host-ready booleans, runtime-attestation hashes, and success-shaped route payloads remain untrusted. A host-bound helper exit code of 0 or runtime-attestation hash proves only that the configured or bundled helper process was attempted under the service-owned protocol contract; it is not collected OS-enforcement evidence, cannot satisfy the readiness gate, and must keep `adapter_execution_isolation.os_enforced=false`, `proof_authority="none"`, and `can_certify_ga=false`.

Provider-helper collection bridge manifests bind a successful provider-helper execution attempt back to its ready provider-runner, sandbox-launch, provider-helper host-validation, and provider host-capability artifacts. That host-capability/host-validation binding is an audit and provenance prerequisite only; it is not readiness evidence by itself, not real-Pi evidence, not mathematical proof authority, and not GA certification. Route callers cannot submit OS-enforcement booleans, exit codes, stdout/stderr/transcript hashes, host-capability success metadata, provider-tool witness metadata, provider-family OS-enforcement witness metadata, provider-family execution profile metadata, provider-specific live probe attempt/execution metadata, or runtime-attestation material as collected evidence. Collection must fail closed before invoking the canonical probe writer unless the helper execution carries the current runtime-attestation binding described above, the referenced host-validation manifest still binds a matching observed service-owned provider host-capability probe, and the current provider host-capability artifact still matches the host-validation binding. Stale host-validation bindings or host-capability binding mismatch cases produce replayable blockers before configured probe output can become nested canonical evidence. When no configured or injected collection probe is supplied, the production route runs the bundled CoMath provider-helper collection probe asset with fixed service-owned argv/env. Its stdout JSON must bind the current project, collection, helper execution, runner, launch, adapter, backend, provider, helper exit code, helper stdout/stderr/transcript hashes, host-validation id/path/hash, provider host-capability probe id/path/hash, and observed host-capability status while recording false OS-enforcement facts, producing `blocked_provider_helper_collection_incomplete_os_enforcement` blocker evidence rather than readiness evidence. Operators may configure provider-specific service-owned collection probe executables through `COMATH_AGENT_ADAPTER_OSISO_OCI_COLLECTION_PROBE`, `COMATH_AGENT_ADAPTER_OSISO_NIX_COLLECTION_PROBE`, `COMATH_AGENT_ADAPTER_OSISO_FIREJAIL_COLLECTION_PROBE`, or `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_COLLECTION_PROBE`, with bounded `*_COLLECTION_PROBE_ARGS_JSON` prefixes and `COMATH_AGENT_ADAPTER_OSISO_PROVIDER_HELPER_COLLECTION_PROBE` fallback handles. The default route executes that configured probe with `shell=false`, fixed service-owned argv/env, and disabled-network proof-authority metadata; stdout JSON must bind the same collection facts before any OS-enforcement facts are accepted. Complete provider-helper collection facts must also include a `provider_tool_execution_witness` that binds the service-owned collection probe executable hash, service-derived profile hash, fixed argv template hash, current helper transcript hash, provider, collection, helper execution, runner, launch, disabled network policy, and `proof_authority="none"`, plus provider-specific tool binding to the host-capability provider tool name/hash observed by the current service-owned capability probe, plus `provider_family_os_enforcement_witness` material bound to the current provider family, host-capability artifact/tool binding, helper transcript, complete OS-enforcement facts, disabled network policy, and `proof_authority="none"`, plus `provider_family_execution_profile` material bound to the service-derived provider-family execution kind, profile hash, and argv hash, plus `provider_specific_live_probe_attempt` material bound to that same provider-family execution profile and complete OS facts, plus `provider_specific_live_probe_execution` material from a separately configured service-owned live OS probe subprocess bound to live probe tool/argv/stdout/stderr/transcript hashes, before readiness can accept the nested canonical evidence. Readiness rechecks the current host-capability artifact path/hash, provider tool name/hash, provider-family witness hash, provider-family execution profile hash, provider-specific live probe attempt hash, and provider-specific live probe execution hash against the saved collection/evidence/probe records. Invalid binding, process failure, missing host-capability binding, missing provider-tool witness binding, missing provider-specific tool binding, missing provider-family OS-enforcement witness binding, missing provider-family execution profile binding, missing provider-specific live probe attempt binding, missing provider-specific live probe execution binding, missing facts, false facts, or stale pre-Task203/pre-Task204/pre-Task205/pre-Task206/pre-Task207/pre-Task208 evidence remain replayable blockers. A provider-specific internal service-owned collection probe can produce collected evidence only when its exit/stdout/stderr/transcript hashes exactly match the provider-helper execution manifest, it records service-owned `collection_source="service_owned_os_probe"`, helper exit `0`, all process, filesystem, network, no-new-privileges, and escape-prevention facts as true, and carries the provider-tool witness, host-capability provider tool binding, provider-family OS-enforcement witness, provider-family execution profile, provider-specific live probe attempt, and provider-specific live probe execution bindings. Incomplete facts are recorded under `blocked_provider_helper_collection_incomplete_os_enforcement`; complete facts without a witness are recorded under `blocked_provider_helper_collection_provider_tool_witness_missing`; complete facts without provider-specific tool binding are recorded under `blocked_provider_helper_collection_provider_specific_tool_binding_missing`; complete facts without provider-family OS-enforcement witness binding are recorded under `blocked_provider_helper_collection_provider_family_os_enforcement_witness_missing`; complete facts without provider-family execution profile binding are recorded under `blocked_provider_helper_collection_provider_family_execution_profile_missing`; complete facts without provider-specific live probe attempt binding are recorded under `blocked_provider_helper_collection_provider_specific_live_probe_attempt_missing`; complete facts without provider-specific live probe execution binding are recorded under `blocked_provider_helper_collection_provider_specific_live_probe_execution_missing`. The wrapper manifest is not readiness evidence by itself and must keep its own `adapter_execution_isolation.os_enforced=false`; only the nested canonical Task170 probe/evidence artifact can satisfy the readiness review, and the result still keeps `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task201 keeps the helper release chain check-debug guard explicit across release-hardening suites, stale host-validation regressions, and host-capability binding mismatch cases. It preserves the Task190-200 non-authority boundary and only adds fail-closed regression coverage.

Task202 keeps the bundled provider-helper collection probe explicit for the no-env default path. The bundled collection probe binds the current release-chain facts and leaves missing OS facts visible as blockers.

Task203 keeps provider-specific executed-tool witness gating explicit. The witness is provenance and anti-stale-evidence binding only; all existing non-authority boundaries continue to apply.

Task204 keeps provider-specific tool binding explicit. Complete collection evidence must bind the provider-tool witness to a host-capability provider tool name/hash from the current service-owned capability artifact.

Task205 keeps provider-family OS-enforcement witness binding explicit. Complete collection evidence must carry `provider_family_os_enforcement_witness` material bound to the current provider family, host-capability artifact/tool binding, helper transcript, complete OS-enforcement facts, disabled network policy, and `proof_authority="none"`.
Task206 keeps provider-family execution profile binding explicit. Complete collection evidence must also carry `provider_family_execution_profile` material bound to the service-derived provider-family execution kind, profile hash, and argv hash. This remains provenance/readiness gating only and is non-authoritative, non-release-grade, and non-shipping metadata.
Task207 keeps provider-specific live probe attempt binding explicit. Complete collection evidence must also carry `provider_specific_live_probe_attempt` material bound to the current provider-family execution profile, provider tool hashes, helper transcript, complete OS facts, disabled network policy, and non-authority boundary.
Task208 keeps provider-specific live probe execution binding explicit. Complete collection evidence must also carry `provider_specific_live_probe_execution` material from a separately configured service-owned live OS probe subprocess, bound to the current provider-family execution profile, provider tool hashes, helper transcript, live probe tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary.
Task209 keeps provider-specific live probe collection binding explicit. Configured provider-helper collection stdout must bind the same service-executed `provider_specific_live_probe_execution` id/hash/tool/argv/stdout/stderr/transcript material before complete collection facts can satisfy readiness.
Task210 keeps provider control-plane execution witness binding explicit. Complete collection evidence must also carry `provider_control_plane_execution_witness` material bound to the current provider-family execution profile, provider tool hashes, helper transcript, and provider-specific live probe execution id/hash/tool/argv/stdout/stderr/transcript hashes. This remains provenance/readiness gating only and is non-authoritative, non-release-grade, and non-shipping metadata.
Task211 keeps the Task202-210 provider-helper witness chain explicit as check-debug coverage. The check-debug suite guards bundled/configured collection discovery, provider-tool/family/live-probe/control-plane witness-chain registration, route sanitization, config flags, blocker/veto wiring, public wording, and runtime cleanliness without making the chain proof authority, real-Pi evidence, broad provider support, production helper shipment, daemon/policy inspection, OS-enforcement proof, or GA certification.
Task211 also fixes the contract reading of the main provider-helper collection paragraph: the Task202-210 witness chain now includes Task209 collection-side live-probe execution binding and Task210 provider control-plane execution witness binding in addition to the earlier provider-tool, provider-specific-tool, provider-family witness/profile, and live-probe attempt/execution checks. Missing collection manifests, missing collection-side live-probe binding, or missing control-plane witness material must fail closed before readiness and must not fall back to evidence-only acceptance.
Task212 keeps a Windows AppContainer production-helper profile contract explicit. Provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests carry `helper_profile_source`, `production_helper_configured`, and `bundled_protocol_asset` so audits can distinguish host operator configured Windows AppContainer helpers from bundled provider-helper protocol assets. The profile contract is non-secret lineage metadata only; neither profile source is proof authority, real-Pi evidence, broad provider support, executed OS-enforcement proof, readiness evidence by itself, or GA certification.
Task222 keeps an OCI/Docker/Podman production-helper profile contract explicit. OCI provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests carry a service-derived `production_helper_profile_contract` binding the helper profile source, helper binary hash, Docker/Podman facility family, disabled network policy, no-new-privileges requirement, command/env override denial, no daemon/socket/container inspection, no container launch requirement, path-free `oci_docker_cli` / `oci_podman_cli` names, and `proof_authority="none"`. The contract is non-secret lineage metadata only; it is not a Docker/Podman runner, not a daemon/socket/container probe, not proof authority, not real-Pi evidence, not broad provider support, not executed OS-enforcement proof, not readiness evidence by itself, and not GA certification.
Task232 keeps a Nix sandbox production-helper profile contract explicit. Nix provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests carry a service-derived `production_helper_profile_contract` binding the helper profile source, helper binary hash, Nix sandbox facility family, disabled network policy, no-new-privileges requirement, command/env override denial, no Nix command execution requirement for the profile contract, no store/profile inspection, path-free `nix_cli` / `nix_store_cli` names, and `proof_authority="none"`. The contract is non-secret lineage metadata only; it is not a Nix runner, not a Nix store/profile probe, not proof authority, not real-Pi evidence, not broad provider support, not executed OS-enforcement proof, not readiness evidence by itself, and not GA certification.
Task234 keeps a Firejail production-helper profile contract explicit. Firejail provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests carry a service-derived `production_helper_profile_contract` binding the helper profile source, helper binary hash, Firejail facility family, disabled network policy, no-new-privileges requirement, command/env override denial, no Firejail command execution requirement for the profile contract, no Firejail profile/policy inspection, path-free `firejail_cli` name, and `proof_authority="none"`. The contract is non-secret lineage metadata only; it is not a Firejail runner, not a Firejail profile/policy probe, not proof authority, not real-Pi evidence, not broad provider support, not executed OS-enforcement proof, not readiness evidence by itself, and not GA certification.
Task236 supersedes the removed platform-specific environment-adaptation documentation. Public adapter contracts, sample config, and release criteria must not list the removed platform's helper handles, host-facility tools, profile-contract kinds, or focused release-hardening suites as current GA scope.

Task237 keeps the prepared handoff operator-review surface outside adapter authority. The Pi checklist may display sanitized Task231 prepared checkpoint refs and manual-review items, but it is not an adapter manifest, not service-owned evidence, not artifact freshness verification, not execution approval, not durable transport, not proof authority, and not GA certification.

Task239 keeps the service-owned unattended handoff review outside adapter authority. The review may re-read canonical Pi/Codex lifecycle artifacts, compare public aliases and hashes as expected values, and persist an append-only freshness/chain manifest, but it is not an adapter manifest, not operator approval, not unattended execution, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task240 keeps the Pi unattended handoff review consumer outside adapter authority. The consumer may call the Task239 service route through host confirmation and pass public path/hash expected values, but it is not an adapter manifest, not operator approval, not unattended execution, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task241 keeps unattended real-host execution readiness outside adapter authority. The readiness blocker may re-read the canonical Task239 review artifact, persist blocked pre-execution evidence, and list missing approval/executor/transport prerequisites, but it is not an adapter manifest, not operator approval, not unattended execution, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task242 keeps the Pi unattended execution readiness consumer outside adapter authority. The consumer may call the Task241 service route through host confirmation and translate public lifecycle artifact aliases to service-canonical paths inside the request, but it is not an adapter manifest, not operator approval, not unattended execution, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task243 keeps the service-owned unattended real-host operator approval checkpoint outside adapter authority. The approval artifact may bind a current Task239 handoff-review chain and let readiness clear only the missing-approval blocker, but it is not an adapter manifest, not unattended execution authorization, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task244 keeps the Pi-facing unattended real-host operator approval consumer outside adapter authority. The consumer may call the Task243 service route through host confirmation and may pass a complete approval id/path/hash binding to readiness, but it is not an adapter manifest, not unattended execution authorization, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task245 keeps the service-owned unattended real-host executor contract outside adapter authority. The executor contract may bind a current Task239 handoff-review chain and let readiness clear only the missing-executor blocker, but it is not an adapter manifest, not an executor implementation, not unattended execution authorization or completion, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task246 keeps the Pi-facing executor-contract consumer outside adapter authority. The Pi tool and `/cm:release` command may call only the Task245 service route through host confirmation and may pass a complete executor-contract id/path/hash binding to readiness, but they are not adapter manifests, not executor implementations, not unattended execution authorization or completion, not durable transport, not Lean execution, not proof authority, and not GA certification.

Task247 keeps the service-owned durable-transport prerequisite contract outside adapter authority. The contract may bind a current Task239 handoff-review chain, Task243 approval artifact, Task245 executor contract, and Task229 continuity checkpoint so readiness can clear only the missing-durable-transport blocker, but it is not an adapter manifest, not a CoMath transport stack, not an open durable/live channel, not unattended execution authorization or completion, not Lean execution, not proof authority, and not GA certification.

Task248 keeps the Pi-facing durable-transport prerequisite consumer outside adapter authority. The Pi tool and `/cm:release` command may call only the Task247 service route through host confirmation and may pass a complete durable-contract id/path/hash binding to readiness, but they are not adapter manifests, not a CoMath transport stack, not open durable/live transport, not unattended execution authorization or completion, not Lean execution, not proof authority, and not GA certification.

Task249 keeps the unattended real-host execution attempt gate outside adapter/proof authority. The service route may consume prerequisite-recorded readiness and optionally run an explicit allowlisted executor command with fixed argv/env, `shell:false`, bounded timeout, and sanitized output, but the resulting attempt/blocker manifests are operational provenance only. Executor stdout/stderr, process exit codes, agent logs, transport continuity, CAS/SMT/SAT hints, and adapter votes remain `proof_authority=none`; only Lean4/mathlib clean replay can promote mathematical claims.

Task250 keeps the Pi-facing execution attempt consumer outside adapter/proof authority. The Pi tool and `/cm:release` command may call only the Task249 service route through host confirmation and may translate the readiness public alias to a service-canonical path inside the request, but they are not adapter manifests, not public executor launchers, not terminal unattended completion evidence, not Lean execution, not proof authority, and not GA certification. Public Pi schema must not expose `executor_command`; attempt/blocker manifests remain service-owned operational provenance with `proof_authority=none`.

Task251 keeps terminal attempt review outside adapter/proof authority. The service route may re-read Task249 attempt evidence, prerequisite-chain artifacts, and attempt result manifests, but this review is operational provenance and blocker classification only. Executor stdout/stderr, process exit code 0, terminal review text, CAS/SMT/SAT hints, adapter votes, or agent logs still cannot become proof authority; no Task251 artifact can promote a mathematical claim without Lean4/mathlib clean replay and the ordinary final authority gates.

Task252 keeps the Pi-facing terminal attempt-review consumer outside adapter/proof authority. The Pi tool and `/cm:release` command may call only the Task251 service route through host confirmation and may translate the attempt public alias to a service-canonical path inside the request, but they are not adapter manifests, not public executor/result launchers, not terminal unattended completion evidence, not Lean execution, not proof authority, and not GA certification. Public Pi schema must not expose `executor_command` or caller-provided attempt-result material; attempt-review manifests remain service-owned operational provenance with `proof_authority=none`.

Task253 keeps terminal completion-certification prerequisite evidence outside adapter/proof authority. The service route may re-read Task251 attempt-review evidence and classify the missing terminal-completion certificate, but this is operational blocker provenance only. Executor stdout/stderr, process exit code 0, terminal review text, CAS/SMT/SAT hints, adapter votes, agent logs, or the prerequisite artifact itself still cannot become proof authority; no Task253 artifact can promote a mathematical claim without Lean4/mathlib clean replay and the ordinary final authority gates.

Task254 keeps the Pi-facing terminal completion-certification prerequisite consumer outside adapter/proof authority. The Pi tool and `/cm:release` command may call only the Task253 service route through host confirmation and may translate the attempt-review public alias to a service-canonical path inside the request, but they are not adapter manifests, not public executor/result/certificate launchers, not terminal unattended completion evidence, not Lean execution, not proof authority, and not GA certification. Public Pi schema must not expose `executor_command`, caller-provided attempt-result material, or completion certificates; prerequisite manifests remain service-owned operational provenance with `proof_authority=none`.

Task255 keeps Pi goal-mode intake manifests outside adapter/proof authority. Paper paths, attachments, and workspace refs may become service-owned intake provenance only after `comathd` path-policy checks, project-relative normalization, and file hashing; they are not literature adapters, theorem-search results, ingestion authority, Lean replay evidence, or proof artifacts. Intake manifests, document text, TeX/PDF/Markdown files, directory refs, and Pi payloads must remain `proof_authority=none` and cannot promote claims or certify GA.

Task256 turns intake provenance into adapter execution planning, not adapter execution or proof evidence. `goal_mode_research_plan.json` may list local PDF/TeX/Markdown/workspace ingestion tasks, literature retrieval tasks, theorem-search tasks, and lemma-planning seeds, but every listed task remains `execution_state="planned"`, `service_owned_execution_required=true`, `proof_authority=none`, and `can_promote_claim=false` until a future maintained adapter run produces its own service-owned manifest. Planned adapter tasks cannot be displayed as retrieval results, citation evidence, LeanRunManifest evidence, or promotion-grade proof artifacts.

Task257 turns planned goal-mode adapter work into service-owned execution manifests, not adapter results or proof evidence. `goal_mode_adapter_execution_manifest.json` binds the Task256 research-plan path/hash, preserves the Task255 intake-manifest path/hash binding, emits one run envelope per planned task, re-checks local file input hashes, records local directories as references only, and blocks network/theorem-search tasks with `goal_mode_live_adapter_execution_required`. These run envelopes are provenance and scheduling guards only: they contain no trusted extracted claims, no retrieval result, no theorem-search hit, no LeanRunManifest evidence, no proof authority, and no promotion-grade material.

Task258 turns the local-ingestion run envelopes into service-owned local draft evidence, not proof evidence. `goal_mode_local_ingestion_evidence.json` binds the Task256 research-plan path/hash, the Task257 adapter-execution manifest path/hash, and the Task255 intake-manifest binding; it may extract line-anchored theorem or lemma drafts from local Markdown and TeX only after prompt-injection scan. Prompt-injected local text is blocked without copying the injected theorem as an extracted claim. PDF refs stay blocked behind a maintained parser adapter, and external Lean repo refs stay blocked behind a maintained repo-inspection adapter. Every ingestion record remains `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`.

Task259 turns allowed local draft extracted claims into skeleton-planning hints, not adapter evidence or proof evidence. `proof/formalization_hints.json` binds `knowledge_pack.json` and `goal_mode_local_ingestion_evidence.json` by path/hash, consumes only prompt-injection-scanned Markdown/TeX extracted claims whose kinds are formalization candidates, and records that hints can inform the blueprint/skeleton only. They cannot change the locked statement, satisfy literature evidence, satisfy theorem-search evidence, supply Lean replay evidence, promote claims, or certify GA.

Task260 turns skeleton-planning hints into a service-owned blueprint, not new theorem obligations or proof evidence. `proof/blueprint.json` binds `proof/formalization_hints.json` by path/hash, emits only planning-step metadata, and carries that binding into `plan.json`, `lemma_dag.json`, `line_map.json`, and `skeleton_report.md`. Blueprint steps may seed lemma-DAG metadata, but they cannot create proof obligations without FormalSpecLock/StatementDiffGate, cannot change the locked statement, cannot satisfy literature or theorem-search evidence, cannot supply LeanRunManifest evidence, and cannot promote claims or certify GA.

Task261 turns the service-owned blueprint into bounded candidate-generation input, not proof evidence. `candidate_generation_request.json` binds `proof/blueprint.json` and `proof/formalization_hints.json` by path/hash, carries blueprint step ids into `candidate_generation.json`, and passes the same non-authoritative planning context into each `lemma_sprint` task card. Candidate generation must re-check the current blueprint hash before writing candidate artifacts; stale or tampered blueprint material fails closed. The request, task cards, and generated candidates remain adapter/agent orchestration material with `proof_authority=none`; they cannot satisfy LeanRunManifest evidence, promotion, or GA certification.

Task262 turns blueprint-bound candidate workspaces into Lean attempt workspaces, not checked proofs. Each candidate may receive `lean_candidate_attempt_plan.json` and `LeanCandidate.lean` that bind `proof/Skeleton.lean`, the current skeleton blueprint, blueprint step ids, and locked statement guards. These artifacts are service-owned draft material for later LeanRunner and repair-loop stages only: they may contain `sorry`, carry no LeanRunManifest evidence, and cannot satisfy promotion or GA certification until service-owned Lean checks and final clean replay succeed.

Task263 turns those draft attempt workspaces into preflight/repair routing material, not Lean replay evidence. `lean_candidate_attempt_check_report.json` may bind candidate manifests, attempt plans, draft Lean files, source skeleton hashes, blueprint hashes, and locked statement boundaries, then classify attempts as `repair_required`, `blocked`, or `ready_for_lean_runner`. While `sorry` remains, the report records `lean_runner_invocations=0`, empty LeanRunManifest paths, `proof_authority=none`, and no promotion/GA authority. Adapters and agents may use it to plan repairs, but not as proof evidence.

Task264 turns repair-required preflight into explicit repair work orders, not adapter results or proof evidence. `lean_candidate_attempt_repair_batch.json` and per-candidate `lean_candidate_repair_task.json` may assign A0/A5/A8 repair responsibilities, bind the source check report by path/hash, list allowed inputs, and require replacement of `sorry` placeholders. They may not carry proof claims, LeanRunManifest paths, FinalReplayManifest paths, trusted statement changes, or promotion-grade material. Adapter or agent outputs from these tasks must return as candidates for later service-owned verification and Lean replay.

Task265 turns repair work orders into re-ingested candidate drafts, not proof evidence. `lean_candidate_attempt_repair_execution.json` and per-candidate repair execution manifests may record original snapshots, repaired draft hashes, placeholder markers, and the next `candidate_verification` stage. They may not carry adapter proof results, LeanRunManifest paths, FinalReplayManifest paths, or promotion-grade material. A repaired no-sorry draft is only ready to be submitted to the service-owned LeanRunner; it is not itself proof authority.

Task266 turns ready repaired drafts into service-owned LeanRunner attempt evidence, not final proof evidence. `lean_candidate_attempt_leanrunner_execution.json` may bind the source check report, per-candidate LeanRunManifest paths, candidate-manifest updates, and all-rejected blocker status. Failed LeanRunManifests and blockers are replayable repair inputs only and must keep proof/promotion/GA flags false. Placeholder-bearing drafts cannot become proof-grade through adapter or runner shims; only an exit-0 service-owned LeanRunner result over a placeholder-free candidate may proceed to ordinary candidate arbitration, and final promotion still requires downstream clean replay gates.

Task267 turns all-rejected LeanRunner diagnostics into repair feedback, not adapter authority. `lean_candidate_attempt_repair_feedback_batch.json` may bind the canonical LeanRunner execution artifact plus each failed LeanRunManifest stdout/stderr path and hash, and the follow-up repair batch may cite those diagnostics as allowed inputs. The repair stage still cannot invoke LeanRunner, emit new LeanRunManifest or FinalReplayManifest paths, accept adapter proof results, change the locked statement, or promote claims; failed Lean output remains diagnostic repair material until a later service-owned Lean pass and final clean replay succeed.

Task268 binds external wheel registry descriptors into the Task267 repair loop as repair context, not live adapter proof evidence. `lean_candidate_attempt_repair_hint_bundle.json` may cite theorem-search, retrieval, computation, proof-search-backend, and external-Lean-repo adapter descriptors, plus the goal-mode research plan, adapter execution manifest, local ingestion evidence, and repair feedback batch by path/hash. The bundle path/hash must be copied into the replacement repair batch, per-candidate repair tasks, and repair execution, and stale bundle hashes must fail closed before candidate drafts are overwritten. Descriptor-derived hints must keep `proof_authority=none`, `network_execution_performed=false`, and all promotion/GA flags false; live provider calls, literature condition checks, CAS reports, LeanRunManifest output, FinalReplayManifest output, and claim promotion remain separate gated paths.

Task269 records service-owned repair hint execution artifacts from those descriptors without upgrading adapter output into proof evidence. `lean_candidate_attempt_repair_hint_execution.json` may capture stubbed theorem-search, retrieval, computation, proof-search-backend, and external-Lean-repo result summaries with request/result hashes, provider terms, capability metadata, and no-authority promotion vetoes. Its path/hash must be copied into the replacement repair batch, per-candidate repair tasks, and repair execution, and stale execution hashes must fail closed before candidate drafts are overwritten. These results must keep `proof_authority=none`, `network_execution_performed=false`, `live_provider_execution_performed=false`, and all promotion/GA flags false; real provider calls, LeanRunManifest output, FinalReplayManifest output, and claim promotion remain separate gated paths.

Task273 extends that execution contract with one opt-in live retrieval provider path. When live repair-hint execution is explicitly enabled and allowlisted, `retrieval:jina_search` may call a configured Jina Search-compatible URL through service-owned HTTP. The request envelope must hash only sanitized request metadata, may send `COMATH_JINA_API_KEY` only as an in-memory bearer header, and must persist no secret values. The live result may record `adapter_execution_state="live_provider_result_recorded"`, provider status, response/content hashes, prompt-injection scan, capability metadata, terms, and no-authority promotion vetoes. It must still keep `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, and cannot emit LeanRunManifest, FinalReplayManifest, proof claims, or promotion artifacts.

Task274 permits the live retrieval result summary to include bounded `extracted_lean_suggestions` from fenced Lean/Lean4 snippets after the live response prompt-injection scan passes. Each suggestion must carry a content hash, declaration kind/name, normalized statement, `proof_authority=none`, `can_promote_claim=false`, and `result_can_be_used_as_proof=false`. Repair execution may consume a suggestion only when it exactly matches the rejected locked theorem/lemma name and normalized statement and passes no-cheat filtering; the result is a repaired candidate draft, not an adapter proof result. Repaired candidates must be checked by LeanRunner, and reruns must use content-sensitive run ids so earlier failed LeanRunManifests remain append-only.

Task275 extends the final replay material contract for candidates that were repaired through live retrieval. When such a repaired candidate passes LeanRunner and is selected, `candidate_replay_project_descriptor.json` and `candidate_final_replay_material_source.json` must include `candidate_repair_provenance` binding the source repair-hint execution, campaign repair execution, and per-candidate repair execution by path/hash with `proof_authority=none`, `can_promote_claim=false`, and `result_can_be_used_as_proof=false`. This provenance cannot promote claims or replace FinalReplayManifest authority; it only makes the candidate's non-authoritative repair inputs auditable.

Task276 extends the repair hint execution contract with one opt-in live theorem-search provider path. When live repair-hint execution is explicitly enabled and allowlisted, `theorem_search:loogle` may call a configured Loogle-compatible URL through service-owned HTTP. The request envelope must hash only sanitized request metadata, may send `COMATH_LOOGLE_API_KEY` only as an in-memory bearer header, and must persist no secret values. The live result may record `adapter_execution_state="live_provider_result_recorded"`, provider status/content type, response body hash, prompt-injection scan, bounded declaration result summaries, capability metadata, terms, and no-authority promotion vetoes. It must still keep `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, and cannot emit LeanRunManifest, FinalReplayManifest, proof claims, or promotion artifacts.

Task277 permits a narrow consumer of live theorem-search declaration results. Repair execution may use a live `theorem_search:loogle` result only when the persisted result is `adapter_execution_state="live_provider_result_recorded"`, carries `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, has a passing live-provider prompt-injection scan, and contains declaration `And.intro` with non-authoritative flags. Even then, the repair executor may materialize only the locked same-name `True /\\ True` / `True ∧ True` theorem/lemma as `exact And.intro trivial trivial`, recorded with `placeholder_free_repair_strategy="live_theorem_search_and_intro_conjunction_repair"`. It must not infer imports, synthesize general proofs from provider types, change the locked statement, emit LeanRunManifest or FinalReplayManifest authority, or promote claims.

Task278 extends the repair hint execution contract with one opt-in live computation provider path. When live repair-hint execution is explicitly enabled and allowlisted, `computation:sympy` may POST a structured repair-context request to a configured SymPy-compatible URL through service-owned HTTP. The request envelope must hash only sanitized request metadata, may send `COMATH_SYMPY_API_KEY` only as an in-memory bearer header, and must persist no secret values. The live result may record `adapter_execution_state="live_provider_result_recorded"`, provider status/content type, response body hash, prompt-injection scan, bounded computation report summaries, capability metadata, terms, and no-authority promotion vetoes. It must still keep `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, and cannot emit LeanRunManifest, FinalReplayManifest, proof claims, or promotion artifacts.

Task279 permits a narrow consumer of live computation results. Repair execution may use a live `computation:sympy` result only when the persisted result is `adapter_execution_state="live_provider_result_recorded"`, carries `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, has a passing live-provider prompt-injection scan, and its bounded report is exact-symbolic with normalized expression `True`. Even then, the repair executor may materialize only a same-name/same-statement closed decidable arithmetic equality candidate using `by decide`, recorded with `placeholder_free_repair_strategy="live_computation_sympy_true_decide_repair"`. It must not use SymPy output as proof authority, infer imports, perform arbitrary CAS proof search, change the locked statement, emit LeanRunManifest or FinalReplayManifest authority, or promote claims.

Task280 extends public goal-mode export with sanitized repair provenance for live-computation-shaped candidates. After final Lean clean replay authority has passed, `exportCampaignGoalModeEvidence()` may expose the selected candidate's `candidate_repair_provenance` from `candidate_final_replay_material_source.json`. The exported record must be the no-authority provenance already bound into final replay material, include only relative path/hash references for repair hint execution, campaign repair execution, and per-candidate repair execution, keep `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, and must not leak host roots or provider secrets. Exporting provenance cannot turn any adapter output into proof authority.

Task281 extends the Pi public campaign export consumer for that provenance without relaxing the general public sanitizer. `comath.campaign.export` and `/cm:campaign export` may preserve the three known `candidate_repair_provenance` reference paths only when they are safe relative `.comath/...` paths with hash-bound no-authority records. All other trusted runtime paths, host roots, encoded runtime paths, secrets, proof-success wording, and promotion/GA flags in Pi-visible output remain redacted or forced non-authoritative.

Task270 allows a bounded repair executor to materialize placeholder-free Lean candidates only when the locked goal is an inline theorem/lemma target whose proposition is exactly `True`. This rewrite is local candidate materialization, not adapter proof evidence: it must remove actual code `sorry`, leave comments non-authoritative, avoid `?comath_repair_placeholder`, and still require service-owned LeanRunner plus downstream arbitration/red-team/final clean replay gates. Inline theorem headers with `:= by sorry` must not be accepted by live-candidate native adapter parsing as already solved output.

Task271 turns a selected bounded Pi goal-mode candidate into final replay material without changing adapter authority. The LeanRunner-checked candidate remains candidate evidence; the service writes a separate `final_replay_project` and descriptor for the existing integration/final replay path. Adapter outputs, repair hints, and candidate drafts still cannot emit FinalReplayManifest authority or promote claims; only the service-owned clean replay and ordinary promotion gates can complete the proof.

Task272 adds a conservative `rfl` materialization strategy for syntactically reflexive locked equality statements. Repair execution may record `placeholder_free_repair_strategy="locked_reflexive_equality_rfl"` only when the candidate theorem/lemma statement contains one top-level equality with identical sides; otherwise the draft must remain on the existing repair/blocker path. The `rfl` rewrite is candidate material only and cannot replace LeanRunner, final clean replay, FinalReplayManifest verification, or ordinary promotion.

Task238 keeps the prepared Mathlib fixture preflight outside adapter authority. The preflight may report that pinned trusted licensed Mathlib manifest material and local `.lake/packages/mathlib` hashes are ready for later Task218/219 diagnostics, but it is not a theorem-search adapter, not a Lake import resolver, not a dependency download/install step, not a Lean/Lake execution result, not final replay allocation, not proof authority, and not GA certification.

Sandbox execution probe manifests bridge the sandbox-launch preflight to canonical OS-isolation evidence. They may bind a ready sandbox-launch manifest to a service-owned execution probe and then emit the same collected probe/evidence artifacts consumed by the readiness gate. Route callers and Pi payloads still cannot submit success-shaped execution metadata; only an internal service-owned execution probe callback can produce collected evidence. The Pi `/cm:release agent-adapter-os-isolation-sandbox-execution` consumer is host-confirmed release tooling for this route only; it strips confirmation ids, sanitizes host paths/secrets/proof-success and transport overclaims, and cannot write `.comath/` directly. The bridge manifest remains routing/operator evidence until the internal callback writes canonical artifacts, and the Pi consumer is operator UX only. Only canonical service-owned probe/evidence artifacts can feed readiness gates; the bridge and consumer are not mathematical proof authority, GA certification, or a guarantee that every supported provider has a production runner on the current host.

Caller-supplied request fields, Pi payloads, operator attestations, contract-only metadata, package launch metadata, and health-probe text cannot satisfy the OS-isolation readiness gate. They remain blocker evidence or diagnostics with `proof_authority: "none"`, `can_promote_claim: false`, and `can_certify_ga: false`.

## Literature And Ingestion Adapters

Providers may include arXiv, Semantic Scholar, OpenAlex, Crossref, Unpaywall, Jina Reader/Search, AnySearch, local PDF, local TeX, local Markdown, BibTeX, and Zotero-compatible metadata.

Every literature evidence record must include:

- provider and source reference
- URL, DOI, arXiv ID, local artifact, or bibliography key where available
- retrieval timestamp
- content hash
- page, section, paragraph, line, or byte anchors
- prompt-injection scan result
- license/terms note
- extracted-claim kind
- `proof_authority: "none"`

Only extracted definitions, theorem statements, lemma statements, and proof-step text may become formalization candidates. They still require FormalSpecLock and Lean replay.

## Computation Adapters

Providers may include SymPy, SageMath, Python exact arithmetic, Z3, cvc5, SAT solvers, MATLAB/Mathematica/Maple when explicitly configured, and domain-specific exact scripts.

Computation reports may support:

- counterexample search
- exact computation
- symbolic exploration
- finite model search
- candidate lemma discovery
- sanity checks

They cannot promote `formally_checked` unless their certificate is formalized and accepted by Lean final replay.

## Adapter Configuration Sample

See `config/comath.sample.json` for a non-secret configuration shape. Live credentials must stay in service-owned environment variables or local secret stores and must not appear in Pi payloads, evidence packs, or committed config samples.
