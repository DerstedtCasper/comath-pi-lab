# Threat Model

CoMath is a local research workbench. Its highest-value assets are theorem boundary integrity, replay authenticity, dependency provenance, evidence hashes, user attachments, API credentials, and runtime project state under `.comath/`.

## Trust Boundaries

| Boundary | Trusted writer | Rule |
| --- | --- | --- |
| User goal and attachments | User/Pi input | Immutable input hashes; attachment content is data, not instruction. |
| `.comath/` runtime state | `comathd` only | Pi, agents, docs, and adapter outputs do not write trusted state directly. |
| FormalSpecLock and AssumptionLedger | `comathd` | No proof search without approved statement and sourced assumptions. |
| Agent output | Untrusted | `proof_authority=none`; strict JSON; no trusted mutation. |
| Literature and RAG output | Untrusted | Anchored evidence only; no proof status. |
| CAS/SAT/SMT output | Untrusted | Hints, refutation leads, or certificates to be formalized; not proof authority. |
| LeanRunManifest | Conditionally trusted | Only service-owned LeanRunner can write it. |
| FinalReplayManifest | Trusted if gates pass | Requires hermetic clean replay and integrity audit. |

## Primary Threats

### Statement Drift

An agent or repair loop may weaken the theorem, add hidden assumptions, change domains, or alter quantifiers. Mitigation: FormalSpecLock, AssumptionLedger, StatementDiffGate, locked statement hashes, and statement-drift red-team reports. Non-exact equivalence requires Lean-proved equivalence replay.

### Fake Proof Evidence

An agent can write stdout, pass logs, JSON manifests, or reviewer notes that mimic successful Lean output. Mitigation: service-owned LeanRunManifest, hashed stdout/stderr, path-bound runs, final clean replay, and rejection of agent-written proof logs.

### Lean Escape Hatches

Final proof artifacts may use `sorry`, `admit`, `axiom`, `constant`, `unsafe`, `opaque`, shadowing, or import pollution. Mitigation: LeanIntegrityScannerV2, target-bound AxiomProfileV2, dependency closure, allowed import prefixes, namespace/import checks, and no-cheat gates.

### Dependency Supply Chain Drift

External Lean repositories, mathlib revisions, Lake manifests, or toolchain binaries may change after a result is claimed. Mitigation: DependencyLock, pinned commits, manifest hashes, license checks, source hashes, network-disabled final replay, and the `trusted_replay_dependency` state machine.

### Literature/RAG Prompt Injection

Papers, TeX, Markdown, web pages, and search results can contain instructions such as 鈥渋gnore the system prompt,鈥?鈥渟kip Lean,鈥?or credential exfiltration text. Mitigation: treat document text as quoted data, scan for prompt injection, require anchors and hashes, and keep adapter output `proof_authority=none`.

### Copyright And Terms Leakage

Evidence packs may accidentally redistribute full copyrighted PDFs or API-restricted material. Mitigation: store metadata, hashes, anchors, excerpts, and local/user-provided paths unless license permits redistribution.

### Runtime And Secret Leakage

Snapshots, logs, adapter outputs, and evidence packs may expose host paths, API keys, tokens, private keys, or local PDFs. Mitigation: secret scanning, host-path scrubbing, `.comath/` git ignore, service-owned config, and no Pi exposure of API key values.

### Agent Or Adapter Escape

Child processes may write outside scope, run shell strings, or consume unbounded resources. Mitigation: allowlisted realpaths, `shell:false`, scoped cwd/log paths, writer locks, rpm limits, timeouts, byte caps, cancellation, and future OS-level isolation before stronger production claims.

### Sandbox Preflight Confusion

Provider-specific sandbox launch manifests may be mistaken for executed isolation evidence. Mitigation: sandbox-launch preflight records only a service-owned provider-command contract, keeps command overrides and caller-supplied success metadata untrusted, and cannot satisfy the adapter OS-isolation readiness gate. Readiness still requires a later service-owned collected execution probe with bound stdout/stderr/transcript hashes and OS-enforcement checks.

### Sandbox Execution Probe Confusion

Sandbox execution route and Pi consumer payloads may be mistaken for a production OS sandbox runner or for collected OS-enforcement evidence. Mitigation: sandbox execution probe manifests must bind an existing ready sandbox-launch preflight, reject caller-supplied success-shaped execution metadata, and feed readiness only through an internal service-owned execution probe callback that writes canonical Task170 probe/evidence artifacts. The Pi consumer strips confirmation ids, sanitizes host-path/secret/proof-success/long-lived transport overclaims, and can only call the service route through host confirmation. The bridge and consumer are operator-routing and UX artifacts with `proof_authority=none`; only canonical service-owned probe/evidence artifacts and readiness reviews can feed release gates, and they still do not certify GA or guarantee broad provider support.

### Provider Runner Contract Confusion

Provider-runner contract manifests may be mistaken for executed sandboxing because they contain fixed argv templates and environment policy. Mitigation: provider-runner manifests require a ready service-owned sandbox-launch preflight, reject caller command/argv/env/hash/success metadata, record unavailable blockers when no service-owned helper is configured, and cannot satisfy readiness without later canonical collected probe/evidence artifacts.

### Provider Host Capability Probe Confusion

macOS is outside the current GA environment adaptation scope.

Provider host capability probe manifests may be mistaken for production helper readiness or OS enforcement because they can record service-observed provider-family tool, platform, and kernel-feature facts. Mitigation: the host capability probe is diagnostics only. The default route probe may observe provider/platform compatibility, bundled provider-helper protocol asset metadata, Windows AppContainer host facility / `windows_checknetisolation` diagnostics, OCI container host facility / Docker/Podman CLI presence-hash diagnostics, and Nix sandbox / Firejail facility diagnostics from service-owned state, but route callers cannot submit platform strings, tool paths, kernel-feature booleans, proof-authority fields, or GA-certification flags as capability evidence; host facility diagnostics must not execute host tools, inspect daemon/socket/container/store/profile/sandbox-policy state, record tool versions, or expose PATH/system/executable paths; Pi consumers for the route are host-confirmed thin clients that forward only sanitized `platform` / `notes` diagnostics; manifests keep `adapter_execution_isolation.os_enforced=false`, `proof_authority=none`, and `can_certify_ga=false`; provider-helper host validation must bind an observed service-owned probe before running a host validator; and readiness still requires canonical service-owned probe/evidence artifacts, not host capability manifests, default-probe diagnostics, host-facility diagnostics, or host-validation bindings.

Configured provider-helper asset environment variables may be mistaken for broad OS sandbox support. Mitigation: OCI, Nix, Firejail, and Windows AppContainer helper paths must be absolute service-owned executables configured in the host environment, and public manifests record only the helper binary hash, fixed argv/env policy, provider/platform contract result, self-test hashes/status, runtime-attestation bound status/hash, and non-secret diagnostics. The default host validator uses the service-observed platform, rejects platform-specific provider mismatches, and requires env-configured helpers to pass a fixed CoMath provider-helper self-test that binds the current project id, host-validation id, provider-runner id, and sandbox-launch id before helper execution can be unlocked; generic reusable self-test success output fails closed. The helper execution path also requires stdout runtime attestation binding the current project id, helper-execution id, provider-runner id, sandbox-launch id, adapter, backend, provider, disabled network policy, and proof-authority boundary before provider-helper collection can invoke canonical probe writing; generic reusable runtime success output fails closed. The production route's default provider-helper collection probe runs a bundled CoMath provider-helper collection probe asset, binds persisted helper exit/stdout/stderr/transcript hashes plus host-validation and host-capability facts, and records false OS-enforcement facts until a provider-specific OS probe reports complete service-owned facts. A configured collection probe must also bind the current provider-helper host-validation artifact and the observed service-owned provider host-capability probe artifact; stale host-validation bindings, tampered provider host-capability artifact files, host-capability binding mismatch cases, or generic host-capability success metadata fail closed before collected evidence is accepted. Task203 additionally requires any complete collection evidence to carry a provider-tool execution witness bound to service-derived tool/profile/fixed-argv-template hashes and the current helper transcript, so stale pre-Task203/pre-Task204/pre-Task205/pre-Task206/pre-Task207/pre-Task208/pre-Task209/pre-Task210 collection artifacts or self-attested witness hashes fail closed. Task204 adds provider-specific tool binding so that the witness must also match a host-capability provider tool name/hash from the current service-owned capability artifact, and readiness rechecks current host-capability artifact path/hash drift before accepting nested evidence. Task205 adds provider-family OS-enforcement witness binding so complete facts must also carry `provider_family_os_enforcement_witness` material bound to the provider family, host-capability artifact/tool binding, helper transcript, complete OS-enforcement facts, disabled network policy, and `proof_authority="none"`. Task206 adds provider-family execution profile binding so that the family witness must also match a service-derived provider-family execution kind, profile hash, and argv hash before readiness can accept nested evidence. Task207 adds provider-specific live probe attempt binding so complete facts must also carry `provider_specific_live_probe_attempt` material bound to the same family profile, provider tool hashes, helper transcript, complete OS facts, disabled network policy, and non-authority boundary. Task208 adds provider-specific live probe execution binding so complete facts must also carry `provider_specific_live_probe_execution` material from a separate service-owned live OS probe subprocess, bound to the same family profile, provider tool hashes, helper transcript, live probe tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary. Task209 adds collection-side live probe binding so configured collection-probe stdout must bind the same service-executed live probe execution id/hash/tool/argv/stdout/stderr/transcript material before complete facts can satisfy readiness. Task210 adds provider control-plane execution witness binding so complete facts must also carry `provider_control_plane_execution_witness` material bound to the same family profile, provider tool hashes, helper transcript, and service-executed live probe id/hash/tool/argv/stdout/stderr/transcript material before readiness can accept nested evidence. Task211 revalidates the Task202-210 provider-helper witness chain as check-debug coverage for suite discovery, route sanitization, config flags, blocker/veto wiring, public wording, missing collection-manifest fail-closed behavior, and runtime cleanliness. Task201 keeps this helper release chain risk under explicit check-debug coverage. Task202 keeps the bundled provider-helper collection probe asset under explicit default-path coverage. The configured asset, runtime attestation, host-capability binding, provider-tool witness, host-capability provider tool binding, provider-family OS-enforcement witness, provider-family execution profile, provider-specific live probe attempt, provider-specific live probe execution, provider control-plane execution witness, and default collection blocker still do not prove OS enforcement, do not expose the helper path or raw helper output to Pi payloads, and cannot satisfy readiness without later canonical collected probe/evidence artifacts.
Task207 adds provider-specific live probe attempt binding to this risk surface: complete collection evidence must also carry `provider_specific_live_probe_attempt` material bound to the current provider-family execution profile, provider tool hashes, helper transcript hash, complete OS facts, disabled network policy, and non-authority boundary before readiness accepts nested evidence.
Task208 adds provider-specific live probe execution binding to this risk surface: complete collection evidence must also carry `provider_specific_live_probe_execution` material from a separately configured service-owned live OS probe subprocess before readiness accepts nested evidence.
Task209 adds provider-specific live probe collection binding to this risk surface: configured collection-probe stdout must bind the same service-executed `provider_specific_live_probe_execution` id/hash/tool/argv/stdout/stderr/transcript material before complete collection facts can satisfy readiness.
Task210 adds provider control-plane execution witness binding to this risk surface: complete collection evidence must also carry `provider_control_plane_execution_witness` material bound to the current provider family execution profile, provider tool hashes, helper transcript, and provider-specific live probe execution material before readiness accepts nested evidence.
Task211 keeps the Task202-210 provider-helper witness chain under explicit check-debug coverage and confirms there is still no direct Pi payload/consumer path for collection or witness-chain material.
Task212 adds a Windows AppContainer production-helper profile contract to this risk surface: manifests must distinguish env-configured Windows AppContainer helpers from bundled provider-helper protocol assets through non-secret `helper_profile_source`, `production_helper_configured`, and `bundled_protocol_asset` fields. Mitigation: treat those fields as lineage and anti-confusion metadata only; they do not ship a production helper, prove OS enforcement, create Pi evidence, alter Lean proof authority, satisfy readiness without canonical service-owned evidence, or certify GA.
Task222 adds an OCI/Docker/Podman production-helper profile contract to this risk surface: OCI manifests carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Docker/Podman facility family, disabled network policy, no-new-privileges requirement, and path-free `oci_docker_cli` / `oci_podman_cli` names. Mitigation: this contract is lineage and anti-confusion metadata only; it must not launch containers, inspect Docker/Podman daemon/socket/container state, expose helper paths or socket paths, ship a production helper, prove OS enforcement, create Pi evidence, alter Lean proof authority, satisfy readiness without canonical service-owned evidence, or certify GA.
Task232 adds a Nix sandbox production-helper profile contract to this risk surface: Nix manifests carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Nix sandbox facility family, disabled network policy, no-new-privileges requirement, and path-free `nix_cli` / `nix_store_cli` names. Mitigation: this contract is lineage and anti-confusion metadata only; it must not run Nix, inspect Nix store/profile state, expose helper paths or Nix paths, ship a production helper, prove OS enforcement, create Pi evidence, alter Lean proof authority, satisfy readiness without canonical service-owned evidence, or certify GA.
Task234 adds a Firejail production-helper profile contract to this risk surface: Firejail manifests carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Firejail facility family, disabled network policy, no-new-privileges requirement, and path-free `firejail_cli` name. Mitigation: this contract is lineage and anti-confusion metadata only; it must not run Firejail, inspect Firejail profile/policy state, expose helper paths or profile paths, ship a production helper, prove OS enforcement, create Pi evidence, alter Lean proof authority, satisfy readiness without canonical service-owned evidence, or certify GA.
Task236 removes the excluded platform from current environment-adaptation threat-model scope: public release docs, sample configuration, and smoke lists must not advertise removed-platform helper handles, host-facility names, profile-contract kinds, or focused release-hardening suites as current GA scope.

Task223 adds a read-only interactive real-Pi checkpoint UX to the Pi surface. Threat: a planner-style UI could be mistaken for service-owned evidence, automatic lifecycle execution, durable transport, or proof authority, and could echo trusted runtime paths, path-shaped IDs, Pi host paths, secrets, or success-shaped wording from operator cursors. Mitigation: the UX never calls `comathd`, writes `.comath`, echoes caller-supplied trusted runtime paths or path-shaped IDs, auto-executes actions, accepts confirmation ids, or opens transport; it sanitizes host paths, secrets, proof-success vocabulary, and long-lived transport wording, and every mutating checkpoint remains behind the existing host-confirmed service command.

Task224 adds a service-owned guided execution terminal chain review. Threat: planner output, a guided execution artifact without heartbeat, or caller-supplied success metadata could be mistaken for a fully executed real-Pi workflow, durable transport, proof authority, or GA readiness. Mitigation: the review reads existing service-owned runtime probe, operator session, recovery, lease, heartbeat, and guided execution artifacts, recomputes current hashes, requires one project/session/lease/run chain, rejects missing or poisoned heartbeat material before persistence, sanitizes proof-success and long-lived transport wording, and records `proof_authority="none"` / `can_certify_ga=false`. It is release-review evidence only and does not run Lean or replace an operator-controlled Pi execution.

Task225 adds a service-owned operator/service maintained transport contract. Threat: a terminal review plus arbitrary route text could be mistaken for durable operator transport, a live long-lived SSE/WebSocket session, Pi mutation authority, proof authority, or GA readiness. Mitigation: the contract consumes only a Task224 terminal review, re-reads the current Task221 heartbeat artifact, rejects stale heartbeat hashes, binds the existing `GET /agent/run/<run_id>/log-session` Node HTTP route and Pi `fetch`/`getText` client primitive, re-runs bounded `formatAgentRunLogSseSession()` with explicit limits, sanitizes secrets/proof-success/long-lived transport wording, and records `proof_authority="none"` / `can_certify_ga=false`. It is append-only provenance only and does not open durable transport or run Lean.

Task226 adds the Pi-side consumer for the operator/service maintained transport contract. Threat: a Pi tool or `/cm:release` command could be mistaken for live durable transport, direct Pi writes, trusted state mutation, proof authority, or a way to smuggle model-provided confirmation ids. Mitigation: the consumer is host-confirmed, calls only the service-owned Task225 route, strips `confirmation_id`, sanitizes actor/path/request/result surfaces, exposes only fixed maintained primitive enums, and centrally forces `live_transport_open`, `pi_direct_write_allowed`, `direct_trusted_state_mutation`, proof promotion, GA certification, durable transport, and long-lived stream flags false in public output. The interactive real-Pi planner only renders the contract checkpoint; it does not execute it.

Task227 adds service-owned automatic real-Pi checkpoint-chain orchestration. Threat: a successful orchestration manifest could be mistaken for durable operator transport, direct trusted Pi mutation, proof authority, GA readiness, or a replacement for a real operator-controlled host run. Mitigation: the orchestrator composes only existing service-owned runtime probe, operator session, recovery, bounded lease, heartbeat/rebind, guided execution, terminal review, and operator/service transport contract producers; it binds every checkpoint id/path/hash; it advances heartbeat cursor from the service-owned AgentRun log-session lease; and it rejects non-service-owned operator routes before writing the final orchestration artifact. The manifest remains `proof_authority="none"`, cannot promote claims, cannot certify GA, and forces durable/live/long-lived transport plus direct Pi mutation flags false.

Task228 adds the Pi-side consumer for that automatic real-Pi checkpoint-chain orchestration. Threat: a Pi tool or `/cm:release` command could be mistaken for an unattended real-host executor, durable transport stack, direct trusted Pi mutation, proof authority, or a way to smuggle model-provided confirmation ids into the service route. Mitigation: the consumer is host-confirmed, calls only the Task227 service route, strips `confirmation_id`, sanitizes nested checkpoint request/result surfaces, and centrally forces proof promotion, GA certification, durable/live/long-lived stream, direct-Pi-write, and trusted-state-mutation flags false in public output. The interactive real-Pi planner only renders the automatic orchestration checkpoint; it remains read-only and does not call `comathd` or execute lifecycle actions.

Task229 adds a service-owned operator/service transport continuity checkpoint. Threat: a resume artifact could be mistaken for durable long-lived operator transport, live stream ownership, proof authority, or GA readiness, or a caller could try to resume from a stale/tampered Task225 transport contract. Mitigation: the continuity recorder consumes only the persisted Task225 contract artifact, requires and verifies the caller-supplied contract hash, resumes only from `log_session_next_cursor`, re-runs bounded `formatAgentRunLogSseSession()` over the existing Node HTTP AgentRun log-session route plus Pi `fetch`/`getText` primitive, and fails closed on stale or missing contract-hash material. The artifact records durable resume provenance only and keeps durable/live/long-lived transport, direct Pi mutation, proof promotion, and GA certification flags false.

Task230 adds the Pi-side consumer for that operator/service transport continuity checkpoint. Threat: a Pi tool or `/cm:release` command could be mistaken for durable live transport, trusted Pi mutation, proof authority, or a way to smuggle model-provided confirmation ids or untrusted `.comath` paths into public UI. Mitigation: the consumer is host-confirmed, calls only the Task229 service route, requires the contract hash, strips `confirmation_id`, keeps the service contract path only in the service request, sanitizes confirmation/notification/result surfaces, and centrally forces proof promotion, GA certification, durable/live/long-lived stream, direct-Pi-write, and trusted-state-mutation flags false. The interactive real-Pi planner only renders the continuity checkpoint; it remains read-only and does not call `comathd` or execute lifecycle actions.

Task231 adds a prepared unattended real-Pi handoff mode to the existing interactive planner. Threat: a read-only handoff view could be mistaken for an operator-free executor, durable transport, proof authority, service-owned evidence creation, direct Pi mutation, or GA readiness merely because all checkpoint references are present. Mitigation: `unattended-handoff` remains inside the non-mutating `comath.release.piCodexLifecycleInteractiveRealPi` planner; it does not call `comathd`, ask for mutation confirmation, accept confirmation ids, write `.comath`, or auto-execute lifecycle actions. It marks a handoff ready only when every prepared service-owned checkpoint reference has a sanitized `service-owned-pi-lifecycle/.../*.json` path plus 64-hex hash, blocks on missing or poisoned refs, and sanitizes host paths, trusted runtime paths, secrets, proof-success wording, long-lived transport claims, and operator-free/unattended-executor overclaims. The handoff is operator UX only and cannot certify GA or affect Lean proof authority.

Task237 adds a prepared handoff operator review checklist mode to the same interactive planner. Threat: a checklist over complete prepared refs could be mistaken for artifact verification, operator approval, execution authorization, service-owned evidence creation, durable transport, proof authority, or GA readiness. Mitigation: `operator-review-checklist` remains inside the non-mutating `comath.release.piCodexLifecycleInteractiveRealPi` planner; it reuses Task231 ref-shape checks, renders manual-review items, blocks on missing or poisoned refs, ignores approval-shaped caller fields, and forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, service-evidence, and execution flags false. It does not call `comathd`, persist a review manifest, verify artifact freshness, run Lean, promote claims, or certify GA.

Task239 adds a service-owned unattended real-host handoff review manifest. Threat: a persisted review over prepared refs could be mistaken for operator approval, automatic unattended execution, durable transport, direct Pi mutation, proof authority, or GA readiness, or a caller could try to make public `service-owned-pi-lifecycle/...` aliases authoritative. Mitigation: the service derives canonical `.comath/release/pi-codex-lifecycle/...` paths from ids, re-reads and hash-binds runtime registration, operator session, recovery, lease, heartbeat, guided execution, terminal review, maintained transport contract, automatic orchestration, and continuity artifacts, and treats public aliases plus hashes only as expected values. Stale hashes, non-canonical aliases, chain tampering, duplicate review ids, secrets, proof-success wording, long-lived transport wording, or approval-shaped caller flags fail closed or are sanitized. The manifest records freshness/chain review only and keeps operator approval, handoff execution, unattended authorization/completion, durable/live transport, direct trusted mutation, proof promotion, and GA certification false.

Task240 adds the Pi-facing consumer for the service-owned unattended handoff review. Threat: a Pi tool or `/cm:release` command could be mistaken for operator approval, execution authorization, production unattended execution, durable/live transport, proof authority, GA readiness, or a channel for model-provided confirmation ids. Mitigation: the consumer is host-confirmed, calls only the Task239 service route, requires prepared automatic-orchestration and transport-continuity public path/hash expected values, strips `confirmation_id`, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, operator approval, and unattended-execution flags false. The interactive planner only renders the next checkpoint and remains read-only.

Task241 adds a service-owned unattended real-host execution readiness blocker. Threat: a post-review readiness artifact could be mistaken for execution authorization, operator approval, unattended host action completion, durable/live transport, proof authority, or GA readiness. Mitigation: the readiness producer consumes only the canonical Task239 handoff-review artifact plus caller-supplied expected hash, rejects stale hashes, non-canonical paths, and promotional or execution-shaped handoff-review artifacts, then persists only a blocked readiness manifest with explicit blocker reasons for missing operator approval artifact, missing service-owned unattended executor, and missing durable transport. The manifest and route sanitize secrets, proof-success wording, durable/live transport wording, and approval/execution-shaped caller fields, and keep operator approval, handoff execution, unattended authorization/completion, direct Pi mutation, proof promotion, and GA certification false.

Task242 adds the Pi-facing consumer for that readiness blocker. Threat: a Pi tool or `/cm:release` command could be mistaken for operator approval, execution authorization, production unattended execution, durable/live transport, proof authority, GA readiness, or a path for public lifecycle aliases to become trusted `.comath` source paths. Mitigation: the consumer is host-confirmed, calls only the Task241 service route, strips `confirmation_id`, requires handoff-review id/path/hash material, translates public `service-owned-pi-lifecycle/...` aliases to service-canonical paths only inside the `comathd` request, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, operator approval, and unattended-execution flags false. The interactive planner only renders the next checkpoint after handoff review and remains read-only.

Task243 adds a service-owned manual operator approval artifact contract. Threat: a recorded approval checkpoint could be mistaken for execution authorization, completed unattended execution, durable/live transport, proof authority, or GA readiness, or a caller could try to use approval-shaped fields to flip readiness authority. Mitigation: the service derives the approval artifact path from `approval_id`, re-reads the canonical Task239 handoff review artifact plus caller-supplied expected hash, rejects stale hashes, duplicate approval ids, non-canonical approval paths, route/transport drift, promotional approval material, and secrets or authority wording in public/audit surfaces. Task241 readiness accepts a current approval artifact only to remove the missing-approval blocker; service-owned unattended executor and durable transport blockers remain, and all execution, proof, promotion, direct Pi mutation, live transport, and GA flags stay false.

Task244 adds the Pi-facing consumer for that operator approval checkpoint. Threat: a Pi tool, `/cm:release` command, or interactive planner step could be mistaken for an operator-free execution approval, a durable transport channel, proof authority, GA readiness, or a path for model-provided `confirmation_id` values and public aliases to become trusted service input. Mitigation: the consumer is host-confirmed, calls only the Task243 service route, strips `confirmation_id`, converts public lifecycle aliases to service-canonical paths only inside the comathd request, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, operator-approved, and unattended-execution flags false. The interactive planner only renders the operator-approval checkpoint before readiness and remains read-only.

Task245 adds a service-owned unattended executor contract checkpoint. Threat: a recorded executor prerequisite could be mistaken for an invoked production executor, unattended execution authorization/completion, durable/live transport, proof authority, or GA readiness, or a caller could try to poison readiness with a current-hash executor artifact carrying execution-shaped fields. Mitigation: the service derives the executor contract path from `executor_contract_id`, re-reads the canonical Task239 handoff review artifact plus caller-supplied expected hash, rejects stale hashes, duplicate contract ids, invalid modes/kinds/states, non-canonical paths, route/transport drift, promotional or execution-shaped executor contracts consumed by readiness, and secrets or authority wording in public/audit surfaces. Task241 readiness accepts a current executor contract only to remove the missing-executor blocker; durable transport remains blocked, and all executor-invoked, execution, proof, promotion, direct Pi mutation, live transport, and GA flags stay false.

Task246 adds the Pi-facing consumer for that executor contract checkpoint. Threat: a Pi tool, `/cm:release` command, or interactive planner step could be mistaken for an executor invocation, unattended execution authorization, durable/live transport, proof authority, GA readiness, or a path for model-provided `confirmation_id` values and public aliases to become trusted service input. Mitigation: the consumer is host-confirmed, calls only the Task245 service route, strips `confirmation_id`, converts public lifecycle aliases to service-canonical paths only inside the comathd request, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, executor-invoked, and unattended-execution flags false. The interactive planner only renders the executor-contract checkpoint before the durable-transport prerequisite and remains read-only.

Task247 adds a service-owned durable-transport prerequisite contract. Threat: a prerequisite artifact over existing maintained transport checkpoints could be mistaken for a newly opened durable/live channel, a CoMath-owned transport stack, unattended execution authorization, direct Pi mutation, proof authority, or GA readiness. Mitigation: the contract re-reads the canonical Task239 handoff review, Task243 approval artifact, Task245 executor contract, and Task229 continuity artifact by id/path/hash, rejects CoMath-owned transport-stack contract kinds, stale hashes, non-canonical paths, route/transport drift, and poisoned live/promotional durable artifacts, and records only prerequisite evidence. Readiness may clear `durable_transport_not_provided` when the contract is current, but approval, execution, durable/live transport, proof promotion, direct Pi mutation, and GA flags remain false.

Task248 adds the Pi-facing consumer for that durable-transport prerequisite contract. Threat: a Pi tool, `/cm:release` command, or interactive planner step could be mistaken for an opened durable/live channel, a CoMath-owned transport stack, unattended execution authorization, proof authority, GA readiness, or a path for model-provided `confirmation_id` values and public aliases to become trusted service input. Mitigation: the consumer is host-confirmed, calls only the Task247 service route, strips `confirmation_id`, converts public lifecycle aliases to service-canonical paths only inside the comathd request, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, executor-invoked, and unattended-execution flags false. The interactive planner only renders the durable prerequisite checkpoint between executor contract and readiness and remains read-only.

Task249 adds the service-owned unattended real-host execution attempt gate. Threat: the service could trust a stale or poisoned readiness JSON, skip re-reading prerequisite artifacts, leak host paths/secrets through executor output, or treat executor exit code 0 as terminal unattended completion, Lean authority, or GA certification. Mitigation: the attempt route accepts only prerequisite-recorded readiness, re-reads the canonical handoff review, operator approval, executor contract, durable-transport prerequisite, and continuity chain before writing evidence, requires explicit allowlisted absolute executor programs with fixed argv/env and `shell:false`, sanitizes stdout/stderr, writes a replayable blocker when no executor command is provided, and keeps completion, Pi direct mutation, durable/live transport, proof authority, promotion, and GA flags false.

Task250 adds the Pi-facing consumer for that execution attempt gate. Threat: a Pi tool, `/cm:release` command, or interactive planner step could be mistaken for terminal unattended completion, a public executor launcher, proof authority, GA readiness, direct Pi mutation, or a channel for model-provided `confirmation_id` / `executor_command` / trusted runtime paths. Mitigation: the consumer is host-confirmed, calls only the Task249 service route, strips `confirmation_id`, accepts only readiness id/path/hash plus optional attempt id/mode, converts public readiness aliases to service-canonical paths only inside the comathd request, does not expose or forward public `executor_command`, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, executor-invoked, execution-attempt-success, and unattended-completion flags false in public output. The interactive planner renders only the next command after readiness and remains read-only.

Task251 adds the service-owned terminal attempt review gate. Threat: an attempt manifest, executor exit code 0, stdout text, or public caller path could be mistaken for terminal unattended completion, Lean authority, or GA release evidence. Mitigation: the review route derives the canonical attempt path from the attempt id, treats caller path/hash only as expected values, re-reads and validates the Task249 attempt, replays the readiness/handoff/approval/executor/durable chain, verifies any attempt result artifact, and persists only append-only review evidence. Executor-unavailable and failed attempts remain blockers, and a successful bounded attempt is still classified as requiring terminal completion evidence; all proof, GA, terminal-completion, durable/live transport, public executor, and direct-Pi-mutation flags remain false.

Task252 adds the Pi-facing consumer for that terminal attempt review. Threat: a Pi tool, `/cm:release` command, or interactive planner step could be mistaken for terminal unattended completion, public executor/result authority, Lean replay, GA readiness, direct Pi mutation, or a channel for model-provided `confirmation_id`, `executor_command`, caller attempt results, or trusted runtime paths. Mitigation: the consumer is host-confirmed, calls only the Task251 service route, strips `confirmation_id`, accepts only attempt id/path/hash plus optional review id/mode, converts public attempt aliases to service-canonical paths only inside the comathd request, does not expose or forward public `executor_command` or attempt-result material, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, terminal-completion, and unattended-completion flags false in public output. The interactive planner renders only the next command after the attempt and remains read-only.

Task253 adds the service-owned terminal completion-certification prerequisite gate. Threat: a Task251 review artifact, exit-code-0 attempt, or path/hash supplied by a caller could be mistaken for terminal unattended completion, Lean authority, direct Pi mutation, or GA release evidence. Mitigation: the prerequisite route derives the canonical Task251 review path from the review id, treats caller path/hash only as expected values, re-reads the review artifact bytes, rejects stale hashes, duplicate prerequisite ids, non-canonical paths, malformed JSON, and promotional review artifacts even when hash-current, then persists only append-only prerequisite evidence. The persisted state is explicitly `blocked_terminal_unattended_completion_certification_required` with `completion_certificate_available=false`; all proof, GA, terminal-completion, durable/live transport, public executor, and direct-Pi-mutation flags remain false.

Task254 adds the Pi-facing consumer for that completion-certification prerequisite. Threat: a Pi tool, `/cm:release` command, or planner step could be mistaken for a terminal completion certificate, public executor/result authority, Lean replay, GA readiness, direct Pi mutation, or a channel for model-provided `confirmation_id`, `executor_command`, caller attempt results, completion certificates, or trusted runtime paths. Mitigation: the consumer is host-confirmed, calls only the Task253 service route, strips `confirmation_id`, accepts only attempt-review id/path/hash plus optional prerequisite id/mode, converts public attempt-review aliases to service-canonical paths only inside the comathd request, does not expose or forward public `executor_command`, caller attempt-result material, or completion certificates, sanitizes confirmation/notification/result surfaces, and centrally forces proof, GA, durable/live transport, direct-Pi-write, trusted-state mutation, terminal-completion, completion-certificate, and unattended-completion flags false in public output. The interactive planner renders only the next command after attempt review and remains read-only.

Task284 binds the Task253 blocker into the older lifecycle readiness aggregate. Threat: real-host install evidence, durable service lifecycle evidence, and Codex account/network evidence could all be green while a separate completion-certification prerequisite says production unattended completion is still missing, making release readiness look complete. Mitigation: the readiness review accepts prerequisite id/path/hash only as an expected canonical artifact reference, re-reads `.comath/release/pi-codex-lifecycle/<id>/unattended-real-host-completion-certification-prerequisite.json`, rejects stale hashes, non-canonical paths, self-hashed or promotional prerequisite artifacts, records only path/hash/status as non-authoritative input, and appends `terminal_unattended_completion_certification_required` with `proof_authority=none` and `can_certify_ga=false`.

Task285 exposes that prerequisite-bound lifecycle review through the Pi consumer. Threat: a Pi tool or `/cm:release pi-codex-lifecycle` command could omit the Task253 prerequisite, forward model-supplied completion claims, or leak service response proof-success/GA wording while making readiness look complete to an operator. Mitigation: the Pi schema and command accept only optional prerequisite id/path/hash expected values, forward them to the existing service-owned review route under host confirmation, and sanitize notification output for secrets, host paths, proof-success wording, terminal-completion wording, completion-certificate claims, and GA claims. The consumer remains a thin client and cannot create completion certificates, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA.

Task286 adds a service-owned terminal completion certificate design gate. Threat: a design artifact, caller-supplied prerequisite path/hash, exit-code-0 attempt review, or model text could be mistaken for an actual terminal completion certificate, public executor authority, durable/live transport, Lean authority, or GA readiness. Mitigation: the design gate consumes only the Task253 prerequisite id/path/hash as expected canonical material, re-reads the prerequisite bytes, rejects stale/non-canonical/promotional artifacts, records only a blocked design artifact plus required future evidence list, strips completion-certificate overclaims from actor text, and keeps completion, proof, GA, durable/live transport, direct-Pi-mutation, and trusted-state flags false. The design artifact cannot certify unattended completion, expose executor output, open transport, run Lean, promote claims, or certify GA.

Task287 exposes that design gate through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, wrong lifecycle artifacts, completion certificates, executor material, host paths, secrets, or promotional terminal-completion/GA wording while making the operator believe Task286 has certified completion. Mitigation: the Pi bridge calls only the Task286 route under host confirmation, strips confirmation ids before POST, accepts only the completion-prerequisite public alias for canonical path translation, rejects wrong artifact ids/filenames before service calls, omits executor/result/certificate fields from public schema and request bodies, sanitizes all public output, and leaves terminal completion, proof, GA, durable/live transport, direct-Pi-mutation, executor, and trusted-state flags false. The interactive planner remains read-only and cannot call comathd.

Task288 adds a service-owned terminal completion certificate evidence gate. Threat: a caller could pass a stale design, tamper with the prerequisite or attempt-review chain after design creation, use exit-code-0 result text as proof authority, leak executor stdout/stderr/result payloads, or confuse Pi/Codex lifecycle completion with Lean/math GA certification. Mitigation: the certificate gate re-reads the canonical Task286 design, Task253 prerequisite, Task251 attempt-review, and current attempt-result artifact bytes by id/path/hash, rejects stale, non-canonical, duplicate, or tampered chains, persists an append-only certificate artifact plus audit event, exposes only a neutral attempt-result evidence hash/size summary, sanitizes actor text, and keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`. The certificate can close only the unattended lifecycle blocker; it cannot promote a mathematical claim, run Lean, or certify GA.

Task289 exposes that certificate gate through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, wrong lifecycle artifacts, caller-provided certificates, executor result payloads, host paths, secrets, or promotional Lean/GA wording while making the operator believe Task288 has certified mathematical correctness or GA. Mitigation: the Pi bridge calls only the Task288 route under host confirmation, strips confirmation ids before POST, accepts only the terminal-certificate-design public alias for canonical path translation, rejects wrong artifact ids/filenames before service calls, omits executor/result/caller-certificate fields from public schema and request bodies, sanitizes public output, preserves only service-returned lifecycle certificate flags, and keeps proof authority, claim promotion, GA certification, durable/live transport, direct Pi mutation, and trusted-state flags non-authoritative. The interactive planner remains read-only and cannot call comathd.

Task290 adds a service-owned operator/service transport closure review gate. Threat: a terminal completion certificate, durable-transport prerequisite, continuity checkpoint, maintained transport contract, or caller-supplied route text could be mistaken for an actual durable/live transport stack, Lean proof authority, or GA release certification; stale or tampered chain artifacts could also make old transport evidence look current. Mitigation: the closure review derives canonical artifact paths from ids, treats caller hashes only as expected values, re-reads the Task288 certificate plus the bound Task247 durable-transport prerequisite, Task229 continuity, and Task225 transport contract bytes, rejects stale hashes, non-canonical paths, duplicate ids, tampering, and chain mismatch, persists append-only no-authority evidence plus audit provenance, sanitizes actor text, and keeps `durable_transport_provided=false`, `live_transport_open=false`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`. The review cannot open transport, create a Pi consumer, mutate trusted state, run Lean, promote claims, or certify GA.

Task291 exposes that closure review through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, wrong lifecycle artifacts, caller-provided certificates, executor result payloads, host paths, secrets, proof/GA wording, or durable/live transport claims while making the operator believe Task290 opened transport or certified GA. Mitigation: the Pi bridge calls only the Task290 route under host confirmation, strips confirmation ids before POST, accepts only the terminal-completion-certificate public alias for canonical path translation, rejects wrong artifact ids/filenames before service calls, omits executor/result/caller-certificate fields from public schema and request bodies, sanitizes public output, preserves only service-returned closure lifecycle flags, and keeps proof authority, claim promotion, GA certification, durable/live transport, direct Pi mutation, and trusted-state flags non-authoritative. The interactive planner remains read-only and cannot call comathd.

Task292 adds a service-owned GA operational readiness review. Threat: a release-candidate aggregate could be mistaken for final GA certification or could accept forged transport/OS-isolation material. Mitigation: the review re-reads only canonical Task290 transport-closure and Task167 adapter OS-isolation readiness artifacts by id/path/hash, rejects stale, non-canonical, tampered, or promotional material, records OS-isolation-not-ready as a blocker, and keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true` even when it reports release-candidate operational readiness. It does not run Lean, certify GA, open durable/live transport, mutate trusted Pi state, or bypass the final GA audit.

Task293 exposes that readiness review through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, forged service-owned aliases, caller proof/GA certificates, Lean replay material, executor commands, durable transport session payloads, host paths, secrets, proof/GA wording, or durable/live transport claims while making the operator believe Task292 certified GA. Mitigation: the Pi bridge calls only the Task292 route under host confirmation, strips confirmation ids before POST, accepts only Task290 transport-closure and Task167 adapter OS-isolation public aliases for canonical path translation, rejects mismatched aliases before service calls, omits caller proof/GA/executor/Lean/durable-session fields from public schema and request bodies, sanitizes public output, preserves only service-returned readiness flags, and keeps proof authority, claim promotion, GA certification, durable/live transport, direct Pi mutation, and trusted-state flags non-authoritative.

Task294 adds a service-owned GA certification review precondition. Threat: a blocked certification-precondition review could be misrepresented as GA certification, or a caller could submit forged operational-readiness/acceptance/proof-breadth material to bypass final audit. Mitigation: the review consumes only the current Task292 operational readiness artifact by id/path/hash, re-reads canonical bytes, rejects stale or promotional readiness material, writes the GA acceptance diagnostic only as no-authority support evidence, records proof-breadth and final-audit blockers, and always keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true` while those prerequisites remain incomplete. It does not run Lean, accept caller proof artifacts, promote claims, open durable/live transport, mutate trusted Pi state, or certify GA.

Task295 exposes that certification-precondition review through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, forged service-owned aliases, caller acceptance reports, proof-breadth matrices, final GA audits, proof claims, GA certificates, Lean replay material, executor commands, durable transport session payloads, host paths, secrets, proof/GA wording, or durable/live transport claims while making the operator believe Task294 certified GA. Mitigation: the Pi bridge calls only the Task294 route under host confirmation, strips confirmation ids before POST, accepts only Task292 operational-readiness public aliases for canonical path translation, rejects mismatched aliases before service calls, omits caller acceptance-report/proof-breadth/final-audit/proof/GA/executor/Lean/durable-session fields from public schema and request bodies, sanitizes public output, preserves only service-returned blocked precondition flags, and keeps proof authority, claim promotion, GA certification, durable/live transport, direct Pi mutation, and trusted-state flags non-authoritative.

Task296 adds a service-owned final GA audit blocker. Threat: a caller could present a stale or promotional Task294 certification review, tamper with its acceptance-support artifact, or treat a final-audit artifact as a GA certificate. Mitigation: the audit consumes only the current Task294 certification review id/path/hash, re-reads the canonical review, re-reads the bound Task292 operational-readiness and acceptance-support artifacts by path/hash, rejects stale or promotional material, writes an append-only audit artifact, and records only a blocked final-audit status while proof breadth is incomplete. The audit may say final-audit evidence is available, but it must keep `final_ga_audit_passed=false`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certificate_available=false`; it cannot run Lean, promote claims, mutate trusted Pi state, or certify GA.

Task297 exposes that final-audit blocker through the Pi consumer. Threat: a Pi tool, `/cm:release` command, planner output, confirmation prompt, or service response could forward model-supplied confirmation ids, forged service-owned aliases, caller acceptance reports, proof-breadth matrices, final audit payloads, proof claims, GA certificates, Lean replay material, executor commands, durable transport session payloads, host paths, secrets, proof/GA wording, or durable/live transport claims while making the operator believe Task296 certified GA. Mitigation: the Pi bridge calls only the Task296 route under host confirmation, strips confirmation ids before POST, accepts only Task294 certification-review public aliases for canonical path translation, rejects mismatched aliases before service calls, omits caller acceptance-report/proof-breadth/final-audit/proof/GA/executor/Lean/durable-session fields from public schema and request bodies, sanitizes public output, preserves only service-returned blocked final-audit flags, and keeps proof authority, claim promotion, GA certification, final-audit pass, GA-certificate, durable/live transport, direct Pi mutation, and trusted-state flags non-authoritative.

Task298 adds a service-owned release-candidate proof-breadth review. Threat: a caller could submit a forged proof-breadth matrix, stale proof counts, or proof-success wording to make the final GA audit look unblocked without Lean clean replay breadth. Mitigation: the review derives the current 100-task positive manifest and positive-matrix batch inside `comathd`, ignores caller proof-breadth material, writes append-only manifest/batch/review artifacts by path/hash, records caller material as ignored when present, and keeps `proof_breadth_complete=false`, `clean_replay_passed_count=0`, `replayable_blocker_count=100`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`. It cannot run Lean, accept proof artifacts, unblock final GA audit, promote claims, or certify GA.

Task299 enables PM-001 task-local final-authority packaging. Threat: restoring PM-001 packaging could accidentally treat the historical representative workflow fixture as PM-001 proof breadth, or make a PM-001-inclusive tranche look promotion-grade without task-local Lean evidence. Mitigation: PM-001 packaging writes the same canonical task-local packaging/blocker artifacts as other positive-matrix tasks, remains `blocked_missing_final_evidence` without project-local Lean Authority v3 material, keeps `proof_authority=none` and `can_promote_claim=false`, and leaves positive-matrix batch consumers reporting PM-001 as a replayable blocker with `positive_matrix_representative_fixture_not_task_local_clean_replay` until real task-local clean replay evidence exists.

Task300 adds a service-owned proof-breadth closure verifier. Threat: a caller or tampered project state could try to close release-candidate proof breadth with missing task reports, blocked reports, caller matrices, or GA wording. Mitigation: the verifier derives the current 100-task manifest inside `comathd`, reads only canonical final-authority packaging report paths, requires every task to report verified Lean Authority v3 final evidence with task-local `lean_kernel_clean_replay` authority and project-local source-verification metadata, records path/hash/size for every accepted report, and remains blocked on missing or non-verified task reports. Even complete proof breadth cannot promote claims or certify GA; it only supplies a closure artifact for the separate final GA audit.

Task301 binds proof-breadth closure into final GA audit. Threat: a caller could present a stale, non-canonical, incomplete, or promotional proof-breadth closure to make final GA audit appear passed. Mitigation: the audit accepts only canonical Task300 closure id/path/hash material, re-reads and hash-checks the closure artifact, rejects promotional or boundary-violating closure bodies, keeps missing/incomplete closure blocked, and may set `final_ga_audit_passed=true` only for complete 100-task Lean-authority closure while still forcing `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certificate_available=false`.

Task302 binds final GA audit into GA certification review. Threat: a caller could present stale, non-canonical, blocked, or promotional final-audit material to make the certification review look ready or certified. Mitigation: the review accepts only canonical Task301 final-audit id/path/hash material, re-reads and hash-checks the audit artifact, rejects boundary-violating audit bodies, keeps missing/blocked audit material as blockers, and may mark only `ready_for_ga_certificate_gate`; it still forces `can_certify_ga=false`, `ga_certificate_available=false`, and certificate generation through a separate gate.

Task303 issues a service-owned GA certificate artifact. Threat: a caller could present a forged ready review or tamper with its bound final audit to obtain a certificate, then treat the release certificate as direct proof-claim promotion. Mitigation: the certificate gate accepts only canonical Task302 review id/path/hash material, re-reads the review, re-reads the review-bound final audit by hash and size, rejects blocked or stale material, and records `claim_promotion_requires_ordinary_gate=true` with `can_promote_claim=false`; the certificate certifies release readiness, not individual theorem claims outside Lean Authority gates.

Task304 closes a production helper readiness confusion path. Threat: a bundled provider-helper protocol asset could produce a complete-looking service-owned OS evidence chain and be mistaken for an operator-configured production helper, including by rewriting only the collection manifest's source fields. Mitigation: the OS-isolation readiness review now records and gates on `helper_profile_source`, `production_helper_configured`, `bundled_protocol_asset`, the current provider-helper `helper_execution_artifact`/helper execution manifest, and provider-family profile-contract provenance; bundled protocol assets or collection-only source rewrites receive `adapter_os_isolation_production_helper_source_missing` and cannot satisfy GA readiness, while all helper-source material remains `proof_authority="none"`.

Task305 closes a release-chain consumption gap. Threat: a legacy or forged adapter OS-isolation readiness review could still look `ready_for_os_isolation_release_review` to GA operational readiness because it had `os_enforced=true`, no vetoes, and an evidence artifact, while omitting Task304 production helper source provenance. Mitigation: GA operational readiness now requires the adapter review's `checks.production_helper_source` and `adapter_execution_isolation` fields to agree on an operator-configured production helper with `production_helper_configured=true` and `bundled_protocol_asset=false`; stale legacy ready material fails before an operational-readiness artifact is written.

Task306 closes the downstream GA certificate source-bound consumption gap. Threat: a ready GA certificate could be consumed as a publishable workbench release signal even though its upstream operational-readiness artifact predates Task305 and lacks source-bound production helper provenance. Mitigation: the certificate consumption gate re-reads the certificate, certification review, final GA audit, proof-breadth closure, and operational-readiness artifacts by canonical path/hash, requires `adapter_production_helper_source_bound=true` plus the operator-configured helper-source fields, and fails closed before writing any consumption artifact when the chain is stale, mixed, or source-less.

Task307 closes the public/source-review release package gap. Threat: a public package or route payload could present stale Task306 consumption material, leak host paths/secrets, or imply proof authority through source-review wording. Mitigation: the source-bound release package gate re-reads the Task306 certificate-consumption artifact by canonical id/path/hash before writing any package state, rejects stale or source-less material, generates source-review Markdown/HTML/JSON reports through the sanitized public archive path, runs `reviewGoal3PublicArchiveSurfaces()`, and forces `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `package_is_proof_authority=false`.

Task308 closes the source-only open-source review artifact gap. Threat: a public source artifact could be generated from stale package material, a dirty worktree, tracked build/runtime/cache files, or route/manifest wording that leaks host paths, secrets, or proof-authority claims. Mitigation: the source-only review artifact gate re-reads the Task307 package by canonical id/path/hash before writing any source artifact, requires a clean Git root, rejects forbidden tracked entries before `git archive`, writes only project-relative `.comath` artifact paths, runs `reviewGoal3PublicArchiveSurfaces()`, and forces `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `source_artifact_is_proof_authority=false`.

Task309 closes the source artifact presentation review gap. Threat: a public download descriptor could present stale `source.tar` bytes, expose host paths or secrets, imply the archive is restorable runtime state, or use proof/GA authority wording for source-review material. Mitigation: the presentation review gate re-reads the Task308 source artifact by canonical id/path/hash, verifies the bound `source.tar` hash and byte size immediately before writing presentation state, rejects Task308 manifests with proof-authority/GA overclaims, writes only project-relative `.comath` paths, runs `reviewGoal3PublicArchiveSurfaces()`, and forces `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `presentation_is_proof_authority=false`.

Task310 closes the source release public checklist gap. Threat: a final public checklist could be generated from stale Task309 presentation material, stale `source.tar` bytes, a tampered public archive review, or wording that implies the source archive is restorable, externally notarized, immutable, or proof-authoritative. Mitigation: the checklist gate re-reads the Task309 presentation by canonical id/path/hash, revalidates the bound public archive review and source archive hash/size before writing checklist state, rejects restorable/proof-authority overclaims, writes only project-relative `.comath` paths, runs `reviewGoal3PublicArchiveSurfaces()`, and forces `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `checklist_is_proof_authority=false`, with external notarization and OS immutable storage recorded as `not_configured`.

Task311 closes the source release external evidence binding gap. Threat: operator-provided notarization or immutable-storage evidence could be stale, bound to a different checklist or `source.tar`, expose host paths or secrets, or overclaim that external verification, restore, proof authority, claim promotion, or GA certification has occurred. Mitigation: the external evidence binding gate re-reads the Task310 checklist by canonical id/path/hash, revalidates the bound source archive hash/size, reads separate external-notarization and OS-immutable-storage evidence files by path/hash, rejects mismatched target/archive bindings and overclaiming evidence, writes only project-relative `.comath` paths, runs `reviewGoal3PublicArchiveSurfaces()`, and forces `external_verification_performed=false`, `requires_future_external_verification=true`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `binding_is_proof_authority=false`.

Task312 source release evidence chain check-debug closes a stale public-review chain threat. Threat: a Task310 checklist could remain hash-current while its bound public archive review manifest is later deleted or tampered, allowing Task311 to bind operator evidence to an incomplete release-review chain. Mitigation: the external evidence binding gate re-reads the Task310 checklist public archive review by canonical id/path, requires a passing non-authoritative public review with no vetoes, rejects missing or tampered review manifests before writing binding material, and emits the checklist public archive review artifact hash/current status in the binding and audit payload. This mitigation does not claim live external-provider verification, OS-level immutable storage, Lean replay, proof promotion, restore capability, or GA certification.

Task313 external provider verification closes a provider-response binding threat. Threat: a caller, stale operator file, or provider shim could claim that external notarization or immutable-storage evidence was verified while binding the response to a different checklist, traversal-shaped `source.tar` path, evidence file, provider, or receipt, or while overclaiming restore, proof authority, promotion, OS immutability, or GA certification through extra response fields. Mitigation: the provider verification gate re-reads the current Task311 binding by canonical id/path/hash, re-checks the Task310 public-review chain and selected operator evidence bytes, rejects source archive traversal before provider calls, calls the configured provider from the service boundary, rejects mismatched provider responses and authority overclaims before writing material, stores only provider request/response hashes and sanitized metadata, runs the public archive review gate with failed-review no-persist semantics, and forces `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `provider_result_is_proof_authority=false`, and `requires_separate_lean_authority=true`.

Task314 provider policy inspection closes a provider-policy drift threat. Threat: a caller, stale Task313 artifact, or provider policy shim could claim that a notarization or immutable-storage provider policy was current while binding the policy response to a different provider verification, binding, source archive, evidence file, receipt, provider response hash, or while overclaiming restore, proof authority, OS immutability, promotion, or GA certification. Mitigation: the provider policy inspection gate re-reads the current Task313 verification by canonical id/path/hash, re-checks its public-review provenance, source archive bytes, selected operator evidence, provider response hashes, and receipt id, calls the configured provider policy endpoint from the service boundary, rejects mismatched policy responses and authority overclaims before writing material, stores only request/response hashes and sanitized metadata, runs the public archive review gate with failed-review no-persist semantics, and forces `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `policy_result_is_proof_authority=false`, `os_immutability_enforced=false`, `requires_separate_lean_authority=true`, and `requires_separate_os_immutability_attestation=true`.

Task315 OS immutability attestation closes an attestation-binding drift threat. Threat: a caller, stale Task314 artifact, notary-only artifact, or provider shim could claim OS immutability while binding an attestation response to a different policy inspection, verification, source archive, operator evidence file, receipt, daemon identity, policy document, or provider policy response, or could smuggle restore/proof/promotion/GA/enforcement authority through response fields or headers. Mitigation: the OS attestation gate consumes only `os_immutable_storage` Task314 policy inspections by canonical id/path/hash, re-reads Task314 public-review provenance, Task313 verification bytes, selected operator evidence, source archive bytes, provider policy response hashes, daemon identity, policy document hash, and receipt id, calls the configured OS attestation endpoint from the service boundary, rejects mismatched or unknown attestation response fields before writing material, stores only request/response hashes and sanitized metadata, runs the public archive review gate with failed-review no-persist semantics, and forces `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `storage_is_proof_authority=false`, `attestation_is_proof_authority=false`, `attestation_is_restore_source=false`, `co_math_os_immutability_enforced=false`, and `requires_separate_lean_authority=true`.

Task316 Pi source-release OS attestation consumer closes a public-surface smuggling threat. Threat: a Pi caller or model-supplied command could bypass host confirmation, translate a policy-inspection alias for the wrong id, forward caller proof claims, Lean manifests, GA certificates, restore manifests, attestation artifacts, provider response payloads, secrets, host paths, or wording that implies OS attestation is proof or restore authority. Mitigation: the Pi consumer requires host confirmation, strips model-supplied confirmation ids from runtime schemas and service calls, translates only matching public policy-inspection aliases, forwards only the Task315 service input shape, keeps the interactive planner read-only, and sanitizes/drops proof, restore, GA, secret, host-path, `.comath`, and CoMath-owned OS-enforcement overclaims before public display.

Task317 durable transport release-signoff prerequisite closes a stale nested transport-chain threat. Threat: a Task292 operational-readiness artifact could remain hash-current while the Task290 transport-closure review's bound terminal certificate, durable-transport prerequisite, continuity checkpoint, or maintained transport contract is later deleted, tampered, or rewritten to claim live/durable transport. Mitigation: the prerequisite gate re-reads Task292, Task290, and all nested transport artifacts by canonical path/hash/size, rejects stale bytes and hash-current live/durable transport overclaims before writing state, and persists only a blocked prerequisite with `durable_long_lived_transport_not_provided`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`. It does not open transport, build a WebSocket/SSE stack, run Lean, mutate Pi state, promote claims, or certify GA.

Task318 final release-signoff decision closes a cross-chain signoff-overclaim threat. Threat: a caller could combine current-looking durable-transport prerequisite material with source-release OS attestation material, stale source archive bytes, caller-supplied GA wording, or certificate-shaped fields to imply final GA signoff despite unresolved transport and certificate blockers. Mitigation: the signoff decision gate consumes only Task317 and Task315 artifacts by canonical id/path/hash, re-reads source archive, operator evidence, and policy-inspection bytes, rejects proof/restore/GA/transport overclaims before writes, and persists only a blocked decision with `ga_release_signoff_ready=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task319 durable transport release-signoff verification closes an external-transport evidence binding threat. Threat: a caller could use stale Task317 prerequisite material, forged service-route/session text, ad hoc WebSocket/SSE URLs, custom transport claims, or proof/GA wording to make durable transport appear ready for final signoff. Mitigation: the verification gate re-reads Task317 and its nested operational-readiness, transport-closure, terminal-certificate, durable-contract, continuity, and maintained-contract artifacts by canonical path/hash/size, rejects stale or overclaiming prerequisite material, accepts only a service-owned `goal3_external_durable_transport_evidence` artifact for a maintained external operator transport primitive, requires the existing AgentRun log-session route plus Pi fetch client primitive and daemon/session-policy hashes, and persists either a blocked evidence-not-bound artifact or non-proof-authority transport provenance with `ga_release_signoff_ready=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task320 Task319 final release-signoff consumption closes a stale-or-mismatched transport readiness threat. Threat: a caller could pair a valid source-release chain with a Task319 verification for another Task317 prerequisite, an expired external transport session, or a transport artifact carrying proof/GA/custom-stack overclaims, then imply final release signoff readiness. Mitigation: the final release-signoff decision gate accepts optional Task319 verification only by canonical id/path/hash, requires it to reference the same Task317 prerequisite, re-checks external transport freshness at signoff time, rejects stale or overclaiming transport material before writes, and sets `ga_release_signoff_ready=true` only when verified Task319 transport and current Task306 GA certificate consumption are both bound. The signoff artifact remains `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task321 final release-chain check-debug closes public drift and regression-coverage confusion for the Task317-320 release chain. Threat: after the final signoff aggregation path exists, later edits could drop capability/smoke discovery, weaken Task319 freshness and canonical-evidence coverage, or let README/TODO/AGENTS/release docs imply that final signoff provenance is Lean proof authority or GA certification. Mitigation: the Task321 focused suite asserts capability exposure, phase0 smoke discovery, Task318/319/320 source guard strings, and public release-hardening documentation while keeping `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false` for release-chain provenance. The check-debug task adds no route, transport stack, Lean run, claim promotion, or GA certificate.

Task322 final release signoff certification-boundary review closes a stale-signoff and certificate-boundary confusion threat. Threat: an operator could point at a formerly ready Task320 signoff after the source archive, external durable transport evidence, Task315 OS attestation, Task319 verification, or Task306 certificate-consumption chain has drifted, or could present a boundary review as a new GA certificate or Lean proof authority. Mitigation: the Task322 gate accepts only canonical id/path/hash signoffs, re-reads every bound artifact by hash/size, re-checks external transport freshness at review time, rejects blocked signoffs and caller proof/GA-certificate overclaims before writes, and persists append-only provenance with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `boundary_review_is_certificate=false`, and `ga_certificate_issued=false`.

Task323 final release-candidate closure audit closes a post-boundary-review consumer gap. Threat: an operator could present a stale Task322 boundary review after the Task320 signoff, Task306 certificate consumption, Task319 durable transport verification, external transport evidence, Task315 OS attestation, source archive, or operator evidence has drifted; could reduce the Task315 policy-inspection chain or Task319 nested transport chain while preserving top-level-looking fields; or could present the closure audit as a new certificate/proof authority. Mitigation: the Task323 gate accepts only canonical id/path/hash Task322 reviews, re-reads the bound artifacts by hash/size, requires Task315 policy-inspection material and Task319-bound Task317 nested transport material to remain present and current, re-checks external transport freshness after Task322, rejects blocked reviews and caller proof/GA-certificate overclaims before writes, and persists append-only provenance with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `release_candidate_closure_audit_is_certificate=false`, and `ga_certificate_issued=false`.

Task324 final release-candidate check-debug closes drift risk after the Task323 consumer landed. Threat: later edits could drop the Task322/323 suites from smoke/default discovery, remove capability exposure, weaken reduced Task315/Task319-chain regression coverage, break route wiring, or let public docs imply that closure audit provenance is a GA certificate or Lean proof authority. Mitigation: the Task324 focused suite asserts capability exposure, phase0 discovery, Task322/323 service route wiring, Task323 source guard strings, reduced-chain regression strings, and public release-hardening documentation while adding no route, Pi consumer, Lean replay, claim promotion, closure certificate, or GA certification.

Task325 Pi final release-candidate closure audit consumer closes the public/Pi invocation gap after Task323 without moving authority into Pi. Threat: a model or operator could try to feed caller-supplied proof claims, Lean manifests, GA certificates, proof-breadth matrices, closure-certificate-shaped payloads, durable transport sessions, stale/mismatched public aliases, host paths, secrets, or closure-certificate wording through the Pi surface and make the Task323 provenance appear certifying. Mitigation: the Task325 Pi tool and `/cm:release` command require host confirmation, translate only matching Task322 public aliases before POST, forward only Task323 service input fields, strip model confirmation ids and caller proof/Lean/GA/proof-breadth/closure-certificate/durable-transport material, keep the planner read-only, and force public output to `proof_authority="none"`, no promotion, no GA certification, no certificate issuance, and no direct trusted-state mutation.

Task326 proof-breadth execution bridge closes a release-candidate execution-planning gap without creating a proof authority. Threat: a caller could submit a forged proof-breadth matrix, proof-success wording, or GA certification flags and ask CoMath to treat broad proof breadth as complete, or the bridge could accidentally run Lean and blur planner/executor/closure boundaries. Mitigation: the bridge derives the current 100-task positive manifest inside `comathd`, reads only canonical per-task final-authority packaging reports, selects a bounded next missing/blocked PM tranche, records append-only bridge provenance and audit events, marks caller proof-breadth material ignored, and forces `runs_lean=false`, `executes_proofs=false`, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. Task300 remains the closure verifier and Task301 remains the final-audit binding.

Task327 proof-breadth bridge check-debug closes drift risk after the execution bridge landed. Threat: later edits could drop Task326 from smoke/default discovery, weaken its no-authority source guards, confuse it with Task300 closure or Task301 final audit, or let public docs imply that bridge output executes Lean, closes proof breadth, or unblocks GA. Mitigation: the Task327 focused suite asserts capability exposure, phase0 discovery, Task326 source guards and tranche-selection regression strings, Task298/300/301 route/authority boundaries, and public release-hardening documentation while adding no route, Pi consumer, Lean replay, proof synthesis, closure, final-audit unblock, promotion, or certification.

Task328 Pi proof-breadth review consumer closes the public/Pi invocation gap for Task298 without moving proof-breadth authority into Pi. Threat: a model or operator could try to feed caller proof-breadth matrices, matrix path/hash/json material, proof-success wording, Lean manifests, proof claims, GA certificates, executor commands, durable transport sessions, host paths, secrets, or final-audit-unblock wording through the Pi surface and make the blocked Task298 review appear complete. Mitigation: the Task328 Pi tool and `/cm:release` command require host confirmation, forward only project/actor material plus optional review id/mode, strip model confirmation ids and caller proof/Lean/GA/transport/material payloads, keep the planner read-only, and force public output to blocked-review/no-authority semantics with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. Task300 remains closure verification, Task301 remains final-audit binding, and Task326 remains execution planning only.

Task329 Pi proof-breadth closure consumer closes the public/Pi invocation gap for Task300 without moving closure authority into Pi. Threat: a model or operator could feed caller proof-breadth matrices, caller closure JSON, Task326 bridge output, proof-success wording, Lean manifests, proof claims, GA certificates, executor commands, durable transport sessions, host paths, secrets, or GA/final-audit wording through the Pi surface and make Pi appear to close proof breadth or certify GA. Mitigation: the Task329 Pi tool and `/cm:release` command require host confirmation, forward only project/actor material plus the closure id/mode, strip model confirmation ids and caller proof/Lean/GA/bridge/transport/material payloads, keep the planner read-only, and sanitize public output so only service-owned Task300 closure status/counts survive while proof-authority wording, claim-promotion flags, GA-certification flags, and certificate availability remain non-authoritative. Task300 remains closure verification, Task301 remains final-audit binding, and Task326 remains execution planning only.

Task330 proof-breadth Pi closure check-debug closes drift risk after adding both Pi proof-breadth consumers. Threat: Task328 blocked review semantics, Task329 non-promotional closure display, Task326 execution planning, Task300 closure verification, Task301 final-audit binding, smoke/package discovery, or docs could drift apart and make the public layer appear to run Lean, consume bridge output as closure evidence, unblock final GA audit, promote proof claims, or certify GA. Mitigation: the focused check-debug suite asserts capability exposure, phase0 discovery, Pi package discovery, runtime registration, planner ordering, request-whitelist/public-sanitizer source guards, Task326/300/301 service boundaries, and release-hardening documentation while adding no route, Pi tool, proof execution, final-GA-audit input consumer, claim promotion, or certification authority.

Task331 Pi final-GA-audit closure-binding consumer closes the public input gap between Task329 and Task301 without moving final-audit authority into Pi. Threat: a model or operator could send partial or forged proof-breadth closure bindings, caller closure JSON, Task326 bridge output, proof-success wording, Lean manifests, proof claims, GA certificates, executor commands, durable transport sessions, host paths, secrets, or model confirmation ids through the final-audit Pi surface and make Pi appear to close proof breadth, promote a claim, or certify GA. Mitigation: the Task331 Pi tool and `/cm:release` command require host confirmation, forward only the Task294 certification-review triple plus an optional all-or-none Task300 closure id/path/hash triple, translate only matching `service-owned-goal3-proof-breadth-closure/<id>/closure.json` aliases, reject partial or mismatched closure bindings before POST, strip caller proof/Lean/GA/bridge/transport/material payloads, keep the planner read-only, and sanitize public output so a Task301 service-owned final-audit pass remains non-certifying with proof authority redacted and promotion/certification/certificate/transport/trusted-state flags false. Task300 remains closure verification and Task301 remains final-audit binding.

Task332 proof-breadth execution follow-through closes a service-side observability gap after Task326 without creating execution or proof authority. Threat: a caller could replay a stale bridge, pass forged proof material, or confuse a selected-tranche witness with full proof-breadth closure, final-audit unblock, or GA certification. Mitigation: the follow-through accepts only a Task326 bridge id/path/hash triple, re-reads the canonical bridge bytes, rejects stale or mismatched bridge material, inspects only the bridge-selected canonical PM final-authority packaging report paths, records append-only selected verified/blocked/missing status plus packaging report hashes, ignores caller proof material, and forces `runs_lean=false`, `executes_proofs=false`, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. It can only mark the selected tranche ready for a later Task300 closure recheck; Task300 and Task301 remain the closure/final-audit authorities.

Task333 proof-breadth follow-through check-debug closes drift risk after Task332. Threat: later edits could let the follow-through run Lean, write missing PM packaging reports, inspect tasks outside the bridge-selected tranche, expose a Pi/public command, treat selected-tranche readiness as global proof-breadth closure, or let docs/smoke/status drift into a misleading certification story. Mitigation: the Task333 focused suite asserts capability exposure, phase0/default discovery, Task332 source guards and focused regression strings, Task326/300/301 authority boundaries, route wiring, no Pi/public surface, and release-hardening documentation while adding no route, Pi consumer, Lean replay, proof synthesis, missing packaging writer, closure, final-audit unblock, promotion, or certification.

Task334 task-local Lean Authority packaging follow-through closes the gap between Task326 selected tranche planning and canonical PM packaging reports without making the follow-through proof authority. Threat: a caller could supply forged success metadata, proof-authority strings, closure/final-audit JSON, or evidence for unselected PM tasks, or a later edit could make selected-tranche packaging appear to close global proof breadth. Mitigation: the Task334 route accepts only a Task326 bridge id/path/hash, rejects unselected PM evidence, reuses the existing project-local v3 final-authority packaging verifier, writes only selected canonical PM packaging reports, records non-certifying append-only provenance, and keeps the follow-through artifact `proof_authority="none"`, `runs_lean=false`, `executes_proofs=false`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. Task300 remains the global proof-breadth closure verifier and Task301 remains the final-audit binding.

Task335 selected-tranche packaging results follow-up closes a currentness gap after Task334 without turning selected-tranche readiness into proof-breadth closure. Threat: a caller could replay a stale Task334 artifact, tamper with a selected PM packaging report after Task334, forge Task334 selected ids that differ from the Task326 bridge, or hide invalid material in unselected PM reports to confuse the recheck. Mitigation: the Task335 route accepts only a Task326 bridge id/path/hash plus a Task334 follow-through id/path/hash, re-reads both artifacts by current hash, rejects bridge/Task334 selected-tranche drift, re-hashes only Task334's selected canonical PM packaging report artifacts, ignores caller proof/GA material, embeds the Task332 selected-tranche recheck witness, and keeps `proof_authority="none"`, `runs_lean=false`, `executes_proofs=false`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. It cannot write Task300/301 artifacts, inspect unselected PM tasks, expose Pi/public authority, promote claims, or certify GA.

Task336 selected-tranche packaging results check-debug closes drift risk after Task335. Threat: later edits could drop Task335 from smoke/default discovery, weaken Task334 artifact hash binding or selected report re-hashing, let Task335 inspect unselected PM tasks, expose a Pi/public command, call Task300/301 directly, or let docs imply that selected-tranche currentness closes global proof breadth. Mitigation: the Task336 focused suite asserts capability exposure, phase0/default discovery, Task335 source guards and focused regression strings, Task334/332/326/300/301 authority boundaries, route wiring, no Pi/public surface, and release-hardening documentation while adding no route, Pi consumer, Lean replay, proof synthesis, packaging writer, closure, final-audit unblock, promotion, or certification.

Task337 selected-tranche closure recheck closes the Task335-to-Task300 handoff gap without making selected-tranche evidence global closure authority. Threat: a caller could replay a stale Task335 follow-up, tamper with selected PM packaging reports after currentness was recorded, submit proof/GA success material, or confuse a Task300 recheck wrapper with proof-breadth closure or final-audit authority. Mitigation: the Task337 route accepts only a Task335 follow-up id/path/hash triple, re-reads it by current hash, requires ready-for-closure-recheck semantics, re-hashes selected canonical PM packaging reports before invoking Task300, records the Task300 closure artifact hash as non-certifying provenance, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no Task301 writes, no promotion, and no GA certification.

Task338 selected-tranche next execution bridge continues the selected-tranche proof-breadth loop without turning a partial recheck into proof authority. Threat: a caller could replay a stale Task337 recheck, tamper with the Task337-bound Task300 closure artifact or completed selected PM packaging reports, submit proof/GA success material, or make the next bridge appear to execute Lean or close global proof breadth. Mitigation: the Task338 route accepts only a Task337 recheck id/path/hash triple, re-reads Task337 and the bound Task300 closure by current hash, re-hashes the completed selected PM packaging reports, records caller proof/GA material as ignored, delegates only to Task326 for the next bounded missing/blocked PM tranche, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no Task301 writes, no promotion, and no GA certification.

Task339 selected-tranche next bridge check-debug closes drift risk after Task338. Threat: later edits could drop Task338 from smoke/default discovery, weaken Task337 recheck hash binding, stop re-reading the Task300 closure artifact, skip completed selected PM report re-hashing, expose a Pi/public command, call Task301 directly, or let docs imply that selected-tranche continuation closes global proof breadth. Mitigation: the Task339 focused suite asserts capability exposure, phase0/default discovery, Task338 source guards and focused regression strings, Task337/326/300/301 authority boundaries, route wiring, no Pi/public surface, and release-hardening documentation while adding no route, Pi consumer, Lean replay, proof synthesis, packaging writer, closure, final-audit unblock, promotion, or certification.

Task340 selected-tranche next packaging follow-through closes the gap between Task338's next execution bridge and Task334's selected packaging writer without creating proof authority. Threat: a caller could replay a stale Task338 bridge, tamper with the delegated Task326 bridge, inject PM evidence outside the selected next tranche, use a complete/no-next Task338 artifact to fabricate packaging work, or make selected-tranche packaging look like global proof-breadth closure. Mitigation: the Task340 route accepts only a Task338 id/path/hash triple, re-reads Task338 and the delegated Task326 bridge by current hash, rejects missing next bridges, rejects Task338/delegated selected-tranche drift, rejects unselected PM evidence before Task334 delegation, records non-certifying append-only provenance, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no Task300/Task301 writes, no promotion, and no GA certification.

Task341 selected-tranche next packaging results follow-up closes the currentness gap after Task340 without creating closure authority. Threat: a caller could replay a stale Task340 artifact, tamper with Task340's delegated Task326 bridge or Task334 artifact after packaging, alter selected PM packaging reports after Task340, or describe selected-tranche currentness as proof-breadth completion. Mitigation: the Task341 route accepts only a Task340 id/path/hash triple, re-reads Task340 by current hash, requires Task340 to bind delegated Task326 and Task334 artifact references, delegates selected-tranche currentness to Task335, records only non-certifying append-only provenance, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no packaging writes, no Task300/Task301 writes, no promotion, and no GA certification.

Task342 selected-tranche next packaging currentness check-debug closes drift risk after Task341. Threat: later edits could drop Task340/341 from smoke/default discovery, weaken Task340's Task338 or delegated Task326 hash binding, let Task340 accept unselected PM evidence, weaken Task341's Task340 or Task335 currentness binding, expose a Pi/public command, call Task300/301 directly, or let docs imply that selected-tranche packaging/currentness closes global proof breadth. Mitigation: the Task342 focused suite asserts capability exposure, phase0/default discovery, Task340/341 source guards and focused regression strings, Task338/335/334/326/300/301 authority boundaries, route wiring, no Pi/public surface, and release-hardening documentation while adding no route, Pi consumer, Lean replay, proof synthesis, packaging writer, closure, final-audit unblock, promotion, certification, or certificate issuance.

Task343 selected-tranche next closure recheck closes the handoff gap after Task341. Threat: a caller could replay stale Task341 currentness material, tamper with selected PM packaging reports after Task341, inject proof/GA success material into the request, or describe a bounded selected-tranche recheck as global proof-breadth closure. Mitigation: the Task343 route accepts only a Task341 id/path/hash triple, re-reads that artifact by current hash, re-hashes selected canonical PM packaging reports before closure semantics, records non-certifying append-only provenance, ignores caller proof/GA material, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no packaging writes, no Task301 writes, no promotion, and no GA certification.

Task344 selected-tranche next closure execution bridge closes the handoff gap after Task343 without making the selected-tranche loop a proof authority. Threat: a caller could replay stale Task343 next-closure material, tamper with the Task343-bound Task300 closure or selected PM packaging reports, inject proof/GA success material, or make the closure-to-next-execution bridge appear to execute proofs or close global proof breadth. Mitigation: the Task344 route accepts only a Task343 next-closure recheck id/path/hash triple, re-reads that artifact by current hash, delegates stale closure and selected-report re-hash checks to the existing Task338 semantics, records caller proof/GA material as ignored, delegates only to Task338/Task326 for bounded next-tranche execution planning, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no packaging/currentness/closure/final-audit writes, no promotion, and no GA certification.

Task345 selected-tranche next closure bridge check-debug closes drift risk after Task344 landed. Threat: later edits could drop the Task343/344 suites from smoke/default discovery, remove the check-debug status capability, weaken Task344's Task343 hash binding or Task338-only delegation, add a public Pi surface, or let public docs imply that selected-tranche closure-to-execution plumbing runs Lean, writes proof artifacts, closes global proof breadth, or certifies GA. Mitigation: the Task345 focused suite asserts capability exposure, phase0/default discovery, Task343/344/338/326/300/301 source and route boundaries, no Pi surface, and release-hardening documentation while adding no route, proof execution, proof synthesis, proof-breadth closure, final-audit unblock, claim promotion, or certification authority.

Task346 selected-tranche next closure packaging follow-through closes the next handoff gap after Task344 without creating proof authority. Threat: a caller could replay a stale Task344 bridge, tamper with its delegated Task338/Task326 bridge material, inject PM evidence outside Task344's next selected tranche, use a complete/no-next Task344 artifact to fabricate packaging work, or make selected-tranche packaging look like global proof-breadth closure. Mitigation: the Task346 route accepts only a Task344 id/path/hash triple, re-reads Task344 and delegated Task338 by current hash, requires Task338-bound Task326 bridge material to remain bound, rejects missing next bridges and unselected PM evidence before Task340 delegation, records non-certifying append-only provenance, and keeps `proof_authority="none"`, no Lean execution, no Pi surface, no Task300/Task301 writes, no promotion, and no GA certification.

Task347 selected-tranche next closure packaging results follow-up closes the currentness gap after Task346 without creating proof authority. Threat: a caller could replay a stale Task346 packaging follow-through, redirect Task346-embedded Task344/Task338 provenance to a current wrong file, tamper with Task346's delegated Task340 packaging output or selected PM reports, inject proof/GA success material, or make selected-tranche packaging results look like Task300 proof-breadth closure, Task301 final-audit, or GA-certificate authority. Mitigation: the Task347 route accepts only a Task346 id/path/hash triple, re-reads Task346 by current hash, canonical-validates Task344/Task338 schema/project/id/path/no-authority flags, delegates Task340 report currentness to existing Task341/Task335 guards, preserves Task344/Task338/Task340/Task334 artifact bindings, ignores caller proof/GA material, writes no Task300/Task301/GA-certificate artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task348 selected-tranche next closure packaging/currentness check-debug is a drift guard over that mitigation. Threat: future edits could silently drop Task346/347 route/status/smoke/default discovery, weaken Task344/338 or Task341/335 currentness binding, leak a Pi surface, or blur Task300/301/303 authority separation. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries and remains non-authoritative regression coverage only.

Task349 selected-tranche next closure packaging results closure recheck closes the next Task347-to-Task343 handoff without creating new proof authority. Threat: a caller could replay a stale Task347 artifact, forge Task347-embedded Task346/344/338/340/334/341 references, tamper with selected PM packaging reports after Task347 currentness, inject proof/GA metadata, or make a selected-tranche recheck look like final audit or certificate authority. Mitigation: the Task349 route accepts only a Task347 id/path/hash triple, re-reads Task347 by current hash, recomputes embedded currentness references before delegation, lets existing Task343/Task300 semantics perform the closure recheck, writes no Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task350 selected-tranche next closure packaging results closure execution bridge closes the next Task349-to-Task344/338 handoff without creating new proof authority. Threat: a caller could replay a stale Task349 artifact, weaken Task349 no-authority flags, inject proof/GA metadata, or make a selected-tranche execution bridge look like final audit or certificate authority. Mitigation: the Task350 route accepts only a Task349 id/path/hash triple, re-reads Task349 by current hash, delegates through existing Task344/338 bridge semantics, writes no Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task351 selected-tranche next closure results-to-execution check-debug is a drift guard over that mitigation. Threat: future edits could silently drop Task349/350 route/status/smoke/default discovery, weaken Task349's Task347/343/300 binding, weaken Task350's Task349/344/338 binding, leak a Pi surface, or blur Task300/301/303 authority separation. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries and remains non-authoritative regression coverage only.

Task352 selected-tranche next closure execution packaging follow-through closes the next Task350-to-Task346/340/334 packaging handoff without creating new proof authority. Threat: a caller could replay a stale Task350 artifact, weaken Task350 no-authority flags, inject proof/GA metadata, provide evidence outside the Task350-selected tranche, or make selected-tranche packaging look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task352 route accepts only a Task350 id/path/hash triple, re-reads Task350 by current hash, delegates packaging through existing Task346/340/334 semantics, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task353 selected-tranche next closure execution packaging results follow-up closes the next Task352-to-Task347/341/335 currentness handoff without creating new proof authority. Threat: a caller could replay a stale Task352 artifact, weaken Task352 no-authority flags, drift the embedded delegated Task346 reference, inject proof/GA metadata, or make selected-tranche packaging-results currentness look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task353 route accepts only a Task352 id/path/hash triple, re-reads Task352 by current hash, recomputes delegated Task346 artifact references, reuses existing Task347/341/335 selected-tranche packaging-results currentness semantics, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task354 selected-tranche closure execution packaging results currentness check-debug is a drift guard over the Task352/353 mitigation. Threat: future edits could silently drop Task352/353 route/status/smoke/default discovery, weaken Task352's Task350/346/340/334 packaging binding, weaken Task353's Task352/347/341/335 currentness binding, leak a Pi surface, or blur Task300/301/303 authority separation. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries and remains non-authoritative regression coverage only.

Task355 selected-tranche next closure execution packaging results closure recheck closes the Task353-to-Task349/343/300 recheck handoff without creating new proof authority. Threat: a caller could replay a stale Task353 artifact, weaken Task353 no-authority flags, drift the embedded delegated Task347 reference, inject proof/GA metadata, or make a selected-tranche closure recheck look like final audit or certificate authority. Mitigation: the Task355 route accepts only a Task353 id/path/hash triple, re-reads Task353 by current hash, recomputes delegated Task347 artifact references, delegates actual selected-tranche closure recheck through existing Task349/343/300 semantics, writes no Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task356 selected-tranche next closure execution packaging results closure execution bridge closes the Task355-to-Task350/344/338 execution handoff without creating new proof authority. Threat: a caller could replay a stale Task355 artifact, weaken Task355 no-authority flags, drift the embedded delegated Task349 reference, inject proof/GA metadata, or make a selected-tranche closure execution bridge look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task356 route accepts only a Task355 id/path/hash triple, re-reads Task355 by current hash, recomputes delegated Task349 artifact references, delegates actual selected-tranche closure execution through existing Task350/344/338 semantics, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task357 selected-tranche closure recheck-to-execution check-debug is a drift guard over the Task355/356 mitigation. Threat: future edits could silently drop Task355/356 route/status/smoke/default discovery, weaken Task355's Task353/349/343/300 closure-recheck binding, weaken Task356's Task355/350/344/338 closure-execution binding, leak a Pi surface, or blur Task300/301/303 authority separation. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries and remains non-authoritative regression coverage only.

Task358 selected-tranche next closure execution packaging results closure execution packaging follow-through closes the Task356-to-Task352/346/340/334 packaging handoff without creating new proof authority. Threat: a caller could replay a stale Task356 artifact, weaken Task356 no-authority flags, drift the embedded delegated Task350 reference, inject proof/GA metadata, or make selected-tranche closure execution packaging look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task358 route accepts only a Task356 id/path/hash triple, re-reads Task356 by current hash, recomputes delegated Task350 artifact references, delegates packaging through existing Task352/346/340/334 semantics, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task359 selected-tranche next closure execution packaging results closure execution packaging results follow-up closes the Task358-to-Task353/347/341/335 packaging-results currentness handoff without creating new proof authority. Threat: a caller could replay a stale Task358 artifact, weaken Task358 no-authority flags, drift the embedded delegated Task352 reference, inject proof/GA metadata, or make selected-tranche closure execution packaging results look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task359 route accepts only a Task358 id/path/hash triple, re-reads Task358 by current hash, recomputes the delegated Task352 artifact reference, delegates currentness through existing Task353/347/341/335 semantics, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task360 selected-tranche closure execution packaging results currentness check-debug is a drift guard over the Task358/359 mitigation. Threat: future edits could silently drop Task358/359 route/status/smoke/default discovery, weaken Task358's Task356/352/346/340/334 packaging binding, weaken Task359's Task358/353/347/341/335 currentness binding, add a packaging/currentness artifact writer, leak a Pi surface, or blur Task300/301/303 authority separation. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries, preserves no packaging/currentness artifact writer semantics, and remains non-authoritative regression coverage only.

Task361 selected-tranche next closure execution packaging results closure execution packaging results closure recheck closes the Task359-to-Task355/349/343/300 closure recheck handoff without creating new proof authority. Threat: a caller could replay a stale Task359 artifact, weaken Task359 no-authority flags, drift the copied Task358/Task356/Task355 references or delegated Task353 reference, inject proof/GA metadata, or make a selected-tranche closure recheck look like final audit or certificate authority. Mitigation: the Task361 route accepts only a Task359 id/path/hash triple, re-reads Task359 by current hash, recomputes copied and delegated artifact references, delegates actual selected-tranche closure recheck through existing Task355/349/343/300 semantics only when Task359 is ready, writes no Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task362 selected-tranche next closure execution packaging results closure execution packaging results closure execution bridge closes the Task361-to-Task356/350/344/338 closure execution handoff without creating new proof authority. Threat: a caller could replay a stale Task361 artifact, weaken Task361 no-authority flags, drift the copied Task356/Task355 references or delegated Task355 reference, inject proof/GA metadata, or make a selected-tranche closure execution bridge look like proof-breadth closure, final audit, or certificate authority. Mitigation: the Task362 route accepts only a Task361 id/path/hash triple, re-reads Task361 by current hash, recomputes copied and delegated artifact references, delegates actual selected-tranche closure execution through existing Task356/350/344/338 semantics only when Task361 has a delegated Task355, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task363 selected-tranche closure execution packaging results closure execution packaging results recheck-to-execution check-debug is a drift guard over the Task361/362 mitigation. Threat: future edits could silently drop Task361/362 route/status/smoke/default discovery, weaken Task361's Task359/Task355/349/343/300 closure-recheck binding, weaken Task362's Task361/Task356/350/344/338 closure-execution binding, add a route or release writer, leak a Pi surface, run Lean, call lower producer paths directly, write Task300/Task301/Task303 artifacts, or blur closure/final-audit/certificate authority. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries, preserves no route, no Pi surface, no Lean execution, no direct lower producer calls, no claim promotion, no certificate issuance, and no GA certification semantics, and remains non-authoritative regression coverage only.

Task364 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging follow-through closes the Task362-to-Task358/352/346/340/334 packaging handoff without creating new proof authority. Threat: a caller could replay a stale Task362 artifact, weaken Task362 no-authority flags, drift copied/delegated Task356/Task355 material, inject proof/GA metadata, or make selected-tranche closure execution packaging evidence look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task364 route accepts only a Task362 id/path/hash triple, re-reads Task362 by current hash, recomputes copied and delegated artifact references, delegates selected-tranche closure execution packaging through existing Task358/352/346/340/334 semantics only when Task362 has delegated Task356 material, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task365 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results follow-up closes the Task364-to-Task359/353/347/341/335 packaging-results currentness handoff without creating new proof authority. Threat: a caller could replay a stale Task364 artifact, weaken Task364 no-authority flags, drift copied Task362/Task361/Task356/Task355 refs or the delegated Task358 ref, inject proof/GA metadata, or make selected-tranche packaging-results currentness look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task365 route accepts only a Task364 id/path/hash triple, re-reads Task364 by current hash, recomputes copied artifact references, delegates packaging-results currentness only through existing Task359 when Task364 has delegated Task358 material, writes no Task300/Task301/Task303 artifacts, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task366 selected-tranche closure execution packaging results closure execution packaging results closure execution packaging results currentness check-debug is a drift guard over the Task364/365 mitigation. Threat: future edits could silently drop Task364/365 route/status/smoke/default discovery, weaken Task364's Task362/Task358/352/346/340/334 closure execution packaging binding, weaken Task365's Task364/Task359/353/347/341/335 packaging-results currentness binding, add a route or release writer, leak a Pi surface, run Lean, call lower producer paths directly, write Task300/Task301/Task303 artifacts, or blur closure/final-audit/certificate authority. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries, preserves no route, no Pi surface, no Lean execution, no direct lower producer calls, no claim promotion, no certificate issuance, and no GA certification semantics, and remains non-authoritative regression coverage only.

Task367 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results closure recheck closes the Task365-to-Task361/355/349/343/300 closure-recheck handoff without creating new proof authority. Threat: a caller could replay a stale Task365 artifact, weaken Task365 no-authority flags, drift copied Task364/Task362/Task361/Task356/Task355 refs or delegated Task359 material, inject proof/GA metadata, or make selected-tranche closure recheck look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task367 route accepts only a Task365 id/path/hash triple, re-reads Task365 by current hash, recomputes copied artifact references, delegates closure recheck only through existing Task361 when Task365 has delegated Task359 material, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task368 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results closure execution bridge closes the Task367-to-Task362/356/350/344/338 closure-execution handoff without creating new proof authority. Threat: a caller could replay a stale Task367 artifact, weaken Task367 no-authority flags, drift copied Task365/Task364/Task362/Task361/Task356/Task355 refs or delegated Task361 material, inject proof/GA metadata, or make selected-tranche closure execution look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task368 route accepts only a Task367 id/path/hash triple, re-reads Task367 by current hash, recomputes copied artifact references, delegates closure execution only through existing Task362 when Task367 has delegated Task361 closure-recheck material, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task369 selected-tranche closure execution packaging results closure execution packaging results closure execution packaging results recheck-to-execution check-debug is a drift guard over the Task367/368 mitigation. Threat: future edits could silently drop Task367/368 route/status/smoke/default discovery, weaken Task367's Task365/Task361/355/349/343/300 closure recheck binding, weaken Task368's Task367/Task362/356/350/344/338 closure execution bridge binding, add a route or release writer, leak a Pi surface, run Lean, call lower producer paths directly, write Task300/Task301/Task303 artifacts, or blur closure/final-audit/certificate authority. Mitigation: the focused check-debug suite statically revalidates those source and documentation boundaries, preserves no route, no Pi surface, no Lean execution, no direct lower producer calls, no claim promotion, no certificate issuance, and no GA certification semantics, and remains non-authoritative regression coverage only.

Task370 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results closure execution packaging follow-through closes the Task368-to-Task364/358/352/346/340/334 closure-execution-packaging handoff without creating new proof authority. Threat: a caller could replay a stale Task368 artifact, weaken Task368 no-authority flags, drift delegated Task362 material, inject proof/GA metadata, or make selected-tranche closure execution packaging look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task370 route accepts only a Task368 id/path/hash triple, re-reads Task368 by current hash, validates delegated Task362 by current artifact reference, delegates closure execution packaging only through existing Task364 when Task368 has delegated Task362 material, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task371 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results closure execution packaging results follow-up closes the Task370-to-Task365/359/353/347/341/335 packaging-results currentness handoff without creating new proof authority. Threat: a caller could replay a stale Task370 artifact, weaken Task370 no-authority flags, drift delegated Task364 material, inject proof/GA metadata, or make selected-tranche packaging-results currentness look like Task300 closure, Task301 final audit, or Task303 certificate authority. Mitigation: the Task371 route accepts only a Task370 id/path/hash triple, re-reads Task370 by current hash, validates delegated Task364 by current artifact reference, delegates packaging-results currentness only through existing Task365 when Task370 has delegated Task364 material, exposes no Pi surface, and keeps `proof_authority="none"`, no Lean execution, no promotion, and no GA certification.

Task255 adds service-owned Pi goal-mode intake manifests. Threat: user-supplied paper paths, TeX/PDF/Markdown attachments, workspace refs, absolute host paths, or Pi payload text could be mistaken for proof authority, trusted `.comath` state, or safe-to-display provenance, and prompt-injection content inside documents could try to override Lean authority. Mitigation: `/campaign/start` writes the intake manifest only through `comathd`, normalizes refs to project-relative paths, checks project-root confinement and realpath escape, hashes only existing files, records directories without recursive proof claims, redacts raw host roots, exposes only manifest path/hash plus non-authority flags in goal export, and labels all external inputs as hint/evidence/refutation sources. The manifest cannot promote claims, run Lean, satisfy clean replay, or certify GA.

Task256 adds service-owned Pi goal-mode research plans. Threat: a plan that names maintained ingestion, retrieval, theorem-search, or lemma-planning adapters could be misread as executed retrieval evidence, trusted document extraction, Lean proof progress, or promotion readiness. Mitigation: the knowledge-pack stage writes only `goal_mode_research_plan.json` through `comathd`, binds the Task255 intake manifest hash, records adapter tasks as planned-not-executed, records `network_execution_performed=false`, keeps local refs project-relative, marks every task and lemma-planning seed `proof_authority=none`, and requires later service-owned adapter run manifests before any retrieved source, extracted claim, theorem-search hit, or candidate Lean file can enter evidence gates.

Task257 adds service-owned Pi goal-mode adapter execution manifests. Threat: a manifest that enumerates adapter runs could be mistaken for completed retrieval, trusted document extraction, theorem-search evidence, or promotion-grade proof progress; tampered local inputs could also be silently carried from intake into later evidence extraction. Mitigation: the knowledge-pack stage writes only `goal_mode_adapter_execution_manifest.json` through `comathd`, binds the Task256 research-plan hash, preserves the Task255 intake-manifest hash binding, re-checks local file hashes before recording local run envelopes, blocks mismatched inputs with `goal_mode_input_hash_mismatch`, records directory refs without recursive claims, keeps `network_execution_performed=false`, marks live provider tasks as `goal_mode_live_adapter_execution_required`, and leaves every run with `proof_authority=none`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task258 adds service-owned Pi goal-mode local ingestion evidence. Threat: local Markdown or TeX text could contain prompt injection or proof-authority overclaims, and PDF or external Lean repository refs could be silently treated as parsed evidence even though CoMath has no maintained parser/repo adapter in this slice. Mitigation: the knowledge-pack stage writes only `goal_mode_local_ingestion_evidence.json` through `comathd`, consumes the Task256 research-plan hash and Task257 adapter-execution manifest hash, reuses the Task255 intake-manifest binding, treats document text as data rather than instructions, extracts only line-anchored Markdown/TeX theorem or lemma drafts after prompt-injection scan, redacts blocked injected text from extracted claims, blocks PDF refs with `goal_mode_pdf_ingestion_adapter_required`, blocks external Lean repo refs with `goal_mode_external_lean_repo_inspection_required`, and leaves every record with `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`.

Task259 adds service-owned Pi goal-mode skeleton formalization hints. Threat: extracted paper theorem or lemma text could drift the locked statement, bypass FormalSpecLock approval, or be misread as literature/theorem-search/Lean proof evidence once it reaches the skeleton stage. Mitigation: the skeleton gate writes only `proof/formalization_hints.json` through `comathd`, binds the consumed `knowledge_pack.json` and Task258 local evidence by path/hash, accepts only prompt-injection-scanned formalization-candidate claim kinds, records `can_change_locked_statement=false`, requires FormalSpecLock approval plus StatementDiffGate for any statement change, keeps every hint `proof_authority=none`, and makes the hint artifact a required skeleton-gate artifact whose deletion rewinds later stages to `skeleton_gate`.

Task260 adds service-owned Pi goal-mode skeleton blueprints. Threat: planning steps derived from document hints could be mistaken for a proof route, silently create extra obligations, or weaken the locked theorem boundary before Lean replay. Mitigation: the skeleton gate writes only `proof/blueprint.json` through `comathd`, binds `proof/formalization_hints.json` by path/hash, carries the blueprint path/hash into `plan.json`, `lemma_dag.json`, `line_map.json`, and `skeleton_report.md`, records each step as `proof_authority=none`, and explicitly sets `can_create_proof_obligation_without_formal_spec_lock=false`, `can_change_locked_statement=false`, and `requires_statement_diff_gate=true`. The blueprint cannot promote claims, satisfy Lean replay, certify GA, or add proof obligations without later locked-statement gates.

Task261 adds blueprint-bound Pi goal-mode candidate generation. Threat: candidate generation could run from stale or tampered blueprint metadata, or agents could mistake blueprint context for proof authority once it reaches eight variant task cards. Mitigation: `line_map_gate` writes `candidate_generation_request.json` with current `proof/blueprint.json` and `proof/formalization_hints.json` path/hash bindings, blueprint step ids, and statement-boundary guards; `candidate_generation` re-checks those bindings before spawning candidates; tampered blueprint material fails closed; and `candidate_generation.json` plus every `lemma_sprint` task card carries the blueprint only as `proof_authority=none` planning context. Blueprint metadata, agent output, votes, and candidates remain unable to satisfy Lean replay, promote claims, or certify GA.

Task262 adds blueprint-bound Lean candidate attempt materialization. Threat: generated candidate Lean files could be mistaken for checked proof artifacts, or `sorry` placeholders could leak into promotion paths because the files live in candidate workspaces. Mitigation: `runGaAgentStageCandidates()` writes `lean_candidate_attempt_plan.json` and `LeanCandidate.lean` only as service-owned draft material, binds the source `proof/Skeleton.lean`, current skeleton blueprint, blueprint step ids, and statement boundary, and records `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`. The candidate manifest and agent output expose these files as draft attempt artifacts only; they do not satisfy LeanRunManifest evidence, final clean replay, claim promotion, or GA certification.

Task263 adds service-owned Lean candidate attempt preflight. Threat: a static pass over `LeanCandidate.lean` files could be mistaken for Lean replay, or a `sorry`-bearing draft could enter arbitration as proof-grade material after being "checked" by a non-kernel gate. Mitigation: `candidate_verification` writes `lean_candidate_attempt_check_report.json` through `comathd`, re-hashes each attempt plan and draft Lean file, verifies source skeleton and blueprint bindings, checks locked statement boundary consistency, detects `sorry`, and routes such candidates to repair-required evidence with `lean_runner_invocations=0` and empty LeanRunManifest paths. The report remains `proof_authority=none`, cannot promote claims, cannot certify GA, and cannot satisfy LeanRunManifest or final clean replay requirements.

Task264 adds service-owned Lean attempt repair routing. Threat: routing a blocked candidate into `repair` could be mistaken for proof progress, or a repair task could become a place to weaken the locked statement, emit a fake proof claim, or smuggle LeanRunManifest-shaped evidence before Lean has run. Mitigation: `candidate_arbitration` writes only `lean_candidate_attempt_repair_batch.json` and per-candidate `lean_candidate_repair_task.json` work orders through `comathd`, binds the Task263 check report by path/hash, assigns A0/A5/A8 roles, forbids statement changes without FormalSpecLock and StatementDiffGate, forbids proof-claim and replay-manifest outputs, keeps `lean_runner_invocations=0`, and records `proof_authority=none`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task265 adds service-owned repair execution and re-ingestion. Threat: removing `sorry` from a draft could be mistaken for having found a proof, especially if a placeholder-shaped Lean term reaches candidate verification. Mitigation: the repair stage writes snapshots, repaired drafts, `lean_candidate_attempt_repair_execution.json`, and per-candidate execution manifests only as candidate material; repaired drafts carry an explicit `comath_repair_placeholder`, the execution manifest keeps `lean_runner_invocations=0`, empty LeanRunManifest paths, `proof_authority=none`, `can_promote_claim=false`, and `can_certify_ga=false`, and the campaign must return to `candidate_verification` before any later service-owned LeanRunner attempt can classify the candidate.

Task266 adds service-owned LeanRunner execution over ready repaired attempts. Threat: a failed Lean run, a runner shim, or a placeholder-bearing candidate could be confused with proof authority because a LeanRunManifest exists. Mitigation: `candidate_verification` separates static ready preflight from actual runner execution, failed manifests and all-rejected blockers keep `proof_authority=none`, promotion and GA flags false, candidate manifests are updated to failed with hard vetoes, and placeholder-bearing attempts are rejected as non-authoritative even if a test runner exits 0. Only later clean Lean replay and the ordinary authority gates may promote a claim.

Task267 adds service-owned LeanRunner failure feedback into the repair loop. Threat: failed Lean stdout/stderr could be treated as a proof oracle, a terminal blocker could become an unreviewed state rewind, or a no-sorry failed candidate could loop without changing repair inputs. Mitigation: terminal resume is allowed only for explicit goal-mode campaigns with canonical `all_attempts_rejected` LeanRunner execution, the feedback batch binds LeanRunManifest/stdout/stderr paths and hashes with `proof_authority=none`, the second repair batch forbids LeanRunner/FinalReplayManifest/proof-claim outputs, and repair execution records `comath_repair_feedback` markers before returning to `candidate_verification`. Failed Lean diagnostics remain non-authoritative repair evidence and cannot promote claims or certify GA.

Task268 adds non-authoritative external-wheel repair hints to that feedback loop. Threat: theorem-search, retrieval, CAS/SMT, proof-search backend, or external Lean repo descriptors could be mistaken for proof evidence, live provider results, or promotion-grade citation/computation support; a stale hint bundle could silently steer repair after its sources changed. Mitigation: the hint bundle is descriptor-derived and network-free, binds source research-plan/adapter-manifest/local-ingestion/feedback artifacts by path/hash, sets `proof_authority=none`, `network_execution_performed=false`, and all promotion/GA flags false, and repair execution re-hashes the bundle before writing candidate drafts. Hints only add `comath_repair_hints` markers and repair context; they cannot write LeanRunManifest/FinalReplayManifest artifacts, change the locked statement, promote claims, or certify GA.

Task269 adds service-owned repair hint execution artifacts to that same loop. Threat: stubbed theorem-search, retrieval, CAS/SMT, proof-search backend, or external Lean repo results could be mistaken for live provider evidence, proof authority, or promotion-grade support; a stale execution artifact could steer repair after the bound hint bundle changed. Mitigation: the execution artifact binds the source feedback batch and hint bundle by path/hash, records only stubbed result summaries with request/result hashes, provider terms, capability metadata, and `external_adapter_result_has_no_proof_authority` vetoes, sets `proof_authority=none`, `network_execution_performed=false`, `live_provider_execution_performed=false`, and all promotion/GA flags false, and repair execution re-hashes the execution artifact before writing candidate drafts. Results only add `comath_repair_hint_execution` markers and repair context; they cannot write LeanRunManifest/FinalReplayManifest artifacts, change the locked statement, promote claims, or certify GA.

Task273 adds opt-in live retrieval repair-hint execution for Jina Search-compatible endpoints. Threat: a live provider response could leak host configuration, persist bearer credentials, inject instructions into repair flow, or be mistaken for proof/citation authority. Mitigation: live execution is gated by explicit env opt-in and provider allowlist, `COMATH_JINA_API_KEY` is sent only as an in-memory bearer header, persisted artifacts store sanitized request/response hashes and provider status but not the token or host root, retrieved content is represented by content hash plus prompt-injection scan, and every live result keeps `proof_authority=none`, `result_can_be_used_as_proof=false`, and `external_adapter_result_has_no_proof_authority` vetoes. Repair can consume the result as context only; it still cannot write LeanRunManifest/FinalReplayManifest artifacts, change the locked statement, promote claims, or certify GA.

Task274 lets that live retrieval context contribute bounded Lean repair suggestions. Threat: fenced provider text could be treated as proof authority, smuggle an altered theorem statement, include `sorry`/holes/axioms/unsafe commands, overwrite failed LeanRunManifest evidence, or bypass LeanRunner after an all-rejected terminal blocker. Mitigation: suggestion extraction runs only after a passing prompt-injection scan, records bounded fenced Lean snippets with hashes and no-authority flags, accepts only same-name theorem/lemma suggestions whose normalized statement exactly matches the locked candidate, rejects cheat surfaces (`sorry`, holes, `axiom`, `unsafe`, `opaque`, `admit`, `run_cmd`, `elab`, `#eval`, `set_option`), and reruns the repaired candidate through service-owned LeanRunner using a Lean-file-hash-specific run id. Suggestions remain candidate material only; final authority still requires downstream clean replay and promotion gates.

Task275 binds live-retrieval repair provenance into final replay material. Threat: an old failed LeanRunner veto could persist after a repaired candidate passes, causing a valid candidate to be blocked or tempting operators to ignore hard vetoes; conversely, a final replay package could hide that a candidate was shaped by non-authoritative provider text. Mitigation: a passing repaired LeanRunner rerun clears only the transient `service_owned_lean_runner_rejected_candidate_attempt` veto for that candidate and leaves all other hard vetoes intact; the selected candidate descriptor and final replay material source carry path/hash-bound repair provenance for the repair hint execution, campaign repair execution, and per-candidate repair execution, all with `proof_authority=none` and `can_promote_claim=false`. The provenance is audit evidence, not proof authority; final proof status still depends on clean Lean replay.

Task276 adds opt-in live theorem-search repair-hint execution for Loogle-compatible endpoints. Threat: a live theorem-search response could persist bearer credentials, leak host configuration, inject instructions into repair flow, or be mistaken for proof authority because it returns Lean declarations. Mitigation: live execution is gated by explicit env opt-in, provider allowlist, and configured base URL; `COMATH_LOOGLE_API_KEY` is sent only as an in-memory bearer header; persisted artifacts store sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, bounded declaration result summaries, and no secrets or host roots; every result keeps `proof_authority=none`, `result_can_be_used_as_proof=false`, and `external_adapter_result_has_no_proof_authority` vetoes. Repair can consume declarations as context only; it cannot write LeanRunManifest/FinalReplayManifest artifacts, change the locked statement, promote claims, or certify GA.

Task277 lets one live theorem-search declaration guide a narrow conjunction repair. Threat: seeing `And.intro` in a live result could be overgeneralized into proof search, could hide statement drift, or could make declaration lookup look like proof authority. Mitigation: repair accepts only a live, prompt-injection-scanned, non-authoritative `theorem_search:loogle` result containing `And.intro`; the current theorem/lemma statement must normalize exactly to `True /\\ True` or `True ∧ True`; the repair may materialize only `exact And.intro trivial trivial` for the same declaration name; and the result remains candidate material with `proof_authority=none`, no promotion, no GA certification, and no final authority until service-owned LeanRunner plus clean replay pass.

Task278 adds opt-in live computation repair-hint execution for SymPy-compatible endpoints. Threat: a live computation response could persist bearer credentials, leak host configuration, inject instructions into repair flow, or be mistaken for proof authority because it reports exact symbolic output. Mitigation: live execution is gated by explicit env opt-in, provider allowlist, and configured base URL; `COMATH_SYMPY_API_KEY` is sent only as an in-memory bearer header; persisted artifacts store sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, bounded allowlisted computation report summaries, and no secrets or host roots; every result keeps `proof_authority=none`, `result_can_be_used_as_proof=false`, and `external_adapter_result_has_no_proof_authority` vetoes. Repair can consume the computation report as context only; it cannot write LeanRunManifest/FinalReplayManifest artifacts, change the locked statement, promote claims, or certify GA.

Task279 lets one live computation result guide a narrow `by decide` repair. Threat: an exact-symbolic `True` report could be overgeneralized into CAS proof authority, hidden statement drift, arbitrary simplification, or import inference. Mitigation: repair accepts only a live, prompt-injection-scanned, non-authoritative `computation:sympy` result whose bounded report is exact-symbolic and normalized to `True`; the current theorem/lemma statement must be a same-name closed decidable arithmetic equality over allowlisted identifiers; and the repair may materialize only `by decide`. The result remains candidate material with `proof_authority=none`, no promotion, no GA certification, and no final authority until service-owned LeanRunner plus clean replay pass.

Task280 exposes live computation repair provenance in public goal-mode export. Threat: public export could hide that a final proof candidate was shaped by non-authoritative SymPy output, or conversely could make that provenance look like proof evidence. Mitigation: export reads provenance only after final Lean clean replay authority has passed, copies only the already-bound selected-candidate final replay material provenance, validates `proof_authority=none`, `can_promote_claim=false`, `result_can_be_used_as_proof=false`, permits only relative path/hash references, and rejects host-root leakage. The exported provenance is an audit trail for candidate shaping, not proof authority.

Task283 lets one live theorem-search declaration guide a narrow iff repair. Threat: seeing `Iff.intro` in a live result could be overgeneralized into proof search, could hide statement drift, or could make declaration lookup look like proof authority. Mitigation: repair accepts only a live, prompt-injection-scanned, non-authoritative `theorem_search:loogle` result containing `Iff.intro`; the current theorem/lemma statement must normalize exactly to `True <-> True`; the repair may materialize only `exact Iff.intro (fun _ => trivial) (fun _ => trivial)` for the same declaration name; and the result remains candidate material with `proof_authority=none`, no promotion, no GA certification, and no final authority until service-owned LeanRunner plus clean replay pass.

Task270 hardens the repair-to-LeanRunner boundary for placeholder-free candidates. Threat: comments containing `sorry`, missing theorem targets, Lean holes, or `?comath_repair_placeholder` could be misclassified as proof-ready; an inline theorem header with `:= by sorry` could be treated as a solved native adapter result; a bounded `True` repair could be overclaimed as proof promotion. Mitigation: static preflight strips Lean line comments before `sorry`/hole detection, requires theorem/lemma declarations, treats placeholders and holes as repair-required, blocks incomplete inline headers from native-adapter solved parsing, and records placeholder-free `True` rewrites as candidate material only. LeanRunner success may route to arbitration/red-team flow, but final proof authority still requires downstream clean replay and promotion gates.

Task271 hardens the candidate-to-final-replay handoff. Threat: a LeanRunner-passing candidate workspace could be treated as final replay material even though it is a draft-shaped `LeanCandidate` project, contains comment-only placeholder text, lacks campaign-bound FormalSpecLock/AssumptionLedger scope, or cannot be copied into the clean replay workspace. Mitigation: accepted candidates preserve their original LeanRunner evidence and write a separate `final_replay_project` with comment-stripped `MathResearch/Target.lean`, audit file, Lake files, campaign-bound FormalSpecLock/AssumptionLedger records, and a descriptor consumed by the existing integration/final replay gates. Only FinalReplayManifest v3 plus derived bindings and ordinary promotion may mark `completed_formal_proof`.

Task272 hardens the first non-`True` placeholder-free repair class. Threat: a syntactic `rfl` shortcut could be over-broadened into expression normalization, hidden statement drift, or proof-search-like equality repair; a repair-stage artifact could be mistaken for final proof authority. Mitigation: the repair executor only selects `locked_reflexive_equality_rfl` for locked theorem/lemma statements with one top-level equality whose sides are textually identical after whitespace normalization, keeps repair output as candidate material, and still requires service-owned LeanRunner plus final clean replay, FinalReplayManifest v3 verification, derived bindings, and ordinary promotion before any proof claim.

Task233 adds check-debug coverage over the Task220-231 operator/service transport chain. Threat: accumulated lease, heartbeat, terminal review, maintained contract, continuity, and prepared handoff wording could drift into implying live stream ownership, direct Pi mutation, proof authority, or release readiness. Mitigation: the focused Task233 suite exercises only existing bounded producers and consumers, keeps the fixed `GET /agent/run/<run_id>/log-session` plus Pi `fetch`/`getText` primitive contract visible, preserves duplicate-id/stale-contract/missing-hash fail-closed behavior, and verifies public/audit surfaces force proof, GA, live-stream, long-stream, direct-Pi-write, and trusted-state-mutation flags false. It does not add a new route, Pi tool, background process, WebSocket/SSE owner, Lean run, or release certification path.

Task213 adds a campaign-native live Mathlib replay breadth gate to the Lean-authority surface. Mitigation: final replay requests that opt into `campaign_live_mathlib_non_toy` must be campaign-scoped, use `primary_dependency: "Mathlib"`, import Mathlib in the theorem source, and reject toy smoke evidence such as `True`, default `n : Nat`, `by trivial`, `by omega`, or positive-matrix release paths before final replay workspace allocation. This gate is anti-confusion metadata and preflight enforcement only; it does not prove the theorem without the service-owned Lean clean replay and promotion gates.

Task214 adds a Mathlib dependency-material gate to the same opt-in Lean-authority surface. Mitigation: campaign-native Mathlib final replay requests must provide a Mathlib `require` in `lakefile.lean`, a `lake-manifest.json` mathlib package pinned to a 40-hex commit SHA, a trusted `leanprover-community/mathlib4` source URL, a non-unknown license, and no local `Mathlib` module shadowing before final replay workspace allocation. The blocker artifact remains non-authoritative and cannot promote a claim.

Task215 upgrades the final clean replay dependency artifact from the legacy nonempty-file closure to `DependencyClosureV2`. Mitigation: final replay writes V2 `dependency_closure.json` content with package pins/licenses/trust, import closure, local shadowing, symlink escape, and hard-veto material, and FinalReplayManifest v3 dependency locks bind the V2 package revision list. A failed V2 dependency closure fails final replay even if Lean/Lake command stdout appears successful.

Task216 adds a campaign-native Mathlib provisioning diagnostic to the same Lean-authority surface. Mitigation: Task213/214-valid `campaign_live_mathlib_non_toy` requests must have locally materialized `.lake/packages/mathlib` source material before final replay workspace allocation, the package file hashes are recorded as non-authoritative provenance, and final clean replay copies the materialized package into its clean workspace when present. Missing, empty, or symlink-bearing material fails closed as a replayable blocker; provisioning metadata remains `proof_authority=none` and cannot promote a claim without the later network-disabled Lean clean replay and ordinary promotion gates.

Task217 hardens FinalReplayManifest v3 dependency-lock verification. Mitigation: manifest verification keeps the `lean-toolchain`, `lakefile.lean`, and `lake-manifest.json` dependency-lock paths fixed to the clean workspace, recomputes their hashes, binds `lean_toolchain` text to the clean `lean-toolchain` file, verifies `external_revisions_sha256`, and compares dependency-lock external revisions to `DependencyClosureV2` package material when the dependency closure report is V2. Tampered lock paths, hashes, toolchain text, or package material fail closed before local evidence can satisfy final-replay verification.

Task218 adds a campaign-native Mathlib host replay diagnostic before final replay allocation. Mitigation: Task213-216-valid `campaign_live_mathlib_non_toy` requests must write `mathlib_host_replay_diagnostic.json` with service-owned Lean/Lake version probes, expected Lean toolchain match, binary hashes, replay plan, provisioning-diagnostic hash, safe replay arguments, and lakefile-declared build targets. Missing Lean/Lake binaries, failed probes, toolchain mismatch, unsafe replay arguments, or undeclared build targets fail closed as replayable blockers before `.comath/lean/final_replay` is allocated. Windows command-script Lean/Lake helpers are invoked only after replay arguments are checked for command metacharacters. This diagnostic remains `proof_authority=none`, records no executable absolute paths, and cannot promote a claim without the later network-disabled Lean clean replay and ordinary promotion gates.

Task219 adds a campaign-native Mathlib import-graph diagnostic before final replay allocation. Mitigation: Task213-218-valid `campaign_live_mathlib_non_toy` requests must write `mathlib_import_graph_diagnostic.json` with service-owned `lake env lean --deps` theorem/audit probes, command argv, exit codes, stdout/stderr/output hashes, host-diagnostic hash, and deps-stdout-only coarse Mathlib presence. Failed probes, empty deps stdout, stderr-only dependency mentions, option-shaped theorem/audit file args, or missing primary-dependency evidence fail closed before `.comath/lean/final_replay` is allocated. CoMath records this maintained-tool output as non-authoritative provenance only; it must not grow into a CoMath-owned Lean parser, Lake import resolver, theorem generator, or proof authority.

Task238 adds a no-download fixture preflight for prepared campaign-native Mathlib material. Mitigation: the preflight composes the Task214 dependency-material gate and Task216 provisioning diagnostic before host/import diagnostics, requires pinned trusted licensed Mathlib manifest material plus an already materialized `.lake/packages/mathlib`, rejects local `Mathlib` shadowing, floating revisions, missing package material, and package-internal symlinks, and records only local package hashes. It executes no Lean/Lake command, attempts no dependency download, allocates no final replay workspace, and remains `proof_authority=none`; readiness for Task218/219 diagnostics is not proof authority or claim promotion.

Task220 hardens bounded Pi/operator transport leases against route-text confusion. Mitigation: lease opening now parses the requested route as `/agent/run/<run-id>/log-session`, requires it to match the recovery checkpoint's canonical log-session route, proves it is readable through the service-owned AgentRun log-session formatter, canonicalizes the persisted route, and records cursor/event-count/body-hash provenance with `proof_authority=none`. Guided real-Pi execution rebinds persisted lease provenance against current service-owned AgentRun logs and rejects expired leases, tampered body hashes/cursors/event counts, old artifacts without this binding, wrong-project runs, wrong-recovery routes, or malformed route material. This is bounded observability only: arbitrary route text, secrets, stale run ids, or success-shaped transport wording cannot create or consume a lease, and the binding still does not provide durable long-lived SSE/WebSocket transport, proof authority, or GA certification.

Task221 adds a bounded heartbeat/rebind checkpoint to the same transport surface. Mitigation: heartbeat recording must read a live Task220-bound lease, re-run the service-owned AgentRun log-session formatter for the requested cursor, bind the current session/recovery/lease artifact hashes, and write only a new append-only heartbeat artifact. It rejects expired leases, static lease tampering, non-monotonic live-log rebinds, wrong-project or wrong-chain artifacts, and stale client epochs before any heartbeat file is written; same-route/same-run live log growth is accepted only as bounded rebind provenance. The Pi consumer exposes `/cm:release lifecycle-operator-transport-heartbeat` through host confirmation, strips confirmation ids, sanitizes host paths/secrets/proof-success/long-lived transport claims, and keeps lease-kind vocabulary in parity with comathd. Heartbeats are not lease mutation, proof authority, durable long-lived SSE/WebSocket, a CoMath transport stack, or GA certification.

Configured provider-helper execution args-prefix environment variables may be mistaken for an executed sandbox profile or public helper script. Mitigation: provider-specific `*_HELPER_ARGS_JSON` handles and the fallback args-prefix handle are host configuration only; public manifests record only the args-prefix hash and count, never the prefix argument values or helper script paths. The configured prefix can help run a service-owned helper asset after validated host binding, but helper execution still does not prove OS enforcement and cannot satisfy readiness without later canonical collected probe/evidence artifacts.

### Bundled Provider Helper Protocol Confusion

The bundled CoMath provider-helper protocol asset may be mistaken for a production OCI/Nix/Firejail/AppContainer sandbox helper because it can pass the fixed self-test and emit runtime attestation under service-owned argv/env. Mitigation: the bundled asset is packaged protocol material only. It runs through the current Node executable, records only executable hashes plus args-prefix hash/count, exposes no helper paths or raw stdout/stderr text, keeps `proof_authority=none`, `adapter_execution_isolation.os_enforced=false`, and `can_certify_ga=false`, and cannot satisfy readiness unless a separate canonical service-owned probe/evidence artifact passes the readiness review.

### Provider Helper Host Validation Confusion

Provider-helper host-validation manifests may be mistaken for executed sandboxing because they bind a service-owned helper binary hash, supported platform list, fixed environment policy, and possibly a successful provider-helper self-test. Mitigation: host-validation manifests require a ready provider-runner manifest plus a matching service-owned provider host capability probe, reject caller helper-host-ready booleans, command/argv/env/hash/version/self-test metadata, record blockers for missing validators, binary mismatches, platform mismatches, missing or mismatched host capability probes, and failed self-tests, and cannot satisfy readiness without later canonical collected probe/evidence artifacts. Pi host-validation consumers are host-confirmed thin clients that forward only sanitized `platform` / `notes` diagnostics and cannot promote caller helper-readiness metadata.

### Provider Helper Execution Confusion

Provider-helper execution attempt manifests may be mistaken for collected OS-enforcement evidence because they record a service-owned helper process exit code and stdout/stderr/transcript hashes. Mitigation: helper execution manifests require a ready provider-runner manifest plus a prior service-owned provider-helper host-validation artifact that is validated and exactly bound to the same project, adapter, backend, provider, launch artifact, runner artifact, runner binary hash, and helper binary hash before any helper config resolver or helper process can run. Missing, unvalidated, or mismatched host-validation artifacts fail closed. Caller-supplied helper command, argv, env, exit codes, stdout/stderr hashes, host-ready booleans, and route payloads are ignored. A host-bound helper exit code of 0 records only an attempted helper execution and cannot satisfy readiness without later canonical Task170/Task172 collected probe/evidence artifacts.

### Provider Helper Collection Confusion

Provider-helper collection bridge manifests may be mistaken for readiness evidence because they can contain a canonical probe result when a service-owned collector succeeds. Mitigation: the route cannot accept collector callbacks or caller-supplied OS-enforcement booleans, exit codes, stdout/stderr/transcript hashes, or provider-tool witness hashes. The internal collector must bind to the ready helper execution, provider runner, and launch artifacts, and its exit/stdout/stderr/transcript hashes must exactly match the helper execution manifest before `comathd` writes canonical Task170 probe/evidence artifacts. A matching hash set is not enough: missing or false service-owned process, filesystem, network, no-new-privileges, escape-prevention, collection-source, helper-exit facts, or provider-tool execution witness binding produce explicit blockers. The witness must bind service-derived executable/profile/fixed-argv-template hashes plus the current helper transcript and cannot be replaced by self-attested 64-character hash strings. The collection wrapper manifest itself remains non-authoritative, keeps its top-level `adapter_execution_isolation.os_enforced=false`, and is rejected by the readiness gate; only the canonical probe/evidence artifact with complete facts and provider-tool witness binding can be reviewed.

## Residual Risks

- Pattern-based secret scanning is not full DLP.
- Service-level network-denial metadata is not equivalent to OS/kernel-enforced network isolation.
- Adapter sandbox-launch preflight is not equivalent to actually executing adapters inside an OS-enforced sandbox.
- Adapter provider-runner contracts, provider host capability probes, provider-helper host validations, provider-helper execution attempts, provider-helper collection bridge manifests, sandbox-execution probe bridging, and Pi consumer wiring are not equivalent to broad production OS-enforced execution across OCI/Nix/Firejail/AppContainer helpers and hosts.
- Injected-client Codex API tests are not live production account validation.
- Broad matrix-style proof campaigns are final-release-candidate audit debt. During product-completion work, representative acceptance harnesses and 1-3 theorem-specific Lean/mathlib smokes may validate product behavior, but they are not broad mathematical coverage or proof authority.
- Documentation must continue to distinguish implemented trust gates from final global GA completion.
