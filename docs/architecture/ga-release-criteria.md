# GA Release Criteria

This document is the Goal 3 public-release gate. It is stricter than historical Phase 18-81 vertical-slice readiness and must be read together with `docs/progress/goal-3-ga-gap-matrix.md` and `goal-3/tasks.md`.

macOS is outside the current GA environment adaptation scope.

## Release Positioning

Allowed wording:

```text
CoMath is an open-source agentic formal mathematics workbench built around Lean4/mathlib. It does not implement its own theorem prover or mathematical kernel. It orchestrates external proof, search, retrieval, computation, and agent tools, and promotes a mathematical claim only after a clean Lean replay and integrity audit pass.
```

Forbidden wording:

```text
CoMath proves arbitrary mathematics by itself.
CoMath verifies theorem truth independently of Lean.
CoMath replaces mathlib.
CoMath has a proprietary internal theorem library.
CoMath agents can certify proofs by vote.
CoMath uses CAS or papers as formal proof authority.
```

## Hard GA Blockers

Any one of these blocks a GA release:

- A production path imports a theorem-family recognizer, Nat-linear synthesizer, default `n : Nat` injection, synthetic V1 winner, or business-layer theorem verifier.
- A claim can reach `formally_checked` without a service-owned FinalReplayManifest whose result is `pass` and matching FinalAuthorityPackagingV3 source report / generic Lean Authority v3 packaging evidence.
- A proof claim lacks FormalSpecLock, AssumptionLedger, dependency lock, toolchain hash, artifact hash, LeanRunManifest, and final replay material.
- Candidate, literature, computation, agent vote, reviewer approval, or MathProve-style audit output can override Lean replay failure.
- Pi or an agent can write trusted `.comath/` proof state directly.
- Adapter OS-isolation readiness can be satisfied by caller-supplied request metadata, Pi payloads, operator attestations, contract-only metadata, or package launch metadata instead of service-owned collector evidence.
- Adapter OS-isolation sandbox-launch preflight manifests can be treated as proof of executed OS isolation, as readiness-review evidence, or as GA certification without a later service-owned collected execution probe.
- Adapter OS-isolation provider-runner contract manifests, fixed argv templates, service-owned unavailable blockers, or route payloads can be treated as executed OS isolation, readiness-review evidence, broad provider support, mathematical proof authority, or GA certification.
- Provider host capability probe manifests, default service-owned host capability probe diagnostics, Windows AppContainer host facility diagnostics, OCI container host facility diagnostics, Nix sandbox / Firejail host facility diagnostics, discovered host tools, platform/kernel feature facts, bundled protocol asset metadata, probe exit status, diagnostics, or route payloads can be treated as provider-helper readiness, helper self-test/runtime-attestation evidence, executed OS isolation, collected OS-enforcement evidence, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.
- Provider-helper host validation can run without a matching service-owned observed provider host capability probe, or a host capability binding can be treated as readiness-review evidence, executed OS isolation, collected OS-enforcement evidence, proof authority, real-Pi execution evidence, broad provider support, or GA certification.
- Pi `/cm:release agent-adapter-os-isolation-provider-host-capability-probe` or `/cm:release agent-adapter-os-isolation-provider-helper-host-validation` payloads, tool outputs, host confirmation prompts, caller-supplied platform/notes, or success-shaped environment fields can be treated as provider-helper readiness, executed OS isolation, collected OS-enforcement evidence, direct `.comath/` writes, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.
- Configured provider-helper asset environment variables, including `COMATH_AGENT_ADAPTER_OSISO_OCI_HELPER`, `COMATH_AGENT_ADAPTER_OSISO_NIX_HELPER`, `COMATH_AGENT_ADAPTER_OSISO_FIREJAIL_HELPER`, `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_HELPER`, their `*_HELPER_ARGS_JSON` args-prefix handles, configured collection-probe handles such as `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_COLLECTION_PROBE` and `*_COLLECTION_PROBE_ARGS_JSON`, and fallback helper/collection-probe handles, can be treated as executed OS isolation, readiness-review evidence, broad provider support, mathematical proof authority, real-Pi execution, or GA certification merely because configuration exists.
- Bundled provider-helper protocol assets, packaged helper scripts, packaged args-prefixes, Node executable resolution, self-test/runtime-attestation hashes, or bundled helper execution/collection wrappers can be treated as production helper binaries, collected OS-enforcement evidence, readiness-review evidence by themselves, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.
- Adapter OS-isolation provider-helper host-validation manifests, helper-host-ready booleans, helper binary hashes, supported-platform lists, provider-helper self-test status/hashes, or route payloads can be treated as executed OS isolation, readiness-review evidence, broad provider support, mathematical proof authority, or GA certification.
- Adapter OS-isolation provider-helper execution manifests, missing/unvalidated/mismatched host-validation bindings, helper runtime-attestation status/hashes, helper args-prefix hash/count metadata, helper exit codes, stdout/stderr/transcript hashes, or route payloads can be treated as collected OS-enforcement evidence, readiness-review evidence, broad provider support, mathematical proof authority, or GA certification.
- Adapter OS-isolation provider-helper collection route payloads, caller-supplied collection booleans, caller-supplied stdout/stderr/transcript hashes, caller-supplied provider-tool witness hashes, helper executions without current runtime-attestation binding, configured collection probes without current provider host-capability and host-validation artifact binding, default route collection blockers, hash-bound service-owned collection probes with incomplete OS-enforcement facts, service-owned collection probes that lack a current provider-specific executed-tool witness binding, provider-specific host-capability tool binding, provider-family OS-enforcement witness, provider-family execution profile binding, provider-specific live probe attempt binding, provider-specific live probe execution binding, provider-specific live probe collection binding back to the service-executed live probe transcript, or provider control-plane execution witness binding, stale pre-Task203/pre-Task204/pre-Task205/pre-Task206/pre-Task207/pre-Task208/pre-Task209/pre-Task210 collection artifacts, current host-capability artifact drift, or wrapper manifests can be treated as collected OS-enforcement evidence without complete process/filesystem/network/no-new-privileges/escape-prevention checks, a provider-tool witness bound to service-derived collection-probe executable/profile/argv hashes, the current helper execution transcript, the current host-capability provider tool name/hash, a `provider_family_os_enforcement_witness` bound to those facts, a provider-family execution profile hash binding, provider-specific live probe attempt/execution bindings, collection-side live-probe execution binding, provider control-plane execution witness binding, and a collected canonical Task170 probe/evidence artifact.
- Task204 keeps provider-specific tool binding under release-hardening coverage: complete provider-helper collection evidence must bind the provider-tool witness to the current host-capability provider tool name/hash.
- Task205 keeps provider-family OS-enforcement witness binding under release-hardening coverage: complete provider-helper collection evidence must carry `provider_family_os_enforcement_witness` material bound to the current provider family, host-capability/tool bindings, helper transcript, and complete OS-enforcement facts.
- Task206 keeps provider-family execution profile binding under release-hardening coverage: complete provider-helper collection evidence must also carry service-derived `provider_family_execution_profile` material binding the provider-family execution kind, profile hash, and argv hash. This is provenance metadata only and remains non-authoritative, non-release-grade, and non-shipping.
- Task207 keeps provider-specific live probe attempt binding under release-hardening coverage: complete provider-helper collection evidence must also carry `provider_specific_live_probe_attempt` material bound to the current provider-family execution profile, provider tool hashes, helper transcript, complete OS facts, disabled network policy, and non-authority boundary.
- Task208 keeps provider-specific live probe execution binding under release-hardening coverage: complete provider-helper collection evidence must also carry `provider_specific_live_probe_execution` material from a separately configured service-owned live OS probe subprocess, bound to the current provider-family execution profile, provider tool hashes, helper transcript, live probe tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary.
- Task209 keeps provider-specific live probe collection binding under release-hardening coverage: configured collection-probe stdout must bind the same service-executed `provider_specific_live_probe_execution` id/hash/tool/argv/stdout/stderr/transcript material before complete provider-helper collection facts can satisfy readiness.
- Task210 keeps provider control-plane execution witness binding under release-hardening coverage: complete provider-helper collection evidence must also carry `provider_control_plane_execution_witness` material bound to the current provider family execution profile, provider tool hashes, helper transcript, provider-specific live probe execution id/hash/tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary.
- Task211 keeps the Task202-210 provider-helper witness chain under check-debug coverage: release-hardening suites, public wording, route sanitization, config flags, blocker/veto wiring, and Task202-210 suite discovery must stay synchronized without turning witness metadata into proof authority, real-Pi evidence, broad provider support, production helper shipment, daemon/policy inspection, OS-enforcement proof, or GA certification.
- Task212 keeps a Windows AppContainer production-helper profile contract under release-hardening coverage: env-configured Windows AppContainer helpers must be distinguishable from bundled provider-helper protocol assets through non-secret manifest profile-source fields, while neither profile source can certify GA, become proof authority, or satisfy readiness without the existing helper witness chain and canonical service-owned evidence.
- Task222 keeps an OCI/Docker/Podman production-helper profile contract under release-hardening coverage: OCI helper manifests must carry a service-derived `production_helper_profile_contract` binding helper profile source, helper executable hash, Docker/Podman facility family, disabled network policy, no-new-privileges requirement, path-free host-facility tool names, and non-authority flags. The focused suite `goal3-task222-agent-adapter-os-isolation-oci-production-helper-profile-contract.test.mjs` must keep this lineage distinct from container launch, daemon/socket/container inspection, OS-enforcement proof, readiness, Lean authority, real-Pi evidence, and GA certification.
- Task232 keeps a Nix sandbox production-helper profile contract under release-hardening coverage: Nix helper manifests must carry a service-derived `production_helper_profile_contract` binding helper profile source, helper executable hash, Nix sandbox facility family, disabled network policy, no-new-privileges requirement, path-free host-facility tool names, and non-authority flags. The focused suite `goal3-task232-agent-adapter-os-isolation-nix-production-helper-profile-contract.test.mjs` must keep this lineage distinct from Nix command execution, store/profile inspection, OS-enforcement proof, readiness, Lean authority, real-Pi evidence, broad provider support, and GA certification.
- Task234 keeps a Firejail production-helper profile contract under release-hardening coverage: Firejail helper manifests must carry a service-derived `production_helper_profile_contract` binding helper profile source, helper executable hash, Firejail facility family, disabled network policy, no-new-privileges requirement, path-free host-facility tool name, and non-authority flags. The focused suite `goal3-task234-agent-adapter-os-isolation-firejail-production-helper-profile-contract.test.mjs` must keep this lineage distinct from Firejail command execution, profile/policy inspection, OS-enforcement proof, readiness, Lean authority, real-Pi evidence, broad provider support, and GA certification.
- Task236 keeps removed-platform documentation out of current release-hardening scope: public docs, sample config, and smoke lists must not advertise platform-specific helper handles, profile-contract kinds, host-facility tool names, or a focused release-hardening suite for the removed platform.
- Task213 keeps campaign-native live Mathlib replay breadth evidence separate from historical toy smoke replay: final replay requests that opt into `campaign_live_mathlib_non_toy` must use campaign scope, `primary_dependency: "Mathlib"`, a theorem source importing Mathlib, and non-toy statement/proof material. `True`, default `n : Nat`, `by trivial`, `by omega`, and positive-matrix release paths fail closed before final replay workspace allocation.
- Task214 keeps campaign-native live Mathlib dependency material explicit before final replay: the same opt-in profile must carry a Mathlib `require` in `lakefile.lean`, a `lake-manifest.json` mathlib package pinned to an immutable commit SHA, a trusted mathlib4 source URL, a recorded non-unknown license, and no local `Mathlib` module shadowing before final replay workspace allocation.
- Task215 keeps final replay dependency evidence on the V2 closure path: the stable `dependency_closure.json` artifact written by final clean replay must carry `comath.dependency_closure.v2` content, and FinalReplayManifest v3 dependency locks must bind any V2 package revision material.
- Task216 keeps campaign-native Mathlib provisioning explicit before final replay: the same opt-in profile must have locally materialized `.lake/packages/mathlib` source material with package hashes and no package-internal symlinks before final replay workspace allocation, and those provisioning diagnostics remain non-authoritative. Final clean replay may copy pinned materialized Mathlib package sources into its clean workspace, but provisioning metadata cannot promote a claim or substitute for a passing network-disabled Lean clean replay.
- Task217 keeps FinalReplayManifest v3 dependency locks consistent at verification time: manifest verification must keep the `lean-toolchain`, `lakefile.lean`, and `lake-manifest.json` dependency-lock paths fixed to the clean workspace, recompute their hashes, bind `lean_toolchain` text to the clean `lean-toolchain` file, and, when the dependency report is `DependencyClosureV2`, match `dependency_lock.external_revisions` against the V2 package material before accepting the manifest.
- Task218 keeps host replay availability visible before final replay allocation: the same opt-in profile must write a non-authoritative `mathlib_host_replay_diagnostic.json` with service-owned Lean/Lake version probes, expected toolchain match, binary hashes, replay plan, provisioning-diagnostic hash, safe replay arguments, and lakefile-declared build targets before allocating `.comath/lean/final_replay`. Missing Lean/Lake, failed probes, toolchain mismatch, unsafe replay arguments, or undeclared build targets fail closed as replayable blockers; the diagnostic is not proof authority and cannot promote a claim.
- Task219 keeps Lean/Lake import-graph evidence visible before final replay allocation: the same opt-in profile must write a non-authoritative `mathlib_import_graph_diagnostic.json` with service-owned `lake env lean --deps` theorem/audit probes, command argv, exit codes, stdout/stderr/output hashes, host-diagnostic hash, and deps-stdout-only coarse Mathlib presence before allocating `.comath/lean/final_replay`. Failed probes, empty deps stdout, stderr-only dependency mentions, option-shaped theorem/audit file args, or missing primary-dependency evidence fail closed as replayable blockers; the diagnostic is not proof authority and cannot promote a claim.
- Task238 keeps prepared local Mathlib fixture readiness explicit before host/import diagnostics: the no-download fixture preflight composes Task214 dependency material and Task216 provisioning checks, requires pinned trusted licensed Mathlib manifest material plus an already materialized `.lake/packages/mathlib`, rejects local `Mathlib` shadowing and package symlinks, records material hashes, and reports whether Task218/219 diagnostics may start. It does not execute Lean or Lake, does not download dependencies, does not allocate `.comath/lean/final_replay`, and cannot promote a claim.
- Task220 keeps bounded Pi/operator transport leases bound to a service-owned `AgentRun` log-session before lease persistence or guided real-Pi execution consumption. Arbitrary, expired, stale, missing, wrong-project, wrong-recovery, or tampered log-session routes/bindings fail closed; the binding records only route/run/cursor/event-count/body-hash provenance with `proof_authority=none` and still does not provide durable long-lived WebSocket/SSE transport or GA certification.
- Task221 keeps bounded Pi/operator transport heartbeats as append-only lease rebind checkpoints: heartbeat/rebind must read an unexpired Task220-bound lease, re-run the service-owned `AgentRun` log-session formatter, write only a new heartbeat artifact, and leave session/recovery/lease artifacts unchanged. Static tampering, expired, wrong-project, stale-client, wrong-chain, non-monotonic live-log, or unreadable leases fail closed; same-route/same-run live log growth is accepted only as bounded rebind provenance. Pi consumers sanitize proof/secret/host-path/long-lived-transport wording and use the service route with host confirmation. Heartbeats do not extend into a CoMath-owned transport stack, durable long-lived WebSocket/SSE, proof authority, or GA certification.
- Task223 keeps the interactive real-Pi checkpoint UX read-only: `comath.release.piCodexLifecycleInteractiveRealPi` and `/cm:release lifecycle-interactive-real-pi` must render sanitized next-action plans over existing host-confirmed lifecycle commands, never call `comathd`, never write `.comath`, never echo caller-supplied trusted runtime paths or path-shaped IDs, never auto-execute lifecycle actions, and never claim proof authority, durable long-lived transport, or GA certification. The focused suite `goal3-task223-pi-interactive-real-pi-checkpoint-ux.test.mjs` must remain in the release-hardening matrix.
- Task224 keeps guided real-Pi terminal evidence service-owned: `reviewPiCodexLifecycleTerminalExecution()` and `POST /release/pi-codex-lifecycle/terminal-execution-review` must bind runtime probe, operator-session manifest, recovery checkpoint, bounded lease, heartbeat/rebind, and guided execution artifact paths/hashes into a single append-only review before release review can consume the workflow chain. Missing heartbeat, poisoned heartbeat, stale hashes, path/id mismatch, secrets, proof-success wording, or long-lived transport wording must fail closed or be sanitized. The focused suite `goal3-task224-pi-guided-execution-terminal-chain-review.test.mjs` must remain in the release-hardening matrix.
- Task225 keeps operator/service transport provenance bound to maintained primitives: `recordPiCodexLifecycleOperatorServiceTransportContract()` and `POST /release/pi-codex-lifecycle/operator-service-transport-contract` must consume a Task224 terminal review, verify the current Task221 heartbeat hash, bind only the existing Node HTTP `GET /agent/run/<run_id>/log-session` route and Pi `fetch`/`getText` client primitive, and re-run bounded `formatAgentRunLogSseSession()` before writing append-only contract evidence. Stale heartbeat hashes, missing reviews, secrets, proof-success wording, or long-lived transport claims must fail closed or be sanitized. The focused suite `goal3-task225-pi-operator-service-transport-contract.test.mjs` must remain in the release-hardening matrix.
- Task226 keeps the Pi consumer for Task225 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleOperatorServiceTransportContract` and `/cm:release lifecycle-operator-service-transport-contract` must call only `POST /release/pi-codex-lifecycle/operator-service-transport-contract` through host confirmation, must not forward model-supplied `confirmation_id`, must sanitize actor/path/result surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state authority flags false. The interactive real-Pi planner may show the contract checkpoint, but it must not auto-execute it. The focused suite `goal3-task226-pi-operator-service-transport-contract-consumer.test.mjs` must remain in the release-hardening matrix.
- Task227 keeps automatic real-Pi execution orchestration service-owned and checkpoint-bound: `orchestratePiCodexLifecycleAutomaticRealPiExecution()` and `POST /release/pi-codex-lifecycle/automatic-real-pi-execution` must compose only the existing runtime probe, operator session, recovery, bounded lease, heartbeat/rebind, guided execution, terminal review, and operator/service transport contract producers. The orchestration manifest must bind every checkpoint id/path/hash, reject non-service-owned operator routes before persistence, and force proof/GA/durable/live/direct-Pi-write/direct-trusted-state authority flags false. The focused suite `goal3-task227-pi-automatic-real-pi-execution-orchestration.test.mjs` must remain in the release-hardening matrix.
- Task228 keeps the Pi consumer for Task227 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleAutomaticRealPiExecution` and `/cm:release lifecycle-automatic-real-pi-execution` must call only `POST /release/pi-codex-lifecycle/automatic-real-pi-execution` through host confirmation, must not forward model-supplied `confirmation_id`, must sanitize nested checkpoint request/result surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state authority flags false. The interactive real-Pi planner may show the automatic orchestration checkpoint, but it must not call `comathd` or auto-execute it. The focused suite `goal3-task228-pi-automatic-real-pi-execution-consumer.test.mjs` must remain in the release-hardening matrix.
- Task229 keeps operator/service transport continuity bound to maintained primitives: `recordPiCodexLifecycleOperatorServiceTransportContinuity()` and `POST /release/pi-codex-lifecycle/operator-service-transport-continuity` must consume a Task225 transport contract artifact, require and verify the caller-supplied contract hash, resume only from the contract `log_session_next_cursor`, and re-run bounded `formatAgentRunLogSseSession()` over the existing Node HTTP AgentRun log-session route plus Pi `fetch`/`getText` primitive. Tampered contracts, stale hashes, missing hashes, secrets, proof-success wording, or long-lived transport claims must fail closed or be sanitized. The continuity checkpoint may record durable resume provenance, but it must not claim durable long-lived transport, live stream ownership, proof authority, direct Pi writes, or GA certification. The focused suite `goal3-task229-pi-operator-service-transport-continuity.test.mjs` must remain in the release-hardening matrix.
- Task230 keeps the Pi consumer for Task229 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleOperatorServiceTransportContinuity` and `/cm:release lifecycle-operator-service-transport-continuity` must call only `POST /release/pi-codex-lifecycle/operator-service-transport-continuity` through host confirmation, require `transport_contract_sha256`, must not forward model-supplied `confirmation_id`, must sanitize actor/path/result surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state authority flags false. The interactive real-Pi planner may show the continuity checkpoint, but it must not call `comathd` or auto-execute it. The focused suite `goal3-task230-pi-operator-service-transport-continuity-consumer.test.mjs` must remain in the release-hardening matrix.
- Task231 keeps the prepared unattended real-Pi handoff as a read-only planner mode over existing service-owned checkpoint references: `comath.release.piCodexLifecycleInteractiveRealPi` with `action="unattended-handoff"` and `/cm:release lifecycle-interactive-real-pi unattended-handoff` must not call `comathd`, must not accept or forward `confirmation_id`, must not ask for mutation confirmation, must not write `.comath`, must not auto-execute lifecycle actions, and must require sanitized service-owned checkpoint path/hash pairs before marking a handoff ready. Missing or poisoned checkpoint refs must block the handoff view. Public output must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/unattended-execution authority flags false and must not echo trusted runtime paths, host paths, secrets, proof-success wording, or operator-free/unattended-executor overclaims. The focused suite `goal3-task231-pi-prepared-unattended-real-pi-handoff-ux.test.mjs` must remain in the release-hardening matrix.
- Task237 keeps the prepared handoff operator review checklist read-only and non-authoritative: `comath.release.piCodexLifecycleInteractiveRealPi` with `action="operator-review-checklist"` and `/cm:release lifecycle-interactive-real-pi operator-review-checklist` must reuse Task231 prepared checkpoint refs, render ordered manual-review items, block on missing or poisoned refs, ignore approval-shaped caller fields, and force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/service-evidence/execution flags false. It must not call `comathd`, persist a review manifest, verify artifact freshness, approve execution, run Lean, promote claims, or certify GA. The focused suite `goal3-task237-pi-prepared-handoff-operator-review-checklist.test.mjs` must remain in the release-hardening matrix.
- Task239 keeps the first service-owned prepared handoff review append-only and non-authoritative: `reviewPiCodexLifecycleUnattendedRealHostHandoff()` and `POST /release/pi-codex-lifecycle/unattended-real-host-handoff-review` must derive canonical `.comath/release/pi-codex-lifecycle/...` artifact paths from checkpoint ids, re-read and hash-bind runtime registration, operator session, recovery, lease, heartbeat, guided execution, terminal review, maintained transport contract, automatic orchestration, and continuity artifacts, and treat Pi public `service-owned-pi-lifecycle/...` aliases plus caller hashes only as expected values. Stale hashes, non-canonical aliases, chain tampering, duplicate review ids, secrets, proof-success wording, long-lived transport wording, or unattended-execution/approval-shaped caller fields must fail closed or be sanitized. The review manifest may record freshness and chain review only; it must not approve execution, create operator approval evidence, run unattended host actions, mutate Pi state, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task239-service-owned-unattended-real-host-handoff-review.test.mjs` must remain in the release-hardening matrix.
- Task240 keeps the Pi consumer for Task239 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostHandoffReview` and `/cm:release lifecycle-unattended-real-host-handoff-review` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-handoff-review` through host confirmation, require automatic-orchestration and transport-continuity path/hash expected values, must not forward model-supplied `confirmation_id`, must sanitize actor/path/result surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/operator-approval/unattended-execution authority flags false. The interactive real-Pi planner may show the handoff-review checkpoint after continuity, but it must not call `comathd` or auto-execute it. The focused suite `goal3-task240-pi-unattended-real-host-handoff-review-consumer.test.mjs` must remain in the release-hardening matrix.
- Task241 keeps unattended real-host execution readiness fail-closed before any production executor work: `recordPiCodexLifecycleUnattendedRealHostExecutionReadiness()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-readiness` must consume a current Task239 handoff review artifact from its canonical path and caller-supplied hash, persist an append-only blocked readiness manifest, and record blocker reasons for missing operator approval artifact, missing service-owned unattended executor, and missing durable transport. Stale review hashes, non-canonical review paths, promotional review artifacts, secrets, proof-success wording, durable/live transport wording, or approval/execution-shaped caller fields must fail closed or be sanitized. The readiness manifest must not approve or execute a handoff, authorize unattended execution, create operator approval evidence, mutate Pi state, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task241-pi-unattended-real-host-execution-readiness-blocker.test.mjs` must remain in the release-hardening matrix.
- Task242 keeps the Pi consumer for Task241 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostExecutionReadiness` and `/cm:release lifecycle-unattended-real-host-execution-readiness` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-execution-readiness` through host confirmation, require handoff-review id/path/hash material, must not forward model-supplied `confirmation_id`, must translate public lifecycle artifact aliases to service-canonical paths only inside the comathd request, must sanitize actor/path/result surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/operator-approval/unattended-execution authority flags false. The interactive real-Pi planner may show the readiness checkpoint after handoff review, but it must not call `comathd`, auto-execute it, or imply execution can proceed. The focused suite `goal3-task242-pi-unattended-real-host-execution-readiness-consumer.test.mjs` must remain in the release-hardening matrix.
- Task243 keeps manual operator approval as service-owned checkpoint evidence rather than execution authority: `recordPiCodexLifecycleUnattendedRealHostOperatorApproval()` and `POST /release/pi-codex-lifecycle/unattended-real-host-operator-approval` must consume a current Task239 handoff review artifact from its canonical path/hash, persist an append-only `comath.pi_codex_unattended_real_host_operator_approval.v1` artifact, reject stale review hashes, duplicate approval ids, promotional approval material, non-canonical approval paths, and route/transport drift, and emit non-authoritative audit evidence. Task241 readiness may consume a current approval artifact and remove only `operator_approval_artifact_missing`; service-owned unattended executor and durable-transport blockers must remain. The approval artifact must not execute a handoff, authorize unattended execution, mutate Pi state, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task243-pi-unattended-real-host-operator-approval-contract.test.mjs` must remain in the release-hardening matrix.
- Task244 keeps the Pi consumer for Task243 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostOperatorApproval` and `/cm:release lifecycle-unattended-real-host-operator-approval` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-operator-approval` through host confirmation, must not forward model-supplied `confirmation_id`, must sanitize actor/path/result/confirmation surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/operator-approved/unattended-execution authority flags false. The interactive real-Pi planner may show the operator-approval checkpoint before readiness, and the readiness Pi consumer may pass a complete approval id/path/hash binding, but neither may call comathd without host confirmation, auto-execute the handoff, imply execution can proceed, open transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task244-pi-unattended-real-host-operator-approval-consumer.test.mjs` must remain in the release-hardening matrix.
- Task245 keeps the service-owned unattended executor prerequisite as a checkpoint contract rather than an executor: `recordPiCodexLifecycleUnattendedRealHostExecutorContract()` and `POST /release/pi-codex-lifecycle/unattended-real-host-executor-contract` must consume a current Task239 handoff review artifact from its canonical path/hash, persist an append-only `comath.pi_codex_unattended_real_host_executor_contract.v1` artifact, reject stale review hashes, duplicate executor-contract ids, invalid execution modes, poisoned or promotional executor contracts consumed by readiness, non-canonical paths, and route/transport drift, and emit non-authoritative audit evidence. Task241 readiness may consume a current executor contract and remove only `service_owned_unattended_executor_not_configured`; `durable_transport_not_provided` must remain. The executor contract must not invoke an executor, execute a handoff, authorize unattended execution, mutate Pi state, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task245-pi-unattended-real-host-executor-contract.test.mjs` must remain in the release-hardening matrix.
- Task246 keeps the Pi consumer for Task245 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostExecutorContract` and `/cm:release lifecycle-unattended-real-host-executor-contract` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-executor-contract`, must not forward model-supplied `confirmation_id`, must sanitize actor/path/result/confirmation surfaces, and must force proof/GA/durable/live/direct-Pi-write/direct-trusted-state/executor-invoked/unattended-execution authority flags false. The interactive real-Pi planner may show the executor-contract checkpoint after operator approval and before the durable-transport prerequisite checkpoint, and the readiness Pi consumer may pass a complete executor-contract id/path/hash binding, but neither may invoke an executor, auto-execute the handoff, open transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task246-pi-unattended-real-host-executor-contract-consumer.test.mjs` must remain in the release-hardening matrix.
- Task247 keeps the service-owned durable-transport prerequisite as a checkpoint contract rather than a transport stack: `recordPiCodexLifecycleUnattendedRealHostDurableTransportContract()` and `POST /release/pi-codex-lifecycle/unattended-real-host-durable-transport-contract` must consume a current Task239 handoff review, Task243 approval artifact, Task245 executor contract, and Task229 continuity artifact, persist an append-only `comath.pi_codex_unattended_real_host_durable_transport_contract.v1` artifact, reject stale hashes, duplicate ids, CoMath-owned transport-stack kinds, poisoned/live/promotional durable contracts consumed by readiness, non-canonical paths, and route/transport drift, and emit non-authoritative audit evidence. Task241 readiness may consume a current durable-transport prerequisite contract and remove only `durable_transport_not_provided`; the readiness result may record prerequisites but must still keep approval, execution, durable/live transport, direct Pi mutation, Lean authority, promotion, and GA flags false. The focused suite `goal3-task247-pi-unattended-real-host-durable-transport-contract.test.mjs` must remain in the release-hardening matrix.
- Task248 keeps the Pi consumer for Task247 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostDurableTransportContract` and `/cm:release lifecycle-unattended-real-host-durable-transport-contract` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-durable-transport-contract`, must not forward model-supplied `confirmation_id`, must translate public lifecycle aliases to service-canonical paths only inside the comathd request, must sanitize actor/path/result/confirmation surfaces, and must force proof/GA/durable-live/direct-Pi-write/direct-trusted-state/executor-invoked/unattended-execution authority flags false. The interactive real-Pi planner may show the durable prerequisite checkpoint between executor contract and readiness, and the readiness Pi consumer may pass a complete durable-contract id/path/hash binding, but neither may open or maintain transport, invoke an executor, auto-execute the handoff, run Lean, promote claims, or certify GA. The focused suite `goal3-task248-pi-unattended-real-host-durable-transport-contract-consumer.test.mjs` must remain in the release-hardening matrix.
- Task249 keeps the first service-owned unattended real-host execution attempt gate bounded and non-authoritative: `recordPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt` must consume only a current `unattended_real_host_execution_prerequisites_recorded` readiness artifact, re-read and hash-bind the Task239 handoff review, Task243 approval, Task245 executor contract, Task247 durable-transport prerequisite, and Task229 continuity chain, and persist append-only `comath.pi_codex_unattended_real_host_execution_attempt.v1` evidence. Missing executor command material must produce `blocked_unattended_real_host_executor_unavailable`; configured attempts must use allowlisted absolute programs, fixed args/env, `shell:false`, bounded timeout, sanitized stdout/stderr, and a separate non-authoritative result artifact. The attempt gate must not mark terminal unattended completion, authorize Pi direct mutation, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task249-pi-unattended-real-host-execution-attempt.test.mjs` must remain in the release-hardening matrix.
- Task250 keeps the Pi consumer for Task249 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostExecutionAttempt` and `/cm:release lifecycle-unattended-real-host-execution-attempt` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt`, must not forward model-supplied `confirmation_id`, must require readiness id/path/hash, must translate only public readiness lifecycle aliases to service-canonical request paths, must not expose `executor_command` in the public Pi schema, must sanitize actor/path/result/confirmation/notification surfaces, and must force proof/GA/durable-live/direct-Pi-write/direct-trusted-state/executor-invoked/execution-attempt-success/unattended-execution authority flags false in public output. The interactive real-Pi planner may show the attempt checkpoint after readiness, but it remains read-only and must not call comathd, invoke an executor, mark terminal completion, run Lean, promote claims, or certify GA. The focused suite `goal3-task250-pi-unattended-real-host-execution-attempt-consumer.test.mjs` must remain in the release-hardening matrix.
- Task251 keeps terminal attempt review service-owned and non-authoritative: `reviewPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt-review` must derive the canonical Task249 attempt path from the attempt id, require the caller hash only as an expected value, re-read and validate the attempt, replay the readiness/handoff/approval/executor/durable chain, verify any attempt result artifact, and persist append-only `comath.pi_codex_unattended_real_host_execution_attempt_review.v1` evidence. Executor-unavailable and failed attempts remain replayable blockers; an exit-code-0 attempt must still be classified `blocked_terminal_unattended_completion_review_required` until stricter terminal completion evidence exists. The review gate must not mark terminal unattended completion, expose public executor launch/configuration, authorize Pi direct mutation, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task251-service-owned-unattended-real-host-execution-attempt-review.test.mjs` must remain in the release-hardening matrix.
- Task252 keeps the Pi consumer for Task251 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostExecutionAttemptReview` and `/cm:release lifecycle-unattended-real-host-execution-attempt-review` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt-review`, must not forward model-supplied `confirmation_id`, must require attempt id/path/hash, must translate only public attempt lifecycle aliases to service-canonical request paths, must not expose `executor_command` or caller-provided attempt-result material in the public Pi schema, must sanitize actor/path/result/confirmation/notification surfaces, and must force proof/GA/durable-live/direct-Pi-write/direct-trusted-state/terminal-completion/unattended-execution authority flags false in public output. The interactive real-Pi planner may show the attempt-review checkpoint after the execution attempt, but it remains read-only and must not call comathd, certify terminal completion, run Lean, promote claims, or certify GA. The focused suite `goal3-task252-pi-unattended-real-host-execution-attempt-review-consumer.test.mjs` must remain in the release-hardening matrix.
- Task253 keeps terminal completion certification prerequisite evidence service-owned and non-authoritative: `recordPiCodexLifecycleUnattendedRealHostCompletionCertificationPrerequisite()` and `POST /release/pi-codex-lifecycle/unattended-real-host-completion-certification-prerequisite` must derive the canonical Task251 review path from the review id, require the caller hash only as an expected value, re-read and validate the attempt-review artifact, reject stale hashes and promotional review artifacts, and persist append-only `comath.pi_codex_unattended_real_host_completion_certification_prerequisite.v1` evidence. The result must be `blocked_terminal_unattended_completion_certification_required` with `completion_certificate_available=false`, `terminal_unattended_completion_certified=false`, `proof_authority="none"`, and `can_certify_ga=false`; exit-code-0 review evidence remains insufficient. The prerequisite gate must not mark terminal unattended completion, expose public executor launch/configuration, authorize Pi direct mutation, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task253-service-owned-unattended-real-host-completion-certification-prerequisite.test.mjs` must remain in the release-hardening matrix.
- Task254 keeps the Pi consumer for Task253 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostCompletionCertificationPrerequisite` and `/cm:release lifecycle-unattended-real-host-completion-certification-prerequisite` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-completion-certification-prerequisite`, must not forward model-supplied `confirmation_id`, must require attempt-review id/path/hash, must translate only public attempt-review lifecycle aliases to service-canonical request paths, must not expose `executor_command`, caller-provided attempt-result material, or completion certificates in the public Pi schema, must sanitize actor/path/result/confirmation/notification surfaces, and must force proof/GA/durable-live/direct-Pi-write/direct-trusted-state/terminal-completion/completion-certificate authority flags false in public output. The interactive real-Pi planner may show the prerequisite checkpoint after attempt review, but it remains read-only and must not call comathd, certify terminal completion, run Lean, promote claims, or certify GA. The focused suite `goal3-task254-pi-unattended-real-host-completion-certification-prerequisite-consumer.test.mjs` must remain in the release-hardening matrix.
- Task284 keeps lifecycle readiness aggregation bound to the latest terminal blocker: when `reviewPiCodexLifecycleReadiness()` or `POST /release/pi-codex-lifecycle/review` receives Task253 completion-prerequisite id/path/hash material, it must derive the canonical prerequisite path from the id, re-read and hash-check the artifact, reject stale, non-canonical, or promotional prerequisite files, record the prerequisite artifact path/hash in `inputs.completion_certification_prerequisite`, and add `terminal_unattended_completion_certification_required` even if the older real-host, durable-service, and Codex checks are green. The review must remain `proof_authority="none"` and `can_certify_ga=false`, and the focused suite `goal3-task284-pi-lifecycle-readiness-completion-prerequisite-binding.test.mjs` must remain in the release-hardening matrix.
- Task285 keeps the Pi consumer for Task284 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleReview` and `/cm:release pi-codex-lifecycle` must expose optional completion-prerequisite id/path/hash fields, forward them only to `POST /release/pi-codex-lifecycle/review`, and sanitize public notifications so secrets, host paths, proof-success wording, terminal-completion wording, completion-certificate claims, and GA claims do not leak. The Pi surface must not create completion certificates, make prerequisite material release authority, mutate trusted state, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task285-pi-lifecycle-readiness-prerequisite-consumer.test.mjs` must remain in the release-hardening matrix.
- Task286 keeps terminal completion certificate design service-owned and non-authoritative: `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign()` and `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate-design` must derive the canonical Task253 prerequisite path from the id, require the caller hash only as an expected value, re-read and validate the prerequisite artifact, reject stale, non-canonical, or promotional prerequisite files, and persist only append-only design evidence listing required future terminal-certificate evidence. The result must remain `blocked_terminal_unattended_completion_certificate_evidence_missing` with `completion_certificate_available=false`, `terminal_unattended_completion_certified=false`, `proof_authority="none"`, and `can_certify_ga=false`. The gate must not create completion certificates, expose executor output, authorize Pi direct mutation, open durable/live transport, run Lean, promote claims, or certify GA. The focused suite `goal3-task286-service-owned-terminal-completion-certificate-design.test.mjs` must remain in the release-hardening matrix.
- Task287 keeps the Pi consumer for Task286 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign` and `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate-design` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate-design`, must not forward model-supplied `confirmation_id`, must require completion-prerequisite id/path/hash, must translate only public prerequisite lifecycle aliases to service-canonical request paths, must not expose `executor_command`, caller-provided attempt-result material, or completion certificates in the public Pi schema, must sanitize actor/path/result/confirmation/notification surfaces, and must force proof/GA/durable-live/direct-Pi-write/direct-trusted-state/terminal-completion/completion-certificate/executor authority flags false in public output. The interactive real-Pi planner may show the design checkpoint after the prerequisite, but it remains read-only and must not call comathd, certify terminal completion, run Lean, promote claims, or certify GA. The focused suite `goal3-task287-pi-unattended-real-host-terminal-completion-certificate-design-consumer.test.mjs` must remain in the release-hardening matrix.
- Task288 keeps terminal completion certificate evidence service-owned and still separate from mathematical authority: `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificate()` and `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate` must derive canonical design and certificate paths from ids, require caller hashes only as expected values, re-read the Task286 design, Task253 prerequisite, Task251 attempt-review, and current attempt-result artifact bytes, reject duplicate, stale, non-canonical, or tampered chains, persist append-only certificate evidence, and append a provenance audit event. The result may mark only Pi/Codex unattended lifecycle completion with `terminal_unattended_completion_certified=true`; it must keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`, and must not expose executor stdout/stderr/result payload material, run Lean, promote mathematical claims, or certify GA. The focused suite `goal3-task288-service-owned-terminal-completion-certificate.test.mjs` must remain in the release-hardening matrix.
- Task289 keeps the Pi consumer for Task288 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificate` and `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate` must call only `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate`, must not forward model-supplied `confirmation_id`, must require terminal-certificate-design id/path/hash, must translate only public design lifecycle aliases to service-canonical request paths, must not expose `executor_command`, caller-provided attempt-result material, or caller completion certificates in the public Pi schema, and must sanitize actor/path/result/confirmation/notification surfaces. The public result may preserve service-returned Pi/Codex lifecycle completion flags, but must force `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it must not run Lean, promote mathematical claims, mutate trusted Pi state, open durable/live transport, or certify GA. The focused suite `goal3-task289-pi-unattended-real-host-terminal-completion-certificate-consumer.test.mjs` must remain in the release-hardening matrix.
- Task290 keeps operator/service transport closure review service-owned and non-authoritative: `recordPiCodexLifecycleOperatorServiceTransportClosureReview()` and `POST /release/pi-codex-lifecycle/operator-service-transport-closure-review` must consume only a current Task288 terminal completion certificate id/path/hash, re-read the canonical certificate plus the bound Task247 durable-transport prerequisite, Task229 continuity checkpoint, and Task225 maintained transport contract bytes, reject duplicate ids, stale hashes, non-canonical paths, tampering, and chain mismatch, persist append-only closure-review evidence, and append a provenance audit event. The result may record reviewed maintained transport primitives, but must keep `durable_transport_provided=false`, `live_transport_open=false`, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it must not open a durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA. The focused suite `goal3-task290-service-owned-operator-transport-closure-review.test.mjs` must remain in the release-hardening matrix.
- Task291 keeps the Pi consumer for Task290 host-confirmed and non-authoritative: `comath.release.piCodexLifecycleOperatorServiceTransportClosureReview` and `/cm:release lifecycle-operator-service-transport-closure-review` must call only `POST /release/pi-codex-lifecycle/operator-service-transport-closure-review`, must not forward model-supplied `confirmation_id`, must require terminal completion certificate id/path/hash, must translate only public terminal-certificate lifecycle aliases to service-canonical request paths, must not expose `executor_command`, caller-provided attempt-result material, or caller completion certificates in the public Pi schema, and must sanitize actor/result/confirmation/notification surfaces. The public result may preserve service-returned closure lifecycle flags, but must force `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `durable_transport_provided=false`, and `live_transport_open=false`; it must not run Lean, promote mathematical claims, mutate trusted Pi state, open durable/live transport, or certify GA. The focused suite `goal3-task291-pi-operator-service-transport-closure-review-consumer.test.mjs` must remain in the release-hardening matrix.
- Task292 keeps the service-owned GA operational readiness review as a release-candidate blocker, not certification: `recordGoal3GaOperationalReadinessReview()` and `POST /release/goal3/ga-operational-readiness-review` must re-read Task290 transport-closure review and Task167 adapter OS-isolation readiness review artifacts by canonical path/hash, reject stale, non-canonical, tampered, or promotional material, persist append-only review evidence, and append a provenance audit event. A ready result may only report `ready_for_ga_release_candidate_review`; it must keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`, and must not run Lean, open durable/live transport, mutate trusted Pi state, promote claims, or certify GA. The focused suite `goal3-task292-service-owned-ga-operational-readiness-review.test.mjs` must remain in the release-hardening matrix.
- Task293 keeps the Pi consumer for Task292 host-confirmed and non-authoritative: `comath.release.goal3GaOperationalReadinessReview` and `/cm:release goal3-ga-operational-readiness-review` must call only `POST /release/goal3/ga-operational-readiness-review`, must not forward model-supplied `confirmation_id`, must require Task290 transport-closure review id/path/hash plus Task167 adapter OS-isolation review id/path/hash, must translate only those service-owned public aliases to service-canonical paths, and must not expose caller proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions in the public Pi schema. Public output may preserve service-returned release-candidate readiness flags, but must force `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, durable/live transport false, and no trusted Pi mutation; it must not run Lean, promote mathematical claims, or certify GA. The focused suite `goal3-task293-pi-ga-operational-readiness-review-consumer.test.mjs` must remain in the release-hardening matrix.
- Task294 keeps GA certification review as a fail-closed precondition gate, not a certificate: `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` must consume only a current Task292 operational readiness review id/path/hash, re-read the canonical readiness artifact, reject stale, non-canonical, tampered, or promotional material, run the existing GA acceptance diagnostic only as no-authority support evidence, persist the acceptance report plus append-only review evidence, and append a provenance audit event. Until release-candidate proof breadth and the final GA certification audit are complete, the result must be `blocked_release_candidate_ga_certification_prerequisites` with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it must not run Lean, promote claims, open durable/live transport, mutate trusted Pi state, or certify GA. The focused suite `goal3-task294-service-owned-ga-certification-review.test.mjs` must remain in the release-hardening matrix.
- Task295 keeps the Pi consumer for Task294 host-confirmed and non-authoritative: `comath.release.goal3GaCertificationReview` and `/cm:release goal3-ga-certification-review` must call only `POST /release/goal3/ga-certification-review`, must not forward model-supplied `confirmation_id`, must require Task292 operational-readiness review id/path/hash, must translate only the operational-readiness service-owned public alias to a service-canonical path, and must not expose caller acceptance reports, proof-breadth matrices, final GA audits, proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions in the public Pi schema. Public output may preserve service-returned blocked certification-precondition flags, but must force `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, durable/live transport false, and no trusted Pi mutation; it must not run Lean, promote mathematical claims, or certify GA. The focused suite `goal3-task295-pi-ga-certification-review-consumer.test.mjs` must remain in the release-hardening matrix.
- Task296 keeps the final GA audit as an append-only fail-closed service audit, not a certificate: `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` must consume only a current Task294 GA certification review id/path/hash, re-read the canonical certification review, re-read the bound Task292 operational-readiness review and Task294 acceptance-support artifact by path/hash, reject stale or promotional material, and persist a service-owned final-audit artifact with `final_ga_audit_available=true` but `final_ga_audit_passed=false` while proof breadth remains incomplete. The result must be `blocked_release_candidate_proof_breadth_incomplete` with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certificate_available=false`; it must not run Lean, promote claims, open durable/live transport, mutate trusted Pi state, or certify GA. The focused suite `goal3-task296-service-owned-final-ga-audit.test.mjs` must remain in the release-hardening matrix.
- Task297 keeps the Pi consumer for Task296 host-confirmed and non-authoritative: `comath.release.goal3FinalGaAudit` and `/cm:release goal3-final-ga-audit` must call only `POST /release/goal3/final-ga-audit`, must not forward model-supplied `confirmation_id`, must require Task294 certification-review id/path/hash, must translate only the certification-review service-owned public alias to a service-canonical path, and must not expose caller acceptance reports, proof-breadth matrices, final GA audit payloads, proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions in the public Pi schema. Public output may preserve service-returned blocked final-audit flags, but must force `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `final_ga_audit_passed=false`, `ga_certificate_available=false`, durable/live transport false, and no trusted Pi mutation; it must not run Lean, promote mathematical claims, or certify GA. The focused suite `goal3-task297-pi-final-ga-audit-consumer.test.mjs` must remain in the release-hardening matrix.
- Task298 keeps release-candidate proof breadth review service-owned and fail-closed: `recordGoal3ReleaseCandidateProofBreadthReview()` and `POST /release/goal3/proof-breadth-review` must derive the current 100-task positive manifest and positive-matrix batch inside `comathd`, persist append-only manifest/batch/review artifacts, reject duplicate review ids, ignore caller-supplied proof-breadth matrices, and append provenance audit events. The review must remain `blocked_positive_matrix_release_candidate_proof_breadth_incomplete` with `proof_breadth_complete=false`, `clean_replay_passed_count=0`, `replayable_blocker_count=100`, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it must not run Lean, accept caller proof material, promote claims, unblock the final GA audit, or certify GA. The focused suite `goal3-task298-service-owned-proof-breadth-review.test.mjs` must remain in the release-hardening matrix.
- Task299 removes the artificial PM-001 final-authority packaging dead end while preserving the no-inherited-fixture rule: `packageGoal3GaPositiveMatrixFinalAuthorityEvidenceV3()` and `packageGoal3GaPositiveMatrixFinalAuthorityEvidenceTrancheV3()` must allow PM-001 task-local packaging/blocker artifacts so a 100-task proof-breadth closure can eventually account for every manifest task. Without PM-001 task-local Lean Authority v3 evidence, PM-001 must remain `blocked_missing_final_evidence` / `proof_authority="none"` / `can_promote_claim=false`; representative workflow fixtures and batch summaries still cannot satisfy PM-001 clean replay. The focused suite `goal3-task299-pm001-task-local-final-authority-packaging.test.mjs` must remain in the release-hardening matrix.
- Task300 adds the service-owned proof-breadth closure verifier: `recordGoal3ReleaseCandidateProofBreadthClosure()` and `POST /release/goal3/proof-breadth-closure` must derive the 100-task manifest inside `comathd`, read only canonical `.comath/release/positive_matrix/<task>/final_authority_packaging_report_v3.json` reports, require every task report to be `verified_final_authority_evidence` with `proof_authority="lean_kernel_clean_replay"` and task-local source-verification metadata, and persist an append-only closure artifact plus provenance audit event. Missing or blocked task reports must keep `proof_breadth_complete=false` and `final_ga_audit_unblocked=false`; complete proof breadth may set `proof_breadth_complete=true` and `final_ga_audit_unblocked=true`, but still `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`. The focused suite `goal3-task300-proof-breadth-closure-verifier.test.mjs` must remain in the release-hardening matrix.
- Task301 binds Task300 proof-breadth closure into the final GA audit path: `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` may accept only canonical Task300 closure id/path/hash material, must hash-bind and validate the closure artifact, and may mark `final_ga_audit_passed=true` only when the closure reports all 100 task-local final-authority reports complete with `proof_authority="lean_kernel_clean_replay"`. Missing or incomplete closure keeps the old fail-closed blocker; passed final audit still cannot promote claims, certify GA, or create a GA certificate. The focused suite `goal3-task301-final-ga-audit-proof-breadth-closure-binding.test.mjs` must remain in the release-hardening matrix.
- Task302 binds passed final GA audit back into GA certification review: `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` may accept only canonical Task301 final-audit id/path/hash material, must re-read and hash-bind it, must keep missing or blocked final audit as certification blockers, and may return `ready_for_ga_certificate_gate` only when operational readiness and passed final audit are both current. Even ready certification review must keep `can_certify_ga=false`, `ga_certificate_available=false`, and `ga_certification_gate_separate=true`; actual certificate generation remains a separate gate. The focused suite `goal3-task302-ga-certification-final-audit-binding.test.mjs` must remain in the release-hardening matrix.
- Task303 adds the service-owned GA certificate gate: `recordGoal3GaCertificate()` and `POST /release/goal3/ga-certificate` must consume only a ready Task302 certification review by canonical id/path/hash, re-read the ready review and its bound final-audit artifact by hash/size, reject blocked or stale material, and persist an append-only GA certificate artifact. The certificate may set `can_certify_ga=true` only as release certification, not proof-claim promotion; it must keep `can_promote_claim=false`, `claim_promotion_requires_ordinary_gate=true`, and `proof_authority="lean_kernel_clean_replay"` as inherited proof-breadth authority. The focused suite `goal3-task303-service-owned-ga-certificate-gate.test.mjs` must remain in the release-hardening matrix.
- Task304 adds the agent-adapter OS-isolation production helper source readiness gate: `reviewAgentAdapterOsIsolationReadiness()` must fail closed with `adapter_os_isolation_production_helper_source_missing` unless the canonical provider-helper collection is bound to an operator-configured production helper and a current matching helper execution manifest, not a bundled protocol asset or collection-only source rewrite. The gate records `production_helper_configured`, `helper_profile_source`, and `bundled_protocol_asset` in review/audit output, preserves `proof_authority="none"`, and the focused suite `goal3-task304-agent-adapter-os-isolation-production-helper-source-readiness-gate.test.mjs` must remain in the release-hardening matrix.
- Task305 binds GA operational readiness to production helper source readiness: `recordGoal3GaOperationalReadinessReview()` and `POST /release/goal3/ga-operational-readiness-review` must reject ready-looking adapter OS-isolation reviews that lack Task304 `checks.production_helper_source.ok=true` plus matching `adapter_execution_isolation.helper_profile_source="operator_configured_provider_helper"`, `production_helper_configured=true`, and `bundled_protocol_asset=false`. The focused suite `goal3-task305-ga-operational-readiness-production-helper-source-binding.test.mjs` must remain in the release-hardening matrix; copied helper-source fields remain non-authoritative and cannot promote claims or certify GA.
- Task306 adds GA certificate source-bound consumption: `recordGoal3GaCertificateConsumptionReview()` and `POST /release/goal3/ga-certificate-consumption-review` must consume only a canonical Task303 certificate and re-read the ready certification review, passed final GA audit, complete proof-breadth closure, and Task305 operational-readiness artifact by path/hash before writing a publishable workbench release-closure artifact. The focused suite `goal3-task306-ga-certificate-source-bound-consumption.test.mjs` must remain in the release-hardening matrix; certificate consumption may certify release readiness but must keep `can_promote_claim=false` and cannot replace ordinary Lean Authority gates for individual proof claims.
- Task307 adds source-bound public release package closure: `recordGoal3SourceBoundReleasePackage()` and `POST /release/goal3/source-bound-release-package` must consume only a current Task306 consumption artifact by canonical id/path/hash, reject stale or source-less release material before writing package state, generate sanitized source-review Markdown/HTML/JSON reports, reuse the source-review public archive assembler, and run the public archive review gate. The focused suite `goal3-task307-source-bound-release-package.test.mjs` must remain in the release-hardening matrix; the package is public diagnostic material with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `package_is_proof_authority=false`.
- Task308 adds source-only open-source review artifact closure: `recordGoal3SourceOnlyOpenSourceReviewArtifact()` and `POST /release/goal3/source-only-open-source-review-artifact` must consume only a current Task307 package by canonical id/path/hash, reject stale package material before writing source artifacts, require a clean Git worktree, reject tracked runtime/build/cache entries, generate a `git archive` tar from `HEAD`, and run the public archive review gate over route and manifest surfaces. The focused suite `goal3-task308-source-only-open-source-review-artifact-binding.test.mjs` must remain in the release-hardening matrix; the artifact is source review material with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `source_artifact_is_proof_authority=false`.
- Task309 adds source artifact presentation review closure: `recordGoal3SourceArtifactPresentationReview()` and `POST /release/goal3/source-artifact-presentation-review` must consume only a current Task308 source artifact by canonical id/path/hash, re-read both the source artifact manifest and bound `source.tar`, reject stale archive bytes before writing presentation material, and run the public archive review gate over route and presentation manifest surfaces. The focused suite `goal3-task309-source-artifact-presentation-review.test.mjs` must remain in the release-hardening matrix; the download descriptor is public diagnostic material with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `presentation_is_proof_authority=false`.
- Task310 adds source release public checklist closure: `recordGoal3SourceReleasePublicChecklist()` and `POST /release/goal3/source-release-public-checklist` must consume only a current Task309 presentation review by canonical id/path/hash, re-read the presentation manifest, its bound public archive review, and bound `source.tar`, reject stale archive bytes and restorable/proof-authority overclaims before writing checklist material, and run the public archive review gate over route and checklist manifest surfaces. The focused suite `goal3-task310-source-release-public-checklist.test.mjs` must remain in the release-hardening matrix; the checklist is public diagnostic material with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `checklist_is_proof_authority=false`, and external notarization / OS immutable storage statuses set to `not_configured`.
- Task311 adds source release external evidence binding closure: `recordGoal3SourceReleaseExternalEvidenceBinding()` and `POST /release/goal3/source-release-external-evidence-binding` must consume only a current Task310 checklist by canonical id/path/hash, re-read the checklist and bound `source.tar`, bind separate operator-provided external-notarization and OS-immutable-storage evidence files by path/hash, reject stale or overclaiming evidence before writing binding material, and run the public archive review gate over route and binding manifest surfaces. The focused suite `goal3-task311-source-release-external-evidence-binding.test.mjs` must remain in the release-hardening matrix; the binding is public diagnostic material with `external_verification_performed=false`, `requires_future_external_verification=true`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `binding_is_proof_authority=false`.
- Task312 source release evidence chain check-debug requires the Task311 external evidence binding gate to re-read the Task310 checklist public archive review by canonical id/path, verify current review bytes against the service-owned public-review audit hash/size, reject missing or tampered review manifests with no partial binding writes, and bind the checklist review artifact hash/current status into the binding and provenance audit event. The focused suite `goal3-task312-source-release-evidence-chain-check-debug.test.mjs` must remain in the release-hardening matrix; this is stale-chain hardening only, not live provider verification, OS-level immutability, Lean replay, proof promotion, restore capability, or GA certification.
- Task313 external provider verification requires `recordGoal3SourceReleaseExternalProviderVerification()` and `POST /release/goal3/source-release-external-provider-verification` to consume only a current Task311 binding by canonical id/path/hash, re-read the Task310 public-review chain and selected operator evidence bytes, issue a service-owned provider verification request, and persist a result only when the provider response binds the same provider id, evidence kind, binding hash, source archive hash/size, evidence hash, and receipt id. The focused suite `goal3-task313-source-release-external-provider-verification.test.mjs` must remain in the release-hardening matrix; accepted provider verification is source-release provenance only with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `provider_result_is_proof_authority=false`, and `requires_separate_lean_authority=true`.
- Task314 provider policy inspection requires `recordGoal3SourceReleaseExternalProviderPolicyInspection()` and `POST /release/goal3/source-release-external-provider-policy-inspection` to consume only a current Task313 provider verification by canonical id/path/hash, re-read its public-review provenance, selected operator evidence, source archive bytes, provider response hashes, and receipt id, issue a service-owned provider policy inspection request, and persist a result only when the policy response binds the same verification hash, provider id, evidence kind, binding/source/evidence hashes, receipt id, and Task313 provider response hash. The focused suite `goal3-task314-source-release-external-provider-policy-inspection.test.mjs` must remain in the release-hardening matrix; accepted policy inspection is source-release provenance only with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `policy_result_is_proof_authority=false`, `os_immutability_enforced=false`, `requires_separate_lean_authority=true`, and `requires_separate_os_immutability_attestation=true`.
- Task315 OS immutability attestation requires `recordGoal3SourceReleaseOsImmutabilityAttestation()` and `POST /release/goal3/source-release-os-immutability-attestation` to consume only a current Task314 policy inspection for `os_immutable_storage` by canonical id/path/hash, re-read its public-review provenance, Task313 verification artifact, selected operator evidence, source archive bytes, provider policy hashes, daemon identity, and policy document hash, issue a service-owned OS attestation request, and persist a result only when the attestation response binds the same Task314/Task313/source/evidence/receipt/provider-policy material. The focused suite `goal3-task315-source-release-os-immutability-attestation.test.mjs` must remain in the release-hardening matrix; accepted OS attestation is source-release provenance only with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `storage_is_proof_authority=false`, `attestation_is_proof_authority=false`, `co_math_os_immutability_enforced=false`, and `requires_separate_lean_authority=true`.
- Task316 Pi source-release OS attestation consumer requires `comath.release.goal3SourceReleaseOsImmutabilityAttestation` and `/cm:release goal3-source-release-os-immutability-attestation` to call only the Task315 route through Pi host confirmation, translate only matching public Task314 policy-inspection aliases, strip model confirmation ids and caller proof/Lean/GA/restore/attestation material, expose the checkpoint in the read-only interactive planner, and sanitize proof/restore/GA/host-path/secret/OS-enforcement overclaims. The focused suite `goal3-task316-pi-source-release-os-immutability-attestation-consumer.test.mjs` must remain in the Pi extension release-hardening matrix; this consumer is not provider execution, direct `.comath` mutation, Lean replay, restore authority, proof promotion, or GA certification.
- Task317 durable transport release-signoff prerequisite requires `recordGoal3DurableTransportReleaseSignoffPrerequisite()` and `POST /release/goal3/durable-transport-release-signoff-prerequisite` to consume only a current Task292 operational-readiness artifact, re-read the bound Task290 transport-closure review, and re-read the nested terminal certificate, durable-transport prerequisite contract, transport continuity checkpoint, and maintained transport contract by path/hash/size. The focused suite `goal3-task317-ga-transport-chain-freshness-review.test.mjs` must remain in the release-hardening matrix; the prerequisite is intentionally blocked with `durable_long_lived_transport_not_provided` and cannot open durable/live transport, run Lean, promote claims, or certify GA.
- Task318 final release-signoff decision requires `recordGoal3FinalReleaseSignoffDecision()` and `POST /release/goal3/final-release-signoff-decision` to consume only a current Task317 durable-transport prerequisite and a current Task315 source-release OS immutability attestation by canonical id/path/hash, re-read the source archive, operator evidence, and policy-inspection material by hash/size, and persist a blocked decision while durable long-lived transport and final certificate consumption remain missing. The focused suite `goal3-task318-final-release-signoff-decision.test.mjs` must remain in the release-hardening matrix; the decision cannot promote proof claims, treat OS/source evidence as proof authority, open transport, or certify GA.
- Task319 durable transport release-signoff verification requires `recordGoal3DurableTransportReleaseSignoffVerification()` and `POST /release/goal3/durable-transport-release-signoff-verification` to consume only a current Task317 prerequisite by canonical id/path/hash, re-read its nested transport-chain artifacts by hash/size, and either persist a blocked verification without external durable transport evidence or verify a service-owned maintained-external transport evidence artifact. The focused suite `goal3-task319-durable-transport-release-signoff-verification.test.mjs` must remain in the release-hardening matrix; verified transport provenance still keeps `ga_release_signoff_ready=false`, `proof_authority=none`, `can_promote_claim=false`, and `can_certify_ga=false`, and cannot build a CoMath transport stack, run Lean, promote claims, or certify GA.
- Task320 Task319 final release-signoff consumption requires `recordGoal3FinalReleaseSignoffDecision()` and `POST /release/goal3/final-release-signoff-decision` to consume optional Task319 durable transport verification by canonical id/path/hash, require it to match the same Task317 prerequisite, and re-check external transport freshness before clearing the durable blocker. The focused suite `goal3-task320-final-release-signoff-task319-consumption.test.mjs` must remain in the release-hardening matrix; final release-signoff readiness requires both verified Task319 transport and current Task306 GA certificate consumption, while the signoff artifact remains `proof_authority=none`, `can_promote_claim=false`, and `can_certify_ga=false`.
- Task321 final release-chain check-debug requires `goal3-task321-final-release-chain-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It revalidates Task317-320, Task306 certificate consumption, Task315 source-release OS attestation, status capability exposure, and public wording drift without adding a new release route, proof authority, claim promotion, CoMath-owned durable transport, or GA certification.
- Task322 final release signoff certification-boundary review requires `recordGoal3FinalReleaseSignoffCertificationBoundaryReview()` and `POST /release/goal3/final-release-signoff-certification-boundary-review` to consume only a current Task320 ready signoff by canonical id/path/hash, re-read its Task306 GA certificate consumption, Task319 durable transport verification, external durable transport freshness, Task315 source-release OS attestation, source archive, and operator evidence by hash/size, and persist append-only review provenance. The focused suite `goal3-task322-final-release-signoff-certification-boundary-review.test.mjs` must remain in the release-hardening matrix and phase0 smoke suite; the review can confirm certification boundary readiness but remains `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, `boundary_review_is_certificate=false`, and `ga_certificate_issued=false`.
- Task323 final release-candidate closure audit requires `recordGoal3FinalReleaseCandidateClosureAudit()` and `POST /release/goal3/final-release-candidate-closure-audit` to consume only a current Task322 certification-boundary review by canonical id/path/hash, re-read the bound Task320 final signoff, Task306 GA certificate consumption, Task319 durable transport verification plus Task317 nested transport chain, external durable transport freshness, Task315 policy-inspection/source-release OS attestation chain, source archive, and operator evidence by hash/size, and persist append-only closure audit provenance. The focused suite `goal3-task323-final-release-candidate-closure-audit.test.mjs` must remain in the release-hardening matrix and phase0 smoke suite; the audit can confirm final release-candidate boundary currentness but remains `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, `release_candidate_closure_audit_is_certificate=false`, and `ga_certificate_issued=false`.
- Task324 final release-candidate check-debug requires `goal3-task324-final-release-candidate-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It revalidates Task322-323, the Task320/306/319/317/315 dependency chain, capability exposure, service route wiring, reduced-chain regression coverage, and public wording drift without adding a new release route, Pi consumer, Lean replay, proof authority, claim promotion, closure certificate, or GA certification.
- Task325 Pi final release-candidate closure audit consumer requires `goal3-task325-pi-final-release-candidate-closure-audit-consumer.test.mjs` to remain in the Pi extension release-hardening matrix and phase0 smoke suite. It exposes `comath.release.goal3FinalReleaseCandidateClosureAudit`, `/cm:release goal3-final-release-candidate-closure-audit`, runtime registration, and the read-only interactive real-Pi planner as host-confirmed thin clients over the Task323 route, with only matching Task322 certification-boundary aliases translated to canonical service paths. The Pi consumer must strip model confirmation ids and caller proof/Lean/GA/proof-breadth/closure-certificate/durable-transport material, sanitize public proof/transport/GA/closure-certificate overclaims, and cannot run Lean, write `.comath`, reimplement Task323 dependency closure, issue certificates, promote claims, or certify GA.
- Task326 proof-breadth execution bridge requires `goal3-task326-proof-breadth-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthExecutionBridge()` and `POST /release/goal3/proof-breadth-execution-bridge` must derive positive-matrix state from canonical final-authority packaging reports, select only a bounded next missing/blocked PM tranche, persist append-only bridge provenance, ignore caller proof-breadth material, and keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. The bridge cannot run Lean, synthesize proofs, replace Task300 closure, replace Task301 final-audit binding, promote claims, or certify GA.
- Task327 proof-breadth bridge check-debug requires `goal3-task327-proof-breadth-bridge-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task326 bridge source guards, focused tranche-selection coverage, Task298/300/301 route and authority boundaries, status capability exposure, smoke discovery, and public release-hardening wording without adding a new route, Pi consumer, Lean replay, proof synthesis, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.
- Task328 Pi proof-breadth review consumer requires `goal3-task328-pi-proof-breadth-review-consumer.test.mjs` to remain in the Pi extension release-hardening matrix and phase0 smoke suite. It exposes `comath.release.goal3ProofBreadthReview`, `/cm:release goal3-proof-breadth-review`, runtime registration, and the read-only interactive real-Pi planner as host-confirmed thin clients over the Task298 route. The Pi consumer must forward only project/actor material, optional `proof_breadth_review_id`, and `requested_review_mode`, strip model confirmation ids and caller proof-breadth matrix/path/hash/json material, proof claims, Lean manifests, GA certificates, executor/attempt material, and durable transport sessions, and sanitize public output back to blocked-review/no-authority semantics. It cannot run Lean, write `.comath`, close proof breadth, consume Task326 execution bridge output, replace Task300 closure verification, replace Task301 final-audit binding, promote claims, unblock final GA audit, or certify GA.
- Task329 Pi proof-breadth closure consumer requires `goal3-task329-pi-proof-breadth-closure-consumer.test.mjs` to remain in the Pi extension release-hardening matrix and phase0 smoke suite. It exposes `comath.release.goal3ProofBreadthClosure`, `/cm:release goal3-proof-breadth-closure`, runtime registration, package-test discovery, and the read-only interactive real-Pi planner as host-confirmed thin clients over the Task300 route. The Pi consumer must forward only project/actor material, `proof_breadth_closure_id`, and `requested_closure_mode`, strip model confirmation ids, caller proof-breadth matrices, closure JSON, Task326 bridge output, proof claims, Lean manifests, GA certificates, executor/attempt material, and durable transport sessions, and sanitize public output so Task300 service-owned closure status/counts remain visible without Pi proof or GA authority. It cannot run Lean, write `.comath`, consume Task326 bridge output as closure evidence, replace Task300 closure verification, replace Task301 final-audit binding, promote claims, unblock final GA audit by itself, or certify GA.
- Task330 proof-breadth Pi closure check-debug requires `goal3-task330-proof-breadth-pi-closure-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task328/329 Pi proof-breadth consumer registration, request whitelists, public sanitizers, package-test discovery, runtime subcommands, planner ordering, Task326 execution-planning-only semantics, Task300 closure-verifier authority, Task301 final-audit closure binding, and public release-hardening wording without adding a route, Pi tool, Lean replay, proof synthesis, proof-breadth execution follow-through, final-GA-audit input consumer, claim promotion, final-audit unblock, or GA certification.
- Task331 Pi final-GA-audit closure-binding consumer requires `goal3-task331-pi-final-ga-audit-closure-binding-consumer.test.mjs` to remain in the Pi extension release-hardening matrix and phase0 smoke suite. It extends `comath.release.goal3FinalGaAudit`, `/cm:release goal3-final-ga-audit`, runtime registration, and the read-only interactive real-Pi planner to accept an optional all-or-none Task300 proof-breadth closure id/path/hash triple together with the required Task294 certification-review id/path/hash. The Pi consumer must translate only matching `service-owned-goal3-proof-breadth-closure/<id>/closure.json` aliases, reject partial or mismatched closure bindings before POST, strip caller proof-breadth matrices, closure JSON, Task326 bridge output, final-audit JSON, proof claims, Lean manifests, GA certificates, executor/attempt material, durable sessions, model confirmation ids, host paths, secrets, and success wording, and sanitize public output so a Task301 closure-bound final-audit pass remains non-certifying. It cannot run Lean, write `.comath`, replace Task300 closure verification, replace Task301 final-audit binding, promote claims, issue certificates, or certify GA.
- Task332 proof-breadth execution follow-through requires `goal3-task332-proof-breadth-execution-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthExecutionFollowThrough()` and `POST /release/goal3/proof-breadth-execution-follow-through` must consume only a current Task326 bridge id/path/hash, re-read that bridge by hash, inspect only the bridge-selected PM tasks' canonical final-authority packaging report paths, persist append-only selected-tranche provenance, and keep `proof_authority="none"`, `runs_lean=false`, `executes_proofs=false`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`. The follow-through can only indicate that the selected tranche is ready for a later Task300 closure recheck; it cannot run Lean, synthesize proofs, write missing packaging reports, expose a Pi consumer, close global proof breadth, replace Task300/Task301, promote claims, or certify GA.
- Task333 proof-breadth follow-through check-debug requires `goal3-task333-proof-breadth-follow-through-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task332 bridge-hash binding, selected-tranche-only canonical packaging report inspection, stale-bridge rejection, append-only ids, no missing-packaging writes, no Lean execution, no Pi/public surface, Task300 closure authority, Task301 final-audit binding, service route wiring, status capability exposure, smoke/default discovery, and release-hardening wording without adding a new route, Pi tool, proof execution, closure, final-audit unblock, claim promotion, or GA certification.
- Task334 task-local Lean Authority packaging follow-through requires `goal3-task334-task-local-lean-authority-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthTaskLocalPackagingFollowThrough()` and `POST /release/goal3/task-local-lean-authority-packaging-follow-through` must consume only a current Task326 bridge id/path/hash, reject evidence for PM tasks outside that selected tranche, call the existing project-local v3 final-authority packaging verifier for selected PM tasks, and persist append-only non-certifying provenance. It may write selected canonical PM packaging reports as verified or blocked according to project-local evidence, but cannot run Lean, trust caller success metadata, write Task300 closure artifacts, write Task301 final-audit artifacts, expose a Pi consumer, close global proof breadth, unblock final GA audit, promote claims, or certify GA.
- Task335 selected-tranche packaging results follow-up requires `goal3-task335-selected-tranche-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTranchePackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-packaging-results-follow-up` must consume only a current Task326 bridge id/path/hash plus a current Task334 follow-through id/path/hash, reject selected-tranche drift between them, re-hash only the selected canonical PM packaging reports recorded by Task334, and reuse the Task332 selected-tranche recheck witness. It can only record currentness for a later Task300 closure recheck; it cannot run Lean, synthesize proofs, trust Task334 summary success, inspect unselected PM tasks, write Task300/301 artifacts, expose a Pi consumer, close global proof breadth, unblock final GA audit, promote claims, or certify GA.
- Task336 selected-tranche packaging results check-debug requires `goal3-task336-selected-tranche-packaging-results-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task335 source guards, Task334 packaging-writer semantics, Task332 selected-tranche recheck semantics, Task326 bridge planning, Task300/301 authority boundaries, service route wiring, status capability exposure, smoke/default discovery, no-Pi-public-surface boundaries, and release-hardening wording without adding a new route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.
- Task337 selected-tranche closure recheck requires `goal3-task337-selected-tranche-closure-recheck.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheClosureRecheck()` and `POST /release/goal3/selected-tranche-closure-recheck` must consume only a current Task335 selected-tranche packaging results follow-up id/path/hash, reject stale Task335 material, re-hash the selected canonical PM packaging reports before invoking Task300, and bind the resulting Task300 proof-breadth closure artifact into non-certifying provenance. It cannot run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, replace Task300, write Task301 artifacts, close global proof breadth by itself, unblock final GA audit by itself, promote claims, or certify GA.
- Task338 selected-tranche next execution bridge requires `goal3-task338-selected-tranche-next-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextExecutionBridge()` and `POST /release/goal3/selected-tranche-next-execution-bridge` must consume only a current Task337 selected-tranche closure recheck id/path/hash, reject stale Task337 or Task300 closure material, re-hash the completed selected PM packaging reports, and delegate only to Task326 for the next bounded execution bridge. It cannot run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task301 artifacts, promote claims, certify GA, or treat selected-tranche progress as global closure.
- Task339 selected-tranche next bridge check-debug requires `goal3-task339-selected-tranche-next-bridge-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task338 source guards, Task337 closure-recheck semantics, Task326 bridge planning, Task300/301 authority boundaries, service route wiring, status capability exposure, smoke/default discovery, no-Pi-public-surface boundaries, and release-hardening wording without adding a new route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.
- Task340 selected-tranche next packaging follow-through requires `goal3-task340-selected-tranche-next-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextPackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-packaging-follow-through` must consume only a current Task338 selected-tranche next execution bridge id/path/hash, reject stale Task338 or delegated Task326 material, reject missing next bridges and selected-tranche drift, reject evidence outside Task338's next selected PM task ids, and delegate only to Task334 for canonical task-local packaging. It cannot run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task300/301 artifacts, promote claims, certify GA, or treat selected-tranche progress as global closure.
- Task341 selected-tranche next packaging results follow-up requires `goal3-task341-selected-tranche-next-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextPackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-packaging-results-follow-up` must consume only a current Task340 selected-tranche next packaging follow-through id/path/hash, reject stale Task340 material, require Task340-bound delegated Task326 and Task334 artifact references, and reuse Task335 currentness semantics to re-hash selected canonical PM packaging reports. It cannot run Lean, synthesize proofs, write packaging reports, trust caller success metadata, expose a Pi consumer, write Task300/301 artifacts, promote claims, certify GA, or treat selected-tranche currentness as global proof-breadth closure.
- Task342 selected-tranche next packaging currentness check-debug requires `goal3-task342-selected-tranche-next-packaging-currentness-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task340/341 source guards, Task338 next-bridge semantics, Task335 selected-report currentness, Task334 packaging boundaries, Task326 bridge planning, Task300/301 authority boundaries, service route wiring, status capability exposure, smoke/default discovery, no-Pi-public-surface boundaries, and release-hardening wording without adding a new route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, certificate issuance, or GA certification.
- Task343 selected-tranche next closure recheck requires `goal3-task343-selected-tranche-next-closure-recheck.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-recheck` must consume only a current Task341 selected-tranche next packaging results follow-up id/path/hash, reject stale Task341 or selected-report material, and bind selected-tranche currentness into selected-tranche closure recheck / Task300 aggregate closure semantics. It cannot run Lean, synthesize proofs, write packaging reports, trust caller success metadata, expose a Pi consumer, write Task301 artifacts, promote claims, certify GA, or treat selected-tranche currentness as global proof-breadth closure.
- Task344 selected-tranche next closure execution bridge requires `goal3-task344-selected-tranche-next-closure-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-execution-bridge` must consume only a current Task343 selected-tranche next closure recheck id/path/hash, reject stale Task343 material, preserve Task343-bound Task300 closure and selected PM packaging-report currentness checks through Task338 semantics, and delegate only to Task338/Task326 for the next bounded execution bridge. It cannot run Lean, synthesize proofs, write packaging/currentness/closure/final-audit artifacts, trust caller success metadata, expose a Pi consumer, promote claims, certify GA, or treat selected-tranche continuation as global proof-breadth closure.
- Task345 selected-tranche next closure bridge check-debug requires `goal3-task345-selected-tranche-next-closure-bridge-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task343/344 route/export/status/default discovery, Task344's Task343 hash binding and Task338 delegation, Task338/326 bounded execution planning, Task300 aggregate closure authority, Task301 final-audit binding, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit artifacts, promote claims, issue certificates, or certify GA.
- Task346 selected-tranche next closure packaging follow-through requires `goal3-task346-selected-tranche-next-closure-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-closure-packaging-follow-through` must consume only a current Task344 selected-tranche next closure execution bridge id/path/hash, reject stale Task344 or delegated Task338 material, require Task344's delegated Task338/Task326 bridge references, reject evidence outside Task344's next selected PM task ids, and delegate packaging only through Task340/Task334. It cannot run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task300/301 artifacts, promote claims, certify GA, or treat selected-tranche progress as global closure.
- Task347 selected-tranche next closure packaging results follow-up requires `goal3-task347-selected-tranche-next-closure-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-follow-up` must consume only a current Task346 selected-tranche next closure packaging follow-through id/path/hash, canonical-validate Task346-embedded Task344/Task338 provenance, reuse Task341/Task335 currentness checks over Task346's Task340/Task334 packaging output, and bind artifacts by reference/hash. It cannot run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer/surface, write Task300/301/GA-certificate artifacts, promote claims, certify GA, or treat selected-tranche packaging results as global proof-breadth closure.
- Task348 selected-tranche next closure packaging/currentness check-debug requires `goal3-task348-selected-tranche-next-closure-packaging-currentness-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task346/347 route/export/status/default discovery, Task346 Task344/338 binding, Task347 Task346/341 binding, Task341/335 currentness semantics, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit/GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task349 selected-tranche next closure packaging results closure recheck requires `goal3-task349-selected-tranche-next-closure-packaging-results-closure-recheck.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-recheck` must consume only a current Task347 selected-tranche next closure packaging results follow-up id/path/hash, revalidate Task347's Task346/344/338/340/341/335/334 references, and delegate selected-tranche closure recheck to existing Task343/Task300 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task350 selected-tranche next closure packaging results closure execution bridge requires `goal3-task350-selected-tranche-next-closure-packaging-results-closure-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-bridge` must consume only a current Task349 selected-tranche next closure packaging results closure recheck id/path/hash and delegate selected-tranche closure execution to existing Task344/338 bridge semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task351 selected-tranche next closure results-to-execution check-debug requires `goal3-task351-selected-tranche-next-closure-results-to-execution-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task349/350 route/export/status/default discovery, Task349 Task347-to-Task343/300 closure-recheck binding, Task350 Task349-to-Task344/338 closure-execution binding, Task344/338/326 execution bridge boundaries, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit/GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task352 selected-tranche next closure execution packaging follow-through requires `goal3-task352-closure-exec-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-follow-through` must consume only a current Task350 selected-tranche next closure packaging results closure execution bridge id/path/hash and delegate packaging to existing Task346/340/334 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task353 selected-tranche next closure execution packaging results follow-up requires `goal3-task353-closure-exec-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-follow-up` must consume only a current Task352 selected-tranche next closure execution packaging follow-through id/path/hash, validate the Task352-bound delegated Task346 reference, and reuse existing Task347/341/335 currentness semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task354 selected-tranche closure execution packaging results currentness check-debug requires `goal3-task354-closure-exec-packaging-results-currentness-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task352/353 route/export/status/default discovery, Task352 Task350-to-Task346/340/334 packaging binding, Task353 Task352-to-Task347/341/335 currentness binding, Task346/347/341/335 delegated currentness boundaries, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit/GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task355 selected-tranche next closure execution packaging results closure recheck requires `goal3-task355-closure-exec-packaging-results-closure-recheck.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-recheck` must consume only a current Task353 selected-tranche next closure execution packaging results follow-up id/path/hash, validate the Task353-bound delegated Task347 reference, and delegate selected-tranche closure recheck to existing Task349/343/300 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call producer paths directly, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task356 selected-tranche next closure execution packaging results closure execution bridge requires `goal3-task356-closure-exec-packaging-results-closure-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-bridge` must consume only a current Task355 selected-tranche next closure execution packaging results closure recheck id/path/hash, validate the Task355-bound delegated Task349 reference, and delegate selected-tranche closure execution to existing Task350/344/338 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call producer paths directly outside the existing Task350 wrapper, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task357 selected-tranche closure recheck-to-execution check-debug requires `goal3-task357-closure-results-to-execution-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task355/356 route/export/status/default discovery, Task355 Task353-to-Task349/343/300 closure-recheck binding, Task356 Task355-to-Task350/344/338 closure-execution binding, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit/GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task358 selected-tranche next closure execution packaging results closure execution packaging follow-through requires `goal3-task358-closure-exec-packaging-results-closure-exec-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-follow-through` must consume only a current Task356 selected-tranche next closure execution packaging results closure execution bridge id/path/hash, validate the Task356-bound delegated Task350 reference, and delegate selected-tranche closure execution packaging through existing Task352/346/340/334 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task359 selected-tranche next closure execution packaging results closure execution packaging results follow-up requires `goal3-task359-closure-exec-packaging-results-closure-exec-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-follow-up` must consume only a current Task358 selected-tranche next closure execution packaging results closure execution packaging follow-through id/path/hash, validate the Task358-bound delegated Task352 reference, and delegate selected-tranche packaging-results currentness through existing Task353/347/341/335 semantics. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task360 selected-tranche closure execution packaging results currentness check-debug requires `goal3-task360-closure-exec-packaging-results-closure-exec-packaging-results-currentness-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task358/359 route/export/status/default discovery, Task358 Task356-to-Task352/346/340/334 packaging binding, Task359 Task358-to-Task353/347/341/335 currentness binding, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, write packaging/currentness/closure/final-audit/GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task361 selected-tranche next closure execution packaging results closure execution packaging results closure recheck requires `goal3-task361-closure-exec-packaging-results-closure-exec-packaging-results-closure-recheck.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-closure-recheck` must consume only a current Task359 selected-tranche next closure execution packaging results closure execution packaging results follow-up id/path/hash, validate copied Task358/Task356/Task355 references plus the delegated Task353 reference, and delegate selected-tranche closure recheck through existing Task355/349/343/300 semantics only when Task359 is ready. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task362 selected-tranche next closure execution packaging results closure execution packaging results closure execution bridge requires `goal3-task362-closure-exec-packaging-results-closure-exec-packaging-results-closure-execution-bridge.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-closure-execution-bridge` must consume only a current Task361 selected-tranche next closure execution packaging results closure execution packaging results closure recheck id/path/hash, validate copied Task356/Task355 references plus the delegated Task355 reference, and delegate selected-tranche closure execution through existing Task356/350/344/338 semantics only when Task361 has a delegated Task355. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task363 selected-tranche closure execution packaging results closure execution packaging results recheck-to-execution check-debug requires `goal3-task363-closure-exec-packaging-results-closure-exec-packaging-results-closure-recheck-to-execution-check-debug.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. It must revalidate Task361/362 route/export/status/default discovery, Task361 Task359-to-Task355/349/343/300 closure-recheck binding, Task362 Task361-to-Task356/350/344/338 closure-execution binding, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no Pi surface, and public no-authority wording. It cannot add a route, expose a Pi tool, run Lean, synthesize proofs, call lower producer paths directly, write Task300/Task301/Task303 artifacts, promote claims, issue certificates, or certify GA.
- Task364 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging follow-through requires `goal3-task364-closure-exec-packaging-results-closure-exec-packaging-results-closure-exec-packaging-follow-through.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-closure-execution-packaging-follow-through` must consume only a current Task362 selected-tranche next closure execution packaging results closure execution packaging results closure execution bridge id/path/hash, validate copied/delegated Task356/Task355 material plus the delegated Task356 closure-execution bridge reference, and delegate selected-tranche closure execution packaging through existing Task358/352/346/340/334 semantics only when Task362 has delegated Task356 material. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task365 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging results follow-up requires `goal3-task365-closure-exec-packaging-results-closure-exec-packaging-results-closure-exec-packaging-results-follow-up.test.mjs` to remain in the release-hardening matrix and phase0 smoke suite. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsClosureExecutionPackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-closure-execution-packaging-results-follow-up` must consume only a current Task364 selected-tranche next closure execution packaging results closure execution packaging results closure execution packaging follow-through id/path/hash, validate copied Task362/Task361/Task356/Task355 material plus the delegated Task358 reference when present, and delegate selected-tranche packaging-results currentness through existing Task359/353/347/341/335 semantics only when Task364 has delegated Task358 material. It cannot expose a Pi consumer/surface, run Lean, synthesize proofs, call lower producer paths directly, write Task300 closure, Task301 final-audit, or Task303 GA-certificate artifacts, promote claims, issue certificates, or certify GA.
- Task255 keeps Pi goal-mode intake service-owned and non-authoritative: `/campaign/start` must persist `.comath/campaign/<campaign_id>/goal_mode_intake_manifest.json` for paper paths, attachments, and workspace refs, attach it to the `initialized` stage, hash existing file inputs, avoid raw host-root leakage, expose only manifest path/hash plus non-authority flags in Pi goal export, and treat external inputs only as hints/evidence/refutation sources. Papers, TeX/PDF/Markdown files, workspace refs, directory presence, Pi payloads, and intake manifests cannot promote claims, satisfy Lean authority, or certify GA. The focused suite `goal3-task255-pi-goal-mode-intake-manifest.test.mjs` must remain in the release-hardening matrix.
- Task256 keeps Pi goal-mode research planning service-owned and non-authoritative: `tickCampaign()` in `knowledge_pack` must persist `.comath/campaign/<campaign_id>/goal_mode_research_plan.json`, bind the Task255 intake manifest path/hash, plan local PDF/TeX/Markdown/workspace ingestion through maintained external wheel registry adapters, plan literature retrieval and Lean theorem-search adapter tasks, bind a lemma DAG / `Skeleton.lean` planning seed, and attach the plan to the `knowledge_pack` stage. The plan must record `execution_status=planned_not_executed`, `network_execution_performed=false`, `proof_authority="none"`, and `can_promote_claim=false`; it must not execute retrieval, parse documents as trusted proof, run Lean, promote claims, or certify GA. The focused suite `goal3-task256-pi-goal-mode-research-plan.test.mjs` must remain in the release-hardening matrix.
- Task257 keeps Pi goal-mode adapter execution manifests service-owned and non-authoritative: `tickCampaign()` in `knowledge_pack` must persist `.comath/campaign/<campaign_id>/goal_mode_adapter_execution_manifest.json`, bind the Task256 research-plan path/hash into `knowledge_pack.json`, preserve the Task255 intake-manifest path/hash binding, emit per-planned-task run envelopes, re-check local file hashes, block tampered inputs, record directory refs without recursive proof claims, and classify network/theorem-search tasks as requiring later live provider execution. The manifest must record `network_execution_performed=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`; it must not execute retrieval, extract trusted document claims, return theorem-search hits, run Lean, promote claims, or certify GA. The focused suite `goal3-task257-pi-goal-mode-adapter-execution-manifest.test.mjs` must remain in the release-hardening matrix.
- Task258 keeps Pi goal-mode local ingestion evidence service-owned and non-authoritative: `tickCampaign()` in `knowledge_pack` must persist `.comath/campaign/<campaign_id>/goal_mode_local_ingestion_evidence.json`, bind the Task256 research-plan path/hash and Task257 adapter-execution manifest path/hash into `knowledge_pack.json`, preserve the Task255 intake-manifest path/hash binding, extract line-anchored Markdown/TeX theorem or lemma drafts only after prompt-injection scan, block prompt-injected local text, block PDF ingestion until a maintained parser adapter exists, and block external Lean repo inspection until a maintained repo adapter exists. The evidence must record `network_execution_performed=false`, `live_provider_execution_performed=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`; extracted claims are formalization candidates only and must not satisfy literature evidence, theorem-search evidence, Lean replay, claim promotion, or GA certification. The focused suite `goal3-task258-pi-goal-mode-local-ingestion-evidence.test.mjs` must remain in the release-hardening matrix.
- Task259 keeps Pi goal-mode skeleton formalization hints service-owned and non-authoritative: `tickCampaign()` in `skeleton_gate` must persist `.comath/campaign/<campaign_id>/proof/formalization_hints.json`, bind `knowledge_pack.json` and `goal_mode_local_ingestion_evidence.json` by path/hash, convert only prompt-injection-scanned formalization-candidate extracted claim kinds into skeleton hints, bind those hints into `plan.json`, keep statement drift guards explicit, and require missing hint artifacts to rewind later stages to `skeleton_gate`. The hints must record `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `result_can_be_used_as_proof=false`, and `can_change_locked_statement=false`; they must not satisfy literature evidence, theorem-search evidence, Lean replay, claim promotion, or GA certification. The focused suite `goal3-task259-pi-goal-mode-skeleton-formalization-hints.test.mjs` must remain in the release-hardening matrix.
- Task260 keeps Pi goal-mode skeleton blueprints service-owned and non-authoritative: `tickCampaign()` in `skeleton_gate` must persist `.comath/campaign/<campaign_id>/proof/blueprint.json`, bind `proof/formalization_hints.json` by path/hash, bind the blueprint into `plan.json`, `lemma_dag.json`, `line_map.json`, and `skeleton_report.md`, and require missing blueprint artifacts to rewind later stages to `skeleton_gate`. Blueprint steps must record `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `result_can_be_used_as_proof=false`, `can_change_locked_statement=false`, `can_create_proof_obligation_without_formal_spec_lock=false`, and `requires_statement_diff_gate=true`; they must not create proof obligations, mutate the locked statement, satisfy literature/theorem-search/Lean replay evidence, promote claims, or certify GA. The focused suite `goal3-task260-pi-goal-mode-skeleton-blueprint-from-hints.test.mjs` must remain in the release-hardening matrix.
- Task261 keeps Pi goal-mode candidate generation bound to the service-owned skeleton blueprint: `line_map_gate` must persist `.comath/campaign/<campaign_id>/candidate_generation_request.json` with `proof/blueprint.json` and `proof/formalization_hints.json` path/hash bindings, blueprint step ids, and statement-boundary guards; `candidate_generation` must re-check those bindings before writing `candidate_generation.json` or the eight `lemma_sprint` task cards. Tampered or stale blueprint material must fail closed as a replayable blocker. The request, generation artifact, and task cards must keep `proof_authority="none"` and `can_promote_claim=false`; they must not treat blueprint metadata, agent output, votes, or candidate drafts as Lean replay evidence, promotion input, or GA certification. The focused suite `goal3-task261-pi-goal-mode-blueprint-bound-candidate-generation.test.mjs` must remain in the release-hardening matrix.
- Task262 keeps Pi goal-mode Lean candidate attempt materialization service-owned and non-authoritative: `candidate_generation` must write each candidate workspace's `lean_candidate_attempt_plan.json` and `LeanCandidate.lean`, bind the source `proof/Skeleton.lean`, current skeleton blueprint path/hash, blueprint step ids, and statement-boundary guards, and list those artifacts in the candidate manifest and agent output. The attempt plan and draft Lean file must keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`; `sorry` placeholders remain draft material and must not satisfy LeanRunManifest evidence, final clean replay, promotion, or GA certification. The focused suite `goal3-task262-pi-goal-mode-blueprint-lean-candidate-attempts.test.mjs` must remain in the release-hardening matrix.
- Task263 keeps Pi goal-mode Lean candidate attempt preflight service-owned and non-authoritative: `candidate_verification` must persist `.comath/campaign/<campaign_id>/lean_candidate_attempt_check_report.json`, re-read candidate manifests plus every `lean_candidate_attempt_plan.json` and `LeanCandidate.lean`, verify source skeleton and blueprint hashes, preserve locked statement boundary checks, detect `sorry`, and route candidates to repair-required or later LeanRunner readiness. The report must keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `result_can_be_used_as_proof=false`, `lean_runner_invocations=0`, and empty LeanRunManifest paths while `sorry` remains; it must not treat static preflight as Lean replay evidence, promotion input, or GA certification. The focused suite `goal3-task263-pi-goal-mode-lean-attempt-check-report.test.mjs` must remain in the release-hardening matrix.
- Task264 keeps Pi goal-mode Lean attempt repair routing service-owned and non-authoritative: `candidate_arbitration` may route explicit goal-mode campaigns with external refs and current `repair_required` `sorry` preflight into `repair`, persist `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_batch.json`, and write one per-candidate `lean_candidate_repair_task.json` work order. The batch and tasks must bind the source check report by path/hash, preserve locked-statement boundaries, assign A0 coordination plus A5/A8 repair roles, keep `lean_runner_invocations=0`, keep `proof_authority="none"`, keep `can_promote_claim=false`, keep `can_certify_ga=false`, forbid LeanRunManifest/FinalReplayManifest output, and forbid proof claims until a later no-sorry preflight plus LeanRunner replay exists. The focused suite `goal3-task264-pi-goal-mode-lean-attempt-repair-routing.test.mjs` must remain in the release-hardening matrix.
- Task265 keeps Pi goal-mode repair execution service-owned and non-authoritative: `repair` must consume only the current service-owned repair batch, snapshot each original candidate `LeanCandidate.lean`, write repaired no-sorry drafts with explicit `comath_repair_placeholder` markers, persist `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_execution.json` plus per-candidate execution manifests, return to `candidate_verification`, and keep `lean_runner_invocations=0`, empty LeanRunManifest paths, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`. Placeholder-bearing repaired drafts must be treated only as ready for service-owned LeanRunner preflight, not as proofs. The focused suite `goal3-task265-pi-goal-mode-repair-stage-reingestion.test.mjs` must remain in the release-hardening matrix.
- Task266 keeps Pi goal-mode ready-attempt LeanRunner execution service-owned and non-authoritative until Lean actually passes: `candidate_verification` must first persist ready preflight evidence, then a later tick may invoke `comathd.LeanRunner`, write append-only LeanRunManifest evidence, update candidate manifests/indexes, and emit a replayable blocker when every ready attempt is rejected. Failed LeanRunManifests and blockers must keep `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`; placeholder-bearing drafts must not become proof-grade even if a runner shim exits 0. The focused suite `goal3-task266-pi-goal-mode-ready-attempt-leanrunner-blocker.test.mjs` must remain in the release-hardening matrix.
- Task267 keeps Pi goal-mode all-rejected LeanRunner feedback service-owned and non-authoritative: a terminal explicit goal-mode campaign with canonical `all_attempts_rejected` execution may resume to `repair` only after writing `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_feedback_batch.json`, binding failed LeanRunManifest stdout/stderr paths and hashes, and materializing a second `lean_candidate_attempt_repair_batch.json`. Feedback-guided repair must keep `lean_runner_invocations=0`, empty repair-stage LeanRunManifest paths, `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `result_can_be_used_as_proof=false`; failed Lean diagnostics are repair evidence only and cannot certify GA or promote claims. The focused suite `goal3-task267-pi-goal-mode-leanrunner-feedback-repair-loop.test.mjs` must remain in the release-hardening matrix.
- Task268 keeps Pi goal-mode repair hint bundles service-owned and non-authoritative: the Task267 terminal feedback resume must also write `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_hint_bundle.json`, bind the repair feedback batch, goal-mode research plan, adapter execution manifest, and local ingestion evidence by path/hash, and expose descriptor-derived theorem-search, retrieval, computation, proof-search-backend, and external-Lean-repo hints only as repair context. The repair batch, tasks, and execution must carry `source_repair_hint_bundle`, keep `network_execution_performed=false`, keep `proof_authority="none"`, keep all promotion/GA flags false, keep repair-stage LeanRunner and LeanRunManifest paths absent, and fail closed on stale or tampered bundle hashes before overwriting candidate drafts. The focused suite `goal3-task268-pi-goal-mode-repair-hint-bundle.test.mjs` must remain in the release-hardening matrix.
- Task269 keeps Pi goal-mode repair hint executions service-owned and non-authoritative: the Task267/268 terminal feedback resume must also write `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_hint_execution.json`, bind the repair feedback batch and hint bundle by path/hash, record stubbed adapter result summaries with request/result hashes, provider terms, capability metadata, and no-authority promotion vetoes, and thread `source_repair_hint_execution` through the repair batch, tasks, and execution. Repair must keep `network_execution_performed=false`, `live_provider_execution_performed=false`, `proof_authority="none"`, all promotion/GA flags false, repair-stage LeanRunner and LeanRunManifest paths absent, and fail closed on stale or tampered execution hashes before overwriting candidate drafts. The focused suite `goal3-task269-pi-goal-mode-repair-hint-execution.test.mjs` must remain in the release-hardening matrix.
- Task270 keeps Pi goal-mode placeholder-free repairs bounded and gate-mediated: inline Lean theorem goals may seed the locked theorem target, but incomplete `:= by sorry` headers cannot be classified as solved adapter output. Static preflight must ignore comment-only `sorry`, require theorem/lemma declarations, route `sorry`, repair placeholders, and Lean holes back to repair, and permit LeanRunner only after all such blockers are absent. The focused suite `goal3-task270-pi-goal-mode-placeholder-free-repair-candidate.test.mjs` must remain in the release-hardening matrix; a LeanRunner-passing placeholder-free candidate still requires ordinary arbitration, red-team, integration, final clean replay, and promotion gates before any proof claim.
- Task271 keeps Pi goal-mode final replay terminal closure on the ordinary Lean Authority v3 path: a selected LeanRunner-passing bounded `True` candidate must write an isolated `final_replay_project` under its workspace, with `MathResearch/Target.lean`, audit file, campaign-bound FormalSpecLock/AssumptionLedger JSON, Lake files, and a candidate replay descriptor. The focused suite `goal3-task271-pi-goal-mode-final-replay-terminal.test.mjs` must remain in the release-hardening matrix and must prove the chain reaches `completed_formal_proof`, `formal_replay_authority_passed=true`, `formally_checked`, and export-ready clean replay evidence. This remains a bounded smoke target, not broad theorem synthesis or GA certification.
- Task272 keeps bounded definitional-equality repair on the same authority path: repair may materialize `by rfl` only for locked theorem/lemma statements whose top-level equality sides are syntactically identical, and must record `locked_reflexive_equality_rfl` as candidate material only. The focused suite `goal3-task272-pi-goal-mode-rfl-final-replay-terminal.test.mjs` must remain in the release-hardening matrix and must prove final clean replay, FinalReplayManifest v3 packaging, ordinary promotion, `formally_checked`, and export readiness. This is not general equality normalization, theorem search, live provider authority, broad mathlib proof breadth, or GA certification.
- Task273 keeps opt-in live retrieval repair-hint execution service-owned and non-authoritative: live `retrieval:jina_search` execution may occur only when `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` explicitly includes `retrieval:jina_search`, and `COMATH_JINA_SEARCH_BASE_URL` is configured. The focused suite `goal3-task273-pi-goal-mode-live-retrieval-repair-hint-execution.test.mjs` must remain in the release-hardening matrix and must prove the service sends `COMATH_JINA_API_KEY` only as an in-memory bearer header, records sanitized request/response hashes, provider status, content hash, prompt-injection scan, and no-authority vetoes, threads the live result into repair tasks/execution, and still emits no LeanRunManifest/FinalReplayManifest, claim promotion, or GA certification.
- Task274 keeps live retrieval suggestion repair bounded and Lean-gated: opt-in `retrieval:jina_search` responses may expose fenced Lean snippets only after a passing prompt-injection scan, and repair may consume a snippet only when it is a same-name theorem/lemma with the exact normalized locked statement, no `sorry`, holes, axioms, `unsafe`, `opaque`, `admit`, `run_cmd`, `elab`, `#eval`, or `set_option`, and no proof-authority flags. The focused suite `goal3-task274-pi-goal-mode-live-retrieval-suggestion-repair.test.mjs` must remain in the release-hardening matrix and must prove the repaired candidate reruns through service-owned LeanRunner with content-hash-specific append-only LeanRunManifest ids; retrieval suggestions still cannot promote claims, emit final replay authority, or certify GA.
- Task275 keeps live-retrieval repair provenance bound into final replay material: a candidate repaired from live retrieval suggestions may proceed through final clean replay only after the repaired LeanRunner rerun clears the previous transient LeanRunner-rejection veto and the selected candidate descriptor plus final replay material source bind the repair hint execution, campaign repair execution, and per-candidate repair execution by path/hash with `proof_authority=none`. The focused suite `goal3-task275-pi-goal-mode-live-retrieval-final-replay-provenance.test.mjs` must remain in the release-hardening matrix and must prove the terminal proof package is traceable without upgrading provider text into proof authority.
- Task276 keeps opt-in live theorem-search repair-hint execution service-owned and non-authoritative: live `theorem_search:loogle` execution may occur only when `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` explicitly includes `theorem_search:loogle`, and `COMATH_LOOGLE_SEARCH_BASE_URL` is configured. The focused suite `goal3-task276-pi-goal-mode-live-theorem-search-repair-hint-execution.test.mjs` must remain in the release-hardening matrix and must prove the service sends `COMATH_LOOGLE_API_KEY` only as an in-memory bearer header, records sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, bounded declaration results, and no-authority vetoes, threads the live result into repair tasks/execution, and still emits no LeanRunManifest/FinalReplayManifest, claim promotion, or GA certification.
- Task277 keeps live theorem-search declaration-guided repair narrow and Lean-gated: a live `theorem_search:loogle` declaration result may materialize a candidate only for the locked `True /\\ True` / `True ∧ True` conjunction class when the result contains non-authoritative `And.intro`, the live provider prompt-injection scan passes, and all no-authority flags remain false. The focused suite `goal3-task277-pi-goal-mode-live-theorem-search-conjunction-repair.test.mjs` must remain in the release-hardening matrix and must prove the repaired candidate is placeholder-free, uses `exact And.intro trivial trivial`, reruns through service-owned LeanRunner, and still cannot promote claims, emit final replay authority, or certify GA from the theorem-search result alone.
- Task278 keeps opt-in live computation repair-hint execution service-owned and non-authoritative: live `computation:sympy` execution may occur only when `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` explicitly includes `computation:sympy`, and `COMATH_SYMPY_COMPUTE_BASE_URL` is configured. The focused suite `goal3-task278-pi-goal-mode-live-computation-repair-hint-execution.test.mjs` must remain in the release-hardening matrix and must prove the service sends `COMATH_SYMPY_API_KEY` only as an in-memory bearer header, records sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, bounded computation report summaries, and no-authority vetoes, threads the live result into repair tasks/execution, and still emits no LeanRunManifest/FinalReplayManifest, claim promotion, or GA certification.
- Task279 keeps live computation-guided repair narrow and Lean-gated: a live `computation:sympy` exact-symbolic `True` report may materialize a `by decide` candidate only for a same-name/same-statement closed decidable arithmetic equality when prompt-injection scan and all no-authority flags pass. The focused suite `goal3-task279-pi-goal-mode-live-computation-decide-repair.test.mjs` must remain in the release-hardening matrix and must prove the repaired candidate is placeholder-free, uses `by decide`, handles typed arithmetic statements without statement drift, reruns through service-owned LeanRunner, and still cannot promote claims, emit final replay authority, or certify GA from the SymPy result alone.
- Task280 keeps live computation repair provenance visible but non-authoritative in public export: after final Lean clean replay passes, `exportCampaignGoalModeEvidence()` may expose the selected candidate's sanitized `candidate_repair_provenance` from final replay material, but it must remain `proof_authority=none`, use only relative path/hash references, and never leak host roots or provider secrets. The focused suite `goal3-task280-pi-goal-mode-live-computation-final-replay-provenance.test.mjs` must remain in the release-hardening matrix and must prove export readiness is still based on Lean clean replay while the live SymPy repair path remains audit provenance only.
- Task281 keeps the Pi public campaign export consumer auditable without opening a general runtime-path echo: `comath.campaign.export` and `/cm:campaign export` may preserve the three known no-authority `candidate_repair_provenance` path/hash references, but ordinary `.comath` paths, host roots, secrets, proof-success wording, and promotion flags must remain sanitized in Pi-visible output. The focused suite `goal3-task281-pi-goal-mode-export-repair-provenance-consumer.test.mjs` must remain in the release-hardening matrix.
- Task282 keeps the Pi `/cm:research` goal-mode loop auditable after terminal proof closure: once campaign export authority is verified, the loop may surface sanitized no-authority `candidate_repair_provenance` in `export_descriptor`, but the descriptor must remain `proof_authority=none` and missing or forged export authority must withhold provenance and degrade proof terminal state to a replayable blocker. The focused suite `goal3-task282-pi-research-loop-export-repair-provenance.test.mjs` must remain in the release-hardening matrix.
- Task283 keeps live theorem-search declaration-guided iff repair narrow and Lean-gated: a live `theorem_search:loogle` declaration result may materialize a candidate only for the locked `True <-> True` class when the result contains non-authoritative `Iff.intro`, the live provider prompt-injection scan passes, and all no-authority flags remain false. The focused suite `goal3-task283-pi-goal-mode-live-theorem-search-iff-repair.test.mjs` must remain in the release-hardening matrix and must prove the repaired candidate is placeholder-free, uses `exact Iff.intro (fun _ => trivial) (fun _ => trivial)`, and still cannot promote claims, emit final replay authority, or certify GA from the theorem-search result alone.
- Task233 keeps the Task220-231 operator/service transport line under check-debug coverage without adding a new transport surface: existing automatic orchestration, maintained transport contract, continuity checkpoint, and prepared handoff references must remain bound to the fixed Node HTTP AgentRun log-session route plus Pi `fetch`/`getText` primitive. Duplicate continuity ids, stale or tampered Task225 contract artifacts, missing contract hashes, secrets, proof-success wording, long stream wording, route overclaims, and public authority flags must fail closed or be sanitized. The focused suite `goal3-task233-pi-operator-service-transport-check-debug.test.mjs` must remain in the release-hardening matrix.
- Adapter OS-isolation sandbox-execution route payloads, caller-supplied execution booleans, process exit codes, or stdout/stderr/transcript hashes can be treated as collected OS-enforcement evidence without an internal service-owned execution probe callback and canonical probe/evidence artifact.
- Pi `/cm:release agent-adapter-os-isolation-sandbox-execution` payloads or tool outputs can be treated as collected OS-enforcement evidence, direct `.comath/` writes, production sandbox-runner evidence, or GA certification.
- Adapter OS-isolation sandbox-execution probe manifests are described as a production provider-specific sandbox runner, broad cross-platform OS-enforced execution support, mathematical proof authority, or GA certification.
- External Lean repositories can enter final replay without license, toolchain, commit, manifest, import, hash, and symlink checks.
- Literature or RAG evidence lacks provider, retrieval timestamp, content hash, terms note, prompt-injection scan, and citation anchors.
- Evidence packs cannot be replayed by a third party or do not distinguish omitted copyrighted material from included redistributable material.
- Public release/source-review archives blur the archive contract: source-review public diagnostic archives must stay non-authoritative (`proof_authority: none`), sanitized `public_download` snapshots must state `can_restore=false` and not a restore source, explicit `internal_restore` snapshots must be byte-for-byte runtime-fidelity restore sources only, and no public archive may substitute for FinalAuthorityPackagingV3 / Lean Authority v3 source-report evidence.

## Required Verification Before GA Tagging

Run at minimum:

```text
corepack pnpm build
corepack pnpm typecheck
corepack pnpm test
corepack pnpm --filter @comath/comathd build
corepack pnpm --filter @comath/comathd typecheck
corepack pnpm --filter @comath/comathd test
corepack pnpm --filter @comath/pi-extension build
corepack pnpm --filter @comath/pi-extension typecheck
corepack pnpm --filter @comath/pi-extension test
node scripts/phase0-smoke.mjs
```

Also run focused Goal 3 acceptance suites:

```text
node services/comathd/tests/unit/goal3-task2-no-toy-production-path.test.mjs
node services/comathd/tests/unit/goal3-task4-formal-spec-lock.test.mjs
node services/comathd/tests/unit/goal3-task5-statement-diff-gate.test.mjs
node services/comathd/tests/unit/goal3-task7-lean-run-manifest-v3.test.mjs
node services/comathd/tests/unit/goal3-task8-lean-authority-v3-final-replay.test.mjs
node services/comathd/tests/unit/goal3-task10-integrity-dependency-axiom-v2.test.mjs
node services/comathd/tests/unit/goal3-task11-external-wheel-registry.test.mjs
node services/comathd/tests/unit/goal3-task13-mathprove-native-stage-machine.test.mjs
node services/comathd/tests/unit/goal3-task14-ga-agent-stage-workflow.test.mjs
node services/comathd/tests/unit/goal3-task16-pi-goal-mode-routes.test.mjs
node services/comathd/tests/unit/goal3-task17-ga-acceptance-workflow.test.mjs
node extensions/comath-pi/tests/goal3-task16-pi-goal-mode.test.mjs
```

Also run focused release-hardening suites for the current adapter/Pi lifecycle boundary:

```text
node services/comathd/tests/unit/goal3-task167-agent-adapter-os-isolation-readiness.test.mjs
node services/comathd/tests/unit/goal3-task168-agent-adapter-os-isolation-probe.test.mjs
node services/comathd/tests/unit/goal3-task170-agent-adapter-os-isolation-host-collection.test.mjs
node services/comathd/tests/unit/goal3-task171-agent-adapter-os-isolation-sandbox-launch.test.mjs
node services/comathd/tests/unit/goal3-task172-agent-adapter-os-isolation-sandbox-execution.test.mjs
node extensions/comath-pi/tests/goal3-task173-pi-agent-adapter-os-isolation-sandbox-execution-consumer.test.mjs
node services/comathd/tests/unit/goal3-task175-agent-adapter-os-isolation-provider-runner.test.mjs
node services/comathd/tests/unit/goal3-task176-agent-adapter-os-isolation-provider-helper-execution.test.mjs
node services/comathd/tests/unit/goal3-task177-agent-adapter-os-isolation-provider-helper-collection.test.mjs
node services/comathd/tests/unit/goal3-task197-agent-adapter-os-isolation-provider-helper-collection-complete-enforcement-gate.test.mjs
node services/comathd/tests/unit/goal3-task198-agent-adapter-os-isolation-provider-helper-default-collection-probe.test.mjs
node services/comathd/tests/unit/goal3-task199-agent-adapter-os-isolation-configured-provider-helper-collection-probe.test.mjs
node services/comathd/tests/unit/goal3-task200-agent-adapter-os-isolation-configured-collection-host-capability-binding.test.mjs
node services/comathd/tests/unit/goal3-task201-agent-adapter-os-isolation-provider-helper-release-chain-check-debug.test.mjs
node services/comathd/tests/unit/goal3-task202-agent-adapter-os-isolation-bundled-provider-helper-collection-probe.test.mjs
node services/comathd/tests/unit/goal3-task203-agent-adapter-os-isolation-provider-tool-witness-gate.test.mjs
node services/comathd/tests/unit/goal3-task204-agent-adapter-os-isolation-provider-specific-tool-binding.test.mjs
node services/comathd/tests/unit/goal3-task205-agent-adapter-os-isolation-provider-family-enforcement-witness-gate.test.mjs
node services/comathd/tests/unit/goal3-task206-agent-adapter-os-isolation-provider-family-execution-profile-gate.test.mjs
node services/comathd/tests/unit/goal3-task207-agent-adapter-os-isolation-provider-specific-live-probe-attempt-gate.test.mjs
node services/comathd/tests/unit/goal3-task208-agent-adapter-os-isolation-provider-specific-live-probe-execution-gate.test.mjs
node services/comathd/tests/unit/goal3-task209-agent-adapter-os-isolation-provider-specific-live-probe-collection-binding-gate.test.mjs
node services/comathd/tests/unit/goal3-task210-agent-adapter-os-isolation-provider-control-plane-execution-witness-gate.test.mjs
node services/comathd/tests/unit/goal3-task211-agent-adapter-os-isolation-provider-helper-witness-chain-check-debug.test.mjs
node services/comathd/tests/unit/goal3-task212-agent-adapter-os-isolation-windows-appcontainer-production-helper-profile-contract.test.mjs
node services/comathd/tests/unit/goal3-task232-agent-adapter-os-isolation-nix-production-helper-profile-contract.test.mjs
node services/comathd/tests/unit/goal3-task213-campaign-live-mathlib-replay-breadth-gate.test.mjs
node services/comathd/tests/unit/goal3-task214-campaign-live-mathlib-dependency-material-gate.test.mjs
node services/comathd/tests/unit/goal3-task215-final-replay-dependency-closure-v2-binding.test.mjs
node services/comathd/tests/unit/goal3-task216-campaign-live-mathlib-provisioning-diagnostic.test.mjs
node services/comathd/tests/unit/goal3-task217-final-replay-dependency-lock-consistency.test.mjs
node services/comathd/tests/unit/goal3-task218-campaign-live-mathlib-host-replay-diagnostic.test.mjs
node services/comathd/tests/unit/goal3-task219-campaign-live-mathlib-import-graph-diagnostic.test.mjs
node services/comathd/tests/unit/goal3-task238-campaign-live-mathlib-no-download-fixture-preflight.test.mjs
node services/comathd/tests/unit/goal3-task220-pi-operator-transport-lease-agentrun-log-session-binding.test.mjs
node services/comathd/tests/unit/goal3-task221-pi-operator-transport-lease-heartbeat-rebind.test.mjs
node extensions/comath-pi/tests/goal3-task221-pi-operator-transport-heartbeat-consumer.test.mjs
node services/comathd/tests/unit/goal3-task225-pi-operator-service-transport-contract.test.mjs
node extensions/comath-pi/tests/goal3-task226-pi-operator-service-transport-contract-consumer.test.mjs
node services/comathd/tests/unit/goal3-task227-pi-automatic-real-pi-execution-orchestration.test.mjs
node extensions/comath-pi/tests/goal3-task228-pi-automatic-real-pi-execution-consumer.test.mjs
node services/comathd/tests/unit/goal3-task229-pi-operator-service-transport-continuity.test.mjs
node extensions/comath-pi/tests/goal3-task230-pi-operator-service-transport-continuity-consumer.test.mjs
node extensions/comath-pi/tests/goal3-task231-pi-prepared-unattended-real-pi-handoff-ux.test.mjs
node extensions/comath-pi/tests/goal3-task237-pi-prepared-handoff-operator-review-checklist.test.mjs
node services/comathd/tests/unit/goal3-task239-service-owned-unattended-real-host-handoff-review.test.mjs
node extensions/comath-pi/tests/goal3-task240-pi-unattended-real-host-handoff-review-consumer.test.mjs
node services/comathd/tests/unit/goal3-task241-pi-unattended-real-host-execution-readiness-blocker.test.mjs
node extensions/comath-pi/tests/goal3-task242-pi-unattended-real-host-execution-readiness-consumer.test.mjs
node services/comathd/tests/unit/goal3-task233-pi-operator-service-transport-check-debug.test.mjs
node services/comathd/tests/unit/goal3-task234-agent-adapter-os-isolation-firejail-production-helper-profile-contract.test.mjs
node services/comathd/tests/unit/goal3-task236-macos-out-of-scope-docs.test.mjs
node services/comathd/tests/unit/goal3-task178-agent-adapter-os-isolation-provider-helper-host-validation.test.mjs
node services/comathd/tests/unit/goal3-task179-agent-adapter-os-isolation-provider-helper-execution-host-validation-binding.test.mjs
node services/comathd/tests/unit/goal3-task181-agent-adapter-os-isolation-configured-provider-helper-asset.test.mjs
node services/comathd/tests/unit/goal3-task182-agent-adapter-os-isolation-configured-helper-execution-collection.test.mjs
node services/comathd/tests/unit/goal3-task184-agent-adapter-os-isolation-cross-provider-helper-assets.test.mjs
node services/comathd/tests/unit/goal3-task185-agent-adapter-os-isolation-helper-self-test-contract.test.mjs
node services/comathd/tests/unit/goal3-task186-agent-adapter-os-isolation-self-test-binding.test.mjs
node services/comathd/tests/unit/goal3-task187-agent-adapter-os-isolation-helper-runtime-attestation.test.mjs
node services/comathd/tests/unit/goal3-task188-agent-adapter-os-isolation-bundled-helper-asset.test.mjs
node services/comathd/tests/unit/goal3-task189-agent-adapter-os-isolation-provider-helper-chain-check-debug.test.mjs
node services/comathd/tests/unit/goal3-task190-agent-adapter-os-isolation-provider-host-capability-probe-contract.test.mjs
node services/comathd/tests/unit/goal3-task191-agent-adapter-os-isolation-provider-host-capability-helper-validation-binding.test.mjs
node extensions/comath-pi/tests/goal3-task192-pi-provider-helper-host-capability-consumer.test.mjs
node services/comathd/tests/unit/goal3-task193-agent-adapter-os-isolation-default-provider-host-capability-probe.test.mjs
node services/comathd/tests/unit/goal3-task194-agent-adapter-os-isolation-windows-appcontainer-host-facility-probe.test.mjs
node services/comathd/tests/unit/goal3-task195-agent-adapter-os-isolation-oci-container-host-facility-probe.test.mjs
node services/comathd/tests/unit/goal3-task196-agent-adapter-os-isolation-remaining-provider-host-facility-probes.test.mjs
node services/comathd/tests/unit/phase43-agent-adapter-package.test.mjs
node services/comathd/tests/unit/phase44-codex-cli-external-invocation.test.mjs
```

## Release Evidence Checklist

Before a public GA announcement, attach evidence for:

- Requirement-by-requirement status against the 2026-05-29 no-reinvent audit and open formal workbench design.
- Static scans proving old toy/Nat production paths are absent from production source.
- Clean runtime state: no tracked `.comath`, `.tmp`, `dist`, `node_modules`, or host-path leakage.
- At least one replayable promoted proof artifact whose clean Lean replay can be reproduced from the evidence pack.
- Negative trust-core cases: fake stdout, agent pass logs, forbidden Lean constructs, statement drift, hidden assumptions, unpinned dependencies, network replay, symlink escape, CAS-only proof, literature-only proof, vote-only proof, and human-review-only proof.
- Explicit blocker list for any deliberately deferred broad proof-test breadth. Broad matrix-style proof campaigns are final-release-candidate audit debt, not a prerequisite for ordinary product-closure tasks.

## Non-GA Labels

Use these labels when evidence is incomplete:

- `research-alpha`: workflow foundation with fail-closed gates.
- `vertical-slice`: executable path over bounded examples only.
- `ga-candidate`: all gates implemented, final review pending.
- `replayable-blocker`: a blocker certificate and resume path exist.
- `draft` or `candidate`: no final clean Lean replay.
