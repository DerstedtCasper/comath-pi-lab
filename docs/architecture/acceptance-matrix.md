# Acceptance Matrix

## Design Documentation Acceptance

| Requirement | Evidence |
| --- | --- |
| Complete project design exists | `COMATH_PI_LAB_DEV_PLAN.md` contains product thesis, architecture, roadmap, milestones, and completion criteria. |
| Goal-mode execution is bounded | `CODEX_GOAL_RUNBOOK.md` contains Phase 0-17 scopes, definitions of done, validation, and stop rules. |
| End-state behavior is defined | `docs/architecture/end-state-blueprint.md`. |
| Phase acceptance is defined | This file and `CODEX_GOAL_RUNBOOK.md`. |
| Agent collaboration is bounded | `docs/architecture/agent-operating-model.md` and `docs/architecture/subagent-concurrency.md`. |
| Risks are explicit | `docs/architecture/risk-register.md`. |
| Handoff exists | `docs/progress/design-handoff.md`. |
| Smoke test covers design docs | `scripts/phase0-smoke.mjs`. |
| Goal 3 release-hardening docs and prompts are checked | `scripts/phase0-smoke.mjs` checks release criteria, threat model, adapter contracts, external Lean supply chain, evidence-pack policy, non-secret config samples, `.pi/agents`, and `.pi/prompts` for Lean-authority and no-reinvent invariants. |

## Phase Acceptance

Goal 3 quarantine note: Phase 18/23/57/67/72-76/81 Nat and theorem-family rows below are retained as chronological or fixture evidence only. They are not current production proof paths and must not be read as acceptance of business-layer theorem recognition, Nat-linear synthesis, default `n : Nat` assumptions, or proof promotion outside FormalSpecLock, AssumptionLedger, StatementDiffGate, dependency locks, Lean Authority v3 replay manifests, and the ordinary promotion gate.

| Phase | Acceptance Evidence |
| --- | --- |
| 0 Repo bootstrap | install/build/typecheck/test pass; `TODO.md` and `REVIEW.md` updated. |
| 1 Contracts | schema/type tests for IDs, claims, artifacts, GraphPatch, and statement hash pass. |
| 2 Service foundation | project init/open/status integration tests and path-policy unit tests pass. |
| 3 Artifact/audit | hash stability, safe copy, path traversal, audit JSONL tests pass. |
| 4 Claims/gate | promotion route fails closed; direct `formally_checked` assignment impossible. |
| 5 Memory | in-memory adapter and StableIdMap tests pass; Trivium shim fails clearly if unavailable. |
| 6 Pi extension | typecheck passes; commands/tools/resources are runtime-tested; mutating descriptors require confirmation and call `comathd`. |
| 7 Workstreams | workstream lifecycle tests pass; GraphPatch does not auto-apply. |
| 8 Subagents | agent prompts constrain write scope; safe parallelism docs exist. |
| 9 MathProve bridge | bridge mock returns structured fail-closed JSON and gate consumes vetoes. |
| 10 Compute | runner timeout/sandbox/replay tests pass. |
| 11 Literature | citation artifact and condition matching stub tests pass. |
| 12 Paper | paper check fails on overclaim and missing provenance. |
| 13 TriviumDB | capability probe, fallback/default-safe factory behavior, optional native snapshot/restore tests, and StableIdMap boundary tests pass without requiring native TriviumDB in default CI. |
| 14 Braid domain | ontology, prompts, sample project, and computation protocols exist. |
| 15 Dashboard | read-only text/TUI renderer tests pass; dashboard does not read/write `.comath/` directly or repair state. |
| 16 Snapshot/replay | service routes, export/verify/restore smoke tests, replay manifest verification, tamper checks, and secret scan gates pass. |
| 17 Evaluation/audits | security review, math integrity review, and evaluation suite exist. |
| 18 GA proof-kernel vertical slices | Historical vertical-slice tests for ResearchCampaign routes, replay-manifest gate shape, 8-candidate artifacts, statement-drift/cheat rejection, exact refutation, snapshot restore/replay, and Pi campaign tool boundaries. Under Goal 3 these do not define the current production proof path. |
| 19 GA ensemble recovery | `phase19-ga-ensemble-recovery.test.mjs` verifies seven failed candidates plus one Lean-valid candidate selects the Lean-valid candidate, preserves all failed routes, and writes the V8 dialectical stress artifact. |
| 20 GA campaign state machine | `phase20-ga-campaign-state-machine.test.mjs` verifies v3 canonical campaign states, terminal-state names, bounded ticks, proof completion, refutation completion, terminal invariants, and unsupported-goal blocking. |
| 21 Service read models | `phase21-read-model-routes.test.mjs` verifies service-owned claim/evidence/gate list routes, and `phase15-dashboard.test.mjs` verifies the Pi dashboard reads those routes without mutation or direct `.comath/` access. |
| 22 Pi research campaign loop | `phase22-research-loop.test.mjs` verifies quote-aware `/cm:research` input, scoped campaign-loop capability checks, bounded start/tick execution through `comathd`, and dashboard return without direct `.comath/` access. |
| 23 Proof-kernel theorem-family registry | Historical theorem-family fixture coverage. `phase23-ga-theorem-family-generalization.test.mjs` and `phase23-ga-integrity-boundaries.test.mjs` remain useful for regression evidence, but Goal 3 quarantine forbids treating theorem-family recognition as a production proof path. |
| 24 Runner re-execution replay | `phase10-compute-runners.test.mjs` verifies canonical replay input capture, and `phase16-snapshot-replay.test.mjs` verifies strict `/replay/verify-manifest` re-executes `sympy-exact` and `counterexample-search`, skips placeholders explicitly, and fails closed on replay/report drift, static snapshot vetoes, script hash drift, input hash drift, oversized replay timeout, report-local stdio hash drift, and untrusted replay argv. |
| 25 Real MathProve external bridge | `phase25-real-mathprove-bridge.test.mjs` verifies controlled `MathProve-Skill` `verify_sympy.py` invocation, missing-runner fail-closed archival, statement-hash mismatch vetoes, runner metadata hashes, and no claim promotion from external MathProve output alone. |
| 26 Pi runtime registration | `phase26-pi-runtime-registration.test.mjs` verifies the Pi 0.75.5-compatible package manifest, CoMath `runtime_registration` contract, global `rpm=4` preservation, Pi host-side mutating-tool confirmation gates, manifest-driven dynamic import, fake Pi API registration, command dispatch, and no Pi proof authority. Installed Pi loader smoke verifies `dist/index.js` loads through `@earendil-works/pi-coding-agent@0.75.5`. |
| 27 AgentRun runtime boundary | `phase27-agent-run-runtime.test.mjs` verifies `ARUN-XXXX` persistence, queued/running/submitted lifecycle, workstream and `.tmp` write-scope confinement, report-heading validation, GraphPatch producer self-review rejection, independent review/apply, failed-run `FailureRoute` memory, and AgentRun audit events. |
| 28 AgentRun process scheduler | `phase28-agent-run-scheduler.test.mjs` verifies real allowlisted child-process launch, `shell:false` execution, absolute-realpath allowlist rejection, serial scheduling under `max_concurrent=1`, enqueue-time rpm rejection, minimal env inheritance with sensitive-env rejection, stdout/stderr logs under `.tmp/comath/<ARUN>/logs`, byte-capped output truncation, report persistence, invalid-report fail-closed handling, timeout failure, queued/running cancellation, process-tree termination attempts, non-authoritative scheduler envelopes, and scheduler audit events. |
| 29 Agent profile service integration | `phase29-agent-profile-integration.test.mjs` verifies the nine GA agent profiles, `rpm<=4`, no proof authority, forbidden direct-promotion tools, service profile list/get routes, profile-backed AgentRun creation, launch-envelope preparation, secret-like env exclusion, unknown-profile rejection, and `agent_profile_service_api` status capability. |
| 30 Pi agent profile runtime UX | `phase30-agent-profile-tools.test.mjs` verifies executable Pi tools for profile list/get, profile-backed AgentRun creation, launch-envelope preparation, `/cm:agent` command dispatch, host confirmation for mutating profile tools, local required-argument validation, runtime registration inclusion, and no direct `.comath/` access from the extension. |
| 31 Lean trust profile hardening | `phase31-lean-trust-profile.test.mjs` verifies configurable axiom allowlists, constructive rejection of `Classical.choice`, required target-theorem axiom reports, and skeleton-only `sorry` allowlisting for static proof-integrity scans. |
| 32 Lean statement signature binding | `phase32-lean-statement-signature.test.mjs` verifies that statement equivalence accepts only a unique target theorem signature and fails closed on missing, ambiguous, substring-only, or mismatched theorem-type output. |
| 33 Proof obligation DAG planning | `phase33-proof-obligation-dag.test.mjs` verifies campaign-scoped `lemma_dag.json`, `line_map.json`, obligation YAML, `Skeleton.lean`, and skeleton report artifacts across all open obligations; validates duplicate-node, unknown-endpoint, unsupported-relation, and cycle rejection; records planning artifacts in campaign stage runs; and proves two campaigns do not overwrite one another's planning artifacts. |
| 34 Campaign-scoped ensemble artifacts | `phase34-campaign-ensemble-isolation.test.mjs` verifies two interleaved supported campaigns in one project root keep candidate workspaces, `candidates.json`, and `decision.json` under `.comath/campaign/<CAM>/ensembles/lemma_sprint/<PO>/`, and that campaign A never reads campaign B candidate runs after B advances. |
| 35 Claim-scoped final replay artifact paths | `phase35-final-replay-artifact-paths.test.mjs` verifies final replay stage-run artifact paths are generated from the active root claim id, including a second supported campaign whose claim is not `C-0001`. |
| 36 Runner replay sandbox and dependency provenance | `phase36-runner-replay-provenance.test.mjs` verifies sandbox-policy and dependency-lock material in compute runner reports and replay manifests, plus fail-closed vetoes when either provenance class is missing. |
| 37 Registered Lean statement alias equivalence | `phase37-lean-statement-alias-equivalence.test.mjs` verifies explicitly registered definitional aliases can accept a target theorem signature such as `Nat.add n 0 = n` for locked `n + 0 = n`, while missing, ambiguous, and non-registered mismatched target output still fails closed. |
| 38 Native TriviumDB target-platform evaluation | `phase38-trivium-native-evaluation.test.mjs` verifies fail-closed unavailable reports and the evaluation-report contract; `eval:trivium` validates real `triviumdb@0.7.1` capability, persistence reopen, search top-hit ratio, and upsert/context timing on the target platform. |
| 39 Project writer session lock | `phase39-project-session-lock.test.mjs` verifies exclusive project writer lock acquisition, active-lock rejection, token-gated release, stale takeover, previous-session provenance, and malformed-lock fail-closed behavior. |
| 40 AgentRun scheduler writer lock integration | `phase40-agent-scheduler-session-lock.test.mjs` verifies scheduled AgentRun process execution fails closed when another active project writer exists, starts no child process, preserves the queued run, acquires/releases the lock for allowed runs, and records lock audit events. |
| 41 Live agent adapter execution | `phase41-live-agent-adapter-execution.test.mjs` and `phase41-agent-execute-tool.test.mjs` verify profile-bound AgentRun creation, real allowlisted adapter process execution through the scheduler, service route execution, Pi runtime tool/command execution, writer-lock integration, and non-authoritative report wrapping. |
| 42 AgentRun observability | `phase42-agent-run-observability.test.mjs` and `phase42-agent-observability-tools.test.mjs` verify capped AgentRun stdout/stderr readback, `GET /agent/run/:id/logs`, bounded adapter health probes, `POST /agent/adapter/health`, Pi `comath.agent.logs`/`comath.agent.health`, and `/cm:agent logs` plus `/cm:agent health`. |
| 43 Agent adapter package registry | `phase43-agent-adapter-package.test.mjs` and `phase43-agent-adapter-package-tools.test.mjs` verify the service-owned `codex-cli` adapter package registry, bundled launcher script, package prepare/execute routes, package health compatibility, Pi package tools, and `/cm:agent packages`, `/cm:agent prepare-package`, `/cm:agent execute-package`. |
| 44 Codex CLI external adapter invocation | `phase44-codex-cli-external-invocation.test.mjs` and `phase44-agent-adapter-external-tools.test.mjs` verify service-configured external CLI invocation, fail-closed missing configuration, fixed argv, untrusted output wrapping, `proof_authority=none`, Pi backend selection, and `/cm:agent prepare-package|execute-package --backend external`. |
| 45 Pi/comathd install-session e2e | `phase45-pi-comathd-install-session.test.mjs` starts a real local `comathd` HTTP server, imports the built Pi package entrypoint from `package.json`, registers into a fake Pi host, and exercises campaign/agent commands through `createComathClient({ baseUrl })` with host confirmation. |
| 46 Cursor-based AgentRun log stream | `phase46-agent-log-stream.test.mjs` and `phase46-agent-log-stream-tools.test.mjs` verify service-owned stdout/stderr byte cursors, `GET /agent/run/:id/log-stream`, bounded chunks, invalid-cursor rejection, audit events, `agent_run_log_stream_cursor`, Pi `comath.agent.streamLogs`, and `/cm:agent stream`. |
| 47 SSE-style AgentRun log subscription | `phase47-agent-log-subscription.test.mjs` and `phase47-agent-log-subscription-tools.test.mjs` verify service-owned `text/event-stream` log snapshot frames, `GET /agent/run/:id/log-subscription`, audit events, `agent_run_log_subscription_sse`, Pi `comath.agent.subscribeLogs`, `createComathClient().getText()`, and `/cm:agent subscribe-logs`. |
| 48 AgentRun operator panel | `phase48-agent-operator-panel.test.mjs` and `phase48-agent-operator-panel-tools.test.mjs` verify service-owned AgentRun operator panels, `GET /agent/run/:id/operator-panel`, status/log/subscription aggregation, action availability metadata, audit events, `agent_run_operator_panel`, Pi `comath.agent.operatorPanel`, and `/cm:agent panel`. |
| 49 Scheduler-backed AgentRun operator cancellation | `phase49-agent-operator-cancel.test.mjs` and `phase49-agent-operator-cancel-tools.test.mjs` verify same-process active scheduler registry cancellation, `POST /agent/run/:id/cancel`, panel cancellability toggling, audit events, `agent_run_operator_cancel`, Pi `comath.agent.cancelRun`, host confirmation, and `/cm:agent cancel`. |
| 50 Multi-event AgentRun log session | `phase50-agent-log-session.test.mjs` and `phase50-agent-log-session-tools.test.mjs` verify bounded multi-event SSE log sessions, `GET /agent/run/:id/log-session`, max-event termination, completion/no-progress termination, audit events, `agent_run_log_session_sse`, Pi `comath.agent.logSession`, and `/cm:agent log-session`. |
| 51 Service-configured Codex API backend | `phase51-codex-api-backend.test.mjs` and `phase51-codex-api-backend-tools.test.mjs` verify `codex-api` packaged-adapter backend selection, service-configured Responses-compatible request shaping, API-key non-disclosure in launch/Pi payloads, missing-key fail-closed behavior, `codex_api_adapter_backend`, Pi `--backend codex-api`, and `proof_authority=none` report wrapping. |
| 52 Codex API retry and rate-limit telemetry | `phase52-codex-api-retry-telemetry.test.mjs` verifies retryable `429`/`5xx` Codex API attempts, capped `Retry-After` handling, bounded `COMATH_CODEX_API_MAX_ATTEMPTS`, attempt/status/rate-limit telemetry in reports and audit events, exhausted-attempt fail-closed behavior, API-key non-disclosure in logs, and status capability `codex_api_retry_telemetry`. |
| 53 Installed Codex CLI validation | `phase53-installed-codex-cli-validation.test.mjs` verifies service-configured installed Codex CLI version and health probes, fail-closed missing `COMATH_CODEX_CLI_PROGRAM`, `/agent/adapter/package/validate-installed-cli`, audit event `agent_adapter.installed_codex_cli_validated`, executable-path non-disclosure, and status capability `installed_codex_cli_validation`. |
| 54 Lean declaration parser signature fallback | `phase54-lean-declaration-parser.test.mjs` verifies conservative theorem/lemma declaration parsing, multi-line binder extraction, namespace-qualified target binding, ambiguous/comment-only fail-closed behavior, `signature_source: lean_declaration_parser`, and status capability `lean_declaration_parser_signature_fallback`. |
| 55 Runner cross-machine replay environment gate | `phase55-runner-cross-machine-replay.test.mjs` verifies validly rehashed snapshot replay metadata still fails closed with `runner_reexecution_environment_mismatch` when recorded Node/platform/arch environment material differs from the current process, blocks runner launch before re-execution, and reports status capability `runner_cross_machine_replay_environment_gate`. |
| 56 Registered Lean logical-equivalence witnesses | `phase56-lean-registered-logical-equivalence.test.mjs` verifies target signatures may pass as `logically_equivalent_with_registered_lemmas` only when a registered entry exactly binds the formal spec and target signature to non-empty lemma names, a `lean_kernel_checked_equivalence` witness kind, witness artifact id, valid SHA-256 artifact hash, and Task112 artifact-backed Lean replay manifest path; missing witness material and wrong target signatures fail closed. |
| 57 Lean theorem template instantiation | Historical registered-template fixture coverage for `nat_zero_add` and old `proof_kernel_theorem_template_instantiation` capability reporting. It is retained to guard old regressions, but it is not accepted as current production theorem recognition or broad theorem synthesis. |
| 58 MathProve final-audit external runner | `phase58-mathprove-final-audit-runner.test.mjs` verifies controlled `MathProve-Skill` `final_audit.py` invocation from a CoMath-owned workspace, fixed argv, generated steps/solution hashes, host-path-scrubbed archived reports, `network=false` metadata, status capability `mathprove_final_audit_external_runner`, and no claim promotion from a passed final-audit report alone. |
| 77 Runner network sandbox policy | `phase77-runner-network-sandbox-policy.test.mjs` verifies compute-runner reports and replay manifests bind `COMATH_RUNNER_NETWORK=disabled`, replay integrity fails closed when the network-denial contract is missing, and re-execution rejects missing policy before launching a runner. |
| 80 bounded equivalence-search witness materialization | `phase80-bounded-equivalence-witness-materialization.test.mjs` verifies a Phase 79 blocked plan can materialize only into non-promotional registered witness metadata with exact source/target binding, safe registered lemma hints, SHA-256 materialization binding, required final Lean Authority v2 gates, and fail-closed unregistered/tampered/wrong-target/missing-id cases. |
| 60 v3 campaign pause/tick contract | `phase60-v3-campaign-pause-resume.test.mjs` verifies paused campaigns reject ticks with `CAMPAIGN_PAUSED`, do not advance stage/artifacts while paused, resume cleanly, and expose `campaign_pause_tick_guard`. |
| 61 v3 candidate manifest and failure aggregate | `phase61-v3-candidate-contract.test.mjs` verifies v3 candidate manifests, manifest validation before arbitration, campaign-scoped failure aggregates, repair/refutation recommendations, and `candidate_manifest_v3_contract` / `failure_route_aggregate_memory`. |
| 62 v3 evidence-weighted decision forest | `phase62-v3-decision-forest.test.mjs` verifies manifest-valid kernel-checked candidate selection, hard-veto blocking, low-weight score/agreement signals, refutation-priority repair routing, failed-only recovery plans, and `evidence_weighted_decision_forest`. |
| 63 v3 native stage-gate artifact coverage | `phase63-v3-stage-gate-artifact-coverage.test.mjs` verifies knowledge, notation, skeleton, line-map, refutation/red-team, integration/refactor, final audit/replay, memory-update artifacts, missing-artifact blockers, and `native_stage_gate_artifact_guard`. |
| 64 Lean Authority v2 final gate hash binding | `phase64-lean-authority-v2-final-gate.test.mjs` verifies final replay artifact hashes/sizes, fresh artifact re-read before promotion, stale/tampered replay rejection, and `lean_authority_v2_final_gate_hash_binding`. |
| 65 failed-route proof-memory retrieval | `phase65-proof-memory-retrieval.test.mjs` verifies typed failed-route proof-memory events, exact/similar retrieval, stale/superseded warnings, knowledge-pack injection, and `proof_memory_failed_route_retrieval`. |
| 66 Pi goal-compatible campaign UX | `phase66-goal-compatible-campaign-ux.test.mjs` verifies `/cm:research --goal`, `/cm:campaign final-audit`, `/cm:campaign replay`, thin-client `comathd` routing, host confirmation, and installed-session compatibility. |
| 67 v3 end-to-end formal campaign slice | Historical registered Nat fixture coverage only: `phase67-v3-formal-campaign-slice.test.mjs` previously exercised `n + 0 = n` through bounded service-owned ticks, v3 stage sequence, 8-candidate arbitration, final static audit, clean replay, old promotion, replay route, and artifact bundle. It is not current production proof promotion under Goal 3 quarantine. |
| 68 v3 negative GA slice runner | `phase68-v3-negative-ga-slices.test.mjs` verifies `POST /release/v3-negative-ga-slices`, persisted `.comath/release/v3_negative_ga_slices.json`, statement drift rejection, cheating Lean artifact rejection, false-theorem refutation, all-candidate failure recovery, and snapshot replay that still cannot promote without fresh hash-bound final replay evidence. |
| 69 v3 terminal vocabulary compatibility | `phase69-v3-terminal-vocabulary.test.mjs` verifies `campaign.external_v3_terminal_state` projections for `formal_proof_verified`, `verified_counterexample`, `user_visible_theorem_repair_required`, `replayable_environment_blocker`, and `user_cancelled`; Phase 22 Pi loop tests verify the projected field is preserved and can terminate the loop without changing trusted internal state. |
| 70 broad theorem planning slice | `phase70-broad-theorem-planning-slice.test.mjs` verifies a non-template theorem target writes `broad_synthesis_plan.json`, `broad_replay_target.json`, and `broad_synthesis_failure.json` against the existing problem lock, obligation DAG, and line map, blocks promotion, keeps `proof_authority: none`, and leaves the claim `conjectural`. |
| 71 stage-gate repair/resume | `phase71-stage-gate-repair-resume.test.mjs` verifies ordinary `/resume` cannot unblock a missing-artifact stage gate, `POST /campaign/:id/repair-resume` requires the persisted `stage_gate_blocker.json` and exact missing artifact set, incomplete repair stays blocked, valid repair writes `stage_gate_repair.json` with `proof_authority: none`, keeps the claim conjectural, and resumes only to the recorded rewind target. |
| 72 theorem-specific Lean target package | Historical fixture coverage only: `phase72-theorem-specific-lean-generation.test.mjs` records the old bounded `n + n = 2 * n` `theorem_specific_lean_project.json` target-package behavior and its bindings. It is not current production Nat-linear proof authority. |
| 73 bounded theorem-specific proof-body synthesis | Historical fixture coverage only: `phase73-bounded-lean-proof-body-synthesis.test.mjs` records the old `bounded_proof_body_synthesis.json` artifact behavior for `n + n = 2 * n`, including `by omega` candidate material, while preserving non-authoritative proof-body semantics. |
| 74 bounded Lean Authority report preparation | Historical fixture coverage only: `phase74-bounded-authority-report-preparation.test.mjs` records old `bounded_authority_report_preparation.json` preview report behavior for the bounded Nat-linear target. Preview reports remain non-authoritative and cannot replace current Goal 3 final authority gates. |
| 75 bounded final clean replay promotion | Historical fixture coverage only: `phase75-bounded-final-clean-replay.test.mjs` records old bounded Nat-linear `final_replay_manifest.json` behavior. It is not accepted as current production proof promotion under Goal 3 quarantine. |
| 76 registered Nat linear identity targets | Historical fixture coverage only: `phase76-registered-nat-linear-targets.test.mjs` records old registered Nat-linear target behavior such as `n + 0 + n = 2 * n`; it is not current production Nat synthesis or proof authority. |
| 81 controlled Nat linear identity synthesis | Historical fixture coverage only: `phase81-controlled-nat-linear-synthesis.test.mjs` records the old safe one-variable Nat-linear fixture `2 * n + 3 = n + n + 3`. It is not current production theorem proving or proof authority. |
| 78 registered transitive statement-equivalence witnesses | `phase78-lean-transitive-equivalence.test.mjs` verifies registered transitive logical-equivalence chains are accepted only when the chain endpoint binds the locked formal spec to the extracted target signature, every intermediate link closes exactly, and every link supplies kernel-checked witness metadata, SHA-256 artifact hashes, non-empty lemma names, and Task112 artifact-backed Lean replay manifest paths. |
| 79 statement-equivalence proof-search plan artifacts | `phase79-lean-equivalence-search-plan.test.mjs` verifies unresolved mismatched target signatures can write service-owned `equivalence_search_plan.json` obligations with `proof_authority: none`, `can_promote_claim: false`, exact source/target binding, safe candidate lemma names, and required next witness artifacts; exact/direct/transitive/missing-output/empty-hint/unsafe-hint paths do not create proof-search authority. |

## Security Acceptance

| Invariant | Evidence |
| --- | --- |
| No outside-root writes | path-policy tests. |
| No unsafe shell by default | runner permission envelope and deny tests. |
| Secrets not imported or snapshotted | artifact import and snapshot export secret-scan tests. |
| Runtime state not committed | `.gitignore` and smoke test. |
| Native backends optional | TriviumDB probe/fallback tests. |
| Native TriviumDB evaluation is explicit | Phase 38 keeps `triviumdb` as a root optional dependency and requires explicit target-platform evaluation before native-backend claims. |
| Project writers are session-scoped | Phase 39 creates a service-owned writer lock under `.comath/sessions/` so concurrent writers have a fail-closed coordination primitive. |
| Scheduled AgentRuns respect writer locks | Phase 40 makes the process scheduler acquire the project writer lock before mutating run state/logs/reports and reject launches when another active writer owns the project. |
| Live adapters remain allowlisted and scoped | Phase 41 executes profile-backed adapters only through scheduler allowlists, scoped AgentRun write paths, host confirmation on Pi, and service-owned audit/report paths. |
| Agent adapter health probes remain non-authoritative | Phase 42 health checks use absolute program validation, `shell:false`, a minimal environment, bounded timeout/output, `COMATH_PROOF_AUTHORITY=none`, host confirmation on Pi, and audit events without trusted-state promotion. |
| Packaged adapters remain service-owned and allowlisted | Phase 43 resolves adapter launch commands from a service-owned package registry and bundled launcher script, preserving absolute program validation, scheduler allowlists, host confirmation on Pi, rpm=4, scoped writes, and audit events. |
| External Codex-compatible CLI remains service-configured | Phase 44 resolves the external program only from service environment configuration, passes it through the bundled launcher with fixed argv and `shell:false`, and never lets Pi/model input provide executable paths or shell strings. |
| AgentRun log streams are cursor-bounded | Phase 46 reads scheduler-owned stdout/stderr files only through validated non-negative byte cursors, bounded `max_bytes`, project-confined paths, and audit events; it does not expose shell access or direct `.comath/` mutation. |
| AgentRun log subscriptions are SSE snapshots | Phase 47 emits `text/event-stream` frames only from the same cursor-bounded service log reader, with project-confined paths, bounded `max_bytes`, audit events, and no shell access or direct `.comath/` mutation. |
| AgentRun operator panels are read-only | Phase 48 aggregates AgentRun status, log cursors, subscription metadata, and endpoint/action availability from service-owned readers only; Pi/model input cannot mutate `.comath/`, provide executable paths, or request cancellation unless a future real scheduler registry proves cancellability. |
| Operator cancellation is scheduler-backed | Phase 49 permits cancellation only when the same `comathd` process has an active scheduler registry entry for the run; Pi cancellation is host-confirmed, calls `comathd`, and never exposes executable paths, shell strings, or direct `.comath/` writes. |
| AgentRun log sessions are bounded | Phase 50 emits multiple SSE frames only through cursor-bounded service log reads, `max_events`, project-confined paths, audit events, and no-progress termination; it does not expose shell access, executable paths, direct `.comath/` mutation, or an indefinite operator channel. |
| Codex API backend credentials stay service-owned | Phase 51 resolves the Codex API key, optional base URL, and model from `comathd` configuration; Pi/model input can select only the backend enum, and launch payloads expose only a key reference/configured metadata rather than secret values. |
| Codex API retries are bounded and secret-free | Phase 52 retries only retryable `429`/`5xx` statuses, caps server-requested delay, bounds attempts through `COMATH_CODEX_API_MAX_ATTEMPTS`, records only status/rate-limit telemetry, and keeps API keys out of reports, logs, audit payloads, and Pi/package launch metadata. |
| Installed Codex CLI validation is service-configured | Phase 53 resolves the external CLI only from service environment configuration, runs bounded version and health probes with `shell:false` and `COMATH_PROOF_AUTHORITY=none`, records path-free diagnostics, and never accepts executable paths from Pi/model payloads. |
| Lean declaration parsing is in-process and fail-closed | Phase 54 parses only supplied Lean source text, executes no code, rejects ambiguous/comment-only matches, and records declaration-parser provenance without adding a new process or network boundary. |
| Runner replay environment drift fails closed | Phase 55 compares replay-run Node version, platform, and architecture metadata with the current process before runner re-execution; mismatches produce `runner_reexecution_environment_mismatch` and no child runner launch. |
| Registered logical equivalence requires kernel witness metadata | Phase 56 accepts logical-equivalence statement binding only from service-owned registered metadata with exact formal-spec/target-signature matching, non-empty lemma names, `lean_kernel_checked_equivalence`, witness artifact id, and SHA-256 witness hash; free-form justification alone cannot pass. |
| Registered transitive logical equivalence requires closed kernel-witness chains | Phase 78 accepts transitive statement binding only from service-owned registered chains whose first endpoint is the locked formal spec, whose final endpoint is the extracted target signature, whose intermediate links close exactly, and whose every link carries `lean_kernel_checked_equivalence`, witness artifact id, SHA-256 witness hash, and non-empty lemma names. |
| Equivalence-search plans are non-authoritative obligations | Phase 79 writes `equivalence_search_plan.json` only under the runtime path policy for unique mismatched target signatures with safe lemma-name hints and no accepted registered witness; the artifact remains `blocked_unproved`, `proof_authority: none`, and `can_promote_claim: false`. |
| Bounded equivalence witness materialization is non-promotional | Phase 80 writes `equivalence_witness_materialized.json` only from a non-authoritative blocked plan with exact source/target binding, registered safe lemma hints, a non-empty witness artifact id, SHA-256 materialization binding, and required final Lean Authority v2 gates; it does not promote claims. |
| Non-exact equivalence replay is artifact-backed | Task112 requires candidate arbitration, StatementDiffGate, registered logical equivalence, and registered transitive equivalence to resolve non-exact equivalence replay evidence to verified service-owned LeanRunManifest v3 or FinalReplayManifest v3 artifacts; marker-only and id-only replay references fail closed. |
| Theorem template instantiation remains registry-bound | Phase 57 instantiates only a service-owned theorem-family registry entry, fixes the Lean target/proof dependency/replay command from code, and still blocks unsupported or tampered theorem-family metadata through existing fail-closed paths. |
| MathProve final audit remains fixed-argv and non-authoritative | Phase 58 invokes only the trusted sibling `MathProve-Skill` `scripts/final_audit.py` with CoMath-generated steps, controlled workspace paths, bounded timeout, `shell:false`, `network=false` metadata, host-path scrubbing, and fail-closed untrusted-root/missing-runner/statement-hash mismatch handling. |
| Runner network denial is service-bound, not OS authority | Phase 77 records and enforces a service process-environment contract for compute-runner execution/replay. It is replayable provenance and a fail-closed preflight gate, not kernel/firewall-enforced network isolation. |
| Negative GA release slices are service-owned | Phase 68 runs the negative release suite inside `comathd`, writes only under `.comath/release/`, records failed promotion gates/audit evidence through the normal promotion path, and does not let Pi, documentation, or snapshot manifests promote claims. |
| External terminal vocabulary is read-only | Phase 69 exposes external v3 terminal names only as an API projection from trusted internal campaign state; it is not persisted into `.comath/campaign/*/status.json` and cannot be supplied by Pi/model input to mutate proof, refutation, blocker, or cancellation authority. |
| Theorem-specific target generation is statement-bound and non-promotional | Phase 72 generates a Lean target package only for the bounded positive `n + n = 2 * n` request, rejects negation/refutation/non-proof prompts with the same formula, and stores all target artifacts under service-owned `.comath/` paths with no proof-authority or promotion capability. |

## Mathematical Integrity Acceptance

| Invariant | Evidence |
| --- | --- |
| Reviewer approval is not proof | gate tests and claim promotion rules. |
| Formal proof requires kernel evidence | Lean artifact/gate tests. |
| Float-only output cannot be symbolic proof | compute/gate tests. |
| Literature support requires exact artifact-grounded citation | literature/gate tests require quoted statements present in source artifacts. |
| Failed routes preserved | workstream/evidence tests. |
| Paper wording cannot overclaim | paper check tests require block-bound margin provenance and reject theorem-like unsupported syntax. |
| GA proof promotion is replay-bound | Phase 18 proof-kernel gate tests require passed final replay manifest for the same claim and reject fake metadata, statement drift, `sorry`, and `axiom`. |
| Candidate voting is not proof | Phase 19 ensemble recovery test selects the only Lean-valid candidate despite seven failures and preserves those failures as proof memory. |
| Campaign completion is state-machine gated | Phase 20 state-machine tests reject old public stage names, verify canonical terminal-state invariants, and block unsupported goals instead of allowing report-writing or hardcoded replay to complete a campaign. |
| Dashboard read models are not proof authority | Phase 21 read-model routes expose claim/evidence/gate state for inspection only; the dashboard remains read-only and cannot promote, repair, or mutate mathematical state. |
| Pi research loop is not proof authority | Phase 22 loop orchestration can start and tick campaigns, but proof authority remains in service-owned gates, artifacts, and final replay. |
| Theorem-family metadata cannot override locked statements | Phase 23 integrity tests block mismatched family/proposition/Lean-target obligations before candidate generation and bind final replay manifests to the claim statement hash. |
| Replay manifests cannot substitute for re-execution | Phase 24 strict replay reconstructs known runner commands from the service registry, uses stored canonical input, and rejects replay/report, script/input/argv/result, timeout, and report-local stdio drift instead of trusting manifest descriptors. |
| MathProve external runner output is not proof authority | Phase 25 external bridge tests allow `verify_sympy.py` to return `ok=true` as archived runner evidence while the promotion gate still rejects `formally_checked` without CoMath proof-kernel replay artifacts. |
| Pi runtime registration is not proof authority | Phase 26 registration exposes executable research/campaign tools through Pi but records `pi_session_is_math_authority=false`, keeps trusted state access `comathd_only`, and requires Pi host-side confirmation before mutating `comathd` calls. |
| AgentRun reports are not proof authority | Phase 27 AgentRun tests require reports to stay scoped artifacts, reject producer self-review of GraphPatch proposals, and preserve failed routes as memory without promoting claims. |
| AgentRun process completion is not proof authority | Phase 28 scheduler tests prove child processes can run and report through AgentRun while scheduler envelopes persist `proof_authority: none`, `supports_claim_status: none`, and `child_stdout_untrusted: true`; claim promotion remains gated by proof-kernel replay/evidence gates and independent GraphPatch review. |
| Agent profiles are not proof authority | Phase 29 profile tests require every profile to carry `proof_authority=none`, `may_mutate_trusted_state=false`, scoped write templates, and forbidden direct-promotion tools before profile-backed runs or launch envelopes can be used. |
| Lean trust profiles are explicit gates | Phase 31 tests require target-bound `#print axioms` evidence and configurable allowed-axiom profiles; `Classical.choice` can be accepted for ordinary classical projects and rejected for constructive projects. |
| Skeleton placeholders cannot leak into final proof artifacts | Phase 31 static-audit tests allow `sorry` only in explicitly listed skeleton files and still fail final Lean files containing `sorry`. |
| Statement equivalence is target-bound | Phase 32 tests reject arbitrary stdout substrings and require a unique target-theorem signature before statement equivalence can pass. |
| Planning skeletons are not proof authority | Phase 33 writes campaign-scoped skeleton/DAG/line-map artifacts with named proof-obligation placeholders only; these artifacts remain planning evidence and cannot promote claims without the existing clean Lean replay and gate path. |
| Campaign ensemble state is campaign-scoped | Phase 34 prevents supported campaigns with reused `PO-0001`/`CAND-0001` local IDs from sharing candidate or arbitration artifacts; proof selection reads only the active campaign's ensemble state. |
| Final replay audit paths are claim-scoped | Phase 35 requires final replay stage-run artifact pointers to use the current claim id, avoiding misleading audit trails when multiple supported campaigns share one project root. |
| Registered statement aliases require witnesses | Phase 37 accepts non-identical Lean target signatures only through an explicit registered definitional-alias witness and keeps missing, ambiguous, or non-registered mismatches as hard vetoes. |
| Native memory backend evidence is non-promotional | Phase 38 target-platform evaluation can validate memory persistence/performance, but it does not promote mathematical claims or bypass `comathd` gates. |
| Writer locks are coordination, not proof | Phase 39 locks protect mutation sessions; they do not certify artifacts, evidence, proofs, or claim promotion. |
| Scheduler lock ownership is non-authoritative | Phase 40 scheduler-held locks coordinate AgentRun mutation, but child-process completion and lock ownership remain non-proof-authority surfaces. |
| Live adapter execution is non-authoritative | Phase 41 adapter output is wrapped as untrusted child stdout with `proof_authority: none`; adapter success cannot promote claims or bypass proof/evidence gates. |
| AgentRun logs are observability artifacts | Phase 42 log reads expose capped stdout/stderr/report paths for inspection only; logs and health results retain `proof_authority: none` and cannot certify claims, GraphPatch application, or final replay. |
| Packaged adapters are not proof authority | Phase 43 packaged adapters emit AgentRun reports with `proof_authority: none`; package selection, health, and successful execution cannot certify claims, apply GraphPatch, or bypass Lean replay. |
| External Codex output is not proof authority | Phase 44 wraps external CLI stdout/stderr as `external_output_untrusted: true` inside a non-authoritative AgentRun report; it cannot promote claims, certify proof candidates, apply GraphPatch, or replace final Lean replay. |
| Cursor log streams are observability artifacts | Phase 46 stream chunks, cursors, and completion flags retain `proof_authority: none`; they cannot certify claims, apply GraphPatch, or replace final Lean replay/static audit. |
| SSE log subscription frames are observability artifacts | Phase 47 event-stream frames retain `proof_authority: none`; they cannot certify claims, apply GraphPatch, or replace final Lean replay/static audit. |
| Operator panels are observability artifacts | Phase 48 operator-panel status, action metadata, log chunks, and subscription metadata retain `proof_authority: none`; they cannot certify claims, apply GraphPatch, cancel runs without a real scheduler registry, or replace final Lean replay/static audit. |
| Operator cancellation is not proof authority | Phase 49 cancellation results retain `proof_authority: none`; stopping a child process cannot certify claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Log sessions are observability artifacts | Phase 50 multi-event SSE log-session frames retain `proof_authority: none`; they cannot certify claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Codex API output is not proof authority | Phase 51 wraps Responses-compatible Codex API output as untrusted AgentRun report/log material with `proof_authority: none`; it cannot certify claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Codex API retry telemetry is not proof authority | Phase 52 attempt counts, HTTP status sequences, rate-limit markers, and exhausted-attempt failures are runtime reliability metadata with `proof_authority: none`; they cannot certify claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Installed CLI validation is not proof authority | Phase 53 version strings, health JSON, capabilities, and missing-configuration diagnostics are runtime readiness metadata with `proof_authority: none`; they cannot certify claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Declaration parser output is not proof authority | Phase 54 parsed declaration signatures are statement-binding metadata only; they cannot certify proof terms, prove definitional/logical equivalence, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Replay environment checks are not proof authority | Phase 55 environment equality checks are replay-integrity metadata only; matching environment data cannot certify mathematical correctness, promote claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Registered logical equivalence is statement binding, not proof authority | Phase 56 registered equivalence witnesses can bind a target theorem statement to a locked spec but cannot certify the theorem proof, promote claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Registered transitive logical equivalence is statement binding, not proof search | Phase 78 transitive witness chains can bind a target theorem statement through a closed service-owned chain of kernel-checked equivalence metadata, but cannot discover equivalence lemmas, certify proof terms, promote claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Equivalence-search plans are not proof authority | Phase 79 unresolved plan artifacts record candidate lemma goals and required next witness artifacts only; they cannot certify proof terms, promote claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit. |
| Materialized equivalence witnesses are not final proof authority | Phase 80 materialization can produce bounded registered witness metadata acceptable to the statement-equivalence gate, but it cannot certify arbitrary semantic equivalence, promote claims, apply GraphPatch, select candidates as authoritative, or replace final Lean replay/static audit/dependency closure/axiom profile. |
| Template-instantiated theorem families still require clean replay | Former Phase 57 template recognition and candidate-generation fixtures are historical regression material and are quarantined. They do not certify claims and are not accepted as current production proof support unless regenerated through Goal 3 lock/ledger/diff gates, Lean Authority v3 evidence, and the ordinary claim gate. |
| MathProve final-audit reports are runner evidence only | Phase 58 final-audit reports, report entries, steps hashes, solution hashes, and runner metadata cannot certify claims, promote `formally_checked`, apply GraphPatch, select candidates as authoritative, or replace CoMath proof-kernel replay/static audit. |
| Negative GA release reports are evidence summaries only | Phase 68 `v3_negative_ga_slices.json` proves required rejection paths preserve evidence and block promotion; it cannot itself certify arbitrary theorem correctness, promote claims, or replace positive clean Lean replay for a specific claim. |
| Broad theorem planning is not proof authority | Phase 70 broad-planning artifacts are replayable synthesis/planning evidence for non-template targets; they cannot promote claims, certify theorem correctness, or replace theorem-specific Lean declaration, statement-equivalence, dependency-closure, axiom-profile, and final clean replay gates. |
| Theorem-specific Lean target packages are not proof authority | Phase 72 target-package artifacts record a proposition, Lean package skeleton, formal spec, and replay command; they deliberately set `proof_authority: none`, `can_run_clean_replay: false`, and `can_promote_claim: false`, and cannot replace a proof body, static audit, statement equivalence, dependency closure, axiom profile, or final clean replay. |
| Bounded proof-body synthesis is not proof authority | Phase 73 writes a bounded `by omega` proof-body candidate and static proof-body audit preview, but it deliberately produces no final replay manifest, no claim promotion, and no `formally_checked` status; final Lean Authority v2 remains the only promotion path. |
| Bounded authority-report preparation is not proof authority | Phase 74 writes campaign-scoped preview reports only; it deliberately keeps `final_replay_manifest_path: null`, writes nothing under `.comath/evidence/<CLAIM>/lean/`, does not call promotion, and cannot replace clean replay or final hash-bound authority reports. |
| Bounded final replay fixtures are historical | Former Phase 75 bounded Nat-linear final-replay promotion is retained as historical fixture evidence only. It is not accepted as a current production Nat-linear proof path under Goal 3 quarantine. |

## GA V3 Vertical-Slice Coverage

| Slice | Current Evidence | Status |
| --- | --- | --- |
| Trivial formal proof | `phase18-ga-campaign-vertical-slice.test.mjs` records the old `Nat.add_zero` campaign fixture. | Historical/quarantined fixture coverage; not production acceptance. |
| Statement drift and cheat rejection | `phase18-ga-proof-kernel-gates.test.mjs` rejects drift, fake metadata, `sorry`, and `axiom`. | Covered for current static checks. |
| Refutation path | `phase18-ga-refutation-path.test.mjs` records exact `n=0` counterexample for `n + 1 = n`. | Covered for the implemented Nat false theorem slice. |
| Snapshot restore plus proof replay | `phase18-ga-snapshot-replay.test.mjs` restores a snapshot and reruns campaign replay. | Covered for campaign proof replay. |
| Ensemble recovery | `phase19-ga-ensemble-recovery.test.mjs` covers the v3 16.4 benchmark and verifies the V8 `dialectical_stress.json` schema. | Covered for the implemented elementary slice. |
| Canonical campaign states | `phase20-ga-campaign-state-machine.test.mjs` rejects old public stage names and verifies `problem_locked`, `candidate_*`, `final_global_replay`, `completed_formal_proof`, and `completed_refutation` API states. | Covered for the implemented proof and refutation slices. |
| Dashboard service read model | `phase21-read-model-routes.test.mjs` and `phase15-dashboard.test.mjs` cover `claim/evidence/gate` list routes and read-only dashboard aggregation. | Covered for product inspection; not mathematical authority. |
| One-command Pi campaign loop | `phase22-research-loop.test.mjs` covers command parsing, scoped capability enforcement, bounded campaign ticks, and dashboard return. | Covered in a mock Pi thin-client harness; Phase 26 adds package/runtime registration and installed-loader smoke. |
| Registered theorem-family proof replay | `phase23-ga-theorem-family-generalization.test.mjs` records the old `Nat.mul_zero` theorem-family fixture while preserving historical `Nat.add_zero` compatibility. | Historical/quarantined fixture coverage; not production acceptance. |
| Compute runner re-execution replay | `phase16-snapshot-replay.test.mjs` verifies strict replay route re-execution for the implemented Python compute runners and per-runner audit summaries. | Covered for service-owned deterministic runner replay; stronger OS/network sandbox and dependency lock replay remain deferred. |
| External MathProve evidence runner bridge | `phase25-real-mathprove-bridge.test.mjs` invokes `MathProve-Skill` `verify_sympy.py` through a fixed command shape and records hashes, stdout/stderr/result metadata, and fail-closed vetoes. | Covered as non-authoritative evidence runner only; broad MathProve proof search and MathProve-as-proof-authority semantics remain deferred. |
| Pi runtime registration | `phase26-pi-runtime-registration.test.mjs` plus installed Pi 0.75.5 loader smoke validate package manifest loading, default export registration, command/tool policy metadata, command dispatch, and Pi host-side mutation confirmation. | Covered for runtime registration contract and loader compatibility; Phase 45 adds the automated local Pi/comathd install-session e2e. Richer real-host Pi UI, manual install walkthrough, and durable service lifecycle management remain deferred. |
| AgentRun runtime boundary | `phase27-agent-run-runtime.test.mjs` covers persisted AgentRuns, scoped write confinement, report validation, producer/reviewer separation, and failure-route memory. | Covered for the auditable contract boundary. |
| AgentRun process scheduler | `phase28-agent-run-scheduler.test.mjs` covers service-side process launch, absolute allowlists, minimal env, scheduler concurrency/rpm, timeout/cancel handling, scoped capped logs, invalid-report fail-closed handling, report persistence, and non-authoritative envelopes. | Covered for allowlisted fixture commands; Phase 39-40 add writer locks and Phase 41-53 add bounded live/profile/Codex adapter surfaces. OS/network sandboxing, indefinite operator sessions, live production account validation, and cross-process scheduler recovery remain deferred. |
| Agent profile service API | `phase29-agent-profile-integration.test.mjs` covers profile validation, service list/get routes, profile-bound AgentRun creation, launch-envelope preparation, audit events, unknown-profile rejection, and status capability reporting. | Covered for service-owned profile/run/launch contracts; Phase 41 adds live profile-backed adapter execution. Richer profile UI, production account/network validation, and OS isolation remain deferred. |
| Pi agent profile UX | `phase30-agent-profile-tools.test.mjs` covers Pi runtime registration and command/tool dispatch for service-owned profile list/get, AgentRun creation, and launch-envelope preparation. | Covered in the fake Pi runtime harness; Phase 41 and Phase 45 cover executable profile tools and a local install-session e2e. Richer real-host Pi UX and durable lifecycle management remain deferred. |
| Lean trust profile hardening | `phase31-lean-trust-profile.test.mjs` covers project-level axiom allowlists and skeleton-aware static scanning. | Covered for configurable allowlists and target-bound axiom-report presence; richer theorem-signature extraction, Lean parser integration, and transitive dependency semantics remain deferred. |
| Lean statement signature binding | `phase32-lean-statement-signature.test.mjs` covers target signature extraction and fail-closed statement-equivalence vetoes for missing/ambiguous/mismatched output. Phase 79 adds non-authoritative unresolved-plan artifacts for unique mismatches with safe hints, and Phase 80 materializes bounded registered hints into non-promotional witness metadata. | Covered for unique target-signature equality, blocked proof-search planning, and bounded registered materialization; automatic proof search/execution beyond registered materializations remains deferred. |
| Proof obligation DAG planning | `phase33-proof-obligation-dag.test.mjs` covers native planning artifacts for lemma DAG, line map, obligation YAML, skeleton Lean placeholders, skeleton report, DAG validation, multi-obligation closure, and campaign-scoped artifact isolation. | Covered for the planning stage of registered theorem-family slices and open-obligation artifact closure; broad lemma decomposition, arbitrary theorem planning, and generic theorem synthesis remain deferred. |
| Campaign-scoped ensemble artifacts | `phase34-campaign-ensemble-isolation.test.mjs` covers interleaved supported campaigns and ensures candidate workspaces, candidate batch indexes, and arbitration decisions are scoped by campaign. | Covered for registered theorem-family proof campaigns; live external child-agent ensemble execution remains deferred. |
| Claim-scoped final replay artifact paths | `phase35-final-replay-artifact-paths.test.mjs` covers second-campaign final replay audit paths using the active root claim id. | Covered for registered theorem-family proof campaigns; richer release-bundle provenance remains deferred. |
| Runner replay sandbox/dependency provenance | `phase36-runner-replay-provenance.test.mjs` covers sandbox-policy and dependency-lock material in compute runner reports and replay manifests, plus fail-closed vetoes when either provenance class is missing. Phase 77 adds `COMATH_RUNNER_NETWORK=disabled` service-environment policy binding and preflight vetoes. | Covered for provenance, integrity gates, and service-level network-denial contract; OS-level isolation, kernel/firewall-enforced network denial, and broader runner-family lockfiles remain deferred. |
| Registered Lean statement alias equivalence | `phase37-lean-statement-alias-equivalence.test.mjs` covers explicit alias acceptance for Lean notation expansion and fail-closed behavior for missing, ambiguous, or unregistered mismatched target signatures. | Covered for registered definitional aliases, Phase 79 unresolved planning, and Phase 80 bounded materialization only; proof-producing logical equivalence execution beyond registered materializations remains deferred. |
| Native TriviumDB target-platform evaluation | `phase38-trivium-native-evaluation.test.mjs`, `test:trivium` with `COMATH_ENABLE_TRIVIUM_TESTS=1`, and `eval:trivium` cover real `triviumdb@0.7.1` loading, adapter write/link/search paths, persistence reopen, target-platform performance metrics, and fail-closed unavailable reports. | Covered for this Windows x64 target and explicit optional backend evaluation; default backend remains memory and broader multi-platform benchmarking remains future work. |
| Project writer/session lock | `phase39-project-session-lock.test.mjs` covers exclusive create, active-lock rejection, token release, stale takeover, malformed-lock fail-closed behavior, and path-policy confinement under `.comath/sessions/`. | Covered as a project-level primitive; full AgentRun scheduler integration and OS-level process sandboxing remain deferred. |
| AgentRun scheduler writer-lock integration | `phase40-agent-scheduler-session-lock.test.mjs` covers active-lock launch rejection, no child/log side effects on blocked launch, queued-run preservation, scheduler-owned acquire/release around successful process execution, and lock audit events. | Covered for the service-side AgentRun scheduler mutation path; OS-level process sandboxing and mandatory external-process locks remain deferred. |
| Live agent adapter execution | `phase41-live-agent-adapter-execution.test.mjs` covers service-side live adapter execution and route behavior; `phase41-agent-execute-tool.test.mjs` covers Pi tool registration, host confirmation, route payloads, runtime registration, and `/cm:agent execute`. | Covered for allowlisted local adapter execution through `comathd`; production Codex CLI/API packaging and interactive operator controls remain deferred. |
| AgentRun observability and adapter health | `phase42-agent-run-observability.test.mjs` covers `readAgentRunLogs()`, `probeAgentAdapterHealth()`, service routes, audit events, and non-authoritative metadata; `phase42-agent-observability-tools.test.mjs` covers Pi tools, host confirmation for health probes, runtime registration, `/cm:agent logs`, and `/cm:agent health`. | Covered for capped readback and bounded health probes; streaming/subscription UI, production adapter lifecycle packaging, and OS/network sandboxing remain deferred. |
| Agent adapter package registry | `phase43-agent-adapter-package.test.mjs` covers `listAgentAdapterPackages()`, `buildAgentAdapterPackageLaunch()`, `executeAgentAdapterPackage()`, package routes, health compatibility, audit events, and bundled adapter script copy into `dist`; `phase43-agent-adapter-package-tools.test.mjs` covers Pi tools, host confirmation, runtime registration, `/cm:agent packages`, `/cm:agent prepare-package`, and `/cm:agent execute-package`. | Covered for service-owned packaged launcher lifecycle; richer operator controls, streaming UI, and OS/network sandboxing remain deferred. |
| Codex CLI external backend | `phase44-codex-cli-external-invocation.test.mjs` covers service-configured external CLI invocation through the packaged launcher, missing-config fail-closed behavior, bounded untrusted output wrapping, and audit metadata; `phase44-agent-adapter-external-tools.test.mjs` covers Pi backend schema/tool/command passthrough. | Covered for a Codex-compatible CLI contract using a fake local CLI; real production Codex CLI/API validation, streaming UI, and OS/network sandboxing remain deferred. |
| Pi/comathd install-session e2e | `phase45-pi-comathd-install-session.test.mjs` covers built package import, runtime registration policy, `rpm=4`, `comathd_only` trusted-state boundary, live HTTP client/server wiring, host-confirmed campaign start/tick, agent package listing, packaged adapter prepare-launch, project status, and resources discovery. | Covered for local automated install-session e2e; richer real Pi host UI, manual install walkthrough, and durable service lifecycle management remain deferred. |
| Cursor-based AgentRun log stream | `phase46-agent-log-stream.test.mjs` covers cursor polling, next cursors, terminal completion, route handling, invalid-cursor rejection, and audit events; `phase46-agent-log-stream-tools.test.mjs` covers Pi `comath.agent.streamLogs`, runtime registration, and `/cm:agent stream`. | Covered for cursor polling; continuous WebSocket/SSE subscriptions, richer interactive operator controls, and OS/network sandboxing remain deferred. |
| SSE-style AgentRun log subscription | `phase47-agent-log-subscription.test.mjs` covers SSE frame formatting, `text/event-stream` route headers/body, retry/event/id/data fields, audit events, and non-authoritative payloads; `phase47-agent-log-subscription-tools.test.mjs` covers Pi `getText`, `comath.agent.subscribeLogs`, runtime registration, and `/cm:agent subscribe-logs`. | Covered for SSE-compatible snapshot frames; indefinite browser-held sessions, richer interactive operator controls, and OS/network sandboxing remain deferred. |
| AgentRun operator panel | `phase48-agent-operator-panel.test.mjs` covers service aggregation of AgentRun status, cursor log chunk, SSE snapshot metadata, action availability, route handling, disabled cancellation semantics, and audit events; `phase48-agent-operator-panel-tools.test.mjs` covers Pi `comath.agent.operatorPanel`, runtime registration, JSON GET routing, and `/cm:agent panel`. | Covered for read-only operator panels; true scheduler-backed live cancellation, indefinite browser-held sessions, production Codex CLI/API validation, and OS/network sandboxing remain deferred. |
| Scheduler-backed AgentRun operator cancellation | `phase49-agent-operator-cancel.test.mjs` covers active registry cancellability, long-running adapter cancellation, route handling, terminal-panel disablement, persisted `cancelled` state, and audit events; `phase49-agent-operator-cancel-tools.test.mjs` covers Pi `comath.agent.cancelRun`, mutation confirmation, runtime registration, POST routing, and `/cm:agent cancel`. | Covered for same-process scheduler-backed cancellation; cross-process cancellation, persistent operator sessions, production Codex CLI/API validation, and OS/network sandboxing remain deferred. |
| Bounded multi-event AgentRun log session | `phase50-agent-log-session.test.mjs` covers multi-frame SSE formatting, `GET /agent/run/:id/log-session`, max-event limits, completion/no-progress termination, audit events, and `proof_authority=none`; `phase50-agent-log-session-tools.test.mjs` covers Pi `comath.agent.logSession`, text GET routing, runtime registration, and `/cm:agent log-session`. | Covered for bounded multi-event SSE responses; indefinite browser-held sessions, richer operator UX, cross-process scheduler recovery, production Codex CLI/API validation, and OS/network sandboxing remain deferred. |
| Service-configured Codex API backend | `phase51-codex-api-backend.test.mjs` covers secret-free prepare-launch envelopes, injected Responses-compatible request shape, API-key fail-closed behavior, non-authoritative report/log wrapping, and route payloads; `phase51-codex-api-backend-tools.test.mjs` covers Pi backend enum, prepare/execute POST payloads, and `/cm:agent prepare-package|execute-package --backend codex-api`. | Covered for backend contract and credential boundary with an injected client; live production API/network validation, retries/rate-limit telemetry, streaming API UX, and OS/network sandboxing remain deferred. |
| Codex API retry telemetry | `phase52-codex-api-retry-telemetry.test.mjs` covers bounded retry behavior for retryable Codex API statuses, capped retry delay, `codex_api_attempts`, `codex_api_statuses`, `codex_api_rate_limited`, audit metadata, fail-closed exhaustion, secret-free logging, and `codex_api_retry_telemetry`. | Covered for injected-client retry/rate-limit reliability telemetry; live production API/network validation, streaming Responses API UX, credential rotation UX, installed production Codex CLI validation, and OS/network sandboxing remain deferred. |
| Installed Codex CLI validation | `phase53-installed-codex-cli-validation.test.mjs` covers service-configured installed CLI version/health probes, missing-configuration fail-closed diagnostics, route-level validation, audit metadata, executable-path non-disclosure, and `installed_codex_cli_validation`. | Covered for installed CLI readiness validation through service-owned configuration; live production API/network validation, streaming Responses API UX, credential rotation UX, and OS/network sandboxing remain deferred. |
| Lean declaration parser signature fallback | `phase54-lean-declaration-parser.test.mjs` covers supplied-source theorem/lemma declaration parsing, multi-line declarations, namespace-qualified targets, ambiguous target rejection, comment-only substring rejection, statement-equivalence fallback, and `lean_declaration_parser_signature_fallback`. | Covered for conservative declaration-header parsing; Phase 79 adds blocked plan artifacts and Phase 80 adds bounded registered materialization, but automatic proof search/execution and broader mathematical-domain trust profiles remain deferred. |
| Runner cross-machine replay environment gate | `phase55-runner-cross-machine-replay.test.mjs` covers tampered-but-rehashed replay environment metadata, fail-closed `runner_reexecution_environment_mismatch`, empty `runner_reexecution` launch summaries, and `runner_cross_machine_replay_environment_gate`. | Covered for Node/platform/arch replay-environment drift; Phase 77 covers service-level runner network-denial environment policy; OS-level isolation, kernel/firewall-enforced network denial, and broader runner-family lockfiles remain deferred. |
| Registered Lean logical-equivalence witnesses | `phase56-lean-registered-logical-equivalence.test.mjs` covers `logically_equivalent_with_registered_lemmas`, `registered_logical_equivalence` witness reports, exact formal-spec/target binding, SHA-256 witness artifact checks, non-empty lemma names, and fail-closed missing/wrong witness cases; capability `lean_registered_logical_equivalence_witnesses` is reported. `phase78-lean-transitive-equivalence.test.mjs` adds closed registered transitive chains and capability `lean_registered_transitive_logical_equivalence_witnesses`; `phase79-lean-equivalence-search-plan.test.mjs` adds blocked plan artifacts and capability `lean_equivalence_search_plan_artifacts`; `phase80-bounded-equivalence-witness-materialization.test.mjs` adds bounded materialization and capability `lean_equivalence_witness_materialization`. | Covered for explicitly registered, witness-backed direct equivalence metadata, explicitly registered closed transitive witness chains, non-authoritative unresolved plan artifacts, and bounded registered materialization; automatic proof search/execution beyond registered materializations remains deferred. |
| Lean theorem template instantiation | `phase57-ga-theorem-template-instantiation.test.mjs` covers historical `0 + n = n` registered-template fixture behavior and its old replay/candidate fields. | Quarantined as historical fixture material under Goal 3; arbitrary theorem synthesis, broad lemma decomposition, and non-registry theorem generation remain deferred. |
| MathProve final-audit external runner | `phase58-mathprove-final-audit-runner.test.mjs` covers `runMathProveFinalAuditExternal()`, controlled `final_audit.py` execution, generated final-audit step payloads, `steps_sha256`/`solution_sha256`, archived `external-final-audit` reports without host paths, `mathprove_final_audit_external_runner`, and fail-closed promotion through `promoteClaimWithMathProveBridge(..., { backend: "external-final-audit" })`. | Covered as non-authoritative final-audit runner evidence; broad MathProve proof search and MathProve-as-proof-authority semantics remain deferred. |
| v3 formal campaign quarantine | `goal4-p0-no-reinvent-violations.test.mjs`, `phase20-ga-campaign-state-machine.test.mjs`, `phase63-v3-stage-gate-artifact-coverage.test.mjs`, and `phase33-proof-obligation-dag.test.mjs` cover the P0 quarantine: former registered `n + 0 = n` theorem-family goals now retain planning/stage artifacts but terminate as replayable blockers rather than `formally_checked` proof claims. | Default acceptance now treats the old registered theorem-family positive slice as historical evidence only; production proof promotion awaits FormalSpecLock/AssumptionLedger/StatementDiffGate and Lean Authority v3 work. |
| v3 required negative slices | `goal4-p0-no-reinvent-violations.test.mjs`, `phase68-v3-negative-ga-slices.test.mjs`, and proof-kernel gate tests cover statement drift, cheating Lean artifacts, false-theorem refutation, all-candidate failure recovery, missing replay evidence, and snapshot/fake-evidence rejection. | Phase 68 is retained as historical negative-slice material but no longer belongs to the default test matrix while its snapshot replay fixture depends on the quarantined theorem-family proof path. |
| v3 external terminal vocabulary | `phase69-v3-terminal-vocabulary.test.mjs` covers the read-only compatibility mapping for all five external v3 terminal names, including projection of quarantined former proof goals to `replayable_environment_blocker`; `phase22-research-loop.test.mjs` covers Pi loop consumption of `external_v3_terminal_state`. | Covered as API/UX vocabulary compatibility; proof authority remains with internal campaign state, gates, and replay evidence. |
| Goal 3 release-hardening docs and prompts | `scripts/phase0-smoke.mjs` validates release-hardening docs, non-secret config samples, `.pi/agents`, and `.pi/prompts` for `proof_authority=none`, `may_mutate_trusted_state=false`, strict JSON/schema output, locked statement hash preservation, no vote/literature/CAS proof authority, and trusted replay dependency policy. | Covered for static public wording and prompt contracts; Task 20 still performs final requirement-by-requirement GA audit. |
| Pi goal-mode intake provenance | `goal3-task255-pi-goal-mode-intake-manifest.test.mjs` covers `/campaign/start` service-owned intake manifests for paper paths, attachments, and workspace refs, project-relative path normalization, file hashes, directory non-proof refs, Pi export path/hash binding, host-root redaction, and `proof_authority=none`. | Covered for intake provenance; deeper document ingestion, retrieval, lemma planning, and Lean replay remain separate product work, and attachments never become proof authority. |
| Pi goal-mode research planning | `goal3-task256-pi-goal-mode-research-plan.test.mjs` covers `knowledge_pack` creation of `.comath/campaign/<campaign_id>/goal_mode_research_plan.json`, intake manifest path/hash binding, local PDF/TeX/Markdown/workspace ingestion planning through the external wheel registry, literature retrieval and Lean theorem-search task planning, lemma DAG / `Skeleton.lean` planning seeds, stage-run attachment, host-root redaction, and `proof_authority=none`. | Covered for service-owned planning only; adapter execution, document extraction, retrieval results, theorem-search hits, Lean replay, automatic repair, and claim promotion remain separate gated work. |
| Pi goal-mode adapter execution manifests | `goal3-task257-pi-goal-mode-adapter-execution-manifest.test.mjs` covers `knowledge_pack` creation of `.comath/campaign/<campaign_id>/goal_mode_adapter_execution_manifest.json`, research-plan and intake-manifest path/hash binding, per-planned-task run envelopes, local input hash re-checks, tamper blockers, missing-artifact rewind to `knowledge_pack`, host-root redaction, blocked live provider execution, and `proof_authority=none`. | Covered for service-owned execution-manifest provenance only; actual adapter execution, prompt-injection-scanned extraction, retrieval results, theorem-search hits, Lean replay, automatic repair, and claim promotion remain separate gated work. |
| Pi goal-mode local ingestion evidence | `goal3-task258-pi-goal-mode-local-ingestion-evidence.test.mjs` covers `knowledge_pack` creation of `.comath/campaign/<campaign_id>/goal_mode_local_ingestion_evidence.json`, research-plan / adapter-execution / intake manifest path-hash binding, prompt-injection-scanned line anchors for local Markdown and TeX theorem or lemma drafts, prompt-injection blockers, PDF parser blockers, external Lean repo inspection blockers, host-root redaction, missing-artifact rewind to `knowledge_pack`, and `proof_authority=none`. | Covered for local draft evidence only; PDF parsing, external Lean repo inspection, live literature retrieval, theorem-search hits, Lean replay, automatic repair, and claim promotion remain separate gated work. |
| Pi goal-mode skeleton formalization hints | `goal3-task259-pi-goal-mode-skeleton-formalization-hints.test.mjs` covers `skeleton_gate` creation of `.comath/campaign/<campaign_id>/proof/formalization_hints.json`, `knowledge_pack.json` and local ingestion evidence path/hash binding, allowed extracted-claim-kind filtering, prompt-injection blocker exclusion, `plan.json` binding, host-root redaction, deletion rewind to `skeleton_gate`, and `proof_authority=none`. | Covered for non-authoritative skeleton planning input only; richer lemma DAG generation, Lean skeleton repair, PDF parsing, external Lean repo inspection, live retrieval/theorem-search, Lean replay, and claim promotion remain separate gated work. |
| Pi goal-mode skeleton blueprint | `goal3-task260-pi-goal-mode-skeleton-blueprint-from-hints.test.mjs` covers `skeleton_gate` creation of `.comath/campaign/<campaign_id>/proof/blueprint.json` from formalization hints, hint path/hash binding, `plan.json` binding, propagation into `lemma_dag.json`, `line_map.json`, and `skeleton_report.md`, host-root redaction, no extra proof-obligation creation from hints, deletion rewind to `skeleton_gate`, and `proof_authority=none`. | Covered for non-authoritative blueprint planning metadata only; actual skeleton repair, candidate orchestration, PDF parsing, external Lean repo inspection, live retrieval/theorem-search, Lean replay, and claim promotion remain separate gated work. |
| Pi goal-mode blueprint-bound candidate generation | `goal3-task261-pi-goal-mode-blueprint-bound-candidate-generation.test.mjs` covers `line_map_gate` creation of blueprint-bound `candidate_generation_request.json`, path/hash binding for `proof/blueprint.json` and `proof/formalization_hints.json`, propagation into `candidate_generation.json` and eight `lemma_sprint` task cards, blueprint tamper fail-closed behavior, and `proof_authority=none`. | Covered for bounded candidate orchestration input only; real Lean skeleton repair, proof-attempt execution, per-candidate Lean replay, PDF parsing, external Lean repo inspection, live retrieval/theorem-search, and claim promotion remain separate gated work. |
| Pi goal-mode Lean candidate attempt materialization | `goal3-task262-pi-goal-mode-blueprint-lean-candidate-attempts.test.mjs` covers candidate workspace creation of `lean_candidate_attempt_plan.json` and `LeanCandidate.lean`, source `proof/Skeleton.lean` path/hash binding, skeleton-blueprint path/hash and step-id binding, manifest and agent-output artifact exposure, host-root redaction, `sorry` draft status, and `proof_authority=none`. | Covered for draft Lean attempt material only; service-owned LeanRunner execution, automatic repair, per-candidate replay promotion, PDF parsing, external Lean repo inspection, live retrieval/theorem-search, and final clean replay remain separate gated work. |
| Pi goal-mode Lean candidate attempt preflight | `goal3-task263-pi-goal-mode-lean-attempt-check-report.test.mjs` covers `candidate_verification` creation of `.comath/campaign/<campaign_id>/lean_candidate_attempt_check_report.json`, per-candidate manifest/attempt-plan/draft-Lean binding, source skeleton and blueprint hash checks, locked-statement boundary checks, `sorry` detection, repair-required routing, host-root redaction, no LeanRunManifest paths, and `proof_authority=none`. | Covered for service-owned static preflight and repair routing only; no LeanRunner invocation is performed while `sorry` remains, and automatic repair, no-sorry Lean replay, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode Lean attempt repair routing | `goal3-task264-pi-goal-mode-lean-attempt-repair-routing.test.mjs` covers `candidate_arbitration` routing of explicit goal-mode campaigns with external refs from blocked `repair_required` preflight into `current_stage="repair"` / `status="repairing"`, creation of `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_batch.json`, per-candidate `lean_candidate_repair_task.json` work orders, check-report path/hash binding, A0/A5/A8 assignment, host-root redaction, no LeanRunner invocation, no LeanRunManifest paths, and `proof_authority=none`. | Covered for repair work-order materialization only; executing repairs, materializing no-sorry attempts, re-running candidate verification, LeanRunner execution, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode repair stage re-ingestion | `goal3-task265-pi-goal-mode-repair-stage-reingestion.test.mjs` covers ticking `current_stage="repair"`, consuming the repair batch, snapshotting original `LeanCandidate.lean` drafts, writing no-sorry repaired drafts with `comath_repair_placeholder` markers, persisting `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_execution.json` plus per-candidate execution manifests, returning to `candidate_verification`, and reclassifying repaired attempts as `ready_for_lean_runner` without LeanRunManifest paths or proof authority. | Covered for repair execution and re-ingestion only; placeholders are not proof, LeanRunner execution, failed-run repair cycling, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode ready attempt LeanRunner blocker | `goal3-task266-pi-goal-mode-ready-attempt-leanrunner-blocker.test.mjs` covers the two-step ready preflight and service-owned LeanRunner execution over repaired no-sorry attempts, append-only LeanRunManifest creation for every ready candidate, candidate manifest/index updates to `candidate_failed`, all-rejected replayable blocker materialization, and placeholder protection that keeps `proof_authority=none` and promotion flags false. | Covered for rejected ready attempts and replayable blocker evidence only; successful candidate arbitration, richer repair from Lean stderr/stdout, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode LeanRunner feedback repair loop | `goal3-task267-pi-goal-mode-leanrunner-feedback-repair-loop.test.mjs` covers ticking an explicit goal-mode all-rejected LeanRunner terminal blocker back into `repair`, persisting `lean_candidate_attempt_repair_feedback_batch.json`, binding failed LeanRunManifest stdout/stderr paths and hashes, materializing a second repair batch/task set, executing feedback-guided repair with `comath_repair_feedback` markers, and returning to `candidate_verification` without LeanRunner/proof/GA authority. | Covered for failed-Lean diagnostic feedback and repair-loop resumption only; successful candidate arbitration, live theorem-search/literature/CAS-guided repair, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode repair hint bundle | `goal3-task268-pi-goal-mode-repair-hint-bundle.test.mjs` covers generating `lean_candidate_attempt_repair_hint_bundle.json` from external wheel registry descriptors during the all-rejected feedback resume, binding research-plan/adapter-manifest/local-ingestion/feedback artifacts by path/hash, threading `source_repair_hint_bundle` and hint descriptors through repair batch/task/execution, adding `comath_repair_hints` markers, and failing closed on stale bundle hashes without overwriting existing repair execution or candidate Lean drafts. | Covered for descriptor-derived non-authoritative repair context only; live theorem-search/literature/CAS execution, successful LeanRunner repair, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode repair hint execution | `goal3-task269-pi-goal-mode-repair-hint-execution.test.mjs` covers generating `lean_candidate_attempt_repair_hint_execution.json` from the bound Task268 hint bundle, recording service-owned stubbed adapter result summaries with request/result hashes, provider terms, capability metadata, and no-authority vetoes, threading `source_repair_hint_execution` and `source_repair_hint_results` through repair batch/task/execution, adding `comath_repair_hint_execution` markers, and failing closed on stale execution hashes without overwriting existing repair execution or candidate Lean drafts. | Covered for non-authoritative stubbed adapter result capture only; live provider/network execution, mathematically meaningful repair synthesis, successful LeanRunner repair, final clean replay, and claim promotion remain separate gated work. |
| Pi goal-mode placeholder-free repair candidate | `goal3-task270-pi-goal-mode-placeholder-free-repair-candidate.test.mjs` covers inline Lean theorem goal locking, comment-aware `sorry` detection, theorem/lemma declaration gating, placeholder/hole repair routing, bounded `True` theorem repair to `trivial`, service-owned LeanRunner execution over placeholder-free candidates, and routing a passing candidate into arbitration/red-team flow. | Covered for bounded placeholder-free `True` repair and LeanRunner pass-through only; mathematically meaningful general repair synthesis, integration, final clean replay, claim promotion, and GA certification remain separate gated work. |
| Pi goal-mode final replay terminal | `goal3-task271-pi-goal-mode-final-replay-terminal.test.mjs` covers carrying the bounded placeholder-free `True` candidate through arbitration, red-team, integration, final static audit, final global clean replay, FinalReplayManifest v3 packaging, ordinary promotion, `completed_formal_proof`, `formally_checked`, and clean replay export readiness. | Covered for the bounded product terminal chain only; general theorem repair, live provider execution, real-Pi/operator closure, OS-isolation production helpers, broad proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode `rfl` final replay terminal | `goal3-task272-pi-goal-mode-rfl-final-replay-terminal.test.mjs` covers conservative placeholder-free repair of syntactically reflexive locked equality statements with `rfl`, repair strategy metadata, LeanRunner pass-through, final replay project packaging, final clean replay, FinalReplayManifest v3, ordinary promotion, `completed_formal_proof`, `formally_checked`, and export readiness. | Covered for exact reflexive-equality repair only; non-reflexive equality repair, normalization, live provider execution, broad mathlib proof breadth, real-Pi/operator closure, OS-isolation production helpers, and GA certification remain separate gated work. |
| Pi goal-mode live retrieval repair hint execution | `goal3-task273-pi-goal-mode-live-retrieval-repair-hint-execution.test.mjs` covers opt-in service-owned live `retrieval:jina_search` execution against a configured Jina Search-compatible endpoint, in-memory bearer header use, sanitized request/response hashes, provider status, content hash, prompt-injection scan, no-authority vetoes, secret/root redaction, and threading live results through repair tasks/execution. | Covered for one live retrieval repair-context provider only; default runs remain stubbed/offline, and theorem-search/CAS live execution, mathematically meaningful repair synthesis, real-Pi/operator closure, OS-isolation production helpers, broad proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live retrieval suggestion repair | `goal3-task274-pi-goal-mode-live-retrieval-suggestion-repair.test.mjs` covers extracting bounded fenced Lean suggestions from opt-in live `retrieval:jina_search` results, exact declaration-name and normalized-statement matching, no-cheat filtering, repaired placeholder-free candidate materialization, Lean-file-hash-specific append-only LeanRunner reruns, and routing a passing repaired candidate into arbitration. | Covered for one live retrieval suggestion-to-candidate repair path only; retrieval output remains non-authoritative, final clean replay and promotion gates remain required, and theorem-search/CAS live execution, broad theorem synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live retrieval final replay provenance | `goal3-task275-pi-goal-mode-live-retrieval-final-replay-provenance.test.mjs` covers carrying a live-retrieval-repaired candidate through terminal clean replay, clearing only stale transient LeanRunner-rejection vetoes after a repaired rerun passes, and binding repair hint execution, campaign repair execution, and per-candidate repair execution provenance into the selected candidate replay descriptor and final replay material source. | Covered for final replay provenance of one live retrieval repair path only; provider text remains non-authoritative, and theorem-search/CAS live providers, broader mathematical repair synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live theorem-search repair hint execution | `goal3-task276-pi-goal-mode-live-theorem-search-repair-hint-execution.test.mjs` covers opt-in service-owned live `theorem_search:loogle` execution against a configured Loogle-compatible endpoint, in-memory bearer header use, sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, no-authority vetoes, secret/root redaction, bounded declaration result summaries, and threading live theorem-search results through repair tasks/execution. | Covered for one live theorem-search repair-context provider only; default runs remain stubbed/offline, declaration results remain non-authoritative, and CAS live execution, mathematically meaningful repair synthesis, real-Pi/operator closure, OS-isolation production helpers, broad proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live theorem-search conjunction repair | `goal3-task277-pi-goal-mode-live-theorem-search-conjunction-repair.test.mjs` covers consuming a live non-authoritative `theorem_search:loogle` `And.intro` declaration result as candidate material for locked `True /\\ True` / `True ∧ True` theorem goals, materializing `exact And.intro trivial trivial`, preserving secret/root redaction and no-authority flags, returning through candidate verification, and requiring service-owned LeanRunner before routing a passing repaired candidate to arbitration. | Covered for one narrow theorem-search-guided conjunction repair only; declaration lookup remains non-authoritative, final clean replay and promotion gates remain required, and broad theorem synthesis, CAS live execution, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live theorem-search iff repair | `goal3-task283-pi-goal-mode-live-theorem-search-iff-repair.test.mjs` covers consuming a live non-authoritative `theorem_search:loogle` `Iff.intro` declaration result as candidate material for locked `True <-> True` theorem goals, materializing `exact Iff.intro (fun _ => trivial) (fun _ => trivial)`, preserving no-authority flags, and keeping the repaired draft candidate material only. | Covered for one narrow theorem-search-guided iff repair only; declaration lookup remains non-authoritative, final clean replay and promotion gates remain required, and broad iff proof synthesis, import inference, CAS live execution, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi lifecycle readiness completion-prerequisite binding | `goal3-task284-pi-lifecycle-readiness-completion-prerequisite-binding.test.mjs` covers extending `reviewPiCodexLifecycleReadiness()` and `POST /release/pi-codex-lifecycle/review` to consume a Task253 completion-certification prerequisite id/path/hash, re-read the canonical artifact, reject stale, non-canonical, or promotional prerequisite files, bind the prerequisite artifact path/hash into review inputs and audit payloads, and add `terminal_unattended_completion_certification_required` while preserving non-authority flags. | Covered for readiness aggregation of the current terminal blocker only; it does not implement a terminal completion certificate, production GA execution, durable long-lived transport, direct Pi mutation, Lean replay, proof authority, claim promotion, or GA certification. |
| Pi lifecycle readiness prerequisite consumer bridge | `goal3-task285-pi-lifecycle-readiness-prerequisite-consumer.test.mjs` covers `comath.release.piCodexLifecycleReview` and `/cm:release pi-codex-lifecycle` exposing optional Task253 prerequisite id/path/hash material, forwarding it to the service-owned readiness review under Pi host confirmation, and sanitizing secret/path/proof-success/GA overclaims from notifications. | Covered for Pi thin-client wiring only; the service-owned Task284 review remains the authority for re-reading and validating prerequisite artifacts, and this consumer does not create terminal certificates, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA. |
| Pi terminal completion certificate design gate | `goal3-task286-service-owned-terminal-completion-certificate-design.test.mjs` covers `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign()` and the matching service route consuming a Task253 prerequisite id/path/hash, re-reading the canonical prerequisite artifact, rejecting stale, non-canonical, or promotional prerequisite material, persisting append-only design evidence, sanitizing completion-certificate overclaims, and listing required future terminal-certificate evidence while preserving non-authority flags. | Covered for service-owned design evidence only; it does not create a terminal completion certificate, certify unattended completion, expose executor output, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA. |
| Pi terminal completion certificate design consumer bridge | `goal3-task287-pi-unattended-real-host-terminal-completion-certificate-design-consumer.test.mjs` covers `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign`, `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate-design`, Pi runtime registration, and the read-only interactive planner consuming only Task253 prerequisite id/path/hash material, translating only the prerequisite public lifecycle alias, stripping model confirmation ids, rejecting wrong alias material before POST, sanitizing public surfaces, and preserving non-authority flags. | Covered for Pi thin-client wiring only; Task286 service remains the authority for re-reading prerequisite artifacts, and this consumer does not create certificates, certify unattended completion, expose executor output, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA. |
| Pi terminal completion certificate evidence gate | `goal3-task288-service-owned-terminal-completion-certificate.test.mjs` covers `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificate()` and the matching service route consuming a Task286 design id/path/hash, re-reading the canonical design, Task253 prerequisite, Task251 attempt review, and current attempt-result artifact bytes, rejecting duplicate, stale, non-canonical, promotional, or tampered chains, persisting append-only certificate evidence, writing provenance audit events, and redacting executor stdout/stderr/result payload surfaces. | Covered for Pi/Codex unattended lifecycle terminal-certificate evidence only; it sets no Lean proof authority, cannot promote mathematical claims, keeps GA certification separate, and does not open durable/live transport, mutate trusted state, run Lean, or certify GA. |
| Pi terminal completion certificate consumer bridge | `goal3-task289-pi-unattended-real-host-terminal-completion-certificate-consumer.test.mjs` covers `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificate`, `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate`, Pi runtime registration, and the read-only interactive planner consuming only Task286 design id/path/hash material, translating only the design public lifecycle alias, stripping model confirmation ids, rejecting wrong alias material before POST, sanitizing public request/result/confirmation/notification surfaces, and preserving service-returned lifecycle certificate flags without proof or GA authority. | Covered for Pi thin-client wiring only; Task288 service remains the authority for re-reading design/prerequisite/review/result artifacts, and this consumer does not expose executor output, accept caller certificates, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA. |
| Operator/service transport closure review gate | `goal3-task290-service-owned-operator-transport-closure-review.test.mjs` covers `recordPiCodexLifecycleOperatorServiceTransportClosureReview()` and the matching service route consuming a Task288 terminal completion certificate id/path/hash, re-reading the canonical certificate plus bound Task247 durable-transport prerequisite, Task229 continuity checkpoint, and Task225 maintained transport contract bytes, rejecting duplicate/stale/non-canonical/tampered/chain-mismatched material, persisting append-only closure-review evidence, and writing provenance audit events. | Covered for service-owned operational closure review only; it records no-authority maintained transport primitive provenance and does not open durable/live transport, mutate trusted Pi state, run Lean, promote mathematical claims, or certify GA. |
| Pi operator/service transport closure review consumer bridge | `goal3-task291-pi-operator-service-transport-closure-review-consumer.test.mjs` covers `comath.release.piCodexLifecycleOperatorServiceTransportClosureReview`, `/cm:release lifecycle-operator-service-transport-closure-review`, Pi runtime registration, and the read-only interactive planner consuming only Task288 terminal completion certificate id/path/hash material, translating only the terminal-certificate public lifecycle alias, stripping model confirmation ids, rejecting wrong alias material before POST, sanitizing public request/result/confirmation/notification surfaces, and preserving service-returned closure lifecycle flags without proof, GA, durable, or live-transport authority. | Covered for Pi thin-client wiring only; Task290 service remains the authority for re-reading certificate/transport-chain artifacts, and this consumer does not expose executor output, accept caller certificates, open durable/live transport, mutate trusted state, run Lean, promote claims, or certify GA. |
| Task292 GA operational readiness review gate | `goal3-task292-service-owned-ga-operational-readiness-review.test.mjs` covers `recordGoal3GaOperationalReadinessReview()` and `POST /release/goal3/ga-operational-readiness-review` consuming current Task290 transport-closure review and Task167 adapter OS-isolation readiness review artifacts by canonical path/hash, rejecting stale or promotional inputs, persisting append-only release-candidate readiness evidence, and writing provenance audit events. | Covered for service-owned release-candidate operational readiness only; ready output still has `proof_authority=none`, cannot promote claims, cannot certify GA, cannot open durable/live transport, cannot mutate trusted Pi state, and does not replace final GA audit or Lean clean replay proof authority. |
| Task293 Pi GA operational readiness review consumer bridge | `goal3-task293-pi-ga-operational-readiness-review-consumer.test.mjs` covers `comath.release.goal3GaOperationalReadinessReview`, `/cm:release goal3-ga-operational-readiness-review`, Pi runtime registration, and the read-only interactive planner consuming only Task290 transport-closure review id/path/hash plus Task167 adapter OS-isolation review id/path/hash under host confirmation, translating only maintained public aliases, stripping model confirmation ids, rejecting mismatched aliases before POST, sanitizing public request/result/confirmation/notification surfaces, and preserving service-returned readiness flags without proof, GA, durable, or live-transport authority. | Covered for Pi thin-client wiring only; Task292 service remains the authority for re-reading readiness inputs, and this consumer does not expose caller proof claims, GA certificates, executor commands, Lean replay material, durable transport sessions, trusted-state mutation, claim promotion, or GA certification. |
| Task294 GA certification review precondition gate | `goal3-task294-service-owned-ga-certification-review.test.mjs` covers `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` consuming the current Task292 operational readiness review by canonical path/hash, rejecting stale or promotional readiness material, persisting a no-authority GA acceptance diagnostic support artifact, persisting append-only certification-review evidence, and writing provenance audit events. | Covered for service-owned fail-closed GA certification preconditions only; output remains `blocked_release_candidate_ga_certification_prerequisites` until proof breadth and final GA audit close, has `proof_authority=none`, cannot promote claims, cannot certify GA, cannot open durable/live transport, cannot mutate trusted Pi state, and does not replace Lean clean replay authority. |
| Task295 Pi GA certification review consumer bridge | `goal3-task295-pi-ga-certification-review-consumer.test.mjs` covers `comath.release.goal3GaCertificationReview`, `/cm:release goal3-ga-certification-review`, Pi runtime registration, and the read-only interactive planner consuming only Task292 operational-readiness review id/path/hash under host confirmation, translating only maintained public aliases, stripping model confirmation ids, rejecting mismatched aliases before POST, sanitizing public request/result/confirmation/notification surfaces, omitting caller acceptance-report/proof-breadth/final-audit/proof/GA/executor/Lean/durable-session material, and preserving service-returned blocked certification-precondition flags without proof, GA, durable, or live-transport authority. | Covered for Pi thin-client wiring only; Task294 service remains the authority for re-reading readiness inputs, running acceptance diagnostics, and recording blockers, and this consumer does not expose caller proof claims, GA certificates, acceptance reports, proof-breadth matrices, final GA audits, Lean replay material, durable transport sessions, trusted-state mutation, claim promotion, or GA certification. |
| Task296 final GA audit blocker gate | `goal3-task296-service-owned-final-ga-audit.test.mjs` covers `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` consuming the current Task294 certification review by canonical path/hash, re-reading the bound Task292 operational-readiness artifact and Task294 acceptance-support artifact, rejecting stale or promotional material, persisting append-only final-audit evidence, and writing provenance audit events. | Covered for service-owned fail-closed final GA audit evidence only; output may mark `final_ga_audit_available=true` because the audit ran, but remains `blocked_release_candidate_proof_breadth_incomplete` with `final_ga_audit_passed=false`, `proof_authority=none`, no claim promotion, no GA certification, no GA certificate, no durable/live transport authority, and no replacement for Lean clean replay authority. |
| Task297 Pi final GA audit consumer bridge | `goal3-task297-pi-final-ga-audit-consumer.test.mjs` covers `comath.release.goal3FinalGaAudit`, `/cm:release goal3-final-ga-audit`, Pi runtime registration, and the read-only interactive planner consuming only Task294 certification-review id/path/hash under host confirmation, translating only maintained public aliases, stripping model confirmation ids, rejecting mismatched aliases before POST, sanitizing public request/result/confirmation/notification surfaces, omitting caller acceptance-report/proof-breadth/final-audit/proof/GA/executor/Lean/durable-session material, and preserving service-returned blocked final-audit flags without proof, GA, final-audit-pass, GA-certificate, durable, or live-transport authority. | Covered for Pi thin-client wiring only; Task296 service remains the authority for re-reading certification-review and support artifacts, validating hashes, and recording final-audit blockers, and this consumer does not expose caller proof claims, GA certificates, acceptance reports, proof-breadth matrices, final GA audit payloads, Lean replay material, durable transport sessions, trusted-state mutation, claim promotion, final-audit pass, or GA certification. |
| Task298 service-owned proof breadth review gate | `goal3-task298-service-owned-proof-breadth-review.test.mjs` covers `recordGoal3ReleaseCandidateProofBreadthReview()` and `POST /release/goal3/proof-breadth-review` deriving the current 100-task positive manifest and positive-matrix batch service-side, persisting append-only manifest/batch/review artifacts by path/hash, exposing the status capability, rejecting duplicate review ids, ignoring caller proof-breadth material, and writing provenance audit events. | Covered for release-candidate proof-breadth audit evidence only; output remains `blocked_positive_matrix_release_candidate_proof_breadth_incomplete` with `proof_breadth_complete=false`, `clean_replay_passed_count=0`, `replayable_blocker_count=100`, `proof_authority=none`, no claim promotion, no GA certification, no final-audit unblock, and no replacement for Lean clean replay authority. |
| Task299 PM-001 task-local final-authority packaging | `goal3-task299-pm001-task-local-final-authority-packaging.test.mjs` covers `packageGoal3GaPositiveMatrixFinalAuthorityEvidenceV3()` and `packageGoal3GaPositiveMatrixFinalAuthorityEvidenceTrancheV3()` accepting PM-001 as a task-local packaging/blocker target, persisting canonical PM-001 final-authority packaging artifacts, and preserving PM-001 batch semantics that reject inherited representative fixture authority. | Covered for removing the 100-task proof-breadth dead end only; PM-001 remains a replayable blocker without task-local Lean Authority v3 evidence, cannot inherit representative fixture clean replay, cannot promote claims, and cannot certify GA by packaging alone. |
| Task300 proof-breadth closure verifier | `goal3-task300-proof-breadth-closure-verifier.test.mjs` covers `recordGoal3ReleaseCandidateProofBreadthClosure()` and `POST /release/goal3/proof-breadth-closure` deriving the 100-task manifest service-side, reading canonical task-local final-authority packaging reports, blocking on missing or non-verified reports, marking proof breadth complete only when all 100 reports are verified with Lean clean replay authority, exposing the status capability, and writing provenance audit events. | Covered for service-owned proof-breadth aggregation only; it does not run Lean, synthesize proofs, promote claims, certify GA, or accept caller proof matrices. Complete proof breadth can unblock final-GA-audit prerequisites but remains separate from GA certification. |
| Task301 final GA audit proof-breadth closure binding | `goal3-task301-final-ga-audit-proof-breadth-closure-binding.test.mjs` covers `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` consuming canonical Task300 proof-breadth closure id/path/hash material, keeping missing or incomplete closure blocked, passing final audit only when complete proof breadth is bound, rejecting stale closure hashes, preserving route semantics, and writing closure-bound provenance audit events. | Covered for final-audit closure awareness only; passed final audit still cannot promote claims, certify GA, expose a GA certificate, run Lean, synthesize proofs, or replace the separate GA certification gate. |
| Task302 GA certification final-audit binding | `goal3-task302-ga-certification-final-audit-binding.test.mjs` covers `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` consuming canonical Task301 final-audit id/path/hash material, keeping missing or blocked final audit as blockers, marking the review ready only when a passed final audit and operational readiness are current, rejecting stale final-audit hashes, preserving route semantics, and writing final-audit-bound provenance audit events. | Covered for pre-certificate readiness only; ready certification review still cannot certify GA, expose a GA certificate, promote claims, run Lean, or replace the separate certificate gate. |
| Task303 service-owned GA certificate gate | `goal3-task303-service-owned-ga-certificate-gate.test.mjs` covers `recordGoal3GaCertificate()` and `POST /release/goal3/ga-certificate` consuming canonical ready Task302 certification-review id/path/hash material, re-reading the review-bound final audit by hash/size, rejecting blocked reviews and stale final-audit material, exposing the status capability, preserving route semantics, and writing certificate provenance audit events. | Covered for release-candidate certification only; the certificate still cannot promote proof claims directly and requires ordinary Lean Authority gates for claim promotion. |
| Pi goal-mode live computation repair hint execution | `goal3-task278-pi-goal-mode-live-computation-repair-hint-execution.test.mjs` covers opt-in service-owned live `computation:sympy` execution against a configured SymPy-compatible endpoint, POST request body binding, in-memory bearer header use, sanitized request/response hashes, provider status/content type, response body hash, prompt-injection scan, no-authority vetoes, secret/root redaction, bounded computation report summaries, and threading live computation results through repair tasks/execution. | Covered for one live computation repair-context provider only; default runs remain stubbed/offline, SymPy reports remain non-authoritative, and CAS-guided repair synthesis, real-Pi/operator closure, OS-isolation production helpers, broad proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live computation decide repair | `goal3-task279-pi-goal-mode-live-computation-decide-repair.test.mjs` covers consuming an opt-in live non-authoritative `computation:sympy` exact-symbolic `True` report as candidate material for a same-name/same-statement closed decidable arithmetic equality, materializing `by decide`, preserving typed statement parsing, secret/root redaction, and no-authority flags, returning through candidate verification, and requiring service-owned LeanRunner before routing a passing repaired candidate to arbitration. | Covered for one narrow SymPy-guided arithmetic decide repair only; CAS output remains non-authoritative, final clean replay and promotion gates remain required, and arbitrary simplification, theorem synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode live computation final replay provenance | `goal3-task280-pi-goal-mode-live-computation-final-replay-provenance.test.mjs` covers carrying a live-computation-`by decide` repaired candidate through terminal clean replay, binding repair hint execution, campaign repair execution, and per-candidate repair execution provenance into the selected candidate descriptor, final replay material source, and public `exportCampaignGoalModeEvidence()` manifest while preserving secret/root redaction and no-authority flags. | Covered for public export provenance of one live computation repair path only; provenance remains non-authoritative, final clean replay is still the only proof authority, and arbitrary CAS proof synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi goal-mode export repair provenance consumer | `goal3-task281-pi-goal-mode-export-repair-provenance-consumer.test.mjs` covers `comath.campaign.export` and `/cm:campaign export` preserving the no-authority `candidate_repair_provenance` path/hash references needed to audit a selected repaired candidate, while still redacting ordinary `.comath` runtime paths, host roots, provider secrets, proof-success wording, and promotion flags from Pi-visible public output. | Covered for Pi public campaign export consumption only; provenance remains audit traceability, not proof authority, and broader repair synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Pi research loop export repair provenance | `goal3-task282-pi-research-loop-export-repair-provenance.test.mjs` covers `/cm:research` goal-mode loop results carrying verified campaign export traceability into `export_descriptor`, preserving sanitized no-authority repair provenance path/hash references in direct command output and runtime notification output, and withholding provenance when export authority is missing or forged. | Covered for Pi research-loop UX consumption only; the descriptor remains non-promotional and broader repair synthesis, real-Pi/operator closure, OS-isolation production helpers, proof breadth, and GA certification remain separate gated work. |
| Global GA readiness | Current test evidence does not cover arbitrary theorem planning, broad MathProve proof search or proof-authority semantics, OS-enforced network replay sandboxing, richer real Pi host UX/service lifecycle management, live production Codex API/network validation, indefinite operator subscriptions beyond bounded responses, or broad theorem synthesis. Phase 77 narrows runner risk to a service-level network-denial process policy but does not provide OS sandboxing. | Not achieved; blocked by deferred generalization work. |
| Theorem-specific broad target package | Former Phase 72/76/81 bounded Nat-linear target-package tests are historical fixture material and are excluded from the default P0 matrix because production Nat-linear proof promotion is now quarantined. | Not accepted as a production proof path until regenerated through lock/ledger/diff gates and Lean Authority v3 per-candidate/final clean replay. |
| Bounded theorem-specific proof-body candidate | Former Phase 73/76/81 bounded `by omega` proof-body candidate tests are historical fixture material and are excluded from the default P0 matrix because business-layer Nat synthesis must not certify proof artifacts. | Proof-body candidates may return only as draft/candidate evidence after the external wheel and Lean Authority v3 gates exist. |
| Bounded theorem-specific authority-preview reports | Former Phase 74/76/81 bounded authority-preview tests are historical fixture material and are excluded from the default P0 matrix because preview reports are not proof authority. | Authority previews remain advisory until tied to FormalSpecLock, AssumptionLedger, StatementDiffGate, dependency lock, and trusted replay manifests. |
| Bounded non-template final clean replay | Former Phase 75/76/81 bounded final-replay promotion tests are historical fixture material and are excluded from the default P0 matrix while Lean Authority v3 is not yet implemented. | No Nat-linear production promotion is accepted in the default matrix; future positive proof evidence must use clean Lean replay with the v3 manifest/provenance bindings. |
| General theorem synthesis | Phase 70 writes fail-closed broad-planning evidence for unsupported non-template targets; P0 quarantine blocks theorem-family/Nat-linear business-layer proof promotion. | Covered as fail-closed planning only; arbitrary proof synthesis remains deferred. |
