# Goal 4 Plan: CoMath Open Formal Workbench GA Rebuild

## 0. Initialization Status

- Goal workspace: `D:\MATH _Studio\comath-pi-lab\goal-4`
- Project root: `D:\MATH _Studio\comath-pi-lab`
- Initialization date: 2026-05-29
- Current round rule: only initialize `input.md`, `plan.md`, and `tasks.md`; do not modify source code before initialization is complete.
- Codex thread goal: already registered by the client for this `/goal` request.

## 1. Source Documents

The user named:

1. `CoMath_Pi_Lab_No_Reinvent_Audit_2026-05-29.md`
2. `CoMath_Open_Formal_Workbench_GA_Design_2026-05-29.md`

Actual files found in `D:\MATH _Studio\comath-lab开发文档区`:

1. `CoMath_No_Reinvent_GA_Audit_v2_2026-05-29.md`
2. `CoMath_Open_Formal_Workbench_GA_Design_v2_2026-05-29.md`
3. `COMATH_AGENT_TEAM_AND_PROMPTS_V2_20260526.md`

Assumption: the `v2` files are the intended documents because they are the only matching 2026-05-29 audit/design files in the specified documentation directory. This assumption must be rechecked at the start of each continuation session.

## 2. Current Context and Constraints

Relevant prior state:

- The repository already exists at `D:\MATH _Studio\comath-pi-lab`.
- Existing goal directories in the repository: `goal-1`, `goal-2`, `goal-3`.
- Current initialization creates `goal-4`; do not overwrite prior goal directories.
- `git status -sb` before initialization showed existing untracked items:
  - `goal-3/`
  - `services/comathd/tests/unit/phase82-controlled-equivalence-proof-execution.test.mjs`
- Those files are treated as user/prior-agent state and must not be reverted or deleted.

Hard project doctrine:

- Lean4/mathlib kernel clean replay is the only final mathematical proof authority.
- CoMath must not implement a theorem prover, general theorem library, business-layer theorem recognizer, Nat-only prover, or proof certification by agent vote.
- External theorem search, literature retrieval, CAS, SMT, SAT, proof-search agents, and votes are hint/evidence/refutation sources only.
- Promotion to proven requires FormalSpecLock, AssumptionLedger, dependency lock, toolchain hash, artifact hash, append-only replay manifest, clean replay, and integrity gate pass.
- Anything without clean Lean replay remains draft, hypothesis, or candidate.

## 3. Document-Derived Architecture Targets

From the audit document:

- Remove or quarantine the wrong trusted path:
  - business-layer theorem recognizer in `theorem-family.ts`
  - campaign tick pollution into `n : Nat`
  - Nat linear synthesis and registered toy targets
  - synthetic swarm and synthetic V1 winner
  - bounded tick loop pretending to be Pi goal-mode
- Replace with:
  - FormalSpecLock
  - AssumptionLedger
  - StatementDiffGate
  - adapter-first external tool registry
  - MathProve-native stage machine
  - real multi-agent swarm
  - Lean Authority v3
  - Pi goal-mode

From the GA design document:

- Product: open-source agentic formal mathematics research workbench on Pi.
- Trusted data/status boundary:
  - trusted: FormalSpecLock, AssumptionLedger, LeanRunManifest, DependencyLock, FinalReplayManifest, PromotionDecision
  - untrusted/proposal: AgentTask, AgentCandidatePack, agent output, external hints
- Stage machine:
  - S0 input_intake
  - S1 problem_lock
  - S2 knowledge_pack
  - S3 formal_spec_and_notation_gate
  - S4 blueprint_and_skeleton_gate
  - S5 line_map_gate
  - S6 lemma_sprint
  - S7 refutation_gate
  - S8 integration_refactor_gate
  - S9 final_lean_authority_replay
  - S10 proof_memory_update
- Agent architecture:
  - A0 Coordinator / PI Agent
  - A1 FormalSpec & Boundary Agent
  - A2 Librarian / Retrieval Agent
  - A3 Blueprint & Lemma DAG Agent
  - A4 Lean Formalization Agent
  - A5 Tactic Sprint / Library Search Agent
  - A6 Computation & Counterexample Agent
  - A7 Integrator & Refactor Agent
  - A8 Integrity Red-Team Auditor
- Stage-local variants:
  - V1 Direct Formalist
  - V2 Library / Premise Hunter
  - V3 Lemma Decomposer
  - V4 Tactic Sprinter
  - V5 Algebraic / Computational Verifier
  - V6 Boundary Refuter
  - V7 Statement-Equivalence Auditor
  - V8 Dialectical Stress / Revision Agent

From the prompt document:

- Agents must never certify mathematics by confidence or vote.
- Candidate and final replay evidence must be generated by the trusted runtime, not written by agents.
- Final terminal states must remain explicit and public.
- Prompt packs, candidate manifests, static auditor, replay auditor, and paper overclaim auditor must become repository-native artifacts.

## 4. Execution Strategy

This goal is large and must be executed as small, independently verifiable tasks. Each continuation session must:

1. Fully read:
   - `D:\MATH _Studio\comath-pi-lab\goal-4\input.md`
   - `D:\MATH _Studio\comath-pi-lab\goal-4\plan.md`
   - `D:\MATH _Studio\comath-pi-lab\goal-4\tasks.md`
2. Register a local todo list for that session.
3. Execute exactly the first unchecked task in `tasks.md`.
4. Run the task-specific verification listed in that task.
5. If code changed, commit only the relevant change set unless committing is blocked by external state.
6. Update that task's completion record with:
   - work performed
   - verification evidence
   - residual risk
   - next task
7. Stop after one task so the client can auto-continue.

Subagent protocol:

- Use the available subagent mechanism for parallel read-only audit or disjoint write scopes.
- Do not delegate the critical-path next action if the main agent can do it faster locally.
- For implementation tasks, assign disjoint file/module ownership to workers.
- All subagent outputs are untrusted until reviewed by the main agent and verified by tests.

## 5. Verification Policy

Minimum verification layers:

- Unit tests for schemas, gates, adapters, manifests, and prompt loading.
- Negative tests for statement drift, hidden assumptions, weakened conclusions, fake logs, agent-written pass evidence, forbidden Lean constructs, dependency pollution, and vote-only promotion.
- Positive tests for a minimal end-to-end proof research workflow with clean Lean replay where local environment supports it.
- Repository checks using the established command where feasible:
  - `corepack pnpm run ci`
  - narrower `corepack pnpm ...` tests for task-specific slices
- Clean replay tests must be fail-closed. If Lean toolchain or mathlib is unavailable, the result must be `replayable_environment_blocker`, not `proven`.

Every three tasks require a larger check-debug loop recorded in `tasks.md`, covering:

- requirement drift
- typecheck/build/test status
- security and no-cheat gates
- data/provenance consistency
- documentation and prompt synchronization
- rollback path

## 6. Rollback and Safety

- Do not delete existing user/prior-agent files without explicit evidence they are generated by the current task and safe to remove.
- Do not modify production credentials, payment/auth config, or machine-level state.
- Do not restart the machine or steal Windows focus.
- Prefer additive schemas/tests before destructive removal.
- For removal of toy/Nat-only paths, first add tests proving those paths cannot promote artifacts, then remove or quarantine the code.
- If an implementation breaks a broad check, either fix within the same task or record a replayable blocker with exact failing command/log.

## 7. Completion Standard

This goal is complete only when:

- No promoted proof artifact can exist without FormalSpecLock, AssumptionLedger, dependency lock, toolchain hash, artifact hash, append-only replay manifest, provenance audit event, and clean Lean replay.
- Toy/Nat-only production path, synthetic V1 winner, business theorem recognizer, and default `n : Nat` trust pollution are removed or fail-closed.
- Real A0+A1..A8 workflow and stage-local V1..V8 candidates are represented in code and tests.
- External wheel registry and adapters exist for Lean theorem search, literature retrieval, ingestion, and CAS/SMT/SAT hints.
- Lean Authority v3, DependencyClosureV2, LeanIntegrityScannerV2, AxiomProfileV2, No-Cheat Gate, and Statement Drift Red Team are implemented and tested.
- Pi goal-mode can create a research workspace, plan, retrieve, formalize, replay, repair, red-team, and terminate in proof/counterexample/blocker states.
- MathProve-Skill principles are native workflow gates, not an external dependency.
- README, architecture docs, config samples, prompt packs, acceptance tests, and threat model align with code.
- Final broad review finds no known high-risk GA blockers.
