# CoMath Pi Lab

CoMath is an open-source agentic formal mathematics workbench built around Lean4/mathlib. It does not implement its own theorem prover or mathematical kernel. It orchestrates external proof, search, retrieval, computation, and agent tools, and promotes a mathematical claim only after a clean Lean replay and integrity audit pass.

The current repository is a Goal 3 GA-refactor worktree with Research Alpha foundations, a Goal 3 trust-core implementation, and a representative GA acceptance harness. It is not final global GA until the current Goal 3 tracker blockers are closed and a final GA audit passes. In public wording, describe it as an auditable formal mathematics research workbench with strict Lean-authority gates, not as an arbitrary theorem prover.

## What CoMath Owns

- Pi interaction surfaces for `/cm:research`, `/cm:campaign`, `/cm:agent`, and `/cm:release` commands.
- `comathd`, the trusted local mutation boundary for runtime state, artifacts, audit logs, campaign state, and promotion decisions.
- FormalSpecLock, AssumptionLedger, StatementDiffGate, statement-drift red-team reports, and no-cheat gates that preserve theorem boundaries.
- External wheel registry contracts for theorem search, proof-search backends, literature retrieval, ingestion, and computation adapters.
- Lean Authority v3 evidence shapes: service-owned Lean run manifests, dependency locks, final replay manifests, structured audit material, and third-party replay packs whose embedded `FinalReplayManifest.json` and `expected_hashes.json` bind exactly to the project-local final replay evidence.
- A native MathProve-style stage machine and 1 coordinator plus 8 specialist agent workflow where agent outputs remain untrusted proposals.
- Pi goal-mode routing that can end only in formal replay passed, confirmed counterexample, user-visible statement disambiguation, replayable blocker, or resumable budget exhaustion.

## What CoMath Does Not Own

- CoMath does not verify theorem truth independently of Lean.
- CoMath does not replace Lean, mathlib, Lake, or maintained external Lean projects.
- CoMath does not maintain a proprietary theorem library or business-layer theorem prover.
- CoMath does not use theorem-family recognizers, Nat-linear synthesis, or default `n : Nat` injection as a production proof path.
- CoMath does not let agent votes, reviewer approval, papers, CAS/SAT/SMT reports, search results, or MathProve-style audits certify proofs.
- CoMath does not redistribute full third-party papers or external repositories in evidence packs unless license and user consent permit it.

## Current Product State

Goal 3 removed the old toy/Nat-only production path and replaced the trusted path with fail-closed formal-spec, statement-boundary, Lean-run, dependency, integrity, agent-workflow, adapter, Pi goal-mode, and acceptance-harness surfaces. Older Phase 18-81 material remains useful as historical vertical-slice evidence or negative fixtures, but it must not be described as the current production proof path.

macOS is outside the current GA environment adaptation scope.

Implemented Goal 3 trust-core evidence includes:

- `goal3-task2-no-toy-production-path.test.mjs` and `goal4-p0-no-reinvent-violations.test.mjs` for no-reinvent quarantine.
- FormalSpecLock and AssumptionLedger schema and intake tests.
- StatementDiffGate and statement-drift red-team tests.
- LeanRunManifest v3, final replay, dependency closure, integrity scanner, axiom profile, and no-cheat tests.
- Artifact-backed Lean evidence gates for live adapter replay claims and non-exact statement-equivalence replay claims.
- External wheel registry tests for proof-authority-none adapter outputs.
- Native MathProve-style stage-machine and GA agent-stage workflow tests.
- Pi goal-mode route, extension, and service-owned intake manifest tests.
- Goal 3 GA negative and representative positive proof-workflow harnesses.

Task 17's positive matrix is a historical representative harness, not a current product-completion workstream. Broad matrix-style proof campaigns are deferred until the final release-candidate audit; ordinary Goal 3 continuations should prioritize the GA product loop and use only small theorem-specific Lean/mathlib smoke checks when they directly validate product behavior. Task96 hardens batch consumers so representative fixture or aggregate harness evidence is not displayed as per-task clean replay. Task97 hardens the promotion gate so legacy final replay manifests cannot substitute for Lean Authority v3 final replay packaging. Task98 also blocks legacy PM-002 v1 final-authority packaging reports from satisfying `formally_checked`; only generic Lean Authority v3 packaging is promotion-grade. Task99 requires promotion-grade FinalReplayManifest v3 evidence to bind Lean/Lake executable binary hashes to a passing final-replay LeanRunManifest. Task100 prevents campaign/Pi proof-success read models and goal export readiness from treating legacy `completed_formal_proof` as proof success unless an explicit `formal_replay_authority_evidence` envelope is present. Task111-112 require live adapter and non-exact statement-equivalence replay claims to cite verified service-owned Lean replay manifests rather than marker strings. Task213 adds an opt-in campaign-native live Mathlib replay breadth gate for non-toy evidence; `True`, default `n : Nat`, `by trivial`, `by omega`, missing Mathlib import/dependency, or positive-matrix release paths fail before final replay allocation when that breadth profile is requested. Task214 extends that opt-in path with a Mathlib dependency-material gate: pinned `lake-manifest.json` mathlib material, trusted mathlib4 source, recorded license, and no local `Mathlib` shadowing are required before final replay workspace allocation. Task215 upgrades the final clean replay dependency slot to `DependencyClosureV2`, so untracked imports, package trust/pin/license vetoes, symlink escapes, and local Mathlib shadowing can fail final replay even when Lean/Lake command output appears successful. Task216 adds a campaign-native Mathlib provisioning diagnostic: that same opt-in path now requires locally materialized `.lake/packages/mathlib` sources before clean replay allocation, records package hashes as non-authoritative evidence, and copies materialized Mathlib package sources into the clean replay workspace when present. Task217 hardens FinalReplayManifest v3 verification so dependency-lock paths, hashes, toolchain text, and V2 package external revisions are recomputed from the clean workspace and dependency-closure report before a manifest is accepted. Task218 adds a campaign-native Mathlib host replay diagnostic before clean replay allocation, recording service-owned Lean/Lake version probes, expected toolchain match, binary hashes, replay plan, provisioning-diagnostic hash, replay-argument safety, and lakefile-declared build-target binding as non-authoritative provenance. Task219 adds a campaign-native Mathlib import-graph diagnostic before clean replay allocation, recording service-owned `lake env lean --deps` theorem/audit probes, stdout/stderr/output hashes, host-diagnostic hash, deps-stdout-only coarse Mathlib presence, option-argument rejection, and FinalReplayManifest v3 / replay-pack diagnostic binding as non-authoritative provenance rather than a CoMath parser or resolver. Task238 adds a no-download local Mathlib fixture preflight that composes Task214 and Task216, reports whether Task218/219 diagnostics may start, and still executes no Lean/Lake command, downloads no dependency, allocates no final replay workspace, and provides no proof authority. Task239 adds an append-only service-owned unattended real-host handoff review that re-reads canonical Pi/Codex lifecycle artifacts from ids, compares public prepared-checkpoint aliases/hashes only as expected values, records freshness/chain review evidence, and still cannot approve execution, run unattended host actions, provide durable transport, run Lean, promote claims, or certify GA.

Task240 exposes the Task239 unattended handoff review through a host-confirmed Pi consumer and read-only planner checkpoint. It forwards only prepared public path/hash expected values to the service route, strips model confirmation ids, sanitizes public output, and still cannot approve execution, run unattended host actions, provide durable transport, run Lean, promote claims, or certify GA. Task241 adds a service-owned unattended real-host execution readiness blocker that consumes a current Task239 review artifact, persists an append-only blocked readiness manifest, and records missing operator-approval, unattended-executor, and durable-transport blockers while still refusing execution, approval, direct Pi mutation, durable/live transport, Lean authority, claim promotion, and GA certification. Task242 exposes that readiness blocker through a host-confirmed Pi consumer and read-only planner checkpoint, translating public lifecycle aliases to service-canonical artifact paths only inside the comathd request while keeping public output sanitized and non-authoritative. Task243 adds a service-owned manual operator-approval artifact contract over the same reviewed handoff chain; readiness can bind that current approval artifact to remove only the missing-approval blocker, while unattended executor and durable-transport blockers remain fail-closed.

Task244 exposes the Task243 operator-approval checkpoint through a host-confirmed Pi consumer and read-only planner step before readiness. It calls only the service-owned approval route, strips model confirmation ids, sanitizes approval/request/result surfaces, and lets readiness forward a complete approval id/path/hash binding without turning approval into execution authorization, proof authority, durable/live transport, direct Pi mutation, or GA certification.

Task245 adds a service-owned unattended real-host executor contract checkpoint after the same reviewed handoff chain. The checkpoint records that an executor prerequisite has been configured as append-only service evidence, but explicitly records `executor_invoked=false`, `unattended_execution_authorized=false`, `durable_transport_provided=false`, `proof_authority="none"`, and `can_certify_ga=false`. Readiness may bind a current executor contract to remove only the missing-executor blocker; the durable-transport blocker remains fail-closed, and no real host executor, Pi consumer, Lean run, claim promotion, or GA certification is introduced.

Task246 exposes that executor contract checkpoint through `comath.release.piCodexLifecycleUnattendedRealHostExecutorContract` and `/cm:release lifecycle-unattended-real-host-executor-contract`. The Pi layer remains a host-confirmed thin client: it calls only the Task245 service route, strips model confirmation ids, translates public lifecycle aliases to service-canonical request paths, sanitizes executor/proof/transport/GA overclaims, and lets readiness forward a complete executor-contract id/path/hash binding while keeping durable transport and real unattended execution blocked.

Task247 records a service-owned durable-transport prerequisite contract over the current handoff review, operator approval, executor contract, and transport continuity chain. Task248 exposes that checkpoint through `comath.release.piCodexLifecycleUnattendedRealHostDurableTransportContract` and `/cm:release lifecycle-unattended-real-host-durable-transport-contract`; the planner now sequences handoff review -> operator approval -> executor contract -> durable transport prerequisite -> readiness, and readiness may forward a complete durable-contract id/path/hash binding without treating it as an open durable/live transport, execution authorization, proof authority, or GA certification. Task249 adds the service-owned unattended real-host execution attempt gate: `recordPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt` consume current prerequisite-recorded readiness, re-read the handoff/approval/executor/durable chain, and persist either a replayable executor-unavailable blocker or a bounded fixed-argv attempt result while still refusing terminal completion, Pi mutation, Lean authority, promotion, and GA certification. Task251 adds `reviewPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt-review` as the service-owned terminal attempt review gate: it re-reads the canonical attempt, readiness chain, and result artifact when present, then writes an append-only review artifact that classifies executor-unavailable, failed-attempt, or successful-attempt-still-non-terminal blockers without certifying unattended completion. Task252 exposes that review through `comath.release.piCodexLifecycleUnattendedRealHostExecutionAttemptReview` and `/cm:release lifecycle-unattended-real-host-execution-attempt-review` as a host-confirmed Pi thin client plus read-only planner checkpoint after the attempt; it requires attempt id/path/hash, strips model confirmation ids, translates only the attempt public alias inside the service request, and does not expose executor commands, caller attempt results, terminal completion, proof authority, or GA certification. Task253 adds `recordPiCodexLifecycleUnattendedRealHostCompletionCertificationPrerequisite()` and `POST /release/pi-codex-lifecycle/unattended-real-host-completion-certification-prerequisite` as the service-owned prerequisite gate after attempt review: it re-reads the current Task251 review, verifies the hash/path/no-authority boundary, and persists an append-only blocker that says a terminal unattended completion certificate is still missing rather than certifying completion.

## Runtime Baseline

- Node.js: `>=22.19.0`
- Package manager: `pnpm@11.3.0` via Corepack
- Runtime state: `.comath/`, ignored by Git and owned by `comathd`

Local validation:

```text
corepack pnpm install
corepack pnpm build
corepack pnpm typecheck
corepack pnpm test
```

Focused package validation:

```text
corepack pnpm --filter @comath/comathd build
corepack pnpm --filter @comath/comathd typecheck
corepack pnpm --filter @comath/comathd test
corepack pnpm --filter @comath/pi-extension build
corepack pnpm --filter @comath/pi-extension typecheck
corepack pnpm --filter @comath/pi-extension test
```

## Product Entrypoints

Use `comathd` as the trusted runtime owner and the Pi package as the interaction layer:

- Start or open projects through service-owned routes.
- Submit research goals through Pi `/cm:research` goal mode or equivalent service APIs.
- Inspect claims, evidence, gates, campaigns, agent runs, blockers, and exports through read-model routes and Pi tools.
- Export evidence packs only after all promotion gates and replay manifests agree.

Pi remains a thin client. It must not write `.comath/`, mutate trusted proof state, promote claims, or treat UI state as mathematical evidence.

## Proof Authority

The only final mathematical proof authority is clean Lean4/mathlib kernel replay. A promoted proof artifact must bind all of the following:

- FormalSpecLock and AssumptionLedger hashes.
- Locked theorem statement and theorem-header hash.
- DependencyLock with pinned Lean/Lake/mathlib/external repository material.
- Toolchain and artifact hashes.
- Service-owned LeanRunManifest records.
- Structured no-cheat, dependency, axiom, and statement checks.
- FinalReplayManifest and third-party replay command.

Artifacts without clean replay remain `draft`, `hypothesis`, `candidate`, `blocked_with_replayable_certificate`, or equivalent non-promotional states.

## Release Hardening Docs

Public release material uses three separate archive contracts. A source-review public diagnostic archive is non-authoritative (`proof_authority: none`) and may include sanitized source, markdown, HTML, and JSON reports for review, but `public_archive_is_proof_authority=false`; it cannot replace FinalAuthorityPackagingV3 / Lean Authority v3 source-report evidence. Source-review public archives also write a project-relative tamper-evident notarization-policy sidecar that binds the public manifest and report hashes while explicitly recording OS immutable storage and external notarization as `not_configured`; this is policy evidence, not external notarization or proof authority. A default snapshot export is a sanitized `public_download` with `can_restore=false`, is not a restore source, and is rejected by restore with `SNAPSHOT_PUBLIC_DOWNLOAD_NOT_RESTORABLE`. Only an explicit `internal_restore` snapshot is a byte-for-byte runtime-fidelity restore source, and it is not a public distribution artifact. Pi `/cm:release` and release tools expose source-review assembly and public archive review through `comathd` with host confirmation; they do not write `.comath/` directly or turn public diagnostics into proof authority.

Pi/Codex lifecycle release evidence now has service-owned non-authoritative producers for artifact-backed lifecycle intake, durable `comathd` service lifecycle probes, production Codex API account/network validation probes, real-Pi install/runtime-registration probe artifacts, durable operator-session manifests, operator transport recovery checkpoint artifacts, bounded operator transport lease artifacts, service-owned AgentRun log-session-bound lease provenance, guided real-Pi execution evidence chains, guided execution terminal chain reviews, operator/service maintained transport contracts, operator/service transport continuity checkpoints, adapter OS-isolation sandbox-launch preflight manifests, provider-runner contract manifests, provider-helper host-validation manifests, host-validation-bound provider-helper execution attempt manifests, provider-helper OS-isolation collection bridge manifests, adapter OS-isolation sandbox-execution probe manifests, adapter OS-isolation probe artifacts, configured-host OS-isolation collection contracts, and adapter OS-isolation readiness reviews. Pi release tooling covers the lifecycle review, Codex API probe, real-Pi runtime probe, read-only lifecycle walkthrough, bounded `lifecycle-control` operator actions, read-only `lifecycle-session` recovery planning, host-confirmed `lifecycle-operator-session` service persistence, host-confirmed operator transport recovery checkpointing, host-confirmed bounded operator transport lease opening, host-confirmed guided real-Pi execution recording, host-confirmed operator/service transport contract recording, host-confirmed operator/service transport continuity recording, host-confirmed automatic real-Pi lifecycle orchestration, host-confirmed adapter OS-isolation probe recording, and host-confirmed adapter OS-isolation sandbox execution probe recording through `comathd`. Guided execution records also bind the real-Pi runtime snapshot host label to the operator-session host label, reject contradictory explicit Pi host labels, and reject pre-Task220, expired, wrong-recovery, or tampered lease artifacts that lack current service-owned AgentRun log-session binding. Task224 terminal chain reviews bind runtime probe, operator session, recovery, bounded lease, heartbeat/rebind, and guided execution artifact paths/hashes into one append-only service review before release review can treat the run as terminal workflow evidence; missing heartbeat, poisoned heartbeat, stale chain hashes, or caller success wording fail closed before review persistence. Task225 operator/service transport contracts bind that terminal review to the current heartbeat artifact, the existing `GET /agent/run/<run_id>/log-session` Node HTTP route, and the Pi `fetch`/`getText` client primitive through a bounded re-run of `formatAgentRunLogSseSession()`; stale heartbeat hashes, missing reviews, secrets, proof-success wording, and long-lived transport claims fail closed or are sanitized before persistence. Task226 exposes the Task225 contract through Pi as a host-confirmed thin consumer and an interactive real-Pi checkpoint entry while stripping model confirmation ids, sanitizing actor/path/result surfaces, and forcing proof/GA/durable/live/direct-Pi-write/trusted-state-mutation overclaims false. Task229 operator/service transport continuity checkpoints consume a Task225 contract artifact, require and verify its hash, resume only from its `log_session_next_cursor`, and re-run bounded AgentRun log-session reads over the same maintained Node HTTP route plus Pi `fetch`/`getText` primitive; this is durable resume provenance only, not a durable long-lived transport stack. Task230 exposes the Task229 continuity checkpoint through Pi as a host-confirmed thin consumer and interactive real-Pi checkpoint entry while requiring `transport_contract_sha256`, stripping model confirmation ids, redacting trusted runtime paths from public UI, and forcing proof/GA/durable/live/direct-Pi-write/trusted-state-mutation overclaims false. Adapter package launch metadata now explicitly records that GA requires OS-enforced isolation and that the current package launcher boundary is still `process_boundary_only` unless a service-owned OS-isolation probe artifact passes review. The OS-isolation sandbox-launch route records provider-specific preflight manifests only; it rejects caller-supplied command overrides or success metadata as proof of sandbox execution. The provider-runner route binds a ready preflight to fixed provider argv/environment policy and records an unavailable blocker when no service-owned runner helper is configured; caller command, argv, env, hashes, or success-shaped metadata cannot resolve the runner. The provider-helper host-validation route binds a ready provider-runner manifest to a service-owned helper-host validator, verifies helper binary hash and supported platform metadata, and rejects caller-supplied host-ready booleans, command/argv/env/hash/version metadata; host validation is still not executed OS isolation or readiness evidence. The provider-helper execution route now requires a prior append-only service-owned host-validation manifest that is validated and exactly bound to the same project, adapter, backend, provider, launch artifact, runner artifact, runner binary hash, and helper binary hash before any helper config resolver or helper process can run; when that binding passes, it launches the service-owned helper with `shell=false` and fixed argv/env and records only the host-validation artifact hash plus exit status and stdout/stderr/transcript hashes. Missing, unvalidated, or mismatched host-validation artifacts and caller-supplied helper success metadata fail closed; helper exit 0 is still not collected OS-enforcement evidence or readiness evidence. The provider-helper collection route binds a ready helper execution back to its runner and launch artifacts, rejects caller-supplied collection metadata, and can write canonical Task170 probe/evidence artifacts only through an internal service-owned collection callback whose exit/stdout/stderr/transcript hashes exactly match the helper execution manifest. The OS-isolation sandbox-execution route bridges a ready preflight to an internal service-owned execution probe, but route and Pi callers still cannot submit success-shaped execution metadata as collected evidence. The OS-isolation probe route and Pi consumers record blocker evidence by default; caller-supplied request metadata cannot satisfy the readiness review, and collected OS-enforcement evidence can be recorded only through a service-owned collector inside `comathd`. Only canonical service-owned probe/evidence artifacts and their readiness reviews can feed release gates; Pi-facing output can initiate, route, display, or guide those service-owned flows only. These reports, manifests, checkpoints, leases, AgentRun log-session bindings, guided execution records, terminal chain reviews, operator/service transport contracts, operator/service transport continuity checkpoints, walkthroughs, controls, recovery plans, preflight manifests, provider-runner manifests, provider-helper host validations, host-validation-bound provider-helper execution attempts, provider-helper collection bridge manifests, execution-probe manifests, probe artifacts, collection records, Pi-facing output, and readiness reviews cannot promote claims, certify GA, expose API secrets, claim durable long-lived transport, claim indefinite WebSocket/SSE transport, replace an operator-controlled real Pi host run, or by themselves provide a general-purpose OS sandbox for adapter execution.

Task221 adds bounded operator transport heartbeat/rebind checkpoints to that lifecycle line. `POST /release/pi-codex-lifecycle/operator-transport-heartbeat`, `comath.release.piCodexLifecycleOperatorTransportHeartbeat`, and `/cm:release lifecycle-operator-transport-heartbeat` read an unexpired Task220-bound lease, re-run service-owned AgentRun log-session formatting for the requested cursor, and write a new append-only heartbeat artifact without mutating the session, recovery, or lease files. Static tampering, expired, wrong-project, wrong-chain, stale-client, non-monotonic live-log, or unreadable leases fail closed; same-route/same-run live log growth is accepted only as bounded rebind provenance. Pi output sanitizes secrets, host paths, proof-success wording, and long-lived transport overclaims. This is bounded checkpointing only, not durable long-lived SSE/WebSocket transport, proof authority, or GA certification.

Task197 tightens provider-helper collection: even a service-owned callback with matching helper exit/stdout/stderr/transcript hashes must also record complete process, filesystem, network, no-new-privileges, escape-prevention, service-owned collection-source, and helper-exit facts. Incomplete facts now remain replayable blockers, not readiness evidence or proof authority.

Task198 adds the production route/no-injected-callback default for that collection bridge. When a helper execution is collectable and runtime-attested, the public collection route now invokes an internal service-owned default probe that binds the persisted helper exit/stdout/stderr/transcript hashes and writes canonical blocker evidence with false OS-enforcement facts. Caller success-shaped route fields remain ignored, runtime-attestation missing still blocks before probe writing, and the resulting wrapper/probe artifacts remain non-authoritative blockers until a provider-specific OS-enforcement probe reports complete service-owned facts.

Task199 adds the configured provider-helper collection probe path for that same production route. Operators may configure a service-owned provider collection probe executable plus bounded JSON args prefix; `comathd` runs it with `shell=false`, fixed argv/env, and disabled-network `proof_authority=none` metadata after the Task187 runtime-attestation gate. Its stdout JSON must bind the current project, collection id, helper execution id, runner id, launch id, adapter, backend, provider, helper exit code, and helper stdout/stderr/transcript hashes before complete OS-enforcement facts can become nested canonical OS-isolation evidence. The collection wrapper remains non-authoritative and `can_certify_ga=false`; only the nested probe/evidence artifact can satisfy the OS-isolation readiness review, and mathematical proof authority is still exclusively Lean clean replay.

Task200 hardens that configured collection probe path so stdout JSON and persisted wrapper manifests also bind the current provider-helper host-validation artifact and the observed provider host-capability probe artifact. A configured probe that omits or mismatches host-validation id/path/hash, host-capability probe id/path/hash, observed host-capability status, or the provider-host-capability-bound flag fails closed to a replayable blocker. This host-capability/host-validation binding is an audit and provenance prerequisite only: it is not readiness evidence by itself, not mathematical proof authority, not real-Pi evidence, and not GA certification.

Task201 revalidates the Task190-200 provider-helper release chain as a check-debug guard. It keeps release-hardening suite discovery, public wording, route/Pi payload boundaries, runtime cleanliness, wrapper-readiness rejection, and host-capability binding mismatch behavior under executable coverage; stale provider host-capability artifacts or stale host-validation bindings fail closed before configured collection can write nested canonical evidence. This helper release chain check is not readiness evidence by itself, not mathematical proof authority, not real-Pi evidence, and not GA certification.

Task202 adds a bundled provider-helper collection probe asset for the default no-env collection path. The route now runs a service-owned bundled collection probe that binds helper execution, host-validation, host-capability, and hash facts before writing the same incomplete blocker evidence. It keeps the existing non-authority boundary and records the missing OS facts explicitly.

Task203 tightens complete provider-helper collection evidence: complete OS-enforcement facts now also require a provider-specific executed-tool witness bound to service-derived collection-probe executable/profile/fixed-argv-template hashes, the current collection, helper execution, runner, launch, provider, disabled-network policy, and helper transcript hash. Missing, mismatched, or stale pre-Task203/pre-Task204 witness material becomes a replayable blocker, including for internal service-owned callbacks; caller route payloads still cannot provide readiness evidence, and the wrapper remains non-authoritative.

Task204 adds provider-specific tool binding to that witness chain. Complete provider-helper collection evidence must now bind the witness to a host-capability provider tool name/hash observed by the current service-owned capability probe; Task203-valid witness material without that provider-specific tool binding, or evidence whose current host-capability artifact no longer matches that binding, fails closed as a replayable blocker. This is provenance hardening for the helper collection path.

Task205 adds provider-family OS-enforcement witness binding to the same chain. Complete provider-helper collection evidence must now carry `provider_family_os_enforcement_witness` material bound to the current provider family, collection/helper/runner/launch ids, host-capability artifact and provider tool name/hash, helper transcript hash, complete process/filesystem/network/no-new-privileges/escape-prevention facts, disabled network policy, and `proof_authority="none"`. Missing, mismatched, or stale pre-Task205 witness material fails closed before nested canonical evidence can satisfy adapter OS-isolation readiness.
Task206 extends that provider-family witness with provider-family execution profile binding. Complete provider-helper collection evidence must also carry `provider_family_execution_profile` material binding the service-derived provider-family execution kind, profile hash, and argv hash to the same current provider family and helper chain. Missing or mismatched Task206 profile material fails closed before readiness; it is provenance hardening only and remains non-authoritative, non-release-grade, and non-shipping metadata.
Task207 adds provider-specific live probe attempt binding. Complete provider-helper collection evidence must also carry `provider_specific_live_probe_attempt` material bound to the same collection/helper/runner/launch ids, provider-family execution profile, provider tool hashes, helper transcript hash, complete OS-enforcement facts, disabled network policy, and non-authority boundary. Missing or mismatched live-probe material fails closed before readiness.
Task208 adds provider-specific live probe execution binding. Complete provider-helper collection evidence must also carry `provider_specific_live_probe_execution` material produced by a separately configured service-owned live OS probe subprocess, bound to the same helper chain, provider-family execution profile, provider tool hashes, helper transcript hash, live probe tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary. Missing or mismatched live-probe execution material fails closed before readiness.
Task209 adds provider-specific live probe collection binding. Configured provider-helper collection stdout must bind the same service-executed `provider_specific_live_probe_execution` id/hash/tool/argv/stdout/stderr/transcript material before complete collection facts can satisfy readiness; missing or mismatched collection-side binding fails closed.
Task210 adds provider control-plane execution witness binding. Complete provider-helper collection evidence must also carry `provider_control_plane_execution_witness` material bound to the current provider family execution profile, provider tool hashes, helper transcript hash, provider-specific live probe execution id/hash/tool/argv/stdout/stderr/transcript hashes, disabled network policy, and non-authority boundary. Missing or mismatched control-plane witness material fails closed before readiness.
Task211 revalidates the Task202-210 provider-helper witness chain as a check-debug loop. It keeps bundled/configured collection coverage, provider-tool/family/live-probe/control-plane witness-chain suite discovery, route sanitization, config flags, public wording, blocker/veto wiring, and runtime cleanliness synchronized without upgrading witness metadata into proof authority, real-Pi evidence, broad provider support, production helper shipment, daemon/policy inspection, OS-enforcement proof, or GA certification.
Task212 adds a Windows AppContainer production-helper profile contract. Env-configured Windows AppContainer helpers now carry non-secret profile-source fields through provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests so release audits can distinguish `operator_configured_provider_helper` from `bundled_provider_helper_protocol_asset`. These profile fields are lineage metadata only: they do not ship a production AppContainer runner, do not prove OS enforcement, do not satisfy readiness without the existing helper witness chain and canonical service-owned evidence, do not provide real-Pi evidence, do not affect Lean proof authority, and do not certify GA.

Task222 adds an OCI/Docker/Podman production-helper profile contract. OCI provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests now carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Docker/Podman facility family, disabled network policy, no-new-privileges requirement, and path-free host-facility tool names. This is lineage and anti-confusion metadata only: it does not launch containers, inspect Docker/Podman daemon/socket/container state, ship a production helper binary, prove OS enforcement, satisfy readiness without the existing helper witness chain and canonical service-owned evidence, provide real-Pi evidence, affect Lean proof authority, or certify GA.

Task232 adds a Nix sandbox production-helper profile contract. Nix provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests now carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Nix sandbox facility family, disabled network policy, no-new-privileges requirement, and path-free `nix_cli` / `nix_store_cli` host-facility tool names. This is lineage and anti-confusion metadata only: it does not run Nix, inspect Nix store/profile state, ship a production Nix helper binary, prove OS enforcement, satisfy readiness without the existing helper witness chain and canonical service-owned evidence, provide real-Pi evidence, affect Lean proof authority, or certify GA.

Task234 adds a Firejail production-helper profile contract. Firejail provider-runner, helper-host-validation, helper-execution, and provider-helper collection manifests now carry a service-derived `production_helper_profile_contract` that binds the helper profile source, helper binary hash, Firejail facility family, disabled network policy, no-new-privileges requirement, and path-free `firejail_cli` host-facility tool name. This is lineage and anti-confusion metadata only: it does not run Firejail, inspect Firejail profile or policy state, ship a production Firejail helper binary, prove OS enforcement, satisfy readiness without the existing helper witness chain and canonical service-owned evidence, provide real-Pi evidence, affect Lean proof authority, or certify GA.

Task236 removes the excluded platform from current environment-adaptation documentation, sample configuration, and release-hardening smoke scope. The Task235 internal regression remains historical implementation evidence only; current public release docs must not advertise removed-platform helper handles, host-facility names, profile contracts, or focused release-hardening suites.

Task223 adds a read-only interactive real-Pi checkpoint UX on top of the existing lifecycle command chain. `comath.release.piCodexLifecycleInteractiveRealPi` and `/cm:release lifecycle-interactive-real-pi` render ordered next-action plans across runtime probe, operator session, transport recovery, bounded lease, heartbeat/rebind, guided execution, operator/service transport contract, automatic orchestration, Codex API probe, and review checkpoints. The UX never calls `comathd`, writes `.comath`, echoes caller-supplied trusted runtime paths or path-shaped IDs, auto-executes lifecycle actions, accepts model-supplied confirmation ids, exposes host paths/secrets, claims proof authority, or claims durable long-lived transport; every mutating checkpoint still routes through the existing host-confirmed service commands.

Task224 adds a service-owned guided execution terminal chain review. `reviewPiCodexLifecycleTerminalExecution()` and `POST /release/pi-codex-lifecycle/terminal-execution-review` read the existing runtime probe, operator session, recovery, bounded lease, heartbeat/rebind, and guided execution artifacts, recompute their current hashes, verify one project/session/lease/run chain, and persist `.comath/release/pi-codex-lifecycle/<review_id>/terminal-execution-review.json` only after the heartbeat and guided execution are both consumed. This review is release evidence only: it does not run Lean, promote proof claims, certify GA, open durable transport, or replace a real operator-controlled Pi execution.

Task225 adds a service-owned operator/service maintained transport contract. `recordPiCodexLifecycleOperatorServiceTransportContract()` and `POST /release/pi-codex-lifecycle/operator-service-transport-contract` read a Task224 terminal review, verify the current Task221 heartbeat hash still matches the reviewed chain, and bind the existing `GET /agent/run/<run_id>/log-session` Node HTTP route to the Pi `fetch`/`getText` client primitive with bounded `max_bytes`, `max_events`, and `retry_ms` limits. The contract persists `.comath/release/pi-codex-lifecycle/<transport_contract_id>/operator-service-transport-contract.json` with `proof_authority="none"` and cannot promote proof claims, certify GA, open durable transport, or provide indefinite WebSocket/SSE sessions.

Task226 adds the Pi consumer for that operator/service transport contract. `comath.release.piCodexLifecycleOperatorServiceTransportContract` and `/cm:release lifecycle-operator-service-transport-contract` call the service-owned Task225 route through host confirmation, expose only fixed maintained primitive enums, strip any model-supplied `confirmation_id`, and sanitize secrets, host paths, proof-success wording, live transport wording, direct-Pi-write wording, and trusted-state-mutation wording. The read-only interactive real-Pi planner includes the contract checkpoint after guided execution, but it still only renders a command plan and does not execute lifecycle actions.

Task227 adds service-owned automatic real-Pi lifecycle checkpoint-chain orchestration. `orchestratePiCodexLifecycleAutomaticRealPiExecution()` and `POST /release/pi-codex-lifecycle/automatic-real-pi-execution` compose the existing real-Pi runtime probe, operator-session manifest, transport recovery, bounded lease, heartbeat/rebind, guided execution, terminal review, and operator/service transport contract producers, then persist `.comath/release/pi-codex-lifecycle/<orchestration_id>/automatic-real-pi-execution.json` binding every checkpoint id/path/hash. The orchestrator advances the heartbeat cursor from the bounded AgentRun log-session lease, rejects non-service-owned operator routes before writing the final orchestration artifact, and forces proof/GA/durable/live/direct-Pi-write/trusted-state flags false. This is checkpoint-chain provenance only: it does not implement a durable transport stack, write trusted Pi state directly, run Lean, prove mathematics, promote claims, or certify GA.

Task228 adds the Pi consumer for that automatic real-Pi lifecycle orchestration. `comath.release.piCodexLifecycleAutomaticRealPiExecution` and `/cm:release lifecycle-automatic-real-pi-execution` call the Task227 service route through host confirmation, strip model-supplied `confirmation_id`, sanitize nested checkpoint request/result surfaces, and force proof/GA/durable/live/direct-Pi-write/trusted-state-mutation overclaims false. The read-only interactive real-Pi planner now includes the automatic orchestration checkpoint after the operator/service transport contract; it does not call `comathd`, auto-execute lifecycle actions, open durable transport, run Lean, promote claims, or certify GA.

Task229 adds a service-owned operator/service transport continuity checkpoint. `recordPiCodexLifecycleOperatorServiceTransportContinuity()` and `POST /release/pi-codex-lifecycle/operator-service-transport-continuity` consume a Task225 transport contract artifact, require and verify the caller-supplied contract hash, resume only from the contract `log_session_next_cursor`, and persist `.comath/release/pi-codex-lifecycle/<continuity_id>/operator-service-transport-continuity.json` after a bounded `formatAgentRunLogSseSession()` re-read. It records `durable_resume_checkpoint_recorded=true` while keeping `durable_transport_provided=false`, `live_transport_open=false`, `proof_authority="none"`, and `can_certify_ga=false`.

Task230 adds the Pi consumer for that continuity checkpoint. `comath.release.piCodexLifecycleOperatorServiceTransportContinuity` and `/cm:release lifecycle-operator-service-transport-continuity` call only the Task229 service route through host confirmation, require `transport_contract_sha256`, strip model-supplied `confirmation_id`, sanitize actor/path/result surfaces, and keep proof/GA/durable/live/direct-Pi-write/trusted-state-mutation flags false in public output. The read-only interactive real-Pi planner now includes the continuity checkpoint after automatic real-Pi orchestration and before Codex API probe/review; it does not call `comathd`, auto-execute lifecycle actions, open durable transport, run Lean, promote claims, or certify GA.

Task231 adds a prepared unattended real-Pi handoff mode to that same read-only planner. `comath.release.piCodexLifecycleInteractiveRealPi` with `action="unattended-handoff"` and `/cm:release lifecycle-interactive-real-pi unattended-handoff` summarize already prepared service-owned checkpoint path/hash refs for operator handoff, block readiness when a required ref is missing or poisoned, and sanitize trusted runtime paths, host paths, secrets, proof-success wording, long-lived transport claims, and operator-free/unattended-executor overclaims. It does not call `comathd`, ask for mutation confirmation, accept `confirmation_id`, write `.comath`, auto-execute lifecycle actions, create service evidence, open durable transport, promote claims, or certify GA.

Task237 adds an operator review checklist mode to that same read-only planner. `comath.release.piCodexLifecycleInteractiveRealPi` with `action="operator-review-checklist"` and `/cm:release lifecycle-interactive-real-pi operator-review-checklist` reuse the Task231 prepared checkpoint refs, render ordered manual review items, block on missing or poisoned refs, ignore caller approval-shaped fields, and keep proof/GA/durable/live/direct-Pi-write/trusted-state/service-evidence/execution flags false. The checklist validates ref shape and missing fields only; it does not call `comathd`, persist an operator review manifest, verify artifact freshness, approve execution, create service evidence, open transport, run Lean, promote claims, or certify GA.

Task233 revalidates the Task220-231 operator/service transport chain as a check-debug guard. The focused suite keeps automatic orchestration, maintained transport contracts, continuity checkpoints, and prepared handoff references on the fixed Node HTTP AgentRun log-session route plus Pi `fetch`/`getText` primitive, while preserving duplicate-id, stale-contract, missing-hash, route-sanitization, public-authority, and audit-boundary regressions. This is release-hardening coverage only; it does not add a new transport route, Pi tool, background stream owner, Lean execution path, claim promotion path, direct Pi mutation, or GA certification.

Task241 records a blocked pre-execution readiness manifest after a Task239 handoff review. `recordPiCodexLifecycleUnattendedRealHostExecutionReadiness()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-readiness` re-read the canonical handoff-review artifact, require the caller-supplied review hash to match, reject promotional or execution-shaped review artifacts, and persist `.comath/release/pi-codex-lifecycle/<readiness_id>/unattended-real-host-execution-readiness.json` with blocker reasons rather than running a host action. Task242 adds `comath.release.piCodexLifecycleUnattendedRealHostExecutionReadiness` and `/cm:release lifecycle-unattended-real-host-execution-readiness` as the host-confirmed Pi consumer for that service route, plus a read-only interactive planner checkpoint after handoff review. Task243 adds `recordPiCodexLifecycleUnattendedRealHostOperatorApproval()` and `POST /release/pi-codex-lifecycle/unattended-real-host-operator-approval` as an append-only service artifact that re-reads the canonical handoff-review chain, rejects stale hashes and promotional approval material, emits audit evidence, and lets readiness record `operator_approval_artifact_current=true` without authorizing execution. It is not production unattended execution, not durable/live transport, not direct Pi mutation, not Lean execution, not proof authority, and not GA certification.

Task244 adds `comath.release.piCodexLifecycleUnattendedRealHostOperatorApproval` and `/cm:release lifecycle-unattended-real-host-operator-approval` as the host-confirmed Pi consumer for that service route, plus planner sequencing that places operator approval before readiness and passes a complete approval binding to readiness when provided. It remains a thin client and planner checkpoint only: no direct `.comath` write by Pi, no model-supplied confirmation id forwarding, no unattended execution authorization, no durable/live transport, no Lean execution, no proof authority, and no GA certification.

Task246 adds `comath.release.piCodexLifecycleUnattendedRealHostExecutorContract` and `/cm:release lifecycle-unattended-real-host-executor-contract` as the host-confirmed Pi consumer for the Task245 executor-prerequisite route. The read-only planner sequences handoff review -> operator approval -> executor contract -> durable transport prerequisite -> readiness, and the readiness consumer can pass complete approval and executor-contract bindings without treating either checkpoint as execution authorization or durable transport.

Task247 adds `recordPiCodexLifecycleUnattendedRealHostDurableTransportContract()` and `POST /release/pi-codex-lifecycle/unattended-real-host-durable-transport-contract` as a service-owned durable-transport prerequisite contract over the Task239 handoff-review chain, current Task243 approval artifact, current Task245 executor contract, and current Task229 continuity checkpoint. Task248 adds `comath.release.piCodexLifecycleUnattendedRealHostDurableTransportContract` and `/cm:release lifecycle-unattended-real-host-durable-transport-contract` as the host-confirmed Pi consumer for that service route, plus readiness durable-contract binding. Readiness can bind the current contract and clear `durable_transport_not_provided`, recording `unattended_real_host_execution_prerequisites_recorded`; it still does not approve or execute a handoff, open durable/live transport, mutate Pi trusted state, run Lean, promote claims, or certify GA.

Task249 adds `recordPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt` as the service-owned gate after prerequisite-recorded readiness. It replays the readiness boundary by re-reading the Task239 handoff review, Task243 approval, Task245 executor contract, Task247 durable-transport prerequisite, and Task229 continuity material before writing an append-only `comath.pi_codex_unattended_real_host_execution_attempt.v1` manifest. Without an explicit allowlisted executor command it records `blocked_unattended_real_host_executor_unavailable`; with a command it uses fixed argv/env, `shell:false`, bounded timeout, sanitized output, and a separate result artifact. In both cases it is not terminal execution completion, not Pi trusted-state mutation, not Lean/mathlib replay, not proof authority, not claim promotion, and not GA certification.

Task250 adds `comath.release.piCodexLifecycleUnattendedRealHostExecutionAttempt` and `/cm:release lifecycle-unattended-real-host-execution-attempt` as the host-confirmed Pi consumer for the Task249 route, plus the read-only planner checkpoint after readiness. The Pi surface requires readiness id/path/hash, strips model-supplied confirmation ids, translates only the public readiness alias into the service-canonical request path, does not expose `executor_command`, and sanitizes request/result/confirmation/notification output. This gives the product a Pi-facing path to record a replayable attempt blocker, but still does not complete unattended execution, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task251 adds `reviewPiCodexLifecycleUnattendedRealHostExecutionAttempt()` and `POST /release/pi-codex-lifecycle/unattended-real-host-execution-attempt-review` as a service-owned terminal review over Task249 attempt evidence. It accepts attempt id/path/hash, derives the canonical attempt path, re-reads and hash-binds the attempt, replays the readiness/handoff/approval/executor/durable chain, verifies any result artifact, persists `comath.pi_codex_unattended_real_host_execution_attempt_review.v1`, and emits audit evidence. Exit code 0 remains `blocked_terminal_unattended_completion_review_required` until a future stricter completion certificate exists; all review outputs keep proof, GA, durable/live transport, direct Pi mutation, and terminal unattended completion flags false.

Task252 adds `comath.release.piCodexLifecycleUnattendedRealHostExecutionAttemptReview` and `/cm:release lifecycle-unattended-real-host-execution-attempt-review` as the host-confirmed Pi consumer for the Task251 route, plus the read-only planner checkpoint after the execution attempt. The Pi surface requires attempt id/path/hash, strips model-supplied confirmation ids, translates only the public attempt alias into the service-canonical request path, does not expose `executor_command` or caller-provided attempt results, and sanitizes request/result/confirmation/notification output. This lets the product review a recorded attempt from Pi UX, but still does not certify terminal unattended completion, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task253 adds `recordPiCodexLifecycleUnattendedRealHostCompletionCertificationPrerequisite()` and `POST /release/pi-codex-lifecycle/unattended-real-host-completion-certification-prerequisite` as the service-owned prerequisite gate after Task251 review evidence. It accepts attempt-review id/path/hash, derives the canonical review path, rejects stale or promotional review artifacts, persists `comath.pi_codex_unattended_real_host_completion_certification_prerequisite.v1`, emits audit evidence, and records `blocked_terminal_unattended_completion_certification_required` with `completion_certificate_available=false`. This makes the next missing terminal-completion certificate explicit, but still does not certify terminal unattended completion, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task254 adds `comath.release.piCodexLifecycleUnattendedRealHostCompletionCertificationPrerequisite` and `/cm:release lifecycle-unattended-real-host-completion-certification-prerequisite` as the host-confirmed Pi consumer for the Task253 prerequisite gate, plus the read-only planner checkpoint after attempt review. The Pi surface requires attempt-review id/path/hash, strips model-supplied confirmation ids, translates only the public attempt-review alias inside the service request, does not expose `executor_command`, caller attempt results, or completion certificates, and sanitizes request/result/confirmation/notification output. This lets the product record the service-owned completion-certification blocker from Pi UX, but still does not certify terminal unattended completion, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task284 binds that Task253 completion-certification prerequisite back into the older Pi/Codex lifecycle readiness review. When `reviewPiCodexLifecycleReadiness()` or `POST /release/pi-codex-lifecycle/review` receives prerequisite id/path/hash material, it re-reads the canonical artifact, rejects stale, non-canonical, or promotional prerequisite files, records the artifact hash in the readiness review, and adds `terminal_unattended_completion_certification_required` while keeping `proof_authority=none` and `can_certify_ga=false`. This keeps old install/Codex readiness evidence useful, but prevents the release-readiness aggregate from looking complete while Task253 says terminal unattended completion is still missing.

Task285 exposes that prerequisite-bound lifecycle review through the Pi thin client. `comath.release.piCodexLifecycleReview` and `/cm:release pi-codex-lifecycle` now accept optional completion-prerequisite id/path/hash fields and forward them to the existing service-owned review route under Pi host confirmation. This is consumer wiring only: it does not create a completion certificate, certify unattended completion, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task286 adds a service-owned terminal completion certificate design gate. `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign()` and `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate-design` consume the current Task253 prerequisite id/path/hash, re-read the canonical prerequisite artifact, reject stale, non-canonical, or promotional prerequisite material, and persist an append-only design artifact that lists the required future terminal-certificate evidence. The design remains blocked with `completion_certificate_available=false`, `terminal_unattended_completion_certified=false`, `proof_authority=none`, and `can_certify_ga=false`; it does not create or certify a completion certificate, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task287 exposes that design gate through the Pi thin client. `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificateDesign`, `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate-design`, Pi runtime registration, and the read-only interactive planner now consume only the Task253 prerequisite id/path/hash under host confirmation, translate only the prerequisite public alias to the service-canonical request path, strip model confirmation ids, and sanitize public request/result/confirmation/notification surfaces. This is consumer wiring only: it does not create a terminal completion certificate, certify unattended completion, expose executor output, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task288 adds the service-owned terminal completion certificate evidence gate. `recordPiCodexLifecycleUnattendedRealHostTerminalCompletionCertificate()` and `POST /release/pi-codex-lifecycle/unattended-real-host-terminal-completion-certificate` consume the current Task286 design id/path/hash, re-read the canonical design, Task253 prerequisite, Task251 attempt-review, and current attempt-result artifact bytes, reject stale/non-canonical/tampered chains, and persist an append-only certificate manifest plus provenance audit event. The certificate can mark `terminal_unattended_completion_certified=true` only for the Pi/Codex unattended lifecycle, while keeping `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it redacts executor stdout/stderr and result payload naming from public/persisted certificate surfaces and still does not run Lean, promote mathematical claims, or certify GA.

Task289 exposes that certificate gate through the Pi thin client. `comath.release.piCodexLifecycleUnattendedRealHostTerminalCompletionCertificate`, `/cm:release lifecycle-unattended-real-host-terminal-completion-certificate`, Pi runtime registration, and the read-only interactive planner now consume only the Task286 terminal-certificate-design id/path/hash under host confirmation, translate only the design public alias to the service-canonical request path, strip model confirmation ids, and sanitize public request/result/confirmation/notification surfaces. This consumer may show service-returned Pi/Codex unattended lifecycle certificate flags, but it keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it does not expose executor output, accept caller certificates, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task290 adds the service-owned operator/service transport closure review gate. `recordPiCodexLifecycleOperatorServiceTransportClosureReview()` and `POST /release/pi-codex-lifecycle/operator-service-transport-closure-review` consume the current Task288 terminal completion certificate id/path/hash, re-read the canonical certificate plus the bound Task247 durable-transport prerequisite, Task229 transport continuity, and Task225 maintained transport contract artifacts, reject duplicate/stale/non-canonical/tampered/chain-mismatched material, and persist append-only closure-review evidence plus audit provenance. The review may record that the maintained Node HTTP AgentRun log-session route and Pi `fetch`/`getText` primitive closure were reviewed, but it keeps `durable_transport_provided=false`, `live_transport_open=false`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it does not open durable transport, add a Pi consumer, mutate trusted Pi state, run Lean, promote mathematical claims, or certify GA.

Task291 exposes that closure-review gate through the Pi thin client. `comath.release.piCodexLifecycleOperatorServiceTransportClosureReview`, `/cm:release lifecycle-operator-service-transport-closure-review`, Pi runtime registration, and the read-only interactive planner now consume only the Task288 terminal completion certificate id/path/hash under host confirmation, translate only the terminal-certificate public alias to the service-canonical request path, strip model confirmation ids, and sanitize public request/result/confirmation/notification surfaces. This consumer may show service-returned closure lifecycle flags, but it keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, `durable_transport_provided=false`, and `live_transport_open=false`; it does not expose executor output, accept caller certificates, open durable/live transport, mutate trusted Pi state, run Lean, promote claims, or certify GA.

Task292 adds a service-owned GA operational readiness review gate. `recordGoal3GaOperationalReadinessReview()` and `POST /release/goal3/ga-operational-readiness-review` re-read the current Task290 operator/service transport closure review and Task167 adapter OS-isolation readiness review by canonical path/hash, reject stale, non-canonical, tampered, or promotional material, and persist an append-only release-candidate readiness artifact plus audit event. The review can report `ready_for_ga_release_candidate_review` only when the adapter OS-isolation review is already ready, but it still keeps `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it does not run Lean, certify GA, open durable/live transport, mutate trusted Pi state, or replace the final GA audit.

Task293 exposes that operational readiness gate through the Pi thin client and read-only planner. `comath.release.goal3GaOperationalReadinessReview`, `/cm:release goal3-ga-operational-readiness-review`, Pi runtime registration, and the interactive real-Pi planner now pass only Task290 transport-closure review id/path/hash plus Task167 adapter OS-isolation review id/path/hash under host confirmation, translate only the two service-owned public aliases to canonical service paths, strip model confirmation ids, and sanitize request/result/confirmation/notification surfaces. The bridge may show service-returned release-candidate readiness flags, but it keeps proof authority, claim promotion, GA certification, durable/live transport, and trusted-state mutation false; it does not accept caller proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions.

Task294 adds a service-owned GA certification review precondition gate. `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` re-read the current Task292 operational readiness review by canonical path/hash, run the existing GA acceptance diagnostic as no-authority support evidence, persist the acceptance report and append-only review artifact, and fail closed while release-candidate proof breadth and the final GA certification audit remain incomplete. This gate deliberately returns `blocked_release_candidate_ga_certification_prerequisites` with `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it does not turn operational readiness into GA certification, promote proof claims, run Lean, open durable/live transport, or replace Lean clean replay authority.

Task295 exposes that certification-review precondition gate through the Pi thin client and read-only planner. `comath.release.goal3GaCertificationReview`, `/cm:release goal3-ga-certification-review`, Pi runtime registration, and the interactive real-Pi planner now pass only Task292 operational-readiness review id/path/hash under host confirmation, translate only the service-owned operational-readiness public alias to a canonical service path, strip model confirmation ids, and sanitize request/result/confirmation/notification surfaces. The bridge may show service-returned blocked certification-precondition flags, but it keeps proof authority, claim promotion, GA certification, durable/live transport, and trusted-state mutation false; it does not accept caller acceptance reports, proof-breadth matrices, final GA audits, proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions.

Task296 adds a service-owned final GA audit blocker gate. `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` consume only a current Task294 certification-review id/path/hash, re-read that canonical review, re-read its bound Task292 operational-readiness review and acceptance-support artifact by path/hash, and persist an append-only final-audit artifact. This changes the state from missing final-audit evidence to a replayable blocked audit: `final_ga_audit_available=true`, `final_ga_audit_passed=false`, `blocked_release_candidate_proof_breadth_incomplete`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certificate_available=false`. It does not certify GA, promote proof claims, run Lean, open durable/live transport, or replace Lean clean replay authority.

Task297 exposes that final GA audit blocker through the Pi thin client and read-only planner. `comath.release.goal3FinalGaAudit`, `/cm:release goal3-final-ga-audit`, Pi runtime registration, and the interactive real-Pi planner now pass only Task294 certification-review id/path/hash under host confirmation, translate only the service-owned certification-review public alias to a canonical service path, strip model confirmation ids, and sanitize request/result/confirmation/notification surfaces. The bridge may show service-returned blocked final-audit flags, but it keeps proof authority, claim promotion, GA certification, final-audit pass, GA-certificate, durable/live transport, and trusted-state mutation false; it does not accept caller acceptance reports, proof-breadth matrices, final audit payloads, proof claims, GA certificates, executor commands, Lean replay material, or durable transport sessions.

Task298 adds a service-owned release-candidate proof-breadth review gate. `recordGoal3ReleaseCandidateProofBreadthReview()` and `POST /release/goal3/proof-breadth-review` derive the current 100-task positive manifest and positive-matrix batch inside `comathd`, persist append-only manifest/batch/review artifacts, ignore caller-supplied proof-breadth matrices, and emit a provenance audit event. The review is deliberately blocked with `proof_breadth_complete=false`, `clean_replay_passed_count=0`, `replayable_blocker_count=100`, `proof_authority=none`, `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certification_gate_separate=true`; it does not run Lean, unblock final GA audit, promote mathematical claims, or certify GA.

Task299 removes the PM-001 artificial final-authority packaging dead end. PM-001 can now produce the same canonical task-local final-authority packaging report/blocker as the other positive-matrix tasks, and PM-001-inclusive tranches can be used by future proof-breadth closure work. This does not reuse the historical representative fixture as proof breadth: without PM-001 task-local Lean Authority v3 evidence, PM-001 remains blocked with `proof_authority=none` and `can_promote_claim=false`.

Task300 adds a service-owned proof-breadth closure verifier. `recordGoal3ReleaseCandidateProofBreadthClosure()` and `POST /release/goal3/proof-breadth-closure` derive the 100-task manifest inside `comathd`, read only canonical task-local final-authority packaging reports, and mark proof breadth complete only when every task report is verified with Lean clean replay authority. Missing or blocked reports keep the closure blocked; a complete closure can unblock the final-GA-audit prerequisite but still cannot promote claims or certify GA.

Task301 binds that closure into the service final GA audit. `recordGoal3FinalGaAudit()` and `POST /release/goal3/final-ga-audit` can now consume canonical proof-breadth closure id/path/hash material, keep missing or incomplete closure blocked, and set `final_ga_audit_passed=true` only for a complete 100-task Lean-authority closure. This still does not certify GA, promote claims, run Lean, or create a GA certificate.

Task302 binds passed final audit into GA certification review. `recordGoal3GaCertificationReview()` and `POST /release/goal3/ga-certification-review` can now consume canonical final-audit id/path/hash material and return `ready_for_ga_certificate_gate` only when operational readiness and passed final audit are both current. It still keeps `can_certify_ga=false` and `ga_certificate_available=false`; certificate creation remains a separate gate.

Task303 adds the service-owned GA certificate gate. `recordGoal3GaCertificate()` and `POST /release/goal3/ga-certificate` consume a ready Task302 certification review, re-read the review-bound final audit by hash/size, and persist an append-only GA certificate artifact. The certificate certifies release readiness only; proof-claim promotion still requires ordinary Lean Authority gates.

Task304 hardens agent-adapter OS-isolation production helper readiness. Complete OS-enforcement evidence may satisfy release readiness only when the bound provider-helper collection comes from an `operator_configured_provider_helper` with `production_helper_configured=true`, `bundled_protocol_asset=false`, a current matching `helper_execution_artifact`/helper execution manifest, and any provider-family production-helper profile contract hash-bound as non-authoritative provenance; bundled provider-helper protocol assets remain protocol fixtures, not GA readiness evidence.

Task305 binds that source gate into GA operational readiness. `recordGoal3GaOperationalReadinessReview()` and `POST /release/goal3/ga-operational-readiness-review` now reject ready-looking adapter OS-isolation reviews unless their Task304 production helper source fields are present and self-consistent: `checks.production_helper_source.ok=true`, `helper_profile_source="operator_configured_provider_helper"`, `production_helper_configured=true`, and `bundled_protocol_asset=false`. The operational readiness artifact and audit event copy only this non-authoritative provenance and still cannot promote claims or certify GA.

Task306 adds the service-owned GA certificate source-bound consumption gate. `recordGoal3GaCertificateConsumptionReview()` and `POST /release/goal3/ga-certificate-consumption-review` consume a Task303 GA certificate only after re-reading the certificate, ready certification review, passed final GA audit, complete proof-breadth closure, and Task305 operational readiness artifact by canonical path/hash. The gate rejects certificate chains whose operational readiness lacks `adapter_production_helper_source_bound=true`, `adapter_helper_profile_source="operator_configured_provider_helper"`, `adapter_production_helper_configured=true`, and `adapter_bundled_protocol_asset=false`, then records a publishable workbench release-closure artifact without promoting individual proof claims.

Task307 adds the service-owned source-bound public release package gate. `recordGoal3SourceBoundReleasePackage()` and `POST /release/goal3/source-bound-release-package` consume only a current Task306 certificate-consumption artifact by canonical id/path/hash, reject stale or source-less release-closure material before writing package state, generate sanitized Markdown/HTML/JSON source-review reports, assemble the existing source-review public archive, run the public archive review gate, and persist a non-authoritative release package. The package is diagnostic only: `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `package_is_proof_authority=false`.

Task308 adds the service-owned source-only open-source review artifact gate. `recordGoal3SourceOnlyOpenSourceReviewArtifact()` and `POST /release/goal3/source-only-open-source-review-artifact` consume a current Task307 package by canonical id/path/hash, reject stale package hashes, reject dirty Git worktrees and tracked runtime/build/cache entries, generate a `git archive` tar from `HEAD`, run the public archive review gate over the route and manifest surfaces, and persist a non-authoritative source review artifact. The artifact is for open-source review only: `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, and `source_artifact_is_proof_authority=false`.

Task309 adds the service-owned source artifact presentation review gate. `recordGoal3SourceArtifactPresentationReview()` and `POST /release/goal3/source-artifact-presentation-review` consume a current Task308 source artifact by canonical id/path/hash, re-read both `source-artifact.json` and the bound `source.tar`, reject stale archive bytes before writing presentation material, run the public archive review gate over the route and presentation manifest surfaces, and persist a non-authoritative download descriptor. The descriptor is public diagnostic presentation only: `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `presentation_is_proof_authority=false`.

Task310 adds the service-owned source release public checklist gate. `recordGoal3SourceReleasePublicChecklist()` and `POST /release/goal3/source-release-public-checklist` consume a current Task309 presentation review by canonical id/path/hash, re-read the presentation manifest, its bound public archive review, and `source.tar` bytes before checklist writes, run the public archive review gate over the route and checklist manifest surfaces, and persist a non-authoritative checklist artifact. The checklist makes public source-review readiness explicit while keeping external notarization and OS immutable storage at `not_configured`; it remains diagnostic only with `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `checklist_is_proof_authority=false`.

Task311 adds the service-owned source release external evidence binding gate. `recordGoal3SourceReleaseExternalEvidenceBinding()` and `POST /release/goal3/source-release-external-evidence-binding` consume a current Task310 checklist by canonical id/path/hash, re-read the bound `source.tar` bytes, bind operator-supplied external-notarization and OS-immutable-storage evidence files by path/hash, run the public archive review gate over the route and binding manifest surfaces, and persist a non-authoritative external evidence binding artifact. This only records operator evidence as bound review material: `external_verification_performed=false`, `requires_future_external_verification=true`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, and `binding_is_proof_authority=false`.

Task312 source release evidence chain check-debug hardens the Task310->Task311 release chain. `recordGoal3SourceReleaseExternalEvidenceBinding()` now re-reads the Task310 checklist's own public archive review by canonical id/path, checks its current bytes against the service-owned public-review audit hash/size, rejects missing or tampered review manifests before writing binding material, and records the checklist public archive review hash/current status in the external evidence binding and provenance audit event. This remains diagnostic source-release evidence only; it does not perform live external-provider verification, OS-level immutable storage enforcement, Lean replay, proof promotion, or GA certification.

Task313 external provider verification adds a service-owned source release provider check over the current Task311 binding. `recordGoal3SourceReleaseExternalProviderVerification()` and `POST /release/goal3/source-release-external-provider-verification` re-read the Task311 binding, its Task310 public-review chain, the bound `source.tar`, and the selected operator evidence artifact by canonical path/hash before issuing a single service-owned provider verification request. Provider responses must bind the same binding hash, source archive hash/size, evidence hash, receipt id, provider id, and evidence kind, and any restore/proof/promotion/GA overclaim fails closed with no partial verification artifact. The result is still non-authoritative release provenance only: `external_verification_performed=true`, `external_verification_result="provider_verified"`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `provider_result_is_proof_authority=false`, and `requires_separate_lean_authority=true`.

Task314 provider policy inspection adds a service-owned policy-currentness check over the current Task313 provider verification artifact. `recordGoal3SourceReleaseExternalProviderPolicyInspection()` and `POST /release/goal3/source-release-external-provider-policy-inspection` re-read the Task313 verification by canonical id/path/hash, re-check its public-review provenance, source archive bytes, selected operator evidence, provider response hashes, and receipt id, then issue a single sanitized request to the configured provider policy endpoint. Accepted policy responses must bind the same verification hash, provider id, evidence kind, binding hash, source archive hash/size, evidence hash, receipt id, and Task313 provider response hash; restore/proof/promotion/GA/OS-immutability overclaims fail closed with no partial inspection artifact. The result is still non-authoritative release provenance only: `provider_policy_inspection_performed=true`, `provider_policy_result="provider_policy_current"`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `policy_result_is_proof_authority=false`, `os_immutability_enforced=false`, `requires_separate_lean_authority=true`, and `requires_separate_os_immutability_attestation=true`.

Task315 OS immutability attestation adds a service-owned external attestation binding over the current Task314 provider policy inspection artifact. `recordGoal3SourceReleaseOsImmutabilityAttestation()` and `POST /release/goal3/source-release-os-immutability-attestation` consume only `os_immutable_storage` policy inspections by canonical id/path/hash, re-read the Task314 public-review provenance, Task313 verification artifact, selected operator evidence, and bound `source.tar`, then issue one sanitized request to the configured OS attestation endpoint. Accepted attestation responses must bind the same Task314/Task313/source/evidence/receipt/provider-policy hashes plus daemon and policy document hashes; unknown response fields, header leaks, restore/proof/promotion/GA/enforcement overclaims, and non-OS evidence kinds fail closed with no partial artifact. The result is release provenance only: `os_immutability_attestation_performed=true`, `os_immutability_result="provider_attested"`, `provider_os_immutability_attestation_bound=true`, `co_math_os_immutability_enforced=false`, `proof_authority="none"`, `can_restore=false`, `can_promote_claim=false`, `can_certify_ga=false`, `attestation_is_proof_authority=false`, `storage_is_proof_authority=false`, and `requires_separate_lean_authority=true`.

Task316 exposes that Task315 source-release OS attestation gate through the Pi extension as a host-confirmed thin client. `comath.release.goal3SourceReleaseOsImmutabilityAttestation` and `/cm:release goal3-source-release-os-immutability-attestation` forward only project/actor material, optional `attestation_id`, Task314 `policy_inspection_id/path/sha256`, `provider_id`, `os_attestation_url`, and optional `provider_terms_url` to `POST /release/goal3/source-release-os-immutability-attestation`; public `service-owned-goal3-source-release-external-provider-policy-inspection/<id>/policy-inspection.json` aliases are translated only when the id matches. The Pi surface strips model-supplied confirmation ids and caller proof/Lean/GA/restore/attestation material, keeps the interactive real-Pi planner read-only, and sanitizes proof, restore, host-path, secret, GA, and CoMath-owned OS-enforcement overclaims. It does not write `.comath` directly, run Lean, create a restore source, certify GA, or make OS attestation proof authority.

Task317 adds a durable transport release-signoff prerequisite gate. `recordGoal3DurableTransportReleaseSignoffPrerequisite()` and `POST /release/goal3/durable-transport-release-signoff-prerequisite` consume a current Task292 operational-readiness artifact, re-read the bound Task290 transport-closure review, and then re-read the nested terminal certificate, durable-transport prerequisite contract, transport continuity checkpoint, and maintained transport contract by hash/size before writing any prerequisite artifact. The result is deliberately blocked with `durable_transport_signoff_status="blocked_durable_long_lived_transport_not_provided"` and `blocker_reasons=["durable_long_lived_transport_not_provided"]` while preserving `maintained_transport_primitive_bound=true`, `durable_transport_provided=false`, `live_transport_open=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`; it is freshness/signoff evidence, not a WebSocket/SSE stack, Lean authority, proof promotion, or GA certification.

Task318 adds a final release-signoff decision gate. `recordGoal3FinalReleaseSignoffDecision()` and `POST /release/goal3/final-release-signoff-decision` consume the current Task317 durable-transport prerequisite and the current Task315 source-release OS immutability attestation by canonical id/path/hash, re-read the source archive, operator evidence, and policy-inspection material by hash/size, and persist a final signoff decision only after those chains are byte-current. The current decision is intentionally blocked with `durable_long_lived_transport_not_provided` and `final_ga_certificate_not_bound`, keeps `ga_release_signoff_ready=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`, and cannot turn OS attestation, source-release evidence, or transport prerequisites into GA certification.

Task319 adds a durable transport release-signoff verification gate. `recordGoal3DurableTransportReleaseSignoffVerification()` and `POST /release/goal3/durable-transport-release-signoff-verification` consume a current Task317 prerequisite by canonical id/path/hash, re-read the prerequisite's operational-readiness, transport-closure, terminal-certificate, durable-contract, continuity, and maintained-contract artifacts by hash/size, and then either persist a blocked verification when no external durable transport evidence is bound or verify a service-owned `goal3_external_durable_transport_evidence` artifact. Accepted evidence must describe a maintained external operator transport primitive, bind the same `GET /agent/run/<run_id>/log-session` service route and `pi_fetch_get_text` client primitive, carry daemon/session-policy hashes, and explicitly keep CoMath-owned transport stacks false. The verification can record durable transport provenance, but it keeps `ga_release_signoff_ready=false`, `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`; it does not build a WebSocket/SSE stack, run Lean, promote claims, or certify GA.

Task320 adds Task319 final release-signoff consumption by updating the Task318 decision chain to consume durable transport verification together with the existing source-release and GA-certificate consumption path. `recordGoal3FinalReleaseSignoffDecision()` and `POST /release/goal3/final-release-signoff-decision` now accept optional Task319 verification id/path/hash, require it to match the same Task317 prerequisite, re-check the external transport evidence freshness, and clear only the durable transport blocker when that verification is current and verified. Final signoff readiness can become `ga_release_signoff_ready=true` only when both verified Task319 transport and valid Task306 GA certificate consumption are bound; the signoff artifact itself keeps `proof_authority="none"`, `can_promote_claim=false`, and `can_certify_ga=false`.

Task321 final release-chain check-debug revalidates the Task317-320 final release chain, the Task306 GA certificate-consumption dependency, the Task315 source-release OS attestation boundary, capability exposure, smoke discovery, and public release-hardening wording. It is regression coverage and documentation synchronization only: it does not add a new release route, does not make transport evidence proof authority, does not promote claims, and does not certify GA.

Task322 final release signoff certification-boundary review adds a service-owned review gate over the current Task320 ready signoff. `recordGoal3FinalReleaseSignoffCertificationBoundaryReview()` and `POST /release/goal3/final-release-signoff-certification-boundary-review` consume the final signoff by canonical id/path/hash, re-read the bound Task306 GA certificate consumption, Task319 durable-transport verification, external durable-transport evidence freshness, Task315 source-release OS attestation, source archive, and operator evidence by hash/size, and persist append-only review provenance only when all links remain current. The review records that the consumed GA certificate can certify GA, but the boundary review itself keeps `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `boundary_review_is_certificate=false`, and `ga_certificate_issued=false`; it does not issue a new certificate, run Lean, promote claims, or make release-signoff provenance proof authority.

Task323 final release-candidate closure audit adds a service-owned post-Task322 consumer. `recordGoal3FinalReleaseCandidateClosureAudit()` and `POST /release/goal3/final-release-candidate-closure-audit` consume the Task322 certification-boundary review by canonical id/path/hash, re-read its Task320 final signoff, Task306 GA certificate consumption, Task319 durable-transport verification plus Task317 nested transport chain, external durable-transport evidence freshness, Task315 policy-inspection/source-release OS attestation chain, source archive, and operator evidence, and persist append-only closure audit provenance only when every bound link remains current. The consumed GA certificate remains the only certifying artifact; the Task323 closure audit itself keeps `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `release_candidate_closure_audit_is_certificate=false`, and `ga_certificate_issued=false`.

Task324 final release-candidate check-debug revalidates the Task322-323 closure chain plus its Task320/306/319/317/315 dependencies, capability exposure, phase0 smoke discovery, route wiring, reduced-chain regression coverage, and public release-hardening wording. It is regression coverage and documentation synchronization only: it does not add a new release route, run Lean, expose a Pi consumer, promote proof claims, issue a GA certificate, or make closure provenance proof authority.

Task325 exposes the Task323 final release-candidate closure audit through the Pi extension as a host-confirmed thin client. `comath.release.goal3FinalReleaseCandidateClosureAudit` and `/cm:release goal3-final-release-candidate-closure-audit` forward only project/actor material, optional `final_release_candidate_closure_audit_id`, Task322 `certification_boundary_review_id/path/sha256`, and `requested_audit_mode` to `POST /release/goal3/final-release-candidate-closure-audit`; public `service-owned-goal3-final-release-signoff-certification-boundary-review/<id>/review.json` aliases are translated only when the id matches. The Pi surface strips model-supplied confirmation ids and caller proof/Lean/GA/proof-breadth/closure-certificate/durable-transport material, keeps the interactive real-Pi planner read-only, and sanitizes proof, host-path, secret, transport, GA, and closure-certificate overclaims. It does not write `.comath` directly, run Lean, issue a certificate, certify GA, or make Task323 closure provenance proof authority.

Task326 adds a service-owned release-candidate proof-breadth execution bridge. `recordGoal3ReleaseCandidateProofBreadthExecutionBridge()` and `POST /release/goal3/proof-breadth-execution-bridge` read the current 100-task positive manifest and canonical per-task final-authority packaging reports, then persist an append-only bridge artifact selecting the next bounded tranche of missing or blocked PM tasks and their expected packaging report paths. The bridge is execution planning only: it ignores caller proof-breadth material, never runs Lean, never synthesizes proofs, keeps `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`, and leaves completion authority to Task300 closure plus Task301 final-audit binding.

Task327 proof-breadth bridge check-debug revalidates the Task326 bridge plus Task298/300/301 proof-breadth chain, route wiring, phase0/default-test discovery, and public release-hardening wording. It is regression coverage only: it adds no route, Pi consumer, Lean replay, proof synthesis, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.

Task328 exposes the Task298 proof-breadth review through the Pi extension as a host-confirmed thin client. `comath.release.goal3ProofBreadthReview` and `/cm:release goal3-proof-breadth-review` forward only project/actor material, optional `proof_breadth_review_id`, and `requested_review_mode` to `POST /release/goal3/proof-breadth-review`; the Pi surface strips model confirmation ids and caller proof-breadth matrices, proof claims, Lean manifests, GA certificates, executor commands, and durable transport sessions. Public output is sanitized back to blocked-review semantics with `proof_authority="none"`, `can_promote_claim=false`, `can_certify_ga=false`, `proof_breadth_complete=false`, and `final_ga_audit_unblocked=false`; Task300 remains the closure verifier and Task301 remains final-audit binding.

Task329 exposes the Task300 proof-breadth closure verifier through the Pi extension as a host-confirmed thin client. `comath.release.goal3ProofBreadthClosure` and `/cm:release goal3-proof-breadth-closure` forward only project/actor material, `proof_breadth_closure_id`, and `requested_closure_mode` to `POST /release/goal3/proof-breadth-closure`; the Pi surface strips model confirmation ids, caller proof-breadth matrices, closure JSON, Task326 bridge output, proof claims, Lean manifests, GA certificates, executor commands, durable transport sessions, host paths, and secrets. Public output may show service-owned Task300 closure status and counts, including a complete proof-breadth prerequisite when Task300 returns a self-consistent complete closure, but it still redacts proof-authority wording and forces `can_promote_claim=false`, `can_certify_ga=false`, and `ga_certificate_available=false`. Task301 remains the only final-audit binding for that closure.

Task330 proof-breadth Pi closure check-debug revalidates the Task328/329 Pi proof-breadth consumers together with the Task300/301/326 service proof-breadth chain. The focused suite keeps capability exposure, phase0 discovery, Pi package discovery, runtime subcommands, interactive planner ordering, request-whitelist/public-sanitizer boundaries, Task326 non-authoritative execution planning, Task300 service-owned closure verification, Task301 final-audit closure binding, and release-hardening documentation synchronized. It is regression coverage only: it adds no route, no Pi tool, no Lean replay, no proof synthesis, no proof-breadth execution follow-through, no final-GA-audit input consumer, no claim promotion, and no GA certification.

Task331 extends the existing Task297 final GA audit Pi consumer so it can drive the Task301 closure-aware service route. `comath.release.goal3FinalGaAudit`, `/cm:release goal3-final-ga-audit`, the runtime registration, package-test discovery, phase0 smoke discovery, and the read-only interactive real-Pi planner now accept an optional all-or-none Task300 proof-breadth closure `id/path/sha256` triple in addition to the required Task294 certification-review `id/path/sha256`. The Pi layer translates only matching `service-owned-goal3-proof-breadth-closure/<id>/closure.json` aliases, rejects partial or mismatched closure bindings before POST, strips caller proof-breadth matrices, closure JSON, Task326 bridge output, final-audit JSON, proof claims, Lean manifests, GA certificates, executor material, durable sessions, host paths, secrets, and model confirmation ids, and forwards only the canonical closure triple to `POST /release/goal3/final-ga-audit`. Public output may surface a service-owned closure-bound final-audit pass as non-certifying status, but proof authority wording is redacted and claim promotion, GA certification, certificate issuance, direct Pi mutation, and durable/live transport flags remain false.

Task332 adds a service-owned proof-breadth execution follow-through witness over a Task326 bridge tranche. `recordGoal3ReleaseCandidateProofBreadthExecutionFollowThrough()` and `POST /release/goal3/proof-breadth-execution-follow-through` consume only a canonical Task326 bridge `id/path/sha256`, re-read the bridge bytes, then inspect only the selected PM tasks' canonical final-authority packaging report paths. The follow-through artifact records selected verified/blocked/missing status, selected packaging report artifact hashes, append-only provenance, and whether the selected tranche is ready for a Task300 closure recheck. It does not run Lean, synthesize proofs, write missing packaging reports, expose a Pi consumer, close global proof breadth, unblock final GA audit, promote claims, certify GA, or treat caller proof material as evidence.

Task333 proof-breadth follow-through check-debug revalidates Task332 together with the Task326/300/301 proof-breadth chain, status capability exposure, phase0 smoke discovery, route wiring, no-Pi-public-surface boundary, and release-hardening documentation. It is regression coverage only: it adds no route, Pi tool, Lean replay, proof synthesis, missing packaging report writer, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.

Task334 adds a bounded service-owned task-local Lean Authority v3 packaging follow-through for the Task326 selected proof-breadth tranche. `recordGoal3ReleaseCandidateProofBreadthTaskLocalPackagingFollowThrough()` and `POST /release/goal3/task-local-lean-authority-packaging-follow-through` consume only a current Task326 bridge `id/path/sha256`, reject evidence for unselected PM tasks, call the existing project-local v3 final-authority packaging re-verifier for selected PM tasks, and persist append-only non-certifying provenance. It may write canonical selected-task packaging reports as verified or blocked according to existing local evidence, but it does not run Lean, trust caller success metadata, expose a Pi consumer, close global proof breadth, unblock final GA audit, promote claims, or certify GA.

Task335 adds a Task334-bound selected-tranche packaging results follow-up. `recordGoal3ReleaseCandidateProofBreadthSelectedTranchePackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-packaging-results-follow-up` consume a current Task326 bridge `id/path/sha256` plus a current Task334 follow-through `id/path/sha256`, re-read both artifacts by hash, require the Task334 selected task ids and report artifacts to match the bridge-selected tranche, re-hash the current canonical selected PM packaging reports, and then reuse the Task332 selected-tranche recheck witness. The output records currentness for a later Task300 closure recheck only; it does not run Lean, synthesize proofs, trust Task334 summary success, expose a Pi consumer, write Task300/301 artifacts, close global proof breadth, promote claims, or certify GA.

Task336 selected-tranche packaging results check-debug revalidates Task335 together with the Task334/332/326/300/301 proof-breadth chain, status capability exposure, phase0 smoke discovery, service route wiring, no-Pi-public-surface boundary, and release-hardening documentation. It is regression coverage only: it adds no route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.

Task337 adds a Task335-bound selected-tranche closure recheck. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheClosureRecheck()` and `POST /release/goal3/selected-tranche-closure-recheck` consume only a current Task335 selected-tranche packaging results follow-up `id/path/sha256`, re-read and hash-bind that artifact, re-hash the selected canonical PM packaging reports again, then invoke the existing Task300 proof-breadth closure verifier as the only aggregate closure authority. The Task337 artifact records that the selected tranche was handed to Task300 for a global recheck, but it remains non-authoritative: it does not run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, replace Task300, write Task301 final-audit artifacts, close proof breadth by itself, unblock final GA audit by itself, promote claims, or certify GA.

Task338 adds a Task337-bound selected-tranche next execution bridge. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextExecutionBridge()` and `POST /release/goal3/selected-tranche-next-execution-bridge` consume only a current Task337 closure recheck `id/path/sha256`, re-read and hash-bind the Task337 artifact, re-read the Task337-bound Task300 closure artifact, re-hash the completed selected PM packaging reports, and then delegate to the existing Task326 proof-breadth execution bridge to select the next bounded missing/blocked PM tranche. The Task338 artifact is proof-breadth loop plumbing only: it does not run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task301 final-audit artifacts, promote claims, issue certificates, certify GA, or treat a selected tranche as global closure.

Task339 selected-tranche next bridge check-debug revalidates Task338 together with the Task337/326/300/301/334/335/336 proof-breadth chain, status capability exposure, phase0 smoke discovery, service route wiring, no-Pi-public-surface boundary, and release-hardening documentation. It is regression coverage only: it adds no route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, or GA certification.

Task340 adds a Task338-bound selected-tranche next packaging follow-through. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextPackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-packaging-follow-through` consume only a current Task338 selected-tranche next execution bridge `id/path/sha256`, re-read and hash-bind that artifact, re-hash the delegated Task326 bridge it points to, reject selected-tranche drift or missing next bridges, and then delegate selected PM evidence to the existing Task334 task-local packaging follow-through. The Task340 artifact binds Task338, the delegated Task326 bridge, and the Task334 artifact without embedding proof reports, and it remains non-authoritative: it does not run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task300 closure or Task301 final-audit artifacts, promote claims, issue certificates, certify GA, or treat the selected tranche as global proof-breadth closure.

Task341 adds a Task340-bound selected-tranche next packaging results follow-up. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextPackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-packaging-results-follow-up` consume only a current Task340 selected-tranche next packaging follow-through `id/path/sha256`, re-read and hash-bind that artifact, and reuse the existing Task335 selected-tranche packaging results follow-up over Task340's delegated Task326 bridge and Task334 follow-through artifact. The Task341 artifact records current selected-tranche packaging-result freshness for a later closure recheck only; it does not run Lean, write packaging reports, call Task300/301, expose a Pi consumer, promote claims, issue certificates, certify GA, or treat selected-tranche currentness as global proof-breadth closure.

Task342 selected-tranche next packaging currentness check-debug revalidates Task340 and Task341 together with the Task338/335/334/326/300/301 proof-breadth chain, status capability exposure, phase0 smoke discovery, service route wiring, no-Pi-public-surface boundary, and release-hardening documentation. It is regression coverage only: it adds no route, Pi tool, Lean replay, proof synthesis, packaging writer, proof-breadth closure, final-GA-audit unblock, claim promotion, certificate issuance, or GA certification.

Task343 adds a Task341-bound selected-tranche next closure recheck. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-recheck` consume only a current Task341 selected-tranche next packaging results follow-up `id/path/sha256`, re-read and hash-bind that artifact, re-hash the selected canonical PM packaging reports, then bind the selected tranche into the existing selected-tranche closure recheck / Task300 aggregate closure semantics. The Task343 artifact records non-certifying provenance only: it does not run Lean, write packaging reports, expose a Pi consumer, write Task301 final-audit artifacts, promote claims, issue certificates, certify GA, or treat selected-tranche currentness as global proof-breadth closure.

Task344 adds a Task343-bound selected-tranche next closure execution bridge. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-execution-bridge` consume only a current Task343 selected-tranche next closure recheck `id/path/sha256`, re-read and hash-bind that artifact, preserve stale Task343-bound Task300 closure and selected PM packaging-report checks through the existing Task338 bridge semantics, and then delegate to Task338/Task326 for the next bounded PM tranche. The Task344 artifact is non-certifying loop plumbing only: it does not run Lean, synthesize proofs, write packaging/currentness/closure/final-audit artifacts, expose a Pi consumer, close global proof breadth, unblock final GA audit, promote claims, issue certificates, or certify GA.

Task345 selected-tranche next closure bridge check-debug revalidates Task343 and Task344 together with the Task338/326/300/301 proof-breadth chain, status capability exposure, phase0 smoke discovery, route/export wiring, default discovery, no-Pi-public-surface boundary, and release-hardening documentation. It is regression coverage only: it adds no route, Pi tool, Lean replay, proof synthesis, packaging/currentness/closure/final-audit writer, proof-breadth closure, final-GA-audit unblock, claim promotion, certificate issuance, or GA certification.

Task346 adds a Task344-bound selected-tranche next closure packaging follow-through. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingFollowThrough()` and `POST /release/goal3/selected-tranche-next-closure-packaging-follow-through` consume only a current Task344 selected-tranche next closure execution bridge `id/path/sha256`, re-read and hash-bind that artifact, re-hash its delegated Task338 selected-tranche next execution bridge, require the delegated Task326 bridge reference to remain current through Task338, reject selected-tranche drift or missing next bridges, and then delegate selected PM evidence to the existing Task340/Task334 packaging path. The Task346 artifact binds Task344, delegated Task338/Task326, Task340, and Task334 by references/hashes only; it does not run Lean, synthesize proofs, trust caller success metadata, expose a Pi consumer, write Task300 closure or Task301 final-audit artifacts, promote claims, issue certificates, certify GA, or treat the selected tranche as global proof-breadth closure.

Task347 adds a Task346-bound selected-tranche next closure packaging results follow-up. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsFollowUp()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-follow-up` consume only a current Task346 selected-tranche next closure packaging follow-through `id/path/sha256`, re-read and hash-bind that artifact, canonical-validate the embedded Task344/Task338 provenance, reuse Task341 currentness semantics over the Task346-bound Task340 packaging follow-through and Task334 packaging writer output, and preserve the Task335 currentness witness transitively through Task341. The Task347 artifact binds Task346, Task344, Task338, Task340, and Task341 by references/hashes only; Task335 remains the transitive currentness witness inside the delegated Task341 artifact. Task347 does not run Lean, expose a Pi consumer/tool/surface, write Task300 closure, Task301 final-audit, or GA-certificate artifacts, promote claims, issue certificates, certify GA, or treat selected-tranche packaging results as global proof-breadth closure.

Task348 adds selected-tranche next closure packaging/currentness check-debug coverage. `goal3-task348-selected-tranche-next-closure-packaging-currentness-check-debug.test.mjs` keeps Task346/347 route/export/status/smoke/default discovery, Task346 Task344/338 binding, Task347 Task346/341 binding, Task341/335 currentness reuse, Task300 aggregate closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no-Pi-public-surface boundaries, and release-hardening wording synchronized. It adds no route, Pi consumer, Lean replay, proof synthesis, packaging/currentness/closure/final-audit/certificate writer, final-audit unblock, claim promotion, certificate issuance, or GA certification.

Task349 adds a Task347-bound selected-tranche next closure packaging results closure recheck. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureRecheck()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-recheck` consume only a current Task347 selected-tranche next closure packaging results follow-up `id/path/sha256`, re-read and hash-bind that artifact, revalidate its Task346/344/338/340/341/335/334 currentness references, then delegate selected-tranche closure recheck to existing Task343/Task300 semantics. The Task349 wrapper binds Task347, delegated Task343, and Task300 artifacts by references/hashes only; it does not expose a Pi consumer/tool/surface, run Lean, synthesize proofs, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, certify GA, or treat selected-tranche recheck evidence as global proof-breadth completion.

Task350 adds a Task349-bound selected-tranche next closure packaging results closure execution bridge. `recordGoal3ReleaseCandidateProofBreadthSelectedTrancheNextClosurePackagingResultsClosureExecutionBridge()` and `POST /release/goal3/selected-tranche-next-closure-packaging-results-closure-execution-bridge` consume only a current Task349 selected-tranche next closure packaging results closure recheck `id/path/sha256`, re-read and hash-bind that artifact, then delegate next selected-tranche closure execution to existing Task344/338 bridge semantics. The Task350 wrapper records append-only non-certifying provenance and binds Task349 plus delegated bridge artifacts by references/hashes only; it does not expose a Pi consumer/tool/surface, run Lean, synthesize proofs, write Task301 final-audit or Task303 GA-certificate artifacts, promote claims, issue certificates, certify GA, or treat selected-tranche execution evidence as global proof-breadth completion.

Task351 adds selected-tranche next closure results-to-execution check-debug coverage. `goal3-task351-selected-tranche-next-closure-results-to-execution-check-debug.test.mjs` keeps Task349/350 route/export/status/smoke/default discovery, Task349 Task347-to-Task343/300 closure-recheck binding, Task350 Task349-to-Task344/338 closure-execution binding, Task344/338/326 execution bridge boundaries, Task300 aggregate proof-breadth closure authority, Task301 final-audit binding, Task303 GA-certificate isolation, no-Pi-public-surface boundaries, and release-hardening wording synchronized. It adds no route, Pi consumer, Lean replay, proof synthesis, packaging/currentness/closure/final-audit/certificate writer, final-audit unblock, claim promotion, certificate issuance, or GA certification.

Task255 adds service-owned Pi goal-mode intake manifests at `/campaign/start`. When Pi or a service caller supplies a research goal with paper paths, attachments, or workspace references, `comathd` now writes `.comath/campaign/<campaign_id>/goal_mode_intake_manifest.json`, records it as an `initialized` stage artifact, hash-binds it into the goal export, normalizes project-confined refs, hashes existing files, records directories without recursive proof claims, and marks every external input as hint/evidence/refutation source only. This closes a product intake gap without making papers, TeX/PDF/Markdown files, workspace refs, or Pi UI state proof authority.

Task256 connects that intake provenance to the service-owned `knowledge_pack` stage. A running goal-mode campaign now writes `.comath/campaign/<campaign_id>/goal_mode_research_plan.json`, binds the intake manifest path/hash, plans local PDF/TeX/Markdown/workspace ingestion through the external wheel registry, plans literature retrieval and Lean theorem-search adapter tasks, and seeds the existing lemma DAG / `Skeleton.lean` stage. The plan is executable workflow metadata only: adapter tasks are planned but not executed, network work is not performed, document contents are not proof authority, and no claim can be promoted without the normal Lean Authority gates.

Task257 turns that plan into a service-owned adapter execution manifest during `knowledge_pack`. CoMath now writes `.comath/campaign/<campaign_id>/goal_mode_adapter_execution_manifest.json`, binds the research-plan hash into `knowledge_pack.json`, preserves the intake manifest path/hash binding, emits per-planned-task run envelopes, re-checks local input file hashes, records directory refs without recursive claims, and blocks network/theorem-search providers until a later maintained live adapter run. The manifest is still not retrieval evidence, document extraction evidence, theorem-search evidence, Lean replay, proof authority, claim promotion, or GA certification.

Task258 turns those local-ingestion run envelopes into service-owned local draft evidence during `knowledge_pack`. CoMath now writes `.comath/campaign/<campaign_id>/goal_mode_local_ingestion_evidence.json`, binds the research-plan, adapter-execution, and intake-manifest hashes into the evidence chain, extracts prompt-injection-scanned line anchors for local Markdown and TeX theorem or lemma drafts, blocks injected local text without promoting extracted claims, and records replayable blockers for PDF parser work and external Lean repository inspection until maintained adapters exist. These extracted claims are formalization candidates only; they are not literature evidence, theorem-search hits, Lean replay, proof authority, claim promotion, or GA certification.

Task259 connects those prompt-injection-scanned local draft claims to the `skeleton_gate` without changing proof authority. CoMath now writes `.comath/campaign/<campaign_id>/proof/formalization_hints.json`, binds `knowledge_pack.json` plus `goal_mode_local_ingestion_evidence.json` by path/hash, converts only allowed extracted claim kinds into skeleton-planning hints, and records statement-drift guards that forbid changing the locked statement without FormalSpecLock approval and StatementDiffGate. The hints may inform blueprint/skeleton planning only; they are not Lean replay evidence, proof authority, claim promotion input, or GA certification.

Task260 turns those hints into a service-owned skeleton blueprint. During `skeleton_gate`, CoMath now writes `.comath/campaign/<campaign_id>/proof/blueprint.json`, binds `formalization_hints.json` by path/hash, attaches the blueprint to `plan.json`, and carries the same blueprint path/hash into `lemma_dag.json`, `line_map.json`, and `skeleton_report.md`. Blueprint steps may seed planning metadata only: they cannot create new proof obligations without FormalSpecLock/StatementDiffGate, cannot change the locked statement, cannot satisfy Lean replay, and cannot promote or certify claims.

Task261 carries that blueprint into live candidate orchestration. When `line_map_gate` prepares `.comath/campaign/<campaign_id>/candidate_generation_request.json`, it now binds `proof/blueprint.json` and `proof/formalization_hints.json` by path/hash, records the blueprint step ids, and requires the same binding before `candidate_generation` can spawn the eight `lemma_sprint` task cards. The generated `candidate_generation.json` and every task card keep the blueprint as non-authoritative planning context only; tampered blueprint material blocks the stage before candidate artifacts are created, and no candidate can be promoted without Lean Authority replay gates.

Task262 materializes blueprint-bound Lean candidate attempts inside those eight workspaces. During `candidate_generation`, CoMath now writes each candidate's `lean_candidate_attempt_plan.json` and `LeanCandidate.lean`, binding the source `proof/Skeleton.lean`, the current skeleton blueprint, blueprint step ids, and the locked statement boundary. These files are draft candidate material with `sorry` placeholders and `proof_authority=none`; they are designed for the later LeanRunner/repair loop and cannot promote claims, satisfy Lean replay, or certify GA by themselves.

Task263 adds the first service-owned verification surface over those draft attempts. During `candidate_verification`, CoMath now writes `.comath/campaign/<campaign_id>/lean_candidate_attempt_check_report.json`, re-reads every candidate manifest, `lean_candidate_attempt_plan.json`, and `LeanCandidate.lean`, verifies source skeleton and blueprint hashes, checks locked-statement boundary bindings, detects `sorry` placeholders, and routes each candidate to `repair_required`, `blocked`, or `ready_for_lean_runner`. This is preflight/repair evidence only: it performs no LeanRunner invocation while `sorry` remains, emits no LeanRunManifest paths, and cannot promote claims or certify GA.

Task264 connects that preflight surface to the product repair loop. When explicit Pi goal-mode campaigns with external refs reach `candidate_arbitration` with `sorry`-bearing `repair_required` attempts, CoMath now routes the campaign into `repair` instead of terminal-blocking and writes `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_batch.json` plus one repair task per candidate workspace. Those tasks are A0/A5/A8 repair work orders only: they bind the check report and candidate artifacts, forbid statement drift and proof-claim outputs, keep LeanRunner disabled until repaired attempts contain no `sorry`, and remain unable to promote claims or certify GA.

Task265 makes that `repair` stage executable and resumable. A repair tick consumes the service-owned repair batch, snapshots each original `LeanCandidate.lean`, writes a repaired draft with no `sorry` token and an explicit `comath_repair_placeholder`, records `.comath/campaign/<campaign_id>/lean_candidate_attempt_repair_execution.json` plus per-candidate execution manifests, and returns the campaign to `candidate_verification`. This is still candidate material only: no LeanRunner is invoked, no LeanRunManifest is emitted, placeholders are not proof, and the next authority step is service-owned Lean checking.

Task266 connects re-ingested ready attempts to the service-owned LeanRunner. `candidate_verification` now records a ready-for-LeanRunner preflight before running Lean, then a later tick writes append-only LeanRunManifest evidence for each ready candidate attempt. If Lean rejects every repaired attempt, CoMath writes `lean_candidate_attempt_leanrunner_execution.json`, updates candidate manifests as failed, and terminates with a replayable blocker instead of promoting anything. Placeholder-bearing drafts remain non-authoritative even if a runner shim reports success.

Task267 closes the first rejected-Lean feedback loop. A terminal all-rejected LeanRunner blocker in explicit Pi goal-mode can be ticked again to write `lean_candidate_attempt_repair_feedback_batch.json`, bind each failed LeanRunManifest stdout/stderr hash as non-authoritative repair evidence, materialize a second repair batch, and resume `current_stage="repair"` / `status="repairing"`. The follow-up repair tick consumes that feedback, annotates candidate drafts with `comath_repair_feedback`, and returns to `candidate_verification` without invoking LeanRunner, emitting proof claims, or changing the locked statement.

Task268 binds Pi goal-mode repair context from the maintained external wheel registry into that feedback loop. The terminal all-rejected resume now also writes `lean_candidate_attempt_repair_hint_bundle.json`, binding the goal-mode research plan, adapter execution manifest, local ingestion evidence, and repair feedback batch by path/hash. The bundle contributes theorem-search, retrieval, computation, proof-search-backend, and external-Lean-repo descriptors as non-authoritative repair hints; repair tasks and repair execution carry `source_repair_hint_bundle`, add `comath_repair_hints` markers, fail closed on stale bundle hashes, and still emit no LeanRunManifest, FinalReplayManifest, proof claim, promotion, network execution, or GA authority.

Task269 turns those bound descriptors into a service-owned repair hint execution artifact without changing proof authority. The terminal all-rejected resume now writes `lean_candidate_attempt_repair_hint_execution.json`, records stubbed theorem-search, retrieval, computation, proof-search-backend, and external-Lean-repo result summaries with request/result hashes, provider terms, capability metadata, and promotion vetoes, then threads `source_repair_hint_execution` and `source_repair_hint_results` through repair batch, tasks, and repair execution. Repair re-hashes this artifact before overwriting candidate drafts, adds `comath_repair_hint_execution` markers, performs no live provider/network call, emits no LeanRunManifest/FinalReplayManifest, and cannot promote claims or certify GA.

Task270 makes the first Pi goal-mode repair candidate placeholder-free on a bounded locked theorem path. Inline Lean theorem goals such as `theorem goal3_task270 : True := by sorry` are now parsed into the FormalSpecLock-derived target instead of comment-only skeletons, static candidate preflight ignores comment-only `sorry`, blocks candidates without theorem/lemma declarations, rejects repair placeholders and Lean holes before LeanRunner readiness, and the repair stage can materialize `True` targets as `by trivial` rather than `?comath_repair_placeholder`. A service-owned LeanRunner-passing placeholder-free candidate may now advance to ordinary arbitration/red-team flow, but this still emits no FinalReplayManifest, performs no claim promotion, and cannot certify GA without downstream clean replay gates.

Task271 carries that bounded Pi goal-mode success path through the existing final authority machinery. A LeanRunner-passing placeholder-free candidate now materializes an isolated `final_replay_project` under the selected candidate workspace, with `MathResearch/Target.lean`, `Audit/LeanCandidateAudit.lean`, campaign-bound FormalSpecLock/AssumptionLedger material, Lake files, and a replay descriptor that feeds integration, final static audit, final global clean replay, FinalReplayManifest v3 packaging, ordinary promotion, and `completed_formal_proof`. This closes the product terminal chain for the bounded `True` smoke target only; it does not broaden theorem synthesis, live provider execution, mathlib breadth, or GA certification.

Task272 broadens that terminal product chain from `True` to the first bounded definitional-equality repair. When the locked Pi goal-mode theorem/lemma proposition is syntactically reflexive, such as `theorem goal3_task272 : 1 = 1 := by sorry`, the repair stage can materialize a placeholder-free `by rfl` candidate, record `locked_reflexive_equality_rfl`, and carry the candidate through LeanRunner, arbitration, final replay project materialization, clean replay, FinalReplayManifest v3 packaging, ordinary promotion, and export-ready `completed_formal_proof`. This is still a conservative candidate synthesis slice: non-reflexive or ambiguous goals remain repair/blocker work, and final proof authority still comes only from service-owned Lean clean replay.

Task273 adds the first opt-in live retrieval execution path to the repair-hint loop without changing proof authority. When `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` includes `retrieval:jina_search`, and `COMATH_JINA_SEARCH_BASE_URL` is configured, the terminal all-rejected resume may call a Jina Search-compatible endpoint through service-owned HTTP, pass `COMATH_JINA_API_KEY` only as an in-memory bearer header, and record a live `retrieval:jina_search` result summary in `lean_candidate_attempt_repair_hint_execution.json`. The artifact stores sanitized request/response hashes, provider status, content hash, prompt-injection scan, and no-authority vetoes; it never stores the bearer token or host root, cannot promote claims, emits no LeanRunManifest/FinalReplayManifest, and default offline runs remain stubbed.

Task274 lets those opt-in live retrieval results repair a rejected placeholder-free Lean candidate without upgrading retrieval into proof authority. A passing prompt-injection scan may expose bounded fenced Lean suggestions in `lean_candidate_attempt_repair_hint_execution.json`; repair execution accepts one only when it is a theorem/lemma with the same declaration name and normalized statement as the locked candidate, contains no `sorry`, holes, `axiom`, `unsafe`, `opaque`, `admit`, `run_cmd`, `elab`, `#eval`, or `set_option`, and keeps all no-authority flags false. The repaired candidate then uses a Lean-file-hash-specific LeanRunner run id so the second attempt writes a new append-only LeanRunManifest instead of overwriting the rejected run.

Task275 carries that live-retrieval-repaired candidate into the final replay material chain without losing provenance. A repaired rerun that passes LeanRunner now clears only the stale transient LeanRunner-rejection veto from the previous failed attempt, preserving other hard vetoes; the selected candidate replay descriptor and final replay material source bind the non-authoritative repair hint execution, campaign repair execution, and per-candidate repair execution by path/hash. The provenance remains `proof_authority=none` and cannot promote claims, but it makes the final clean replay package auditably traceable back to the live retrieval suggestion that shaped the candidate.

Task276 adds the matching opt-in live theorem-search repair-hint execution path for Loogle-compatible endpoints. When `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` includes `theorem_search:loogle`, and `COMATH_LOOGLE_SEARCH_BASE_URL` is configured, the all-rejected feedback resume can call the configured endpoint through service-owned HTTP, send `COMATH_LOOGLE_API_KEY` only as an in-memory bearer header, and persist sanitized request/response hashes, response status/content type, response body hash, prompt-injection scan, and bounded declaration results in `lean_candidate_attempt_repair_hint_execution.json`. The result is threaded through repair tasks/execution as non-authoritative context only: it keeps `proof_authority=none`, cannot promote claims, emits no LeanRunManifest/FinalReplayManifest, and default offline runs remain stubbed.

Task277 lets that live theorem-search context repair the first extremely narrow conjunction class. If a live `theorem_search:loogle` result exposes the non-authoritative declaration `And.intro`, the repair stage may materialize a placeholder-free candidate for the locked same-name `True /\\ True` or `True ∧ True` theorem/lemma using `exact And.intro trivial trivial`. This is candidate material only: the declaration hint cannot prove the theorem, cannot change the locked statement, and the candidate must still pass service-owned LeanRunner plus downstream clean replay before any proof claim.

Task278 adds the matching opt-in live computation repair-hint execution path for SymPy-compatible endpoints. When `COMATH_ENABLE_GOAL_MODE_LIVE_REPAIR_HINT_EXECUTION=1`, `COMATH_LIVE_REPAIR_HINT_PROVIDERS` includes `computation:sympy`, and `COMATH_SYMPY_COMPUTE_BASE_URL` is configured, the all-rejected feedback resume can POST a structured repair-context request through service-owned HTTP, send `COMATH_SYMPY_API_KEY` only as an in-memory bearer header, and persist sanitized request/response hashes, response status/content type/body hash, prompt-injection scan, and bounded computation report summaries in `lean_candidate_attempt_repair_hint_execution.json`. The result is threaded through repair tasks/execution as non-authoritative context only: it keeps `proof_authority=none`, cannot promote claims, emits no LeanRunManifest/FinalReplayManifest, and default offline runs remain stubbed.

Task279 lets that live computation context repair one conservative arithmetic class. If an opt-in live `computation:sympy` report is live-provider-recorded, prompt-injection-scanned, exact-symbolic, non-authoritative, and reports normalized expression `True`, repair may materialize a same-name theorem/lemma candidate for a syntactically closed decidable arithmetic equality over allowlisted Lean identifiers such as `Int`, `Nat`, and `Bool` using `by decide`. This is candidate material only: SymPy cannot prove the theorem, change the locked statement, or bypass service-owned LeanRunner plus final clean replay.

Task280 carries that live-computation-repaired candidate provenance into public goal-mode export once final Lean clean replay has actually passed. The selected candidate replay descriptor, final replay material source, and `exportCampaignGoalModeEvidence()` manifest now expose the same sanitized `candidate_repair_provenance` record for `live_computation_sympy_true_decide_repair`, binding the repair hint execution, campaign repair execution, and per-candidate repair execution by relative path/hash. The export still reports proof authority only from Lean clean replay; the repair provenance remains `proof_authority=none`, cannot promote claims, and must not leak host roots or provider secrets.

Task281 exposes that no-authority repair provenance safely through the Pi public campaign export consumer. `comath.campaign.export` and `/cm:campaign export` now preserve only the known `candidate_repair_provenance` path/hash references for repair hint execution, campaign repair execution, and per-candidate repair execution while continuing to redact ordinary `.comath` runtime paths, host roots, secrets, proof-success wording, and promotion flags from public Pi output. This is audit traceability only; Pi output still cannot promote claims or replace Lean clean replay authority.

Task282 carries the same audit traceability into the `/cm:research` goal-mode loop result. After a terminal goal-mode proof has verified campaign export authority, the Pi research loop now returns an `export_descriptor` with `lean_clean_replay_authority_verified=true` and the sanitized no-authority `candidate_repair_provenance` path/hash references, while keeping the descriptor itself `proof_authority=none` and non-promotional. If export authority is missing or forged, the loop still degrades to `blocked_with_replayable_certificate` and withholds repair provenance from the research-loop result.

Task283 adds one more narrow live theorem-search repair consumer. If a live `theorem_search:loogle` result exposes the non-authoritative declaration `Iff.intro`, the repair stage may materialize a placeholder-free candidate only for the locked same-name `True <-> True` theorem/lemma using `exact Iff.intro (fun _ => trivial) (fun _ => trivial)`. This is candidate material only: the declaration hint cannot prove the theorem, cannot change the locked statement, cannot infer imports or general `iff` proofs, and the candidate must still pass service-owned LeanRunner plus downstream clean replay before any proof claim.

Boundary unchanged: this provider-helper chain does not alter Lean clean replay authority, live Pi status, provider coverage, or certify/complete release readiness.

Task180 revalidated the Task175-179 provider-helper chain, public route sanitization, current Pi probe/sandbox-execution consumers, and readiness boundary without adding a new authority surface or closing the remaining production-helper, durable-transport, broad OS-enforced execution, Lean replay, or real-Pi blockers.

Task181 adds the first default configured-provider helper asset path for the Windows AppContainer family: a service-owned absolute helper executable can be declared through `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_HELPER`, allowing the default provider-runner and helper-host validation path to bind the helper binary hash without accepting caller command, argv, env, hash, or success metadata. This remains a runner/host-validation asset only; it is not canonical collected OS-enforcement evidence, readiness evidence, mathematical proof authority, broad provider support, real-Pi execution, or GA certification.

Task182 adds a service-owned configured helper execution asset path for that same Windows AppContainer helper family: after a ready provider-runner manifest and a matching validated helper-host artifact, the default helper execution route may run the configured executable with an optional fixed args prefix from `COMATH_AGENT_ADAPTER_OSISO_WINDOWS_APPCONTAINER_HELPER_ARGS_JSON`, followed by fixed service-owned argv and fixed `COMATH_*` env. Public manifests record helper executable hash plus args-prefix hash/count only, not host paths or prefix arguments. Helper execution and public collection route payloads still are not canonical OS-enforcement evidence, readiness evidence, mathematical proof authority, broad provider support, real-Pi execution, or GA certification; only an internal service-owned collection probe can write canonical probe/evidence artifacts for readiness review.

Task184 expands configured helper asset contracts across OCI, Nix, Firejail, and Windows AppContainer provider families, and makes the default helper-host validator fail closed when a platform-specific provider is configured on an incompatible service-observed host platform. Provider-specific helper env vars and args-prefix env vars remain host-only configuration with provider-specific precedence over fallback handles. The platform contract prevents false host validation; it is not a production helper binary, not collected OS-enforcement evidence, not readiness evidence, not proof authority, not broad provider support, and not GA certification.

Task185 adds a fixed CoMath provider-helper self-test contract to the default configured-helper host validator. A configured helper must respond to service-owned fixed argv/env before host validation can unlock helper execution; the manifest records only self-test hashes/status, helper executable hash, args-prefix hash/count, and non-secret diagnostics. This checks the helper protocol surface only: it is not collected OS-enforcement evidence, readiness evidence, proof authority, broad provider support, real-Pi execution, or GA certification.

Task186 hardens that self-test contract so a helper response must bind the current project id, host-validation id, provider-runner id, and sandbox-launch id before default host validation can unlock helper execution. A generic self-test response that merely reports provider/backend/network/proof-authority now fails closed. This remains host-validation metadata only; it does not prove OS enforcement, satisfy readiness, affect mathematical proof authority, or certify GA.

Task187 hardens the runtime side of configured provider-helper execution. A helper execution can feed the provider-helper collection bridge only when the helper stdout emits a service-checked runtime attestation bound to the current project id, helper-execution id, provider-runner id, sandbox-launch id, adapter, backend, provider, disabled network policy, and `proof_authority=none`. Generic runtime success output now blocks collection before any canonical probe writer is invoked. This remains execution/collection metadata only; it does not prove OS enforcement, satisfy readiness by itself, affect mathematical proof authority, or certify GA.

Task188 adds a bundled CoMath provider-helper protocol asset for the no-env-helper default path. The provider-runner, host-validation, and helper-execution routes can use the packaged protocol script through the current Node executable with fixed argv/env, pass the self-test/runtime-attestation binding checks, and still expose only hashes/counts/status in public manifests. This is protocol coverage only: it is not a production OCI/Nix/Firejail/AppContainer helper binary, not collected OS-enforcement evidence, not readiness evidence by itself, not mathematical proof authority, broad provider support, real-Pi execution, or GA certification.

Task189 revalidates the Task184-188 provider-helper and bundled-helper chain as a comprehensive check-debug loop. It keeps configured helper handles, platform contracts, self-test bindings, runtime-attestation bindings, bundled protocol execution, host-validation wrappers, helper-execution wrappers, and collection wrappers non-authoritative: none can satisfy readiness by themselves, prove OS enforcement, affect mathematical proof authority, provide real-Pi evidence, or certify GA. Only canonical service-owned probe/evidence artifacts can feed the adapter OS-isolation readiness review.

Task190 adds a service-owned provider host capability probe contract for adapter OS-isolation provider families. The probe can record service-observed platform/tool/kernel capability facts, sanitized diagnostics, and replayable host blockers before helper host validation, while rejecting caller-supplied platform, tool, kernel, proof-authority, or GA-certification payloads. Host capability manifests remain diagnostics only: they are not provider-helper readiness, helper self-test/runtime attestation, collected OS-enforcement evidence, readiness-review evidence by themselves, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.

Task191 binds provider-helper host validation to a matching service-owned provider host capability probe. Host validation now fails closed unless the referenced append-only probe is observed, project/adapter/backend/provider/platform/artifact-bound, non-authoritative, and still diagnostic-only. This prerequisite does not make host capability metadata provider-helper readiness, collected OS-enforcement evidence, readiness-review evidence, proof authority, real-Pi evidence, or GA certification.

Task192 exposes those provider host capability and provider-helper host-validation boundaries through the Pi thin client as `/cm:release agent-adapter-os-isolation-provider-host-capability-probe` and `/cm:release agent-adapter-os-isolation-provider-helper-host-validation`. The Pi consumer is host-confirmed, strips model-supplied confirmation ids, forwards only sanitized `platform` / `notes` diagnostics from caller environment objects, and keeps outputs non-authoritative: no direct `.comath/` writes by Pi, no readiness-review evidence, no executed OS-isolation evidence, no proof authority, no real-Pi evidence, and no GA certification.

Task193 adds a default service-owned provider host capability probe for the production route path. `comathd` can now record provider-family/platform compatibility and bundled provider-helper protocol asset diagnostics without an injected test callback, while ignoring caller-submitted success, tool, kernel, proof-authority, and GA fields. These default probe manifests still keep `adapter_execution_isolation.os_enforced=false`, remain host-capability diagnostics only, and cannot satisfy readiness, prove OS enforcement, replace helper validation/runtime attestation, provide real-Pi evidence, or certify GA.

Task194 extends that default host capability path with Windows AppContainer host facility diagnostics. On Win32 service hosts, the default probe records a Windows AppContainer facility fact and `windows_checknetisolation` tool presence/hash metadata without exposing system paths or accepting caller-supplied success fields. These diagnostics are still not provider-helper readiness, executed OS isolation, collected OS-enforcement evidence, readiness-review evidence, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.

Task195 extends the same default host capability path with OCI container host facility diagnostics. On service-supported Linux or Windows hosts, the default probe records an OCI facility fact plus Docker/Podman CLI presence/hash metadata without executing those tools, inspecting daemon/socket/container state, exposing executable paths, or accepting caller-supplied tool/success fields. These diagnostics remain host-capability metadata only: not provider-helper readiness, executed OS isolation, collected OS-enforcement evidence, readiness-review evidence, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.

Task196 completes the remaining default host facility diagnostic matrix for Nix sandbox and Firejail. On service-supported host platforms, the default probe records Nix/Firejail facility facts plus `nix`, `nix-store`, or `firejail` executable-candidate presence/hash metadata without running those tools, reading stores/profiles/policies, exposing PATH/executable paths, recording versions, or accepting caller-supplied tool/success fields. These diagnostics remain host-capability metadata only: not provider-helper readiness, executed OS isolation, collected OS-enforcement evidence, readiness-review evidence, mathematical proof authority, real-Pi execution evidence, broad provider support, or GA certification.

- [GA Release Criteria](docs/architecture/ga-release-criteria.md)
- [Threat Model](docs/architecture/threat-model.md)
- [Adapter Contracts](docs/architecture/adapter-contracts.md)
- [External Lean Supply Chain](docs/architecture/external-lean-supply-chain.md)
- [Evidence Pack Policy](docs/architecture/evidence-pack-policy.md)
- [Example Campaigns](docs/examples/README.md)
- [Config Samples](config/README.md)

Canonical project documents:

- [Development Plan](COMATH_PI_LAB_DEV_PLAN.md)
- [Goal Runbook](CODEX_GOAL_RUNBOOK.md)
- [Agent Instructions](AGENTS.md)
- [TODO](TODO.md)
- [Review Log](REVIEW.md)
- [Security Review](SECURITY_REVIEW.md)
- [Mathematical Integrity Review](MATH_INTEGRITY_REVIEW.md)
